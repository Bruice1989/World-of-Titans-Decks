import pygame

def _open_url(url):
    """Відкриває URL у браузері — працює на Windows, macOS та Linux."""
    import sys, subprocess, webbrowser
    try:
        if sys.platform == "win32":
            import os; os.startfile(url)
        elif sys.platform == "darwin":
            subprocess.Popen(["open", url])
        else:
            # Linux: спробувати xdg-open, потім популярні браузери
            browsers = ["xdg-open", "google-chrome", "chromium-browser",
                        "firefox", "opera", "brave-browser"]
            opened = False
            for br in browsers:
                try:
                    subprocess.Popen([br, url],
                                     stdout=subprocess.DEVNULL,
                                     stderr=subprocess.DEVNULL)
                    opened = True
                    break
                except FileNotFoundError:
                    continue
            if not opened:
                webbrowser.open(url)
    except Exception:
        webbrowser.open(url)  # фолбек

import random
import sys
import os
import json
import time
import math

# --- ТУТ ПЕРЕЙМЕНОВУЙ СВОЇ КАРТИ ---
RENAME_DICT = {
    # --- Герої ---
    "Герой 0": "Конан Воїн",
    "Герой 1": "Лицар Світла",
    "Герой 2": "Маг Стихій",
    "Герой 3": "Рицар в латах",
    "Герой 4": "Залізний Паладин",
    "Герой 5": "Тіньовий Асасин",
    "Герой 6": "Богиня Озера",
    "Алмазний Дракон": "Алмазний Дракон",
    "Небесний Фенікс": "Небесний Фенікс",
    "Прадавній Титан": "Прадавній Титан",
    "Ангельський Робот": "Ангельський Робот",
    "Кислотний Бот": "Кислотний Бот",
    "Devil Gods": "Devil Gods",
    "Зоряний Архангел": "Зоряний Архангел",
    "Кристальний Голем": "Кристальний Голем",
    "Алмазний Рицар": "Алмазний Рицар",
    "Сапфірний Дракон": "Сапфірний Дракон",
    "Небесний Страж": "Небесний Страж",
    "Священний Колос": "Священний Колос",
    "Вічний Пророк": "Вічний Пророк",
    "Зоряний Велетень": "Зоряний Велетень",
    "Небесна Богиня": "Небесна Богиня",
    "Діамантовий Феїнт": "Діамантовий Феїнт",
    "Небесний Суддя": "Небесний Суддя",
    "Ангел-Руйнівник": "Ангел-Руйнівник",
    "Кришталевий Воїн": "Кришталевий Воїн",
    "Сонячний Магістр": "Сонячний Магістр",
    "Місячний Страж": "Місячний Страж",
    "Зоряний Лорд": "Зоряний Лорд",
    "Небесний Дракон": "Небесний Дракон",
    "Алмазний Феїнт": "Алмазний Феїнт",
    "Сапфір Короля": "Сапфір Короля",
    "Грозовий Янгол": "Грозовий Янгол",
    "Космічний Страж": "Космічний Страж",
    "Вічний Лицар": "Вічний Лицар",
    "Серафім Небес": "Серафім Небес",
    "Блискавичний Бог": "Блискавичний Бог",
    "Королівство Грифон": "Королівство Грифон",
    "Золотий 101": "Золотий Лицар",
    "Золотий Лицар": "Майстер Рун",
    "НЛО": "НЛО",
    "Леді Ді": "Леді Ді",
    "Angel Lar": "Angel Lar",
    "Золотий Орел": "Золотий Орел",
    "Мисливець Зорі": "Мисливець Зорі",
    "Бронзовий Велетень": "Бронзовий Велетень",
    "Золотий Феїнт": "Золотий Феїнт",
    "Сонячний Паладин": "Сонячний Паладин",
    "Золота Стріла": "Золота Стріла",
    "Руновий Майстер": "Руновий Майстер",
    "Золота Горгона": "Золота Горгона",
    "Янтарний Страж": "Янтарний Страж",
    "Золотий Фенікс": "Золотий Фенікс",
    "Сонячний Воїн": "Сонячний Воїн",
    "Золотий Маг": "Золотий Маг",
    "Янтарний Дракон": "Янтарний Дракон",
    "Золота Відьма": "Золота Відьма",
    "Металевий Лорд": "Металевий Лорд",
    "Бурштиновий Рицар": "Бурштиновий Рицар",
    "Золотий Демон": "Золотий Демон",
    "Сонячний Архімаг": "Сонячний Архімаг",
    "Золотий Голем": "Золотий Голем",
    "Яскравий Страж": "Яскравий Страж",
    "Золотий Пророк": "Золотий Пророк",
    "Янтарна Богиня": "Янтарна Богиня",
    "Сонячний Колос": "Сонячний Колос",
    "Золотий Архангел": "Золотий Архангел",
    "Залізний Вепр": "Залізний Вепр",
    "Гном-коваль": "Гном-коваль",
    "Жива Броня": "Жива Броня",
    "Sniper Bot": "Sniper Bot",
    "Tanos": "Tanos",
    "Army Darkness": "Army Darkness",
    "Сталевий Воїн": "Сталевий Воїн",
    "Залізний Голем": "Залізний Голем",
    "Рудяний Рицар": "Рудяний Рицар",
    "Мідний Страж": "Мідний Страж",
    "Бронзовий Кулак": "Бронзовий Кулак",
    "Залізний Маг": "Залізний Маг",
    "Сталева Горгона": "Сталева Горгона",
    "Рудяний Дракон": "Рудяний Дракон",
    "Металевий Демон": "Металевий Демон",
    "Залізний Страж": "Залізний Страж",
    "Сталевий Колос": "Сталевий Колос",
    "Рудяний Фенікс": "Рудяний Фенікс",
    "Мідний Лицар": "Мідний Лицар",
    "Сталевий Маг": "Сталевий Маг",
    "Рудяний Голем": "Рудяний Голем",
    "Залізний Пророк": "Залізний Пророк",
    "Бронзовий Дракон": "Бронзовий Дракон",
    "Мідна Богиня": "Мідна Богиня",
    "Сталевий Демон": "Сталевий Демон",
    "Рудяний Воїн": "Рудяний Воїн",
    "Залізний Архімаг": "Залізний Архімаг",
    "Бронзовий Феїнт": "Бронзовий Феїнт",
    "Сталевий Лорд": "Сталевий Лорд",
    "Рудяний Архангел": "Рудяний Архангел",
    "Авель": "Авель",
    "Ангел Кастіїл": "Ангел Кастіїл",
    "Сарафаїл": "Сарафаїл",
    "Самандріїл": "Самандріїл",
    "Архангел Гавріїл": "Архангел Гавріїл",
    "Рагуїл": "Рагуїл",
    "Реміїл": "Реміїл",
    "Уріїл": "Уріїл",
    "Рафаїл": "Рафаїл",
    "Архангел Міхаїл": "Архангел Міхаїл",
    "Демон Перехрестя": "Демон Перехрестя",
    "Демон Рубі": "Демон Рубі",
    "Мег": "Мег",
    "Азазель": "Азазель",
    "Кроулі з Пекла": "Кроулі з Пекла",
    "Алестер": "Алестер",
    "Ліліт": "Ліліт",
    "Аббадон": "Аббадон",
    "Люцифер": "Люцифер",
    "Господь Чак": "Господь Чак",
    # --- Картки магазину (Алмази) — 30 унікальних карт ---
    "Діамант. 100": "Алмазний Дракон",
    "Діамант. 101": "Небесний Фенікс",
    "Діамант. 102": "Прадавній Титан",
    "Діамант. 103": "Ангельський Робот",
    "Діамант. 104": "Кислотний Бот",
    "Діамант. 105": "Devil Gods",
    "Діамант. 106": "Зоряний Архангел",
    "Діамант. 107": "Кристальний Голем",
    "Діамант. 108": "Алмазний Рицар",
    "Діамант. 109": "Сапфірний Дракон",
    "Діамант. 110": "Небесний Страж",
    "Діамант. 111": "Священний Колос",
    "Діамант. 112": "Вічний Пророк",
    "Діамант. 113": "Зоряний Велетень",
    "Діамант. 114": "Небесна Богиня",
    "Діамант. 115": "Діамантовий Феїнт",
    "Діамант. 116": "Небесний Суддя",
    "Діамант. 117": "Ангел-Руйнівник",
    "Діамант. 118": "Кришталевий Воїн",
    "Діамант. 119": "Сонячний Магістр",
    "Діамант. 120": "Місячний Страж",
    "Діамант. 121": "Зоряний Лорд",
    "Діамант. 122": "Небесний Дракон",
    "Діамант. 123": "Алмазний Феїнт",
    "Діамант. 124": "Сапфір Короля",
    "Діамант. 125": "Грозовий Янгол",
    "Діамант. 126": "Космічний Страж",
    "Діамант. 127": "Вічний Лицар",
    "Діамант. 128": "Серафім Небес",
    "Діамант. 129": "Блискавичний Бог",
    # --- Картки магазину (Золото) — 30 унікальних карт ---
    "Золотий 100": "Королівство Грифон",
    "Золотий 101": "Золотий Лицар",
    "Золотий 102": "Майстер Рун",
    "Золотий 103": "НЛО",
    "Золотий 104": "Леді Ді",
    "Золотий 105": "Angel Lar",
    "Золотий 106": "Золотий Орел",
    "Золотий 107": "Мисливець Зорі",
    "Золотий 108": "Бронзовий Велетень",
    "Золотий 109": "Золотий Феїнт",
    "Золотий 110": "Сонячний Паладин",
    "Золотий 111": "Золота Стріла",
    "Золотий 112": "Руновий Майстер",
    "Золотий 113": "Золота Горгона",
    "Золотий 114": "Янтарний Страж",
    "Золотий 115": "Золотий Фенікс",
    "Золотий 116": "Сонячний Воїн",
    "Золотий 117": "Золотий Маг",
    "Золотий 118": "Янтарний Дракон",
    "Золотий 119": "Золота Відьма",
    "Золотий 120": "Металевий Лорд",
    "Золотий 121": "Бурштиновий Рицар",
    "Золотий 122": "Золотий Демон",
    "Золотий 123": "Сонячний Архімаг",
    "Золотий 124": "Золотий Голем",
    "Золотий 125": "Яскравий Страж",
    "Золотий 126": "Золотий Пророк",
    "Золотий 127": "Янтарна Богиня",
    "Золотий 128": "Сонячний Колос",
    "Золотий 129": "Золотий Архангел",
    # --- Картки магазину (Руда) — 30 унікальних карт ---
    "Рудний 100": "Залізний Вепр",
    "Рудний 101": "Гном-коваль",
    "Рудний 102": "Жива Броня",
    "Рудний 103": "Sniper Bot",
    "Рудний 104": "Tanos",
    "Рудний 105": "Army Darkness",
    "Рудний 106": "Сталевий Воїн",
    "Рудний 107": "Залізний Голем",
    "Рудний 108": "Рудяний Рицар",
    "Рудний 109": "Мідний Страж",
    "Рудний 110": "Бронзовий Кулак",
    "Рудний 111": "Залізний Маг",
    "Рудний 112": "Сталева Горгона",
    "Рудний 113": "Рудяний Дракон",
    "Рудний 114": "Металевий Демон",
    "Рудний 115": "Залізний Страж",
    "Рудний 116": "Сталевий Колос",
    "Рудний 117": "Рудяний Фенікс",
    "Рудний 118": "Мідний Лицар",
    "Рудний 119": "Сталевий Маг",
    "Рудний 120": "Рудяний Голем",
    "Рудний 121": "Залізний Пророк",
    "Рудний 122": "Бронзовий Дракон",
    "Рудний 123": "Мідна Богиня",
    "Рудний 124": "Сталевий Демон",
    "Рудний 125": "Рудяний Воїн",
    "Рудний 126": "Залізний Архімаг",
    "Рудний 127": "Бронзовий Феїнт",
    "Рудний 128": "Сталевий Лорд",
    "Рудний 129": "Рудяний Архангел",
    # --- Монстри локацій (Карта світів та Титани-монстри) ---
    # Локація 1 — ліс, початок (рівні 1-5)
    "Гоблін-шпигун":       "Гоблін-шпигун",
    "Лісовий Тролль":      "Лісовий Тролль",
    "Болотяна Жаба":       "Болотяна Жаба",
    "Дикий Кабан":         "Дикий Кабан",
    "Скелет-лучник":       "Скелет-лучник",
    "Павук-отруйник":      "Павук-отруйник",
    # Локація 2 — гори (рівні 6-10)
    "Кам'яний Громила":    "Кам'яний Громила",
    "Льодяний Йєті":       "Льодяний Йєті",
    "Вовк-перевертень":    "Вовк-перевертень",
    "Темний Орк":          "Темний Орк",
    "Підземний Крот":      "Підземний Крот",
    "Нічний Злодій":       "Нічний Злодій",
    # Локація 3 — болота (рівні 11-15)
    "Отруйний Павук":      "Отруйний Павук",
    "Вогняний Ящір":       "Вогняний Ящір",
    "Кістяний Воїн":       "Кістяний Воїн",
    "Болотяний Велетень":  "Болотяний Велетень",
    "Тіньовий Лучник":     "Тіньовий Лучник",
    "Зомбі-солдат":        "Зомбі-солдат",
    # Локація 4 — темниця (рівні 16-20)
    "Гігант Боб":          "Гігант Боб",
    "Кам'яний Голем":      "Кам'яний Голем",
    "Залізний Стрілець":   "Залізний Стрілець",
    "Вогняна Горгона":     "Вогняна Горгона",
    "Льодяний Дракон":     "Льодяний Дракон",
    "Темний Маг":          "Темний Маг",
    # Локація 5 — руїни (рівні 21-26)
    "Крижаний Привид":     "Крижаний Привид",
    "Вампір Лорд":         "Вампір Лорд",
    "Страшний Демон":      "Страшний Демон",
    "Залізний Робот":      "Залізний Робот",
    "Хаотичний Дух":       "Хаотичний Дух",
    "Темний Лицар":        "Темний Лицар",
    # Локація 6 — пекло (рівні 27-32)
    "Пекельний DOOM":      "Пекельний DOOM",
    "Архідемон":           "Архідемон",
    "Хаосний Лорд":        "Хаосний Лорд",
    "Нескорений Титан":    "Нескорений Титан",
    "Безмежний Жах":       "Безмежний Жах",
    "Вічний Страж":        "Вічний Страж",
    # Локація 7 — некрополь (рівні 33-38)
    "Некромант":           "Некромант",
    "Скелет-воїн":         "Скелет-воїн",
    "Ожилий Труп":         "Ожилий Труп",
    "Костяний Дракон":     "Костяний Дракон",
    "Мертвий Маг":         "Мертвий Маг",
    "Привид Воїна":        "Привид Воїна",
    # Локація 8 — море (рівні 39-44)
    "Морський Монстр":     "Морський Монстр",
    "Русалка-вбивця":      "Русалка-вбивця",
    "Підводний Дракон":    "Підводний Дракон",
    "Гігант Восьминіг":    "Гігант Восьминіг",
    "Акула-мутант":        "Акула-мутант",
    "Морський Дух":        "Морський Дух",
    # Локація 9 — пустеля (рівні 45-50)
    "Пустельний Скорпіон": "Пустельний Скорпіон",
    "Піщаний Змій":        "Піщаний Змій",
    "Мумія Фараона":       "Мумія Фараона",
    "Піщаний Голем":       "Піщаний Голем",
    "Сфінкс-воїн":         "Сфінкс-воїн",
    "Давній Бог":          "Давній Бог",
    # Локація 10 — космос/фінал (рівні 51-60)
    "Небесний Дракон":           "Небесний Дракон",
    "Зоряний Демон":             "Зоряний Демон",
    "Космічний Жах":             "Космічний Жах",
    "Галактичний Лорд":          "Галактичний Лорд",
    "Всесвітній Руйнівник":      "Всесвітній Руйнівник",
    "Кінець Часів":              "Кінець Часів",
}

# --- ПУЛИ КАРТ ДЛЯ МАГАЗИНІВ (фіксовані імена для відображення картинок) ---
SHOP_DIAMOND_NAMES = [
    "Діамант. 100", "Діамант. 101", "Діамант. 102", "Діамант. 103", "Діамант. 104",
    "Діамант. 105", "Діамант. 106", "Діамант. 107", "Діамант. 108", "Діамант. 109",
    "Діамант. 110", "Діамант. 111", "Діамант. 112", "Діамант. 113", "Діамант. 114",
    "Діамант. 115", "Діамант. 116", "Діамант. 117", "Діамант. 118", "Діамант. 119",
    "Діамант. 120", "Діамант. 121", "Діамант. 122", "Діамант. 123", "Діамант. 124",
    "Діамант. 125", "Діамант. 126", "Діамант. 127", "Діамант. 128", "Діамант. 129",
]
SHOP_GOLD_NAMES = [
    "Золотий 100", "Золотий 101", "Золотий 102", "Золотий 103", "Золотий 104",
    "Золотий 105", "Золотий 106", "Золотий 107", "Золотий 108", "Золотий 109",
    "Золотий 110", "Золотий 111", "Золотий 112", "Золотий 113", "Золотий 114",
    "Золотий 115", "Золотий 116", "Золотий 117", "Золотий 118", "Золотий 119",
    "Золотий 120", "Золотий 121", "Золотий 122", "Золотий 123", "Золотий 124",
    "Золотий 125", "Золотий 126", "Золотий 127", "Золотий 128", "Золотий 129",
]
SHOP_ORE_NAMES = [
    "Рудний 100", "Рудний 101", "Рудний 102", "Рудний 103", "Рудний 104",
    "Рудний 105", "Рудний 106", "Рудний 107", "Рудний 108", "Рудний 109",
    "Рудний 110", "Рудний 111", "Рудний 112", "Рудний 113", "Рудний 114",
    "Рудний 115", "Рудний 116", "Рудний 117", "Рудний 118", "Рудний 119",
    "Рудний 120", "Рудний 121", "Рудний 122", "Рудний 123", "Рудний 124",
    "Рудний 125", "Рудний 126", "Рудний 127", "Рудний 128", "Рудний 129",
]

# --- РІЗНОМАНІТНІ НАЗВИ МОНСТРІВ ДЛЯ ЛОКАЦІЙ (рівні ростуть від лок.1 до лок.10) ---
MONSTER_POOLS = {
    1:  ["Гоблін-шпигун","Лісовий Тролль","Болотяна Жаба","Дикий Кабан","Скелет-лучник","Павук-отруйник"],
    2:  ["Кам'яний Громила","Льодяний Йєті","Вовк-перевертень","Темний Орк","Підземний Крот","Нічний Злодій"],
    3:  ["Отруйний Павук","Вогняний Ящір","Кістяний Воїн","Болотяний Велетень","Тіньовий Лучник","Зомбі-солдат"],
    4:  ["Гігант Боб","Кам'яний Голем","Залізний Стрілець","Вогняна Горгона","Льодяний Дракон","Темний Маг"],
    5:  ["Крижаний Привид","Вампір Лорд","Страшний Демон","Залізний Робот","Хаотичний Дух","Темний Лицар"],
    6:  ["Пекельний DOOM","Архідемон","Хаосний Лорд","Нескорений Титан","Безмежний Жах","Вічний Страж"],
    7:  ["Некромант","Скелет-воїн","Ожилий Труп","Костяний Дракон","Мертвий Маг","Привид Воїна"],
    8:  ["Морський Монстр","Русалка-вбивця","Підводний Дракон","Гігант Восьминіг","Акула-мутант","Морський Дух"],
    9:  ["Пустельний Скорпіон","Піщаний Змій","Мумія Фараона","Піщаний Голем","Сфінкс-воїн","Давній Бог"],
    10: ["Небесний Дракон","Зоряний Демон","Космічний Жах","Галактичний Лорд","Всесвітній Руйнівник","Кінець Часів"],
}

# Рівні монстрів для кожної локації (від легких до важких)
LOCATION_LEVELS = {
    1: (1, 3),    # мін-макс рівень монстрів
    2: (5, 8),
    3: (10, 14),
    4: (16, 20),
    5: (22, 27),
    6: (28, 34),
    7: (35, 42),
    8: (43, 50),
    9: (50, 58),
    10: (58, 65),
}

def get_monster_runes(loc, world):
    """Генерує руни для монстра залежно від локації та світу."""
    # Кількість рун: від 0 (лок1,світ1) до 5 (лок10,світ20)
    base_runes = (loc - 1) // 3           # лок 1-3: 0, 4-6: 1, 7-9: 2, 10: 3
    world_bonus = (world - 1) // 5        # кожні 5 світів +1 руна
    num_runes = min(5, base_runes + world_bonus)
    if num_runes == 0:
        return []
    RUNE_NAMES_POOL = [
        "Руна Удару", "Руна Захисту", "Руна Стремління", "Руна Тіні",
        "Руна Вогню", "Руна Льоду", "Руна Грому", "Руна Отрути",
        "Руна Вітру", "Руна Землі", "Руна Крові", "Руна Хаосу",
    ]
    # Рівень рун: зростає з локацією та світом
    min_lv = max(1, (loc - 1) // 2 + (world - 1) // 3)
    max_lv = min(10, min_lv + 2 + loc // 3)
    runes = []
    pool = random.sample(RUNE_NAMES_POOL, min(num_runes, len(RUNE_NAMES_POOL)))
    for nm in pool:
        runes.append({"name": nm, "level": random.randint(min_lv, max_lv)})
    return runes

def get_monster_name(world_or_loc, index):
    """Повертає ім'я монстра залежно від локації та індексу."""
    pool = MONSTER_POOLS.get((world_or_loc - 1) % 10 + 1, MONSTER_POOLS[1])
    return pool[index % len(pool)]

# --- ЕПІЧНІ ІСТОРІЇ ЛОКАЦІЙ (ЛОР) ---
BATTLE_LORE = {
    1: {
        1: "Світ-1. Локація 1.Узлісся Забутих",
        2: "Туманна Лощина: Густий, отруйний туман огортає ваших воїнів. Монстри тут живляться самим страхом.",
        3: "Кривавий Струмок: Вода тут давно втратила свій колір. Приготуйтеся до кровопролитної січі з нежиттю!",
        4: "Руїни Аванпосту: Відлуння стародавньої магії іскрить у повітрі. Ворог використовує залишки барикад для засідки.",
        5: "Лігво Павучої Королеви: Стіни вкриті липким павутинням. Кожен необачний крок може стати останнім у цій пастці.",
        6: "Печера Відлуння: Темрява тут густа, як смола. З глибин чути брязкіт зброї та пекельне гарчання.",
        7: "Випалене Поле: На місці колишньої битви повстали тіні полеглих. Здолайте їх, щоб знайти шлях далі.",
        8: "Закинута Шахта: Стіни дрижать від кроків підземних потвор. Бережіть фланги від раптового нападу.",
        9: "Міст Відчаю: Вузький прохід над прірвою не залишає шляху для відступу. Тільки перемога або смерть.",
        10: "Цитадель Мороку: Брама відчиняється. Вождь легіону нежиті чекає на вас для вирішального удару!"
    },
    2: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    3: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    4: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    5: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    6: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    7: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    8: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    9: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    10: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    11: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    12: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    13: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    14: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    15: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    16: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    17: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    18: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    19: {
        1: "Золоті Піски: Сонце нещадно палить обладунки. З-під барханів виповзають скарабеї-вбивці.",
        2: "Оазис Мари: Те, що здавалося порятунком, виявилося лігвом пустельних відьом та міражів.",
        3: "Забутий Зиккурат: Стародавні варти прокинулися від вашого кроку. Сталь зіткнеться з прадавнім каменем.",
        4: "Долина Вітрів: Піщана буря засліплює очі. Ворог атакує стрімко, наче сам розлючений вітер.",
        5: "Каньйон Кісток: Шлях завалений останками велетнів. Неупокоєні духи охороняють цей вузький прохід.",
        6: "Селище Кочівників: Колись мирне місце тепер захоплене ордою безжальних пустельних найманців.",
        7: "Солоне Озеро: Біла пустеля, де магія віддзеркалюється. Стережіться власних двійників-тіней.",
        8: "Печера Скорпіонів: Тисячі очей стежать за вами з тріщин. Отрута — їхня єдина і головна зброя.",
        9: "Храм Сонця: Залишки божественної сили все ще боронять вхід. Лише гідні зможуть пройти цей поріг.",
        10: "Трон Пісків: Володар бурі чекає в центрі смерчу. Викличте його на чесний двобій серед пісків!"
    },
    20: {
        1: "Підніжжя Скорботи: Вічна мерзлота сковує рухи. Снігові вовки вже вийшли на своє нічне полювання.",
        2: "Замерзле Плато: Лід під ногами тонкий, а вороги — міцні, наче скелі. Тримайте стрій!",
        3: "Крижаний Лабіринт: Стіни з синього льоду викривляють простір. Тут легко згубити розум і дорогу.",
        4: "Селище Горців: Колись вільний народ тепер поневолений жорстокими крижаними велетнями.",
        5: "Перевал Смерті: Лавини — не єдина загроза. Гарпії чекають моменту, щоб скинути вас у прірву.",
        6: "Печера Морозу: Дихання стародавнього дракона застигло тут тисячі років тому, утворивши смертельні піки.",
        7: "Скеля Вітрів: Льодяний вітер б'є в груди з силою молота. Лише найсильніші зможуть встояти на ногах.",
        8: "Кришталевий Грот: Сяйво магічних кристалів засліплює. Ворог використовує це, щоб підібратися впритул.",
        9: "Врата Півночі: Останній рубіж перед серцем вічної зими. Попереду лише морок, холод і смерть.",
        10: "Палац Мерзлоти: Крижаний Король на своєму троні. Його серце холодніше за саму порожнечу космосу."
    }
}

def get_battle_lore(world, loc):
    if world in BATTLE_LORE and loc in BATTLE_LORE[world]:
        return BATTLE_LORE[world][loc]
    return ""


WIDTH, HEIGHT = 1200, 800
pygame.init()
try:
    pygame.mixer.pre_init(44100, -16, 2, 512)
    pygame.mixer.init()
except Exception as _mixer_err:
    print(f"[ПОПЕРЕДЖЕННЯ] Звук недоступний: {_mixer_err}")
    # Продовжуємо без звуку
_info = pygame.display.Info()
NATIVE_W, NATIVE_H = _info.current_w, _info.current_h
screen = pygame.display.set_mode((NATIVE_W, NATIVE_H), pygame.FULLSCREEN | pygame.NOFRAME)
pygame.display.set_caption("Bruice Heroes: Tactical Battle System")
_is_fullscreen = True

# --- ЗВУКИ ТА МУЗИКА ---
def load_sound(filename):
    for ext in [".wav", ".ogg", ".mp3"]:
        path = os.path.join("sounds", filename + ext)
        if os.path.exists(path):
            try: return pygame.mixer.Sound(path)
            except: pass
    return None

def start_music():
    for ext in [".mp3", ".ogg", ".wav"]:
        for name in ["battle", "music", "theme", "background", "main"]:
            path = os.path.join("music", name + ext)
            if os.path.exists(path):
                try:
                    pygame.mixer.music.load(path)
                    pygame.mixer.music.set_volume(0.5)
                    pygame.mixer.music.play(-1)
                    return
                except: pass
    # Якщо нічого не знайдено — пробуємо будь-який файл у папці music
    if os.path.exists("music"):
        for fname in os.listdir("music"):
            if fname.lower().endswith((".mp3", ".ogg", ".wav")):
                try:
                    pygame.mixer.music.load(os.path.join("music", fname))
                    pygame.mixer.music.set_volume(0.5)
                    pygame.mixer.music.play(-1)
                    return
                except: pass

SND_HIT      = load_sound("hit")
SND_KILL     = load_sound("kill")
SND_HERO_DIE = load_sound("hero_die")
SND_WIN      = load_sound("win")
SND_LOSE     = load_sound("lose")
SND_CLICK    = load_sound("click")
SND_RAMKA    = load_sound("ramka")

# --- Звуки здібностей: назва здібності -> файл у папці sounds/ ---
ABILITY_SOUNDS_MAP = {
    "Удар Вогню":           "ability_fire",
    "Хвиля Води":           "ability_water",
    "Блискавка":            "ability_lightning",
    "Телекінез":            "ability_telekinesis",
    "Подвійний Удар":       "ability_double_hit",
    "Вампіризм":            "ability_vampirism",
    "Вибух":                "ability_explosion",
    "Метеорит":             "ability_meteor",
    "Третє Око":            "ability_eye",
    "Тіньовий Клинок":      "ability_shadow_blade",
    "Зграя Вовків":         "ability_wolves",
    "Укус Дракона":         "ability_dragon_bite",
    "Стріла Долі":          "ability_arrow",
    "Лавовий Потік":        "ability_lava",
    "Тризуб Посейдона":     "ability_trident",
    "Землетрус":            "ability_earthquake",
    "Масове Зцілення":      "ability_mass_heal",
    "Чума":                 "ability_plague",
    "Зоряний Вибух":        "ability_star_burst",
    "Торнадо":              "ability_tornado",
    "Подих Дракона":        "ability_dragon_breath",
    "Крижаний Шторм":       "ability_ice_storm",
    "Магнетизм":            "ability_magnet",
    "Ядерна Хвиля":         "ability_nuclear",
    "Буря Блискавок":       "ability_storm",
    "Заморозка":            "ability_freeze",
    "Яд":                   "ability_poison",
    "Летаргія":             "ability_lethargy",
    "Павутиння":            "ability_web",
    "Сповільнення Серця":   "ability_slow_heart",
    "Туман Невизначеності": "ability_fog",
    "Прокляття":            "ability_curse",
    "Ослаблення Кісток":    "ability_bone_break",
    "Сила Трави":           "ability_grass",
    "Квіткове Відродження": "ability_flower",
    "Кристальна Роса":      "ability_crystal",
    "Регенерація":          "ability_regen",
    "Воскресіння":          "ability_resurrection",
    "Відродження":          "ability_rebirth",
    "Темна Діра":           "ability_black_hole",
    "Непробивний Щит":      "ability_shield",
    "Крило Орла":           "ability_eagle",
    "Удача":                "ability_luck",
    "Веселкова Аура":       "ability_rainbow",
    "Портал":               "ability_portal",
    "Зоряна Сила":          "ability_star_power",
    "Зупинка Часу":         "ability_time_stop",
    "Відбиття":             "ability_reflect",
    "Рев Лева":             "ability_lion_roar",
    "Ключ Долі":            "ability_fate_key",
    "Місячна Тінь":         "ability_moon_shadow",
}
# Завантаження всіх звуків здібностей (ліниво, в словник)
ABILITY_SOUNDS = {name: load_sound(fname) for name, fname in ABILITY_SOUNDS_MAP.items()}

MUSIC_ENABLED = True
SOUNDS_ENABLED = True

start_music()

# --- СМІШНІ ФРАЗИ ДЛЯ БОЮ ---
BATTLE_HIT_PHRASES = [
    "Ой-ой-ой! Боляче!",
    "Отримай, монстре!",
    "Прийми удар, нечисто!",
    "Хрусь! Добре попав!",
    "Ану стій, покидьку!",
    "Це тобі за маму!",
    "Шльоп! Ще хочеш?",
    "Нічого особистого... хоча ні, дуже навіть особисто!",
    "Твій захист — як мокрий папір!",
    "Танцюй, монстре, танцюй!",
]
BATTLE_KILL_PHRASES = [
    "НОКАУТ! Хтось замовляв труну?",
    "Ця потвора більше не буде псувати настрій!",
    "Повалився як мішок картоплі!",
    "До побачення, страшило!",
    "Аут! Рефері рахує до десяти!",
    "Відпочивай, підсмажений!",
    "Твоя HP впала нижче ватерлінії!",
    "Фінальний свисток!",
]
BATTLE_HERO_DIE_PHRASES = [
    "Ні-і-і! Тільки не він!",
    "Герой впав... але легенда живе!",
    "Хтось виклики швидку?",
    "Покладіть квіти на могилу героя...",
    "Цього разу монстри перемогли раунд!",
    "Ваш герой відправився на пенсію... назавжди!",
    "Смерть — це просто відпустка для героїв!",
]
BATTLE_WIN_PHRASES = [
    "ПЕРЕМОГА! Слава героям!",
    "Монстри розбіглися хто куди!",
    "Ворог капітулює! Де мій трофей?",
]
BATTLE_LOSE_PHRASES = [
    "ПОРАЗКА! Але ми ще повернемось!",
    "Сьогодні монстри святкують...",
    "Це лише тимчасово! Реванш буде!",
]

battle_phrase = ""
battle_phrase_timer = 0

# ===== СИСТЕМА ЗДІБНОСТЕЙ =====
# Тип: "damage","heal","debuff","aoe","special","control","buff","passive"
ALL_ABILITIES = [
    # ── АТАКУЮЧІ (48) ──────────────────────────────────────────────────────
    ("🔥","Удар Вогню",          "Палить ворога",                   "damage"),
    ("🌊","Хвиля Води",           "Змиває ворога",                   "damage"),
    ("⚡","Блискавка",            "Миттєвий удар",                   "damage"),
    ("🌀","Телекінез",            "Відкидає ворога",                 "damage"),
    ("⚔️","Подвійний Удар",      "Б'є двічі підряд",               "damage"),
    ("🩸","Вампіризм",            "Краде HP ворога",                 "damage"),
    ("💣","Вибух",                "Масивна шкода",                   "damage"),
    ("☄️","Метеорит",             "Падає з неба на ворога",          "damage"),
    ("👁️","Третє Око",           "Бачить і б'є слабке місце",      "damage"),
    ("🗡️","Тіньовий Клинок",     "Удар крізь захист",               "damage"),
    ("🐺","Зграя Вовків",         "Три укуси поспіль",               "damage"),
    ("🦷","Укус Дракона",         "Прокушує броню",                  "damage"),
    ("🏹","Стріла Долі",          "Не промахується ніколи",          "damage"),
    ("🌋","Лавовий Потік",        "Розпечена лава на ворога",        "damage"),
    ("🔱","Тризуб Посейдона",    "Потрійний удар",                   "damage"),
    # Deck Heroes — Атакуючі
    ("💠","Елементальний Сплеск","Стихійна магія — лід/блискавка/вогонь","damage"),
    ("🌪️","Вихр Сталі",          "Б'є ціль і сусідів ½ шкоди",     "damage"),
    ("⚰️","Удар Милосердя",      "+150% якщо у ворога >50% HP",     "damage"),
    ("🛡️","Розколювач Щитів",    "Ігнорує броню ворога",            "damage"),
    ("↩️","Дзеркальний Випад",   "Контратака після отримання шкоди","damage"),
    ("😤","Жага Крові",           "Атака зростає після кожного удару","damage"),
    ("💥","Ефект Несподіванки",  "Колосальна шкода в перший хід",   "damage"),
    ("🔪","Проникаюче Лезо",     "Частина шкоди дістає героя позаду","damage"),
    ("🌩️","Грозовий Розряд",    "Магічний удар по одній цілі",      "damage"),
    ("🔥","Сфера Попелу",        "Магічний удар + шанс підпалу",    "damage"),
    ("🧊","Кристальний Шип",     "Магічний удар + шанс заморозки",  "damage"),
    ("🌀","Круговий Замах",      "Удар по цілі та двом сусідам",    "damage"),
    ("🗡️","Наскрізний Прокол",   "Удар по цілі і герою за нею",    "damage"),
    ("🩸","Спрага Бою",          "Атака росте після кожного удару", "damage"),
    ("😱","Сліпа Лють",          "Шанс 200%+ шкоди",               "damage"),
    ("💢","Інфернальна Буря",    "Удар по всіх картах суперника",   "aoe"),
    ("🎯","Вивірений Постріл",   "Шанс множинного урону",           "damage"),
    ("⚔️","Шквал Клинків",       "Атака двічі за хід",              "damage"),
    ("🪓","Ослабляючий Розріз",  "Знижує базову атаку цілі при ударі","damage"),
    ("👻","Фантомний Кидок",     "Обходить першу карту — б'є героя","damage"),
    ("💀","Посмертна Детонація", "При смерті — шкода всім ворогам", "special"),
    ("🎯","Вбивство Слабких",    "+шкода якщо у ворога <50% HP",    "damage"),
    ("🔮","Дугова Каскад",       "Удар по 3 випадкових цілях",      "damage"),
    ("☠️","Зеркальний Біль",     "Шкода пропорційна атаці ворога",  "damage"),
    ("⚡","Праведна Помста",      "Атака росте коли втрачає HP",     "damage"),
    # ── МАСОВІ (10) ──────────────────────────────────────────────────────
    ("🌍","Землетрус",            "Трясе всіх ворогів",              "aoe"),
    ("💚","Масове Зцілення",      "Лікує всю армію",                 "aoe"),
    ("🦠","Чума",                 "Заражає всіх ворогів",            "aoe"),
    ("🌟","Зоряний Вибух",        "Шкода всім навколо",              "aoe"),
    ("🌪️","Торнадо",             "Збиває з ніг усіх",               "aoe"),
    ("🐉","Подих Дракона",        "Спалює весь ворожий ряд",         "aoe"),
    ("🧊","Крижаний Шторм",      "Заморожує всіх ворогів",          "aoe"),
    ("🧲","Магнетизм",            "Притягує і ламає всіх",           "aoe"),
    ("💥","Ядерна Хвиля",         "Знищує все у радіусі",            "aoe"),
    ("🌩️","Буря Блискавок",      "Б'є всіх ворогів",                "aoe"),
    # ── ДЕБАФИ / КОНТРОЛЬ (30) ──────────────────────────────────────────
    ("❄️","Заморозка",            "Сповільнює ворога",               "debuff"),
    ("☠️","Яд",                   "Отруює і слабить ворога",         "debuff"),
    ("😴","Летаргія",             "Вводить ворога в сон",            "debuff"),
    ("🕸️","Павутиння",           "Обплутує, ворог не може рухатись","debuff"),
    ("🫀","Сповільнення Серця",   "Зменшує атаку ворога вдвічі",    "debuff"),
    ("🌫️","Туман Невизначеності","Ворог б'є наосліп",               "debuff"),
    ("🪄","Прокляття",            "Ворог втрачає рівень",            "debuff"),
    ("🩻","Ослаблення Кісток",    "Зламана броня ворога",            "debuff"),
    # Deck Heroes — Контроль
    ("🧊","Крiостазис",           "Ворог пропускає повний хід",      "control"),
    ("⚡","Нейрошок",             "Заборона фіз. атаки",             "control"),
    ("🔇","Ментальний Блок",      "Заборона використання навичок",   "control"),
    ("⏰","Часова Петля",         "Відкладає вихід карти з руки",    "control"),
    ("🤪","Заклик Безумства",     "Ворог б'є своїх союзників",       "control"),
    ("⛓️","Кандали Приречених",  "Ціль отримує більше шкоди",       "control"),
    ("🔥","Тліюче Пепелище",      "Вогняна шкода в кінці ходу",      "debuff"),
    ("🔮","Просторова Кишеня",    "Прибирає карту з поля на 2 ходи", "control"),
    ("💪","Атрофія Сил",          "Знижує атаку ворога до мінімуму", "debuff"),
    ("🔇","Печать Тиші",          "Блокує магічні вміння",           "control"),
    ("👀","Погляд Горгони",        "Ціль невразлива але не діє",      "control"),
    ("🌑","Некроз Тканин",        "Знижує макс. HP ворога щохід",    "debuff"),
    ("🕹️","Нитки Ляльковода",    "Перехоплення контролю над картою","control"),
    ("🎲","Рулетка Долі",         "Випадковий негативний ефект",     "control"),
    ("🦠","Едка Іржа",            "Знищує броню цілі",               "debuff"),
    ("😶","Глибока Летаргія",     "Карта спить до отримання шкоди",  "control"),
    # ── ЛІКУВАННЯ / БАФФИ (30) ───────────────────────────────────────────
    ("🌿","Сила Трави",           "Лікує героя",                     "heal"),
    ("🌺","Квіткове Відродження", "Повільне лікування",              "heal"),
    ("🫧","Кристальна Роса",      "Миттєво відновлює HP",            "heal"),
    ("🧬","Регенерація",          "Тіло само себе лікує",            "heal"),
    # Deck Heroes — Зцілення/Баффи
    ("💡","Благодать",            "Лікує найбільш пошкодженого союзника","heal"),
    ("✨","Очисне Сяйво",         "Масове зцілення всіх союзників",  "heal"),
    ("🙏","Причастя",             "Зцілення самого героя",           "heal"),
    ("⚔️","Бойовий Дух",         "Бафф атаки героям тієї ж раси",  "buff"),
    ("💫","Останній Іспит",       "Смерть карти = бафф союзникам",  "buff"),
    ("📜","Поклик Предків",       "Повертає загиблу карту з кладовища","special"),
    ("🔥","Дух Фенікса",          "Шанс відродитись після смерті",   "special"),
    ("💪","Сигнал Авангарду",     "Тимчасовий бафф атаки при виході","buff"),
    ("🌀","Кругова Порука",       "Дає броню всім сусідам",          "buff"),
    ("⚡","Жага Помсти",          "Бафф шкоди після смерті союзника","buff"),
    ("💧","Сльози Богині",        "Постійне зцілення щохід",         "heal"),
    ("🦁","Левине Серце",         "Імунітет до ефектів страху",      "buff"),
    ("❄️","Морозна Мантія",       "Дає союзнику крижаний щит",       "buff"),
    ("💚","Абсолютна Чистота",    "Знімає всі дебаффи з союзників",  "special"),
    ("🎯","Фокус Чаклуна",        "Збільшує шкоду від здібностей",   "buff"),
    ("🩸","Кривавий Пакт",        "Бафф атаки за рахунок власного HP","buff"),
    ("🔄","Бойова Синергія",      "Лікує союзника при атаці",        "buff"),
    # ── ОСОБЛИВІ (30) ───────────────────────────────────────────────────
    ("🔮","Воскресіння",          "Воскрешає загиблого героя",       "special"),
    ("💀","Відродження",          "Повертає з мертвих",              "special"),
    ("🌑","Темна Діра",           "Поглинає ворожу атаку",           "special"),
    ("🛡️","Непробивний Щит",     "Блокує наступний удар",           "special"),
    ("🦅","Крило Орла",           "Ухиляється від удару",            "special"),
    ("🍀","Удача",                "Подвоює нагороду",                "special"),
    ("🌈","Веселкова Аура",       "Імунітет на 1 хід",               "special"),
    ("🕳️","Портал",              "Зникає і з'являється ззаду",      "special"),
    ("🌠","Зоряна Сила",          "Потроює наступний удар",          "special"),
    ("⏳","Зупинка Часу",         "Ворог пропускає хід",             "special"),
    ("🧿","Відбиття",             "Повертає удар ворогові",          "special"),
    ("🦁","Рев Лева",             "Лякає всіх ворогів",              "special"),
    ("🔑","Ключ Долі",            "Відкриває приховану силу",        "special"),
    ("🌙","Місячна Тінь",         "Атакує двічі непомітно",          "special"),
    # Deck Heroes — Особливі/Рідкісні
    ("🌑","Безодня",              "Миттєво вбиває незалежно від HP", "special"),
    ("🌀","Астральна Висилка",    "Виштовхує ворожу карту в колоду", "special"),
    ("🩸","Кривава Жатва",        "Висмоктує HP з усіх ворогів — лікує героя","aoe"),
    ("⚡","Небесний Гнів",        "Глобальна AOE + масовий контроль","aoe"),
    ("🪞","Дзеркальне Втілення",  "Точна копія карти ворога",        "special"),
    ("🌌","Чорна Діра",           "Миттєве вбивство цілі",           "special"),
    ("💀","Дотик Жнеця",          "Шанс вбити будь-яку карту миттєво","special"),
    ("🌑","Темна Жертва",         "Масова шкода + зцілення героя",   "aoe"),
    ("💫","Глобальний Колапс",    "Знищення всіх карт на полі",      "special"),
    ("👻","Безтілесний Дух",      "Невразливість до фіз. шкоди назавжди","special"),
    ("🧠","Викривлення Розуму",   "Ворог використовує здібність на себе","control"),
    ("⚗️","Деградація",           "Перетворює ворога на слабку Слиз","special"),
    ("🔄","Інверсія Параметрів",  "Міняє місцями атаку і HP ворога", "special"),
    ("💀","Смертельні Узи",       "Смерть однієї карти вбиває пов'язану","special"),
    ("🎲","Сліпий Шанс",          "Випадковий потужний ефект",       "special"),
    ("🌟","Вища Форма",           "Активує всі 3 навички одночасно", "special"),
    ("🔇","Мертва Тиша",          "Блокує всі пасивні навички ворогів","control"),
]

# ─────────────────────────────────────────────────────────────────────────────
# НОВА БОЙОВА МЕХАНІКА (Deck Heroes стиль)
# Порядок ходу:
#   1. Всі здібності (магія) карти спрацьовують ПЕРЕД фізичним ATK ударом
#   2. Потім наноситься фізичний ATK-удар
#   3. Після цього ворог б'є у відповідь
#   4. Усе це — один хід
# ─────────────────────────────────────────────────────────────────────────────

ABILITY_CHANCE_BY_STARS = {1: 1.0, 2: 1.0, 3: 1.0, 4: 1.0, 5: 1.0}  # 100% завжди

def create_super_human_card(sh_data):
    """Створює Card для раси Супер Люди.
    Карта потрапляє до МОЯ АРМІЯ БЕЗ здібностей.
    Здібності з'являються на рівнях 20, 40, 60, 80, 100 (по 1 за рівень-поріг).
    Прокачка лише через скормку у ЕВОЛЮЦІЯ РІВНЯ БОЖЕСТВ.
    """
    orig, name, lvl, shards, uah = sh_data
    c = Card(orig, 1)           # Завжди починається з 1 рівня
    c.name = name
    c.is_monster = False
    c.race = "Супер Люди"
    c.max_hp, c.attack = get_super_human_stats(1)
    c.hp   = c.max_hp
    c._super_human      = True
    c._super_human_xp   = 0    # XP для скормки (100 = +1 рівень)
    c._super_human_orig = orig
    c.divine_xp         = 0    # сумісно з DIVINE_LEVEL_EVO
    return c

def get_abilities_for_card(card):
    """Повертає список здібностей картки залежно від рангу/рівня."""
    # ── Супер Люди: здібності з'являються на рівнях 20,40,60,80,100 ──
    if getattr(card, '_arch_card', False):
        lvl = card.level
        if   lvl >= 100: stars = 5
        elif lvl >= 70:  stars = 4
        elif lvl >= 45:  stars = 3
        elif lvl >= 20:  stars = 2
        elif lvl >= 5:   stars = 1
        else:            return []
        seed = sum(ord(ch) for ch in card.original_name) + 13
        result = []
        arch_pool = ARCH_ABILITIES_NEW + ALL_ABILITIES
        used_i = set()
        i = 0
        while len(result) < stars:
            idx = (seed + i * 11 + i * i * 5) % len(arch_pool)
            if idx not in used_i:
                used_i.add(idx); result.append(arch_pool[idx])
            i += 1
        return result

    if getattr(card, '_super_human', False):
        lvl = card.level
        # Кількість здібностей залежно від рівня
        if   lvl >= 100: stars = 5
        elif lvl >= 80:  stars = 4
        elif lvl >= 60:  stars = 3
        elif lvl >= 40:  stars = 2
        elif lvl >= 20:  stars = 1
        else:            return []   # нижче 20 — без здібностей
        seed = sum(ord(ch) for ch in card.original_name)
        result = []
        sh_pool = SUPER_HUMAN_ABILITIES_NEW + ALL_ABILITIES
        used_i  = set()
        i = 0
        while len(result) < stars:
            idx = (seed + i*7 + i*i*3) % len(sh_pool)
            if idx not in used_i:
                used_i.add(idx); result.append(sh_pool[idx])
            i += 1
        return result

    rank_stars = {"НОВАЧОК": 0, "ВЕТЕРАН": 1, "МАЙСТЕР": 2, "ГЕРОЙ": 3,
                  "ЛЕГЕНДА": 4, "БОЖЕСТВО": 5, "БЕЗСМЕРТНИЙ": 5, "ВЕРХОВНИЙ": 5}
    stars = 5 if getattr(card, 'evolved', False) else rank_stars.get(card.rank, 0)
    if stars == 0:
        return []
    seed = sum(ord(c) for c in card.original_name)
    indices = []; used = set(); i = 0
    while len(indices) < stars:
        idx = (seed + i * 7 + i * i * 3) % len(ALL_ABILITIES)
        if idx not in used: used.add(idx); indices.append(idx)
        i += 1
    return [ALL_ABILITIES[idx] for idx in indices]

def _card_center(card):
    """Повертає центр карти (cx, cy) для ефектів."""
    if card and card.rect.width > 0:
        return int(card.rect.centerx), int(card.rect.centery)
    return 300, 300


def apply_ability(ability, hero, monster, all_heroes, all_enemies):
    """Застосовує здібність. Шкода НЕ вбиває одразу — залишає мінімум 1 HP."""
    emoji, name, desc, typ = ability
    rank_mult = {"НОВАЧОК":1.0,"ВЕТЕРАН":1.1,"МАЙСТЕР":1.2,"ГЕРОЙ":1.35,"ЛЕГЕНДА":1.6,"БОЖЕСТВО":2.0,"БЕЗСМЕРТНИЙ":2.5}
    mult = rank_mult.get(hero.rank, 1.0)
    # Базова шкода здібності — 30-50% від ATK (не вбиває одразу)
    base_dmg = max(1, int(hero.attack * mult * 0.40))
    # Рівень здібності дає бонус
    ab_level = getattr(hero, 'ability_levels', {}).get(name, 1)
    ab_bonus = 1.0 + (ab_level - 1) * 0.02   # +2% за рівень здібності

    hcx, hcy = _card_center(hero)
    mcx, mcy = _card_center(monster) if monster else (hcx + 200, hcy)

    def _deal(target, dmg):
        """Наносить шкоду, залишаючи мінімум 1 HP (не вбиває одразу)."""
        target.hp = max(1, target.hp - dmg)

    if typ == "damage":
        dmg = max(1, int(base_dmg * ab_bonus))
        if monster and monster.is_alive: _deal(monster, dmg)
        spawn_ability_effect(name, hcx, hcy, mcx, mcy)
        return f"{emoji} {name}: -{dmg} HP!"

    elif typ == "aoe":
        # AOE — знижена шкода, не вбиває одразу
        dmg = max(1, int(base_dmg * 0.35 * ab_bonus))  # 35% від base
        hit = 0
        for e in all_enemies:
            if e.is_alive: _deal(e, dmg); hit += 1
        spawn_ability_effect(name, hcx, hcy, mcx, mcy)
        return f"{emoji} {name}: -{dmg} × {hit} ворогів!"

    elif typ == "heal":
        heal = max(1, int(hero.max_hp * 0.08 * mult * ab_bonus))
        hero.hp = min(hero.max_hp, hero.hp + heal)
        spawn_ability_effect(name, hcx, hcy, hcx, hcy)
        return f"{emoji} {name}: +{heal} HP!"

    elif typ == "debuff":
        dmg = max(1, int(base_dmg * 0.6 * ab_bonus))
        if monster and monster.is_alive:
            _deal(monster, dmg)
            monster.attack = max(1, int(monster.attack * (0.85 - (ab_level - 1) * 0.005)))
        spawn_ability_effect(name, hcx, hcy, mcx, mcy)
        return f"{emoji} {name}: -{dmg} HP, ATK ворога -15%!"

    elif typ == "control":
        # Контроль — без прямої шкоди, але ослаблення
        if monster and monster.is_alive:
            monster.attack = max(1, int(monster.attack * 0.80))
        spawn_ability_effect(name, hcx, hcy, mcx, mcy)
        return f"{emoji} {name}: ворог ослаблений!"

    elif typ == "buff":
        buff_val = max(1, int(hero.attack * 0.08 * ab_bonus))
        hero.attack = int(hero.attack + buff_val)
        spawn_ability_effect(name, hcx, hcy, hcx, hcy)
        return f"{emoji} {name}: ATK +{buff_val}!"

    elif typ == "special":
        if name in ("Воскресіння","Відродження","Поклик Предків"):
            targets = [h for h in all_heroes if h.is_alive and h.hp < h.max_hp]
            if targets:
                t = min(targets, key=lambda h: h.hp / h.max_hp)
                heal = max(1, int(t.max_hp * 0.10 * mult * ab_bonus))
                t.hp = min(t.max_hp, t.hp + heal)
                spawn_ability_effect(name, hcx, hcy, hcx, hcy)
                return f"{emoji} {name}: {t.name} +{heal} HP!"
        elif name == "Масове Зцілення":
            h_val = max(1, int(hero.max_hp * 0.05 * mult * ab_bonus))
            for h in all_heroes:
                if h.is_alive: h.hp = min(h.max_hp, h.hp + h_val)
            spawn_ability_effect(name, hcx, hcy, hcx, hcy)
            return f"{emoji} {name}: +{h_val} HP всім!"
        elif name in ("Непробивний Щит","Відбиття","Крило Орла","Левине Серце","Морозна Мантія"):
            hero._shielded_until = time.time() + 3
            spawn_ability_effect(name, hcx, hcy, hcx, hcy)
            return f"{emoji} {name}: щит активовано!"
        elif name in ("Безодня","Чорна Діра","Дотик Жнеця","Глобальний Колапс",
                      "Деградація","Астральна Висилка"):
            # Замість миттєвого вбивства — велика шкода (60% HP але залишає 1)
            if monster and monster.is_alive:
                big_dmg = max(1, int(monster.hp * 0.40 * ab_bonus))
                _deal(monster, big_dmg)
                spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                return f"{emoji} {name}: -{big_dmg} HP (важкий удар)!"
        elif name == "Кривава Жатва":
            # AOE без вбивства + зцілення героя
            aoe_dmg = max(1, int(base_dmg * 0.30 * ab_bonus))
            total_heal = 0
            for e in all_enemies:
                if e.is_alive:
                    _deal(e, aoe_dmg)
                    total_heal += aoe_dmg // 2
            hero.hp = min(hero.max_hp, hero.hp + total_heal)
            spawn_ability_effect(name, hcx, hcy, mcx, mcy)
            return f"{emoji} {name}: -{aoe_dmg} AOE, +{total_heal} HP герою!"
        elif name == "Небесний Гнів":
            aoe_dmg = max(1, int(base_dmg * 0.28 * ab_bonus))
            for e in all_enemies:
                if e.is_alive: _deal(e, aoe_dmg)
            spawn_ability_effect(name, hcx, hcy, mcx, mcy)
            return f"{emoji} {name}: -{aoe_dmg} по всіх ворогах!"
        elif name == "Темна Жертва":
            aoe_dmg = max(1, int(base_dmg * 0.32 * ab_bonus))
            total_heal = 0
            for e in all_enemies:
                if e.is_alive: _deal(e, aoe_dmg); total_heal += aoe_dmg // 2
            hero.hp = min(hero.max_hp, hero.hp + total_heal)
            spawn_ability_effect(name, hcx, hcy, mcx, mcy)
            return f"{emoji} {name}: -{aoe_dmg} AOE, +{total_heal} HP!"
        else:
            dmg = max(1, int(base_dmg * 0.8 * ab_bonus))
            if monster and monster.is_alive: _deal(monster, dmg)
            spawn_ability_effect(name, hcx, hcy, mcx, mcy)
            return f"{emoji} {name}: {desc} (-{dmg})!"

    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
    return f"{emoji} {name}: активовано!"

def draw_ability_tooltip(surface, card, mouse_x, mouse_y, sw, sh):
    """Малює підказку зі здібностями при наведенні миші на карту."""
    abilities = get_abilities_for_card(card)
    if not abilities:
        return
    rank_stars = {"НОВАЧОК": 0, "ВЕТЕРАН": 1, "МАЙСТЕР": 2, "ГЕРОЙ": 3, "ЛЕГЕНДА": 4, "БОЖЕСТВО": 5}
    stars = rank_stars.get(card.rank, 0)
    lines = [f"{'★' * stars}  {card.name}  ({'Монстр' if card.is_monster else 'Герой'}, {card.rank})"]
    lines.append("")
    for em, nm, dsc, typ in abilities:
        type_icons = {"damage": "⚔️", "aoe": "💥", "heal": "💊", "debuff": "🌀", "special": "✨"}
        lines.append(f"{em} {nm}  [{type_icons.get(typ,'?')}]  — {dsc}")
    pad = 10
    line_h = 17
    panel_w = max(len(l) * 8 + pad * 2 for l in lines)
    panel_w = max(panel_w, 280)
    panel_h = len(lines) * line_h + pad * 2
    tx = mouse_x + 14
    ty = mouse_y - panel_h // 2
    if tx + panel_w > sw: tx = mouse_x - panel_w - 6
    if ty < 4: ty = 4
    if ty + panel_h > sh: ty = sh - panel_h - 4
    pygame.draw.rect(surface, (10, 10, 25), (tx, ty, panel_w, panel_h), border_radius=9)
    pygame.draw.rect(surface, GOLD, (tx, ty, panel_w, panel_h), 1, border_radius=9)
    for i, line in enumerate(lines):
        col = GOLD if i == 0 else (GRAY if line == "" else WHITE)
        surf = font_small.render(line, True, col)
        surface.blit(surf, (tx + pad, ty + pad + i * line_h))

def draw_stars(surface, stars, x, y, star_size=10):
    """Малює зірки здібностей у рядок."""
    colors = [(100,100,100), (150,220,50), (80,180,255), (255,140,0), (255,60,180), (255,215,0)]
    color = colors[min(stars, 5)]
    for i in range(min(stars, 5)):
        cx = x + i * (star_size + 2) + star_size // 2
        cy = y + star_size // 2
        pts = []
        import math
        for j in range(5):
            angle = math.pi / 2 + j * 2 * math.pi / 5
            pts.append((cx + int(star_size * 0.5 * math.cos(angle)),
                        cy - int(star_size * 0.5 * math.sin(angle))))
            angle2 = angle + math.pi / 5
            pts.append((cx + int(star_size * 0.22 * math.cos(angle2)),
                        cy - int(star_size * 0.22 * math.sin(angle2))))
        pygame.draw.polygon(surface, color, pts)

WHITE = (240, 240, 240)
BLACK = (15, 15, 15)
RED = (200, 30, 30)
GREEN = (30, 200, 30)
GOLD = (255, 215, 0)
BLUE = (50, 150, 255)
BROWN = (139, 69, 19)
DIAMOND_COL = (185, 242, 255)
CARD_BG = (40, 40, 60)
CARD_BORDER = (200, 200, 200)
HIGHLIGHT = (0, 255, 255)
GRAY = (100, 100, 100)

font = pygame.font.SysFont("Verdana", 18)
font_small = pygame.font.SysFont("Verdana", 12)
font_bold = pygame.font.SysFont("Verdana", 14, bold=True)
font_big = pygame.font.SysFont("Verdana", 36, bold=True)

# --- ЗАВАНТАЖЕННЯ ФОТО ДЛЯ ВКЛАДОК МЕНЮ (папка images/World) ---
def load_tab_image(filename, width, height):
    for ext in [".png", ".jpg", ".jpeg"]:
        path = os.path.join("images", "World", filename + ext)
        if os.path.exists(path):
            try:
                img = pygame.image.load(path).convert_alpha()
                return pygame.transform.smoothscale(img, (width, height))
            except:
                pass
    return None

# --- ЗАВАНТАЖЕННЯ ІКОНОК ГАМАНЦЯ (папка images/Icon) ---
def load_icon(filename, size=24):
    for ext in [".png", ".jpg", ".jpeg"]:
        path = os.path.join("images", "Icon", filename + ext)
        if os.path.exists(path):
            try:
                img = pygame.image.load(path).convert_alpha()
                return pygame.transform.smoothscale(img, (size, size))
            except:
                pass
    return None

# Іконки гаманця (завантажуємо один раз)
ICON_DIAMOND = load_icon("diamond")
ICON_GOLD    = load_icon("gold")
ICON_ORE     = load_icon("ore")
ICON_KEY     = load_icon("key")

MENU = "menu"
MY_ARMY = "my_army"
WORLDS = "worlds"
LEVELS = "levels"
BATTLE = "battle"
GAME_OVER    = "game_over"
LIVE_BATTLE      = "live_battle"    # вкладка "БИТВА В ЖИВУ"
BATTLE_HUB       = "battle_hub"     # вкладка "БИТВА" (хаб)
ARENA_STATE      = "arena_state"    # підвкладка "АРЕНА" (СИМ боти)
TOURNAMENT_STATE = "tournament"     # підвкладка "ТУРНИР ГЕРОЇВ" (онлайн PvP)
LB_SIM           = "lb_sim"         # підвкладка: симуляція онлайн
LB_LAN           = "lb_lan"         # підвкладка: LAN мультиплеєр
LB_ONLINE        = "lb_online"      # онлайн через PythonAnywhere

ONLINE_SERVER    = "https://bruice.pythonanywhere.com"  # сервер
ONLINE_POLL_INTERVAL = 1.5          # секунд між запитами стану
LB_LOBBY_HOST    = "lb_lobby_host"  # лобі: хост чекає гравця
LB_LOBBY_JOIN    = "lb_lobby_join"  # лобі: гравець приєднується
LB_BATTLE        = "lb_battle"      # бій у режимі БИТВА В ЖИВУ
CARDS_DIAMOND = "cards_diamond"
CARDS_GOLD = "cards_gold"
CARDS_ORE = "cards_ore"
MARKET = "market"
TITAN_MONSTERS = "titan_monsters"
TITAN_MONSTER_LOCS = "titan_monster_locs"
MY_SHARDS = "my_shards"
SETTINGS  = "settings"
GRAVEYARD = "graveyard"
SUPER_HUMAN_SHOP = "super_human_shop"   # Нова вкладка Ринку — Карти за Осколки 125 LVL
ARCH_SHOP     = "arch_shop"   # Вкладка Ринку — КАРТИ ЗА ОСКОЛКИ ІІ (Архангели та Архдемони)
LUCKY_HEROES  = "lucky_heroes"   # Вдача Героїв — хаб
ENCYCLOPEDIA  = "encyclopedia"    # Енциклопедія
MONOPOLY_STATE = "monopoly"       # Монополія
FORTUNA_STATE  = "fortuna"        # Фортуна

# ── РАСА «АРХАНГЕЛИ ТА АРХДЕМОНИ» — 20 карт ─────────────────────────────────
# Формат: (original_name, display_name, base_level, shard_cost, uah_price)
# uah_price на 10% дорожче ніж SH-карти; осколки 30-60 за логікою сили
ARCH_CARDS = [
    ("AR_Abel",         "Авель",                  1,  30, 122),
    ("AR_Castiel",      "Ангел Кастіїл",          8,  32, 135),
    ("AR_Saraqael",     "Сарафаїл",              15,  35, 148),
    ("AR_Samandriel",   "Самандріїл",            20,  37, 158),
    ("AR_Gabriel",      "Архангел Гавріїл",      28,  40, 172),
    ("AR_Raguel",       "Рагуїл",                35,  42, 183),
    ("AR_Remiel",       "Реміїл",                42,  44, 195),
    ("AR_Uriel",        "Уріїл",                 50,  46, 210),
    ("AR_Raphael",      "Рафаїл",                58,  48, 224),
    ("AR_Michael",      "Архангел Міхаїл",       68,  50, 242),
    ("AR_Crossroads",   "Демон Перехрестя",      22,  33, 163),
    ("AR_Ruby",         "Демон Рубі",            30,  38, 177),
    ("AR_Meg",          "Мег",                   38,  41, 188),
    ("AR_Azazel",       "Азазель",               48,  45, 205),
    ("AR_Crowley",      "Кроулі з Пекла",        60,  49, 228),
    ("AR_Alastair",     "Алестер",               72,  52, 248),
    ("AR_Lilith",       "Ліліт",                 85,  55, 272),
    ("AR_Abaddon",      "Аббадон",               95,  57, 295),
    ("AR_Lucifer",      "Люцифер",              115,  59, 330),
    ("AR_God",          "Господь Чак",          130,  60, 350),
]

# 25 нових здібностей «Архангели та Архдемони» з псевдо-3D (2.5D) ефектами
ARCH_ABILITIES_NEW = [
    # (emoji, name, desc, type)
    ("😇","Небесний Промінь",     "Стовп святого світла наносить шкоду всій лінії",       "aoe"),
    ("⚡","Блискавка Міхаїла",    "Миттєвий небесний розряд: критичний удар ×2.5",        "damage"),
    ("🌟","Аура Благодаті",       "Золота аура відновлює HP союзникам щоходу",            "heal"),
    ("🪽","Крила Бурі",           "Удар крилами відкидає ворога і глушить",               "control"),
    ("🔱","Тризуб Рафаїла",       "Три промені одночасно б'ють 3 цілі",                   "aoe"),
    ("📯","Сурма Апокаліпсису",   "АОЕ звукова хвиля приголомшує всіх ворогів",           "control"),
    ("💠","Кристал Раю",          "Щит із небесного кристалу блокує 50% шкоди",           "special"),
    ("🌈","Веселка Сарафаїла",    "Різнобарвні стріли б'ють 5 разів у випадкових ворогів","aoe"),
    ("✨","Святе Полум'я",        "Опалює демонів: ×3 шкода проти темних рас",            "damage"),
    ("🛡️","Щит Уріїла",          "Абсолютний захист на 1 хід, +20% ATK після",          "special"),
    ("🔥","Грішний Вогонь",       "Пекельний вогонь спалює ворога 3 ходи поспіль",        "debuff"),
    ("🩸","Кров'яний Договір",    "Підписує пакт: +40% ATK але -15% HP щохід",            "buff"),
    ("💀","Дотик Смерті",         "Миттєво знімає 30% HP незалежно від броні",            "damage"),
    ("🌑","Тіньова Тюрма",        "Ув'язнює ворога у тінь: він пропускає 2 ходи",         "control"),
    ("🐍","Зміїний Укус Ліліт",   "Отрута знімає 15% HP/хід протягом 5 ходів",            "debuff"),
    ("👑","Корона Пекла",         "Люцифер: +60% ATK всім союзникам на 2 ходи",           "buff"),
    ("🌪️","Демонічний Вихор",    "Пекельний торнадо б'є 4 рази по всіх ворогах",         "aoe"),
    ("⛓️","Ланцюги Пекла",       "Сковує ворога: ATK -50% на 3 ходи",                    "control"),
    ("🎭","Маска Кроулі",         "Зміна особистості: копіює найкращу здібність ворога",  "special"),
    ("🌊","Хвиля Темної Матерії", "Нищівна чорна хвиля знищує захист і щити",             "damage"),
    ("💥","Ядерний Гнів Божий",   "Господь: масивний вибух ×5 ATK проти всіх",            "aoe"),
    ("🔮","Пророцтво Рагуїла",    "Передбачає удар ворога, ухиляється і контратакує",     "special"),
    ("🌙","Місячний Серп Азазеля","Срібний серп: критична атака зі шансом смерті 15%",    "damage"),
    ("🕊️","Голуб Миру",          "Зцілює всю команду на 25% і знімає дебафи",            "heal"),
    ("⚖️","Суд Небес",            "Рагуїл: баланс сил — рівняє HP ворога з його ATK",     "special"),
]

# ── Призи «Вдача Героїв» — 16 слотів ───────────────────────────────────────
import time as _time_mod

LUCKY_PRIZES = [
    {"id":"hero",       "emoji":"🧝","name":"Герой Lv5",       "type":"hero",     "value":5},
    {"id":"monster",    "emoji":"👾","name":"Монстр Lv5",      "type":"monster",  "value":5},
    {"id":"gold",       "emoji":"💛","name":"1000 Золота",      "type":"gold",     "value":1000},
    {"id":"diamond",    "emoji":"💎","name":"500 Алмазів",      "type":"diamond",  "value":500},
    {"id":"ore",        "emoji":"⛏","name":"750 Руди",          "type":"ore",      "value":750},
    {"id":"key",        "emoji":"🗝","name":"1 Ключ",           "type":"key",      "value":1},
    {"id":"sh_shard",   "emoji":"🧩","name":"Осколок Супер Люди","type":"sh_shard","value":1},
    {"id":"arch_shard", "emoji":"💠","name":"Осколок Архангела","type":"arch_shard","value":1},
    {"id":"titan_shard","emoji":"⚔","name":"10 Осколків Титана","type":"titan_shard","value":10},
    {"id":"essence",    "emoji":"✨","name":"Есенція",           "type":"essence",  "value":1},
    {"id":"rune",       "emoji":"🔮","name":"Руна",              "type":"rune",     "value":1},
    {"id":"skip",       "emoji":"🚫","name":"Пропуск ходу",      "type":"skip",     "value":0},
    {"id":"rare_hero",  "emoji":"🦸","name":"Рідкісний Герой",   "type":"hero",     "value":15},
    {"id":"gold2",      "emoji":"🏆","name":"2000 Золота",       "type":"gold",     "value":2000},
    {"id":"diamond2",   "emoji":"🔷","name":"1000 Алмазів",      "type":"diamond",  "value":1000},
    {"id":"key3",       "emoji":"🔑","name":"3 Ключі",           "type":"key",      "value":3},
]

def apply_lucky_prize(game, prize):
    """Застосовує приз з Вдачі Героїв до стану гри."""
    t = prize["type"]; v = prize["value"]
    if t == "hero":
        c = Card(f"Герой {random.randint(1,5)}", v)
        game.all_heroes.append(c)
        return f"✅ Герой Lv{v} у МОЯ АРМІЯ!"
    elif t == "monster":
        c = Card(get_monster_name(random.randint(1,10), 0), v, True)
        game.all_heroes.append(c)
        return f"✅ Монстр Lv{v} у МОЯ АРМІЯ!"
    elif t == "gold":
        game.gold += v; return f"✅ +{v} золота!"
    elif t == "diamond":
        game.diamonds += v; return f"✅ +{v} алмазів!"
    elif t == "ore":
        game.ore += v; return f"✅ +{v} руди!"
    elif t == "key":
        game.keys += v; return f"✅ +{v} ключ(і)!"
    elif t == "sh_shard":
        _sh = random.choice(SUPER_HUMAN_CARDS)
        game.super_human_shards[_sh[0]] = game.super_human_shards.get(_sh[0], 0) + v
        return f"✅ +{v} осколок → {_sh[1]} (Супер Люди)!"
    elif t == "arch_shard":
        _ar = random.choice(ARCH_CARDS)
        game.arch_shards[_ar[0]] = game.arch_shards.get(_ar[0], 0) + v
        return f"✅ +{v} осколок → {_ar[1]} (Архангели)!"
    elif t == "titan_shard":
        if game.titan_id:
            game.titan_shards[game.titan_id] = game.titan_shards.get(game.titan_id, 0) + v
        return f"✅ +{v} осколків Титана!"
    elif t == "essence":
        game.my_essences.append({"name":"Есенція Фортуни","bonus":"atk","value":3,"level":1})
        return "✅ Есенція додана у МОЇ ЕСЕНЦІЇ!"
    elif t == "rune":
        game.my_runes.append({"name":"Руна Вдачі","level":1,"bonus":"hp","value":50})
        return "✅ Руна додана у МОЇ РУНИ!"
    elif t == "skip":
        return "🚫 Пропуск ходу — нічого не отримано"
    return "✅ Приз отримано!"

def create_arch_card(ar_data):
    """Створює Card для раси Архангели та Архдемони."""
    orig, name, lvl, shards, uah = ar_data
    c = Card(orig, 1)
    c.name = name
    c.is_monster = False
    c.race = "Архангели та Архдемони"
    c.max_hp, c.attack = get_arch_stats(1)
    c.hp   = c.max_hp
    c._arch_card      = True
    c._arch_xp        = 0    # XP для скормки (за рівень корм-карти = +xp)
    c._arch_orig      = orig
    c.divine_xp       = 0
    return c

def get_arch_stats(lvl):
    """Пропорційні стати для Архангелів/Архдемонів (1-130 рівень)."""
    max_hp = int(20 + lvl * 22 + (lvl // 10) * 100)
    attack = int(5  + lvl * 6  + (lvl // 10) * 25)
    return max_hp, attack


SH_WORLDS   = "sh_worlds"    # вкладка "СУПЕР ЛЮДЕЙ" — вибір світу
SH_LEVELS   = "sh_levels"    # вкладка "СУПЕР ЛЮДЕЙ" — вибір локації
AD_WORLDS   = "ad_worlds"    # вкладка "АНГЕЛИ ТА ДЕМОНИ" — вибір світу
AD_LEVELS   = "ad_levels"    # вкладка "АНГЕЛИ ТА ДЕМОНИ" — вибір локації

# ── Назви світів і локацій — АНГЕЛИ ТА ДЕМОНИ ───────────────────────────────
AD_WORLD_NAMES = [
    "Загублений Ангел",         # 1
    "Рід Кроулі",               # 2
    "Планета Демонів",          # 3
    "Монстри-Ангели",           # 4
    "Світ Апокаліпсису",        # 5
    "Небеса у Вогні",           # 6
    "Королівство Нефілімів",    # 7
    "Пекельна Ієрархія",        # 8
    "Зоряне Чистилище",         # 9
    "Розрив Небес",             # 10
    "Армія Сутінків",           # 11
    "Острів Падших",            # 12
    "Темний Рай",               # 13
    "Клітка Архангелів",        # 14
    "Всесвіт Скорботи",         # 15
    "Лабіринт Небес",           # 16
    "Сад Вічних Тортур",        # 17
    "Залізна Благодать",        # 18
    "Кінець Ієрархії",          # 19
    "Остання Битва Небес",      # 20
]

AD_LOCATION_NAMES = {
    1:  ["Небесний Схил","Логово Демонів","Крила Неба","Прах Ангела","Стріла Архангела",
         "Оазіс Пекла","Тьма в Раю","Пекло Кроулі","Дім Архангела","Очі Нефіліма"],
    2:  ["Вулиця Падших","Клан Кроулі","Схованка Демонів","Двір Темряви","Трон Нащадка",
         "Чорний Двір","Нічний Корпус","Сховище Зброї","Пастка Короля","Темна Спадщина"],
    3:  ["Вулканічна Пасть","Кратер Демонів","Лавові Нетрі","Пекельна Лабораторія","Демонічна Арена",
         "Фортеця Зла","Поле Темної Слави","Сірчані Болота","Острів Проклятих","Серце Планети"],
    4:  ["Небесні Нетрі","Ангельський Ліс","Облачний Лабіринт","Крилата Засідка","Зоряна Пастка",
         "Поле Пір'я та Вогню","Підмарево Ангелів","Гора Спокус","Водоспад Благодаті","Вершина Хаосу"],
    5:  ["Першa Печатка","Друга Печатка","Третя Печатка","Четверта Печатка","П'ята Печатка",
         "Шоста Печатка","Сьома Печатка","Арена Кінця Часів","Вершники Апокаліпсису","Фінал Творіння"],
    6:  ["Пожежа Небес","Пепелище Янголів","Обпалений Рай","Остання Хмара","Сходи у Вогні",
         "Арка Попелу","Небесний Бункер","Залізна Брама","Згорілий Трон","Серце Вогняного Неба"],
    7:  ["Нефілімські Рівнини","Гігантська Фортеця","Схрещення Двох Рас","Гібридна Арена","Кров Ангела",
         "Синтез Сил","Нефілімська Вежа","Поле Нечистих","Кістяний Міст","Зала Напівбогів"],
    8:  ["Першa Ступінь","Рада Демонів","Ієрархічна Пастка","Поверх Архідемонів","Відділ Судових Демонів",
         "Табір Вбивць","Корпус Помічників","Сховище Договорів","Трибунал Пекла","Трон Ієрархії"],
    9:  ["Сірі Береги","Тихе Чистилище","Шлях Спокути","Брід Між Світами","Туман Забуття",
         "Перехрестя Душ","Поле Рішень","Суд Останнього Слова","Брама Без Повернення","Нескінченне Чистилище"],
    10: ["Тріщина Небес","Бездонний Розрив","Щілина Між Вимірами","Просторовий Шрам","Нестабільна Зона",
         "Розлом Реальностей","Квантовий Рубець","Вузол Двох Сил","Точка Катастрофи","Остаточний Розрив"],
    11: ["Нічний Авангард","Сутінкова Рота","Бригада Тіней","Засідка Сутінків","Казарма Темряви",
         "Поле Нічної Битви","Опорний Пункт Зла","Лінія Сутінків","Темний Фронт","Штаб Армії Ночі"],
    12: ["Берег Вигнання","Острів Прокляттів","Затока Падших","Скелі Каяття","Підводна Тюрма",
         "Коралові Катакомби","Острів Забутих","Остання Бухта","Маяк Смутку","Архіпелаг Вигнанців"],
    13: ["Чорні Квіти Раю","Отруєний Сад","Смертельний Оазіс","Ліс Злого Раю","Брама Темного Раю",
         "Прихована Змія","Заборонений Плід","Луг Смерті","Джерело Проклять","Трон Темного Раю"],
    14: ["Камера Першого","Клітка Другого","Темниця Гавріїла","Сховище Рафаїла","Полон Уріїла",
         "Цитадель Міхаїла","Арсенал Заточення","Стіни Без Виходу","Зала Невільних Богів","Кінцева Клітка"],
    15: ["Поле Сліз","Долина Втраченого","Ріка Скорботи","Гора Жалю","Море Туги",
         "Безмовний Ліс","Місто Забутих Душ","Храм Смутку","Вічний Похорон","Серце Скорботи"],
    16: ["Перший Хід","Другий Коридор","Небесний Лабіринт","Перехрестя Рівнів","Блукаючий Поверх",
         "Зал Дзеркал","Пастка Маршрутів","Безкінечний Тунель","Завершальний Хід","Центр Лабіринту"],
    17: ["Квітучий Некрополь","Сад Вічної Муки","Красива Пастка","Пишний Жах","Ароматна Загроза",
         "Терновий Трон","Поле Шипів","Смертельний Квітник","Зала Вічного Болю","Серце Саду"],
    18: ["Залізний Параїз","Сталеві Небеса","Бронзові Ворота","Механічна Благодать","Металева Арена",
         "Сталевий Ангел","Залізний Демон","Фортеця Сплаву","Бункер Небес","Металеве Серце"],
    19: ["Останній Ангел","Кінець Демона","Фінальна Місія","Остаточна Жертва","Передфінальна Битва",
         "Зламана Ієрархія","Остання Молитва","Смерть Безсмертних","Межа Кінця","Ворота Фіналу"],
    20: ["Абсолютне Небо","Абсолютне Пекло","Зіткнення Крайнощів","Фінальна Дуель","Суд Творця",
         "Точка Omega","Нуль-Простір","Вузол Долі","Битва Першопочатку","Кінець Усього Сущого"],
}

# ── Назви світів (КАРТА СВІТІВ) ─────────────────────────────────────────────
WORLD_NAMES = [
    "Загублений Світ",    # 1
    "Потойбіччя",          # 2
    "Планета Гігантів",    # 3
    "Монстри-Льоди",       # 4
    "Світ Doom",           # 5
    "Королівство Тіней",  # 6
    "Пекельний Архіпелаг", # 7
    "Зоряний Некрополь",   # 8
    "Проклятий Едем",      # 9
    "Межа Безодні",        # 10
    "Кривавий Горизонт",   # 11
    "Острів Хаосу",        # 12
    "Темний Континент",    # 13
    "Клітка Богів",        # 14
    "Всесвіт Катастрофи",  # 15
    "Загробний Лабіринт",  # 16
    "Смертельний Сад",     # 17
    "Залізна Пустка",      # 18
    "Кінець Епохи",        # 19
    "Останній Рубіж",      # 20
]

# ── Назви локацій (10 на кожен світ, 20 світів) ─────────────────────────────
LOCATION_NAMES = {
    1:  ["Втрачена Пустеля","Логово Орків","Оазіс Османа","Мелісандра Пітьми","Темна Ліщина",
         "Мертвячина","Жереб Долі","Дивна Дяволщина","Пагорб Фавна","Брама Невідомого"],
    2:  ["Провалля Тіней","Болото Примар","Скелі Отруйників","Нетрі Прокляттів","Трясовина Смерті",
         "Печера Зомбі","Ліс Безголових","Руїни Старого Міста","Вежа Мороку","Ворота Пекла"],
    3:  ["Замерзлий Пік","Льодові Катакомби","Буря Вічного Холоду","Хребет Єті","Кратер Блискавок",
         "Ущелина Велетнів","Сніжна Пастка","Привидний Глетчер","Полюс Жаху","Зала Льодяних Богів"],
    4:  ["Лавові Рівнини","Вулканічна Арена","Базальтовий Лабіринт","Пекельний Котел","Жерло Вогню",
         "Сірчані Болота","Острів Попелу","Долина Магми","Криваве Джерело","Серце Вулкана"],
    5:  ["Кривавий Плацдарм","Руїни DOOM","Бункер Хаосу","Арсенал Демонів","Поле Нескінченної Битви",
         "Склад Прокляттів","Форт Безодні","Траншея Загибелі","Залізна Фортеця","Командний Пункт Пекла"],
    6:  ["Тіньовий Ліс","Схованка Вбивць","Нічна Арена","Алея Привидів","Чорний Ринок Тіней",
         "Підземний Клуб","Смугасте Болото","Дорога Прокляттів","Схил Забуття","Палац Темряви"],
    7:  ["Пілястри Архідемонів","Крига Безодні","Вулиця Смерті","Арка Хаосу","Поле Проклять",
         "Сховище Кісток","Брама Нечисті","Тронний Зал Демонів","Підземне Море Вогню","Цитадель Руйнування"],
    8:  ["Зоряне Поле","Руїни Цивілізації","Некрополь Зірок","Кратер Небес","Орбіта Загибелі",
         "Просторовий Розлом","Портал Смерті","Зоряна Фортеця","Темна Матерія","Зала Зоряних Богів"],
    9:  ["Едемський Сад","Отруєна Криниця","Ліс Спокус","Озеро Проклять","Храм Падших Янголів",
         "Луг Забуття","Квітучий Некрополь","Рай-Пастка","Гора Спокути","Брама Загубленого Раю"],
    10: ["Безодня Початку","Темна Щілина","Хаотичний Лабіринт","Нескінченна Прірва","Вузол Реальності",
         "Точка Зламу","Вихор Знищення","Злам Всесвіту","Остання Межа","Серце Темряви"],
    11: ["Кривавий Берег","Скеля Смерті","Затока Катастрофи","Морське Пекло","Підводний Некрополь",
         "Коралова Пастка","Вируюча Безодня","Острів Кісток","Бурхлива Прірва","Акваторія Загибелі"],
    12: ["Острів Прокляттів","Архіпелаг Тіней","Вулкан Острів","Туманний Архіпелаг","Бухта Хаосу",
         "Скелястий Некрополь","Острів Демонів","Піщаний Лабіринт","Маяк Смерті","Затока Кінця Світу"],
    13: ["Темна Саванна","Ліс Хижаків","Рівнина Скелетів","Долина Тіней","Ущелина Хаосу",
         "Пустельний Некрополь","Кам'яний Сад Смерті","Гірський Притулок Демонів","Степ Загибелі","Материкова Безодня"],
    14: ["Олімп Падших","Вершина Богів","Скеля Прокльону","Небесна Темниця","Хмарна Пастка",
         "Розряд Блискавок","Гора Мук","Зала Богів-Тиранів","Поле Небесної Битви","Трон Демонів-Богів"],
    15: ["Галактична Руїна","Зорепад Смерті","Астероїдна Пастка","Міжгалактична Битва","Зоряний Вихор",
         "Космічна Безодня","Планетарна Катастрофа","Темна Енергія","Зона Анігіляції","Кінець Всесвіту"],
    16: ["Підземне Царство","Корідори Забуття","Лабіринт Мертвих","Крипта Жахів","Склеп Вічності",
         "Зала Кісток","Покинутий Некрополь","Темний Тунель","Катакомби Хаосу","Трон Смерті"],
    17: ["Смертельний Квітник","Отруйний Луг","Ліс Павуків","Болото Загибелі","Гнила Діброва",
         "Смердючий Оазіс","Сад Смерті","Квіткове Пекло","Зелений Некрополь","Пастка Природи"],
    18: ["Залізний Бункер","Металева Пустеля","Фортеця Роботів","Завод Знищення","Залізна Клітка",
         "Арсенал Хаосу","Сталева Безодня","Механічний Некрополь","Завод-Пастка","Залізний Кінець"],
    19: ["Передостання Битва","Зламаний Часопростір","Агонія Епохи","Поле Армагеддону","Вихор Кінця",
         "Долина Апокаліпсису","Остання Надія","Зала Вічного Жаху","Межа Реальності","Ворота Кінця"],
    20: ["Абсолютна Прірва","Вершина Хаосу","Останній Бастіон","Серце Всесвіту","Фінальна Арена",
         "Зала Абсолюту","Точка Неповернення","Вузол Доль","Битва Творців","Кінець Усього"],
}

# ── Назви світів (СУПЕР ЛЮДЕЙ) ───────────────────────────────────────────────
SH_WORLD_NAMES = [
    "Зоряне Поле Героїв",  # 1
    "Мегаполіс Мутантів",  # 2
    "Лабораторія Хаосу",   # 3
    "Планета Супер-Сил",   # 4
    "Арена Богів",         # 5
    "Башта Нескінченності",# 6
    "Виміри Тіні",         # 7
    "Хроно-Лабіринт",      # 8
    "Нексус Руйнування",   # 9
    "Поле Квантової Битви",# 10
    "Цитадель Надлюдей",   # 11
    "Острів Генетичних Чудовиськ",# 12
    "Темний Всесвіт",      # 13
    "Галактика Хижаків",   # 14
    "Зоряна Тюрма",        # 15
    "Розлом Реальності",   # 16
    "Остання Надія Героїв",# 17
    "Пекельний Нексус",    # 18
    "Межа Двох Світів",    # 19
    "Абсолютний Фінал",    # 20
]

# ── Назви локацій (СУПЕР ЛЮДЕЙ, 20 світів × 10 локацій) ────────────────────
SH_LOCATION_NAMES = {
    1:  ["База Відступників","Тренувальна Арена","Покинута Лабораторія","Алея Мутантів","Смуга Перешкод",
         "Засекречений Бункер","Поле Радіації","Завод-Пастка","Сховище Сироваток","Зала Перших Героїв"],
    2:  ["Підземний Мегаполіс","Хімічний Завод","Цифрова Пастка","Нічне Гетто","Секретна Лабораторія",
         "Кіберарена","Токсична Зона","Нейромережа","Хакерська Нора","Корпоративна Фортеця"],
    3:  ["Відділ Генетики","Камера Трансформації","Зона Заражень","Резервуар Мутагену","Кімната Тестів",
         "Клітка Звірів","Хаотичний Реактор","Зона Дезактивації","Серце Лабораторії","Ядро Хаосу"],
    4:  ["Гравітаційна Пастка","Стрибок Через Простір","Зона Надваги","Поле Нульової Гравітації","Орбітальна Станція",
         "Кратер Надсили","Зоряна Платформа","Енергетичний Міст","Плаваючий Острів","Вершина Планети"],
    5:  ["Арена Богів","Олімп Хаосу","Поле Небесної Битви","Вежа Сутінок","Трибуна Безсмертних",
         "Схованка Тіней","Зала Переможців","Крига Богів","Смертельна Лотерея","Трон Ворогів"],
    6:  ["Нескінченний Коридор","Пастка Дзеркал","Поверх-Лабіринт","Залізний Ярус","Вишка Неба",
         "Павутина Рівнів","Сходова Клітка Смерті","Горизонт Безкінечності","Зламаний Ліфт","Верхівка Хаосу"],
    7:  ["Тіньовий Клон","Портал Подвійника","Дзеркальна Битва","Відбиття Смерті","Темний Двійник",
         "Тіньова Пастка","Зона Хаотичних Тіней","Брама Відображення","Лабіринт Дзеркал","Корінь Тіні"],
    8:  ["Зона Часового Розриву","Петля Смерті","Анахронізм","Битва Епох","Темпоральна Пастка",
         "Годинник Загибелі","Зворотний Відлік","Паузи Реальності","Минуле Як Зброя","Кінець Часу"],
    9:  ["Нексус Руйнування","Вузол Сили","Зала Дестабілізації","Точка Зламу","Хаотичний Центр",
         "Вир Знищення","Пульсар Смерті","Гравітаційна Сингулярність","Ядро Розкладу","Серце Нексусу"],
    10: ["Квантова Арена","Суперпозиція Смерті","Зона Переплетень","Квантова Пастка","Колапс Частинок",
         "Поле Невизначеності","Квантовий Лабіринт","Спін Долі","Хвильова Функція Бою","Квантовий Фінал"],
    11: ["Залізна Цитадель","Зала Обраних","Випробування Надлюдей","Корпус Еліти","Стіна Чемпіонів",
         "Форт Непереможних","Сховище Легенд","Поле Бойових Богів","Арсенал Надсили","Трон Цитаделі"],
    12: ["Клітка Чудовиськ","Поле Мутацій","Острів Химер","Лабораторія Жахів","Зона Генетичного Хаосу",
         "Пастка Еволюції","Арена Трансформацій","Корінь Мутації","Поле Генів","Острів Ізоляції"],
    13: ["Темний Всесвіт","Зона Ваккуму","Провалля Між Галактик","Темна Матерія","Поглинач Зірок",
         "Нульовий Простір","Чорна Діра Смерті","Зона Анігіляції","Горизонт Подій","Серце Темряви"],
    14: ["Космічний Мисливець","Зоряна Засідка","Галактичне Полювання","Орбітальна Пастка","Зоряна Сітка",
         "Планетарний Крейсер","Зірка-Зброя","Темний Пульсар","Поглинання Зірок","Кінець Галактики"],
    15: ["Камера Одиночного Ув'язнення","Клітка Надлюдей","Кімната Ізоляції","Тюрма Богів","Зал Покарань",
         "Поле Неволі","Темниця Нескінченності","Зона Заборон","Катівня Зірок","Остаточна Клітка"],
    16: ["Тріщина Реальності","Вузол Розломів","Нестабільна Зона","Геологічний Колапс","Простір Між Світами",
         "Зона Нестабільності","Квантовий Розлом","Міжвимірна Пастка","Точка Катастрофи","Злам Усього"],
    17: ["Остання База","Форт Надії","Зала Останніх Героїв","Поле Рятівників","Бункер Порятунку",
         "Дорога Виживання","Зона Фінального Удару","Міст Надії","Останній Рубіж Героїв","Ворота Порятунку"],
    18: ["Пекельний Портал","Зал Проклять","Нексус Темряви","Пекельна Засідка","Кратер Пекла",
         "Вогненна Пастка","Сірчаний Лабіринт","Диявольська Арена","Брама Нечисті","Трон Пекла"],
    19: ["Між Двома Вогнями","Битва Реальностей","Зона Зіткнення","Перетин Доль","Нестабільний Фронт",
         "Межа Двох Сил","Поле Зіткнень","Злам Балансу","Остання Лінія","Ворота Між Двома Світами"],
    20: ["Абсолютна Арена","Зала Фіналу","Поле Абсолютної Битви","Вузол Доль","Точка Omega",
         "Зона Нуль","Кімната Фінального Суду","Останній Крок","Межа Буття","Кінець Усього і Всіх"],
}

# ── РАСА «СУПЕР ЛЮДИ» — 25 карт ───────────────────────────────────────────
# Формат: (original_name, display_name, base_level, shard_cost, uah_price)
# base_level 1-125; shard_cost 1-30; uah_price 111-330
SUPER_HUMAN_CARDS = [
    # (original_name,           display_name,            lvl, shards, uah)
    ("SH_Dorian",    "Доріан Грей",          1,   1, 111),
    ("SH_Dracula",   "Владислав Дракула",   10,   3, 130),
    ("SH_TanSan",    "Тан Сан",             15,   4, 140),
    ("SH_WhiteHulk", "Білий Халк",          20,   5, 155),
    ("SH_Werewolf",  "Оборотень Джон",      25,   6, 165),
    ("SH_CrazySam",  "Несерйозний Сем",     30,   7, 175),
    ("SH_IronFist",  "Залізний Кулак",      35,   8, 185),
    ("SH_ShadowX",   "Тіньовий Ікс",        40,   9, 195),
    ("SH_BladeMax",  "Блейд Макс",          45,  10, 205),
    ("SH_StormKing", "Штормовий Король",    50,  11, 215),
    ("SH_MindBend",  "Ментальний Гнучкий",  55,  12, 222),
    ("SH_FireBorn",  "Вогнений Народжений", 60,  13, 230),
    ("SH_ArcLight",  "Дугове Світло",       65,  14, 238),
    ("SH_VoidWalk",  "Мандрівник Порожнечі",70,  16, 246),
    ("SH_AcidBite",  "Кислотний Укус",      75,  17, 254),
    ("SH_GravityX",  "Гравітаційний Ікс",   80,  18, 262),
    ("SH_PsyBlast",  "Псі-Вибух",           85,  19, 270),
    ("SH_TimeWarp",  "Часовий Спотворювач", 88,  20, 275),
    ("SH_NanoBot",   "Нанобот Хаос",        90,  21, 280),
    ("SH_PlasmaLrd", "Плазмовий Лорд",      95,  22, 288),
    ("SH_OmegaRex",  "Омега Рекс",         100,  24, 296),
    ("SH_AbyssLrd",  "Лорд Безодні",       105,  25, 305),
    ("SH_CosmicSage","Космічний Мудрець",  110,  27, 314),
    ("SH_VoidGod",   "Бог Порожнечі",      118,  29, 322),
    ("SH_OmniForce", "Омні Сила",          125,  30, 330),
]

# Унікальні здібності «Супер Люди» (30 нових + беремо 30 із ALL_ABILITIES)
SUPER_HUMAN_ABILITIES_NEW = [
    # (emoji, name, desc, type)
    ("🧬","Генна Мутація",      "Тіло мутує під час бою, +ATK за хід",          "buff"),
    ("🧠","Телепатичний Удар",  "Зчитує думки й б'є у слабке місце",             "damage"),
    ("💊","Сироватка Сили",     "Ін'єкція подвоює ATK на 1 хід",                 "buff"),
    ("🕶️","Режим Невидимки",   "Ухиляється від наступного удару",               "special"),
    ("⚙️","Кіберудар",          "Механічний кулак пробиває броню",               "damage"),
    ("🌀","Вихор Наночасток",   "Рій нанитів завдає AOE шкоди",                  "aoe"),
    ("🔋","Енергетичний Щит",   "Поглинає шкоду протягом 2 ходів",              "special"),
    ("🧪","Кислотний Плевок",   "Розчиняє броню й наносить DoT",                 "debuff"),
    ("🌩️","ЕМП Розряд",        "Паралізує механічних ворогів",                  "control"),
    ("🩺","Нано-Зцілення",      "Нанити відновлюють HP щоходу",                  "heal"),
    ("🎯","Точний Постріл",     "100% улучення, шкода ×1.5",                     "damage"),
    ("👁️","Ясновидіння",       "Бачить майбутній хід ворога, +50% уникання",    "special"),
    ("⚡","Надшвидкість",       "Б'є тричі за хід зі зменшеною шкодою",          "damage"),
    ("🔥","Плазмовий Струмінь", "Спалює і плавить ворога (AOE)",                  "aoe"),
    ("❄️","Кріо-Заморозка",    "Миттєва повна заморозка на 1 хід",               "control"),
    ("🌑","Тіньовий Клон",      "Створює копію що приймає шкоду",                "special"),
    ("🧲","Магнітна Пастка",    "Притягує і утримує ворога",                     "control"),
    ("💥","Термоядерний Удар",  "Масивний вибух що пошкоджує всіх",              "aoe"),
    ("🌊","Гідро-Волна",        "Потужний водяний потік збиває з ніг",            "damage"),
    ("🌪️","Торнадо-Копія",     "Вихор що б'є 3 рандомних ворогів",              "aoe"),
    ("🛡️","Адамантова Шкіра",  "Знижує всю шкоду на 40%",                       "special"),
    ("⏱️","Уповільнення Часу", "Ворог робить 1 удар замість 2",                  "control"),
    ("🔮","Просторовий Стрибок","Телепортується за спину ворога +ATK",           "damage"),
    ("👊","Мегаудар",           "Вкладає всю силу в 1 удар ×2",                  "damage"),
    ("🩸","Кров'яний Пакт",    "Жертвує HP для різкого ATK бафа",               "buff"),
    ("🌟","Зоряний Вибух ЕX",  "Фінальний вибух зірки AOE MAX",                 "aoe"),
    ("🤖","Нано-Рій",          "Рій роботів постійно атакує ворога",             "damage"),
    ("🎭","Маска Хаосу",       "Випадково змінює тип атаки кожен хід",           "special"),
    ("💫","Гіпер-Регенерація",  "Миттєво відновлює 30% HP",                      "heal"),
    ("🌈","Спектральний Щит",  "Захист від усіх типів шкоди на 1 хід",          "special"),
]

def get_super_human_stats(lvl):
    """Пропорційні стати для карт Супер Люди (1-125 рівень)."""
    max_hp = int(15 + lvl * 18 + (lvl//10)*80)
    attack = int(4  + lvl * 5  + (lvl//10)*20)
    return max_hp, attack

def get_super_human_abilities(card_original_name):
    """Повертає 5 здібностей для карти Супер Люди (50% нових, 50% з ALL_ABILITIES)."""
    idx = next((i for i,c in enumerate(SUPER_HUMAN_CARDS) if c[0]==card_original_name), 0)
    seed = sum(ord(ch) for ch in card_original_name)
    result = []
    # Перші 2-3 — нові здібності
    for i in range(3):
        ab_i = (seed + i*7 + i*i) % len(SUPER_HUMAN_ABILITIES_NEW)
        ab = SUPER_HUMAN_ABILITIES_NEW[ab_i]
        if ab not in result: result.append(ab)
    # Решта — із ALL_ABILITIES
    for i in range(5 - len(result)):
        ab_i = (seed + i*11 + 3) % len(ALL_ABILITIES)
        ab = ALL_ABILITIES[ab_i]
        if ab not in result: result.append(ab)
    return result[:5]
ALTAR = "altar"
ALTAR_HUB = "altar_hub"
ALTAR_ESSENCE = "altar_essence"   # Алтарь Есенція
MY_ESSENCES   = "my_essences"     # Мої Есенції (4×3 сітка)
ABILITY_EVO   = "ability_evo"     # Еволюція / Здібності — прокачка навиків
REMOVE_RUNE_STATE = "remove_rune"

# ── СИСТЕМА ЕСЕНЦІЙ ──────────────────────────────────────────────────────────
ESSENCE_COOLDOWN  = 3 * 3600   # 3 години в секундах
ESSENCE_XP_PER_CARD = 100      # скільки XP есенції = 1 рівень здібності карти

# Для кожної здібності — відповідна есенція (1-50 рівень)
# Назви есенцій відповідають здібностям
ESSENCE_NAMES = [
    # Атакуючі
    "Сутність Вогню",       # Удар Вогню
    "Сутність Води",        # Хвиля Води
    "Сутність Блискавки",   # Блискавка
    "Сутність Телекінезу",  # Телекінез
    "Сутність Подвійного Удару", # Подвійний Удар
    "Сутність Вампіра",     # Вампіризм
    "Сутність Вибуху",      # Вибух
    "Сутність Метеорита",   # Метеорит
    "Сутність Третього Ока",# Третє Око
    "Сутність Тіні",        # Тіньовий Клинок
    "Сутність Зграї",       # Зграя Вовків
    "Сутність Дракона",     # Укус Дракона
    "Сутність Стрілця",     # Стріла Долі
    "Сутність Лави",        # Лавовий Потік
    "Сутність Посейдона",   # Тризуб Посейдона
    # Deck Heroes атакуючі
    "Сутність Стихій",      # Елементальний Сплеск
    "Сутність Сталі",       # Вихр Сталі
    "Сутність Милосердя",   # Удар Милосердя
    "Сутність Руйнівника",  # Розколювач Щитів
    "Сутність Дзеркала",    # Дзеркальний Випад
    "Сутність Крові",       # Жага Крові
    "Сутність Несподіванки",# Ефект Несподіванки
    "Сутність Пронизування",# Проникаюче Лезо
    "Сутність Грому",       # Грозовий Розряд
    "Сутність Попелу",      # Сфера Попелу
    "Сутність Кристалу",    # Кристальний Шип
    "Сутність Кола",        # Круговий Замах
    "Сутність Прориву",     # Наскрізний Прокол
    "Сутність Жаги",        # Спрага Бою
    "Сутність Люті",        # Сліпа Лють
    "Сутність Пекла",       # Інфернальна Буря
    "Сутність Точності",    # Вивірений Постріл
    "Сутність Клинків",     # Шквал Клинків
    "Сутність Розрізу",     # Ослабляючий Розріз
    "Сутність Фантому",     # Фантомний Кидок
    "Сутність Смерті",      # Посмертна Детонація
    "Сутність Мисливця",    # Вбивство Слабких
    "Сутність Каскаду",     # Дугова Каскад
    "Сутність Болю",        # Зеркальний Біль
    "Сутність Помсти",      # Праведна Помста
    # Масові
    "Сутність Землі",       # Землетрус
    "Сутність Зцілення",    # Масове Зцілення
    "Сутність Чуми",        # Чума
    "Сутність Зірок",       # Зоряний Вибух
    "Сутність Торнадо",     # Торнадо
    "Сутність Подиху",      # Подих Дракона
    "Сутність Криги",       # Крижаний Шторм
    "Сутність Магніту",     # Магнетизм
    "Сутність Ядра",        # Ядерна Хвиля
    "Сутність Бурі",        # Буря Блискавок
    # Контроль/Дебафи
    "Сутність Морозу",      # Заморозка
    "Сутність Отрути",      # Яд
    "Сутність Сну",         # Летаргія
    "Сутність Павутини",    # Павутиння
    "Сутність Слабості",    # Сповільнення Серця
    "Сутність Туману",      # Туман Невизначеності
    "Сутність Прокляття",   # Прокляття
    "Сутність Костей",      # Ослаблення Кісток
    "Сутність Кріо",        # Крiостазис
    "Сутність Нейро",       # Нейрошок
    "Сутність Розуму",      # Ментальний Блок
    "Сутність Часу",        # Часова Петля
    "Сутність Безумства",   # Заклик Безумства
    "Сутність Ланцюга",     # Кандали Приречених
    "Сутність Пожежі",      # Тліюче Пепелище
    "Сутність Кишені",      # Просторова Кишеня
    "Сутність Атрофії",     # Атрофія Сил
    "Сутність Тиші",        # Печать Тиші
    "Сутність Горгони",     # Погляд Горгони
    "Сутність Некрозу",     # Некроз Тканин
    "Сутність Ляльки",      # Нитки Ляльковода
    "Сутність Рулетки",     # Рулетка Долі
    "Сутність Іржі",        # Едка Іржа
    "Сутність Летаргії",    # Глибока Летаргія
    # Зцілення/Баффи
    "Сутність Трави",       # Сила Трави
    "Сутність Квітки",      # Квіткове Відродження
    "Сутність Роси",        # Кристальна Роса
    "Сутність Регену",      # Регенерація
    "Сутність Благодаті",   # Благодать
    "Сутність Сяйва",       # Очисне Сяйво
    "Сутність Причастя",    # Причастя
    "Сутність Предків",     # Бойовий Дух / Поклик Предків
    "Сутність Жертви",      # Останній Іспит
    "Сутність Фенікса",     # Дух Фенікса
    "Сутність Авангарду",   # Сигнал Авангарду
    "Сутність Братства",    # Кругова Порука
    "Сутність Гніву",       # Жага Помсти
    "Сутність Богині",      # Сльози Богині
    "Сутність Лева",        # Левине Серце
    "Сутність Мантії",      # Морозна Мантія
    "Сутність Чистоти",     # Абсолютна Чистота
    "Сутність Фокусу",      # Фокус Чаклуна
    "Сутність Пакту",       # Кривавий Пакт
    "Сутність Синергії",    # Бойова Синергія
    # Особливі
    "Сутність Воскресіння", # Воскресіння
    "Сутність Відродження", # Відродження
    "Сутність Темряви",     # Темна Діра
    "Сутність Щита",        # Непробивний Щит
    "Сутність Орла",        # Крило Орла
    "Сутність Удачі",       # Удача
    "Сутність Аури",        # Веселкова Аура
    "Сутність Порталу",     # Портал
    "Сутність Зорі",        # Зоряна Сила
    "Сутність Зупинки",     # Зупинка Часу
    "Сутність Відбиття",    # Відбиття
    "Сутність Рева",        # Рев Лева
    "Сутність Ключа",       # Ключ Долі
    "Сутність Місяця",      # Місячна Тінь
    "Сутність Безодні",     # Безодня
    "Сутність Висилки",     # Астральна Висилка
    "Сутність Жнеця",       # Кривава Жатва
    "Сутність Небес",       # Небесний Гнів
    "Сутність Втілення",    # Дзеркальне Втілення
    "Сутність Колапсу",     # Глобальний Колапс
    "Сутність Духу",        # Безтілесний Дух
    "Сутність Викривлення", # Викривлення Розуму
    "Сутність Деградації",  # Деградація
    "Сутність Інверсії",    # Інверсія Параметрів
    "Сутність Уз",          # Смертельні Узи
    "Сутність Шансу",       # Сліпий Шанс
    "Сутність Форми",       # Вища Форма
    "Сутність Мовчання",    # Мертва Тиша
]

# Кольори есенцій за типом здібності
ESSENCE_COLORS = {
    "damage":  (255, 100, 40),
    "aoe":     (255, 60, 60),
    "debuff":  (100, 200, 80),
    "control": (80, 120, 220),
    "heal":    (60, 220, 140),
    "buff":    (255, 200, 40),
    "special": (180, 80, 255),
    "passive": (160, 160, 160),
}

# Прив'язка назви есенції → здібність (за індексом у ALL_ABILITIES)
def get_essence_for_ability(ability_name):
    """Повертає назву есенції що підходить до здібності."""
    for i, ab in enumerate(ALL_ABILITIES):
        if ab[1] == ability_name and i < len(ESSENCE_NAMES):
            return ESSENCE_NAMES[i]
    return ESSENCE_NAMES[0]  # fallback

def get_ability_for_essence(essence_name):
    """Повертає здібність що відповідає назві есенції."""
    if essence_name in ESSENCE_NAMES:
        idx = ESSENCE_NAMES.index(essence_name)
        if idx < len(ALL_ABILITIES):
            return ALL_ABILITIES[idx]
    return None

def _random_essence():
    """Генерує випадкову есенцію з рівнем 1-50."""
    name  = random.choice(ESSENCE_NAMES)
    level = random.choices(
        range(1, 51),
        weights=[max(1, 51 - i) for i in range(1, 51)]  # нижчі рівні частіші
    )[0]
    ab = get_ability_for_essence(name)
    typ = ab[3] if ab else "special"
    col = ESSENCE_COLORS.get(typ, (180, 80, 255))
    return {"name": name, "level": level, "type": typ, "col": col}

# ─────────────────────────────────────────────────────────────────────────────

# --- РУНИ ---
MY_RUNES       = "my_runes"
RUNE_ALTAR     = "rune_altar"
RUNE_ALTAR_DIA  = "rune_altar_dia"
RUNE_ALTAR_ORE  = "rune_altar_ore"
RUNE_ALTAR_GOLD = "rune_altar_gold"
EVOLUTION       = "evolution"
RUNE_EVOLUTION  = "rune_evolution"
DIVINE_EVO      = "divine_evo"        # Еволюція Божеств (100 рівень → еволюція)
DIVINE_LEVEL_EVO = "divine_level_evo" # Прокачка рівня Божеств (скормка карт)

DIVINE_EVO_COSTS = {
    "diamonds": 5000,
    "ore":      7500,
    "gold":     10000,
}
DIVINE_MAX_LEVEL    = 110   # максимальний рівень еволюціонованої карти (EvI)
DIVINE_REGEN_PER_LVL = 10  # реген ХП за хід на кожен рівень понад 100

SUPREME_EVO       = "supreme_evo"        # Еволюція Верховних Богів (110 → EvII)
SUPREME_LEVEL_EVO = "supreme_level_evo"  # Прокачка рівня Верховних (скормка)
SUPREME_MAX_LEVEL = 120   # максимальний рівень EvII карти
SUPREME_PRICE_UAH = 50    # ціна еволюції в гривнях
SUPREME_REGEN_PER_LVL = 5 # реген ХП за хід на кожен рівень понад 110 (на 120 = 100HP)
MONOBANK_API_TOKEN  = "m0NjcSazfoqE5AftAjlafmQ"  # Еквайринговий токен
MONOBANK_MERCHANT_TOKEN = "m0NjcSazfoqE5AftAjlafmQ"  # Еквайринговий токен
MONOBANK_CARD_ID    = "4441114405419480"  # Номер картки отримувача

ALTAR_COOLDOWN = 3600  # 1 година в секундах

# ── СИСТЕМА РУН ──────────────────────────────────────────────────
# Ціни витягування 5 рун
RUNE_PULL_COSTS = {"dia": 100, "ore": 500, "gold": 1000}
# Ймовірності рівнів 1–10 залежно від типу
RUNE_LEVEL_WEIGHTS = {
    "dia":  [2, 5, 8, 10, 12, 15, 15, 14, 12, 7],
    "ore":  [15, 15, 14, 13, 12, 10, 8, 6, 5, 2],
    "gold": [20, 18, 16, 14, 12, 8, 6, 4, 1, 1],
}

def _rune_random_level(pull_type):
    weights = RUNE_LEVEL_WEIGHTS[pull_type]
    total = sum(weights)
    r = random.randint(1, total)
    acc = 0
    for i, w in enumerate(weights):
        acc += w
        if r <= acc:
            return i + 1
    return 1

def _rune_random_name():
    ab = random.choice(ALL_ABILITIES)
    return ab[1]

def generate_runes(pull_type, count=5):
    runes = []
    for _ in range(count):
        name = _rune_random_name()
        level = _rune_random_level(pull_type)
        for_titan = random.random() < 0.20
        runes.append({"name": name, "level": level, "for_titan": for_titan})
    return runes

def rune_bonus_pct(rune_level):
    return rune_level

def runes_needed_for_upgrade(rune_level):
    return max(1, 11 - rune_level)

def rune_feed_progress(fed_rune_level):
    return fed_rune_level

def draw_rune_circle(surface, x, y, radius, rune_level, color=(130, 80, 220)):
    pygame.draw.circle(surface, color, (x, y), radius)
    pygame.draw.circle(surface, WHITE, (x, y), radius, 2)
    lv_s = font_small.render(str(rune_level), True, WHITE)
    surface.blit(lv_s, (x - lv_s.get_width()//2, y - lv_s.get_height()//2))

# --- ТИТАНИ ---
TITAN_STATE  = "titan"
TITANS_STATE = "titans"

TITANS_DATA = [
    # (id, name, description, color, shard_cost)
    ("titan_1",  "Залізний Страж",    "Непорушний захисник заліза",          (100,130,160), 0),
    ("titan_2",  "Вогняний Колос",    "Повелитель стародавнього вогню",      (200, 80, 20), 50),
    ("titan_3",  "Крижаний Велетень", "Замерзлий вартовий льодових земель",  ( 80,180,220), 80),
    ("titan_4",  "Тіньовий Архонт",   "Народжений у пітьмі безодні",         ( 60, 20,100), 100),
    ("titan_5",  "Золотий Титан",     "Скований із чистого небесного золота",(210,170,  0), 120),
    ("titan_6",  "Кам'яний Велетень", "Дух гір та скель",                    (110, 90, 70), 90),
    ("titan_7",  "Бурхливий Океан",   "Морська стихія в людській подобі",    ( 20,100,200), 110),
    ("titan_8",  "Громовержець",      "Несе блискавку богів",                (150,150,255), 130),
    ("titan_9",  "Пустельний Фенікс", "Відроджується з попелу пустелі",      (220,120, 30), 150),
    ("titan_10", "Лісовий Дух",       "Стародавній хранитель лісів",         ( 30,160, 60), 100),
    ("titan_11", "Небесний Суддя",    "Посланець богів небес",               (200,220,255), 170),
    ("titan_12", "Кривавий Жнець",    "Збирач душ на полі бою",              (180,  0, 20), 160),
    ("titan_13", "Темний Пророк",     "Бачить кінець усього живого",         ( 40, 10, 60), 180),
    ("titan_14", "Залізний Дракон",   "Дракон-механізм з давніх часів",      ( 80,100,100), 190),
    ("titan_15", "Сонячний Архангел", "Несе світло першого сонця",           (255,230,100), 200),
    ("titan_16", "Морозний Демон",    "Заморожує саму душу",                 ( 50,200,230), 210),
    ("titan_17", "Вулканічний Бог",   "Народжений у серці вулкана",          (230, 60,  0), 220),
    ("titan_18", "Квантовий Руйнівник","Розщеплює реальність",               (160,  0,200), 240),
    ("titan_19", "Зоряний Девастатор","Прийшов із далеких зірок",            (100,160,255), 260),
    ("titan_20", "Абсолютний Титан",  "Найсильніша сутність у всесвіті",     (255,215,  0), 300),
]

def get_titan_card_points(card):
    """Очки карти для армії Титана — залежать від рівня."""
    return max(1, card.level // 10 + 1)

def get_titan_army_limit(titan_level):
    """Максимальна сума очок армії залежно від рівня Титана.
    Lv1=26, Lv5=30, Lv10=35, кожен рівень +1 очко."""
    return 25 + titan_level

def get_titan_max_cards(titan_level):
    """Максимальна кількість карт у армії залежно від рівня Титана.
    Lv1=1, Lv2=2, Lv3=3, Lv4=4, Lv5-9=5, Lv10-19=6,
    Lv20-49=7, Lv50-99=8, Lv100+=8."""
    if titan_level <= 4:
        return titan_level
    elif titan_level <= 9:
        return 5
    elif titan_level <= 19:
        return 6
    elif titan_level <= 49:
        return 7
    else:
        return 8

def get_titan_clone_bonus(titan_level):
    """Штрафи/бонуси клона: починає з -35 HP/XP та -30 ATK, +1 з кожним рівнем Титана."""
    hp_xp_bonus = -35 + (titan_level - 1)   # Lv1=-35, Lv2=-34, ..., Lv36=+1...
    atk_bonus   = -30 + (titan_level - 1)   # Lv1=-30, Lv2=-29, ...
    return hp_xp_bonus, atk_bonus

# --- СУНДУКИ ---
CHESTS = [
    # (id,  label,       color_body,        color_lid,         cooldown_sec, reward_type, rw_min, rw_max, icon_key)
    ("diamond", "Алмази",  (30, 80, 200),    (60, 130, 255),    86400, "diamonds", 5,  150,  "diamond"),
    ("gold",    "Золото",   (180, 140, 0),    (255, 210, 0),     21600, "gold",     15, 450,  "gold"),
    ("ore",     "Руда",     (120, 120, 130),  (200, 200, 215),   43200, "ore",      10, 300,  "ore"),
    ("keys",    "Ключі",    (200, 80, 0),     (255, 140, 30),    172800,"keys",     1,  5,    "key"),
]


class Card:
    def __init__(self, name, level, is_monster=False):
        self.original_name = name
        self.name = RENAME_DICT.get(name, name)
        self.level = max(1, min(110, level))
        self.xp = 0
        self.max_xp = 10
        self.rank = self._get_rank(self.level)
        self.is_monster = is_monster
        self.evolved    = False
        self.evo_tier   = 0
        self.divine_xp  = 0
        self.ability_levels = {}   # {ability_name: level 1-100}
        self._update_stats()
        self.hp = self.max_hp
        self.is_alive = True
        self.selected = False
        self.offset_y = 0
        self.rect = pygame.Rect(0, 0, 0, 0)
        self.image = self._load_unique_image()

    def _update_stats(self):
        if getattr(self,'evo_tier',0) == 2 and self.level > DIVINE_MAX_LEVEL:
            # EvII: Верховний Бог 111-120
            _bonus2 = self.level - DIVINE_MAX_LEVEL
            self.max_hp = 10 + 99*12 + 10*50 + 10*150 + _bonus2*250
            self.attack = 3 + 99*4  + 10*15  + 10*50  + _bonus2*80
            self.regen  = _bonus2 * SUPREME_REGEN_PER_LVL
        elif getattr(self,'evolved',False) and self.level > 100:
            _bonus = self.level - 100
            self.max_hp = 10 + 99*12 + 10*50 + _bonus*150
            self.attack = 3 + 99*4  + 10*15  + _bonus*50
            self.regen  = _bonus * DIVINE_REGEN_PER_LVL
        else:
            self.max_hp = 10 + (self.level-1)*12 + (self.level//10)*50
            self.attack = 3  + (self.level-1)*4  + (self.level//10)*15
            self.regen  = 0
        self.rank = self._get_rank(self.level)

    def add_xp(self):
        # Супер Люди НЕ прокачуються у бою — лише через скормку
        if getattr(self, '_super_human', False):
            return
        if self.evolved:
            return
        if self.level < 100:
            self.xp += 1
            if self.xp >= self.max_xp:
                self.level += 1
                self.xp = 0
                self._update_stats()
                self.hp = self.max_hp

    def add_divine_xp(self, amount=1):
        """Прокачка еволюціонованої карти через скормку інших карт."""
        if not self.evolved or self.level >= DIVINE_MAX_LEVEL:
            return False
        self.divine_xp += amount
        xp_needed = 100  # базовий поріг (100 рівнів звичайної карти = 1 рівень Божества)
        if self.divine_xp >= xp_needed:
            self.divine_xp -= xp_needed
            self.level += 1
            self._update_stats()
            self.hp = min(self.hp + 500, self.max_hp)   # поповнюємо HP при підвищенні
            return True
        return False

    def _load_unique_image(self):
        unique_path = f"images/{self.original_name}.png"
        fallback_hero = "images/hero_default.png"
        fallback_monster = "images/monster_default.png"
        possible_paths = [
            unique_path,
            f"images/{self.original_name}.jpg",
            f"images/super_humans/{self.original_name}.png",
            f"images/super_humans/{self.original_name}.jpg",
        ]
        target_path = None
        for path in possible_paths:
            if os.path.exists(path):
                target_path = path
                break
        if not target_path:
            target_path = fallback_monster if self.is_monster else fallback_hero
        try:
            img = pygame.image.load(target_path).convert_alpha()
            return pygame.transform.smoothscale(img, (90, 70))
        except:
            return None

    def _get_rank(self, L):
        if L > DIVINE_MAX_LEVEL: return "ВЕРХОВНИЙ"
        if L > 100: return "БЕЗСМЕРТНИЙ"
        if L >= 100: return "БОЖЕСТВО"
        if L >= 81:  return "ЛЕГЕНДА"
        if L >= 61:  return "ГЕРОЙ"
        if L >= 41:  return "МАЙСТЕР"
        if L >= 21:  return "ВЕТЕРАН"
        return "НОВАЧОК"

    def draw(self, surface, x, y, w, h, grayscale=False):
        self.rect = pygame.Rect(x, y, w, h)
        self.offset_y *= 0.8
        rect_y = y + self.offset_y

        color = (60, 60, 100) if self.selected else CARD_BG
        if grayscale: color = (30, 30, 30)

        # --- Рамка еволюціонованої карти ---
        if self.evolved and not grayscale:
            _pulse = abs(math.sin(time.time() * 2.5))
            _gw = 4 + int(3 * _pulse)
            _gc = (255, int(200 + 55 * _pulse), int(30 * _pulse))
            # зовнішнє золоте сяйво
            for _t in range(_gw, 0, -1):
                _alpha_c = (min(255, _gc[0]), min(255, _gc[1]), min(255, _gc[2]))
                pygame.draw.rect(surface, _alpha_c,
                    (x - _t, rect_y - _t, w + _t*2, h + _t*2), 1, border_radius=12)
            border_col = (255, int(215 + 40 * _pulse), 0)
        else:
            border_col = HIGHLIGHT if self.selected else (RED if self.is_monster else GOLD if self.level > 40 else CARD_BORDER)
            if grayscale: border_col = GRAY

        if self.level >= 80 and not self.evolved:
            pygame.draw.rect(surface, GOLD, (x-2, rect_y-2, w+4, h+4), border_radius=12)

        pygame.draw.rect(surface, color, (x, rect_y, w, h), border_radius=10)
        pygame.draw.rect(surface, border_col, (x, rect_y, w, h), 5 if self.evolved else (3 if self.selected else 1), border_radius=10)

        # --- Мерехтливий фон для еволюціонованих ---
        if self.evolved and not grayscale:
            _shimmer = abs(math.sin(time.time() * 3.0 + x * 0.01))
            _shine_surf = pygame.Surface((w - 4, h - 4), pygame.SRCALPHA)
            _shine_surf.fill((255, 220, 0, int(18 * _shimmer)))
            surface.blit(_shine_surf, (x + 2, rect_y + 2))

        if self.image:
            img_w = int(w * 0.8)
            img_h = int(h * 0.4)
            scaled_img = pygame.transform.smoothscale(self.image, (img_w, img_h))
            surface.blit(scaled_img, (x + (w - img_w) // 2, rect_y + 40))

        rank_surf = font_small.render(self.rank, True, (255, 215, 0) if self.evolved else (GOLD if self.level > 40 else WHITE))
        name_surf = font_bold.render(self.name, True, WHITE)
        lvl_surf  = font_small.render(f"LVL: {self.level}", True, (255, 160, 0) if self.evolved else GREEN)

        surface.blit(rank_surf, (x + 5, rect_y + 5))
        surface.blit(lvl_surf,  (x + 5, rect_y + 22))
        surface.blit(name_surf, (x + (w - name_surf.get_width())//2, rect_y + h - 75))

        # --- Очки карти + значок еволюції ---
        pts_surf = font_bold.render(str(get_titan_card_points(self)), True, (80, 150, 255))
        _pts_x = x + w - pts_surf.get_width() - 4
        if self.evolved:
            _pts_x -= 18   # зсуваємо ліворуч щоб вмістити значок
        surface.blit(pts_surf, (_pts_x, rect_y + 4))
        # Значок еволюції "Ev" або "EvII"
        if self.evolved and not grayscale:
            _pulse2 = abs(math.sin(time.time() * 4.0))
            _tier = getattr(self,'evo_tier',1)
            _ev_label = "EvII" if _tier >= 2 else "Ev"
            _ic = (255, int(180+75*_pulse2), 0) if _tier < 2 else (255, int(100+155*_pulse2), int(20*_pulse2))
            _evo_s = font_small.render(_ev_label, True, _ic)
            surface.blit(_evo_s, (x + w - _evo_s.get_width() - 3, rect_y + 4))

        xp_ratio = max(0.0, min(1.0, self.xp / max(1, self.max_xp)))
        if self.evolved:
            # Прогрес-бар для divine_xp
            _dxp_ratio = max(0.0, min(1.0, self.divine_xp / 100.0))
            pygame.draw.rect(surface, (40, 40, 40),    (x + 5, rect_y + h - 55, w - 10, 12))
            pygame.draw.rect(surface, (200, 140, 0),   (x + 5, rect_y + h - 55, max(0, min(w-10, int((w - 10) * _dxp_ratio))), 12))
            xp_text = font_small.render(f"EVO XP: {self.divine_xp}/100", True, (255, 200, 80))
        else:
            pygame.draw.rect(surface, (40, 40, 40), (x + 5, rect_y + h - 55, w - 10, 12))
            pygame.draw.rect(surface, BLUE, (x + 5, rect_y + h - 55, max(0, min(w-10, int((w - 10) * xp_ratio))), 12))
            xp_text = font_small.render(f"XP: {int(xp_ratio*100)}%", True, WHITE)
        surface.blit(xp_text, (x + w//2 - xp_text.get_width()//2, rect_y + h - 58))

        hp_ratio = max(0.0, min(1.0, self.hp / max(1, self.max_hp)))
        pygame.draw.rect(surface, (100, 0, 0), (x + 5, rect_y + h - 35, w - 10, 12))
        _hp_bar_w = max(0, min(w - 10, int((w - 10) * hp_ratio)))
        pygame.draw.rect(surface, GREEN, (x + 5, rect_y + h - 35, _hp_bar_w, 12))

        hp_text  = font_small.render(f"{int(self.hp)}/{self.max_hp}", True, WHITE)
        atk_text = font_small.render(f"ATK: {self.attack}", True, RED)
        surface.blit(hp_text,  (x + w//2 - hp_text.get_width()//2, rect_y + h - 38))
        surface.blit(atk_text, (x + 5, rect_y + h - 22))

        # Реген для еволюціонованих
        if self.evolved and self.regen > 0 and not grayscale:
            regen_text = font_small.render(f"♻ {self.regen}HP/хід", True, (80, 255, 180))
            surface.blit(regen_text, (x + w - regen_text.get_width() - 4, rect_y + h - 22))

        # --- Зірки здібностей ---
        if not grayscale:
            abilities = get_abilities_for_card(self)
            stars = len(abilities)
            if stars > 0:
                star_size = max(7, min(10, w // 18))
                total_w = stars * (star_size + 2) - 2
                sx = x + (w - total_w) // 2
                sy = rect_y + h - 10
                draw_stars(surface, stars, sx, sy, star_size)

        # --- Руни на карті ---
        card_runes = getattr(self, "runes", [])
        if card_runes and not grayscale:
            img_bottom = int(rect_y + 40 + h * 0.4)
            name_top   = int(rect_y + h - 75)
            rune_y_center = (img_bottom + name_top) // 2
            r_radius = max(8, min(11, w // 16))
            total_rune_w = len(card_runes) * (r_radius * 2 + 3) - 3
            rune_x0 = x + (w - total_rune_w) // 2
            self.rune_rects_on_card = []
            for ri, rune in enumerate(card_runes):
                cx_r = rune_x0 + ri * (r_radius * 2 + 3) + r_radius
                draw_rune_circle(surface, cx_r, rune_y_center, r_radius, rune["level"])
                self.rune_rects_on_card.append(
                    pygame.Rect(cx_r - r_radius, rune_y_center - r_radius, r_radius*2, r_radius*2))


def draw_chest(surface, cx, cy, cw, ch, body_col, lid_col, is_open, label, reward_text, time_left, icon=None):
    """Малює сундук з анімацією відкриття, підписом та таймером."""
    lid_offset = -ch * 0.35 if is_open else 0
    # Тіло
    pygame.draw.rect(surface, body_col, (cx, cy + ch * 0.3, cw, ch * 0.7), border_radius=6)
    pygame.draw.rect(surface, (255, 255, 255), (cx, cy + ch * 0.3, cw, ch * 0.7), 2, border_radius=6)
    # Замок / пряжка
    lock_col = (220, 180, 0) if not is_open else (80, 80, 80)
    lx, ly = cx + cw // 2 - 6, int(cy + ch * 0.52)
    pygame.draw.rect(surface, lock_col, (lx, ly, 12, 10), border_radius=3)
    pygame.draw.arc(surface, lock_col, (lx + 1, ly - 7, 10, 12), 0, 3.14159, 3)
    # Кришка
    lid_y = int(cy + lid_offset)
    pygame.draw.rect(surface, lid_col, (cx - 3, lid_y, cw + 6, int(ch * 0.35)), border_radius=5)
    pygame.draw.rect(surface, (255, 255, 255), (cx - 3, lid_y, cw + 6, int(ch * 0.35)), 2, border_radius=5)
    # Іконка всередині якщо відкритий
    if is_open and icon:
        isize = min(cw - 10, 28)
        scaled = pygame.transform.smoothscale(icon, (isize, isize))
        surface.blit(scaled, (cx + (cw - isize) // 2, int(cy + ch * 0.38)))
    # Підпис
    lbl = font_small.render(label, True, (220, 220, 220))
    surface.blit(lbl, (cx + (cw - lbl.get_width()) // 2, cy + ch + 4))
    # Нагорода або таймер
    if reward_text:
        rs = font_small.render(reward_text, True, (80, 255, 80))
        surface.blit(rs, (cx + (cw - rs.get_width()) // 2, cy + ch + 20))
    elif time_left > 0:
        mins = time_left // 60
        hrs  = mins // 60
        mins = mins % 60
        secs = time_left % 60
        if hrs > 0: tstr = f"{hrs}г {mins:02d}хв"
        else:       tstr = f"{mins}хв {secs:02d}с"
        ts = font_small.render(tstr, True, (180, 180, 80))
        surface.blit(ts, (cx + (cw - ts.get_width()) // 2, cy + ch + 20))
    else:
        ready = font_small.render("Готово!", True, (80, 255, 80))
        surface.blit(ready, (cx + (cw - ready.get_width()) // 2, cy + ch + 20))

# ===== ЕФЕКТИ ЗНИЩЕННЯ КАРТ =====
class DeathParticle:
    """Одна частинка ефекту знищення картки."""
    __slots__ = ['x','y','vx','vy','color','size','life','max_life','shape']
    def __init__(self, x, y, vx, vy, color, size, life, shape='rect'):
        self.x, self.y = float(x), float(y)
        self.vx, self.vy = float(vx), float(vy)
        self.color = color
        self.size = size
        self.life = life
        self.max_life = life
        self.shape = shape  # 'rect', 'circle', 'drop', 'shard'

    def update(self):
        self.x += self.vx
        self.y += self.vy
        self.vy += 0.35   # гравітація
        self.vx *= 0.97
        self.life -= 1

    def draw(self, surface):
        ratio = self.life / self.max_life
        alpha = int(255 * ratio)
        s = max(1, int(self.size * ratio))
        r = min(255, self.color[0])
        g = min(255, self.color[1])
        b = min(255, self.color[2])
        col = (r, g, b)
        ix, iy = int(self.x), int(self.y)
        if self.shape == 'circle':
            pygame.draw.circle(surface, col, (ix, iy), s)
        elif self.shape == 'shard':
            pts = [(ix, iy - s*2), (ix + s, iy + s), (ix - s, iy + s//2)]
            pygame.draw.polygon(surface, col, pts)
        elif self.shape == 'drop':
            pygame.draw.circle(surface, col, (ix, iy), s)
            pygame.draw.circle(surface, col, (ix, iy - s), max(1, s//2))
        else:
            pygame.draw.rect(surface, col, (ix - s//2, iy - s//2, s, s))

import queue as _queue_module

death_effects = []   # глобальний список активних ефектів
_lan_msg_queue = _queue_module.Queue()   # черга повідомлень від LAN потоку до головного
fire_card_effects = []  # вогняні ефекти на рамках карт: {card, end_time, particles}
ice_effects   = []   # крижані ефекти (сосульки)
chain_effects = []   # ланцюгові ефекти
shield_effects= []   # щит-ефекти
grass_effects = []   # трав'яні ефекти
arrow_effects = []   # стрілові ефекти
ability_effects = [] # псевдо-3D ефекти здібностей: {type, cx, cy, t, life, data}

# ══ ЗДІБНОСТІ КОЖНОГО ТИТАНА ══
# Формат: {titan_id: (emoji, name, desc, trigger_hits, handler_key)}
# trigger_hits — список ходів коли спрацьовує
TITAN_ABILITIES = {
    "titan_1":  ("⚔️",  "Клон Стража",       "Клонує випадкового героя",        [3],            "clone"),
    "titan_2":  ("🔥",  "Вогняний Вибух",    "Б'є ворога з макс.HP вогнем",     [6,10,13],      "fire_strike"),
    "titan_3":  ("❄️",  "Льодяна Буря",      "Заморожує ворогів на 2 ходи",     [4,9],          "freeze_all"),
    "titan_4":  ("🌑",  "Тіньовий Удар",     "Атакує 2 рандомних ворогів",      [3,7,11],       "shadow_hit"),
    "titan_5":  ("⚡",  "Золотий Щит",       "Захищає героя від удару на 3 ходи",[5,12],         "shield"),
    "titan_6":  ("🪨",  "Земний Удар",       "Масовий удар по всіх ворогах",    [5,10],         "aoe_hit"),
    "titan_7":  ("🌊",  "Хвиля Відновлення", "Лікує всю армію",                 [4,9,14],       "mass_heal"),
    "titan_8":  ("⛓️",  "Ланцюги Грому",     "Сковує 3 ворогів, урон ×2",       [5,11],         "chain"),
    "titan_9":  ("🌺",  "Шлях Трави",        "Лікує героя від урону що завдав",[3,6,9,12],     "vamp"),
    "titan_10": ("🌿",  "Відродження Лісу",  "Воскрешає загиблого героя",       [7,14],         "revive"),
    "titan_11": ("☀️",  "Небесне Судилище",  "Знімає здібності монстрів 3 ходи",[6,13],         "strip"),
    "titan_12": ("🩸",  "Жнець Душ",         "Краде ХP ворога, дає герою",      [4,8,12],       "steal_xp"),
    "titan_13": ("👁️",  "Пророцтво",         "Уворот від ударів на 3 ходи",     [5,12],         "dodge"),
    "titan_14": ("🐉",  "Залізний Дракон",   "Призиває тінь карти-дракона",     [6],            "shadow_card"),
    "titan_15": ("✨",  "Сонячне Зцілення",  "Масове зцілення всіх героїв",     [3,7,11,15],    "mass_heal"),
    "titan_16": ("🧊",  "Морозний Демон",    "Заморожує ВСІХ ворогів на 3 ходи",[4,10],         "mega_freeze"),
    "titan_17": ("🌋",  "Виверження",        "Урон по 4 рандомних ворогах",     [4,8,12],       "volcano"),
    "titan_18": ("⚛️",  "Квантовий Розрив",  "Крадіж 2 карт монстрів в колоду", [6,12],         "steal_card"),
    "titan_19": ("🌠",  "Зоряні Стріли",     "5 стрілок по 5 різних ворогах",   [3,6,9,12,15],  "arrows"),
    "titan_20": ("💫",  "Абсолютна Сила",    "Усе одразу: зцілення+удар+клон",  [5,10,15],      "ultimate"),
}

def spawn_death_effect(cx, cy, stars):
    """Породжує ефект знищення залежно від кількості зірок атакуючої карти."""
    if stars == 0:
        return
    new_particles = []
    if stars == 1:
        # Кров: червоні краплі
        for _ in range(22):
            vx = random.uniform(-3.5, 3.5)
            vy = random.uniform(-6, 0)
            col = (random.randint(150,220), 0, random.randint(0,30))
            new_particles.append(DeathParticle(cx + random.randint(-30,30),
                cy + random.randint(-40,40), vx, vy,
                col, random.randint(4,10), random.randint(25,45), 'drop'))
    elif stars == 2:
        # Кислота: зелено-жовті краплі + splash
        for _ in range(28):
            vx = random.uniform(-4, 4)
            vy = random.uniform(-5, 1)
            col = (random.randint(80,160), random.randint(180,255), random.randint(0,60))
            new_particles.append(DeathParticle(cx + random.randint(-35,35),
                cy + random.randint(-40,40), vx, vy,
                col, random.randint(5,12), random.randint(30,55), 'circle'))
    elif stars == 3:
        # Розрив: шматки карти, темно-сірі осколки розлітаються
        for _ in range(35):
            angle = random.uniform(0, 6.28)
            speed = random.uniform(3, 9)
            vx = math.cos(angle) * speed
            vy = math.sin(angle) * speed - 3
            col = (random.randint(60,180), random.randint(60,120), random.randint(60,100))
            new_particles.append(DeathParticle(cx + random.randint(-20,20),
                cy + random.randint(-30,30), vx, vy,
                col, random.randint(6,16), random.randint(30,55), 'rect'))
    elif stars == 4:
        # Вогонь: помаранчево-червоні язики + чорний дим
        for _ in range(40):
            vx = random.uniform(-3, 3)
            vy = random.uniform(-9, -2)
            r = random.randint(200,255)
            g = random.randint(60,180)
            col = (r, g, 0)
            new_particles.append(DeathParticle(cx + random.randint(-40,40),
                cy + random.randint(-20,20), vx, vy,
                col, random.randint(6,14), random.randint(30,60), 'circle'))
        # дим
        for _ in range(15):
            vx = random.uniform(-1.5, 1.5)
            vy = random.uniform(-4, -1)
            g2 = random.randint(40,90)
            new_particles.append(DeathParticle(cx + random.randint(-30,30),
                cy - random.randint(0,20), vx, vy,
                (g2,g2,g2), random.randint(8,18), random.randint(20,40), 'circle'))
    elif stars == 5:
        # Вибух: яскраві осколки у всі боки + спалах
        for _ in range(60):
            angle = random.uniform(0, 6.28)
            speed = random.uniform(5, 16)
            vx = math.cos(angle) * speed
            vy = math.sin(angle) * speed
            cols = [(255,220,50),(255,100,0),(255,255,180),(200,80,255),(80,200,255)]
            col = random.choice(cols)
            new_particles.append(DeathParticle(cx, cy, vx, vy,
                col, random.randint(5,14), random.randint(35,65), 'shard'))
        # центральний спалах (великі кола що розширюються)
        for _ in range(10):
            vx = random.uniform(-1, 1)
            vy = random.uniform(-2, 2)
            new_particles.append(DeathParticle(cx, cy, vx, vy,
                (255,255,200), random.randint(12,28), random.randint(12,22), 'circle'))
    death_effects.extend(new_particles)

def spawn_ability_effect(ability_name, cx, cy, target_cx=None, target_cy=None):
    """Псевдо-3D (2.5D) ефект для кожної здібності окремо."""
    global ability_effects
    tcx = target_cx if target_cx is not None else cx + 200
    tcy = target_cy if target_cy is not None else cy
    now = time.time()

    def _fireball(n=20, col=(240,100,0), size_range=(8,22), spread=0.5):
        for _ in range(n):
            angle = random.uniform(-spread, spread)
            spd   = random.uniform(7, 15)
            dx = (tcx-cx)/max(1,abs(tcx-cx)+0.001)*spd*math.cos(angle)
            dy = math.sin(angle)*spd + random.uniform(-2,2)
            r,g,b = col
            ability_effects.append({"type":"fireball","cx":float(cx),"cy":float(cy),
                "dx":dx,"dy":dy,"size":random.randint(*size_range),
                "life":random.uniform(0.4,0.9),"born":now,
                "col":(r,g,b),"shadow_y":random.randint(4,10)})

    def _ring(col, max_r=80, n=20, delay=0):
        for i in range(n):
            ability_effects.append({"type":"ice_ring","cx":float(tcx),"cy":float(tcy),
                "r":10.0,"max_r":float(max_r),"born":now+delay,"life":0.7,
                "angle_off":i*2*math.pi/n,"col":col})

    def _shards(n=18, col=(200,200,80), grav=True, born_off=0):
        for _ in range(n):
            ang = random.uniform(0, 2*math.pi)
            spd = random.uniform(4, 13)
            ability_effects.append({"type":"shard","cx":float(tcx),"cy":float(tcy),
                "dx":math.cos(ang)*spd,"dy":math.sin(ang)*spd-(3 if grav else 0),
                "size":random.randint(5,14),"born":now+born_off,
                "life":random.uniform(0.4,0.8),"col":col,"shadow_y":4})

    def _bolt(col=(255,255,80)):
        sx,sy = float(cx),float(cy)
        ex,ey = float(tcx),float(tcy)
        steps = 14
        for i in range(steps):
            t_ = i/steps
            lx = sx+(ex-sx)*t_+random.uniform(-20,20)
            ly = sy+(ey-sy)*t_+random.uniform(-10,10)
            ability_effects.append({"type":"bolt_seg","x1":sx,"y1":sy,"x2":lx,"y2":ly,
                "born":now,"life":0.32,"col":col,"thick":random.randint(2,5)})
            sx,sy = lx,ly
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.25,"max_r":60.0,"col":col})

    def _shockwave(col=(255,200,60), delay=0, max_r=110):
        ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy)+10,
            "born":now+delay,"life":0.5,"max_r":float(max_r),"col":col})

    def _orbs(n=16, col=(80,255,120), on_hero=False):
        bx,by = (float(cx),float(cy)) if on_hero else (float(tcx),float(tcy))
        for i in range(n):
            ang = i*(2*math.pi/n)
            spd = random.uniform(2,6)
            ability_effects.append({"type":"heal_orb","cx":bx,"cy":by,
                "dx":math.cos(ang)*spd,"dy":math.sin(ang)*spd-4,
                "size":random.randint(6,15),"born":now,"life":random.uniform(0.5,1.1),
                "col":col,"shadow_y":5})
        ability_effects.append({"type":"flash","cx":bx,"cy":by,
            "born":now,"life":0.3,"max_r":50.0,"col":col})

    def _sparks(n=24, cols=None):
        if cols is None: cols=[(255,220,40),(255,80,200),(80,200,255),(80,255,80)]
        for i in range(n):
            ang = random.uniform(0,2*math.pi)
            r   = random.uniform(15,65)
            ability_effects.append({"type":"star_spark",
                "cx":float(tcx)+math.cos(ang)*r,
                "cy":float(tcy)+math.sin(ang)*r,
                "dx":random.uniform(-1,1),"dy":random.uniform(-3,-0.5),
                "size":random.randint(4,11),"born":now,
                "life":random.uniform(0.4,0.95),"col":random.choice(cols)})

    def _tornado(col=(190,220,255)):
        for i in range(32):
            t_ = i/32.0
            sr = 8+t_*60; sa = t_*7*math.pi
            ability_effects.append({"type":"tornado_seg",
                "cx":float(tcx)+math.cos(sa)*sr,
                "cy":float(tcy)+math.sin(sa)*sr*0.4 - t_*35,
                "born":now,"life":0.65+t_*0.35,"col":col,"size":int(5+t_*10)})

    def _shield_hex(col=(80,180,255)):
        for i in range(16):
            ability_effects.append({"type":"shield_hex","cx":float(cx),"cy":float(cy),
                "angle":i*2*math.pi/16,"r":52.0,"born":now,"life":0.85,"col":col})

    def _arrows(n=3, col=(255,220,80)):
        for i in range(n):
            ability_effects.append({"type":"arrow_proj",
                "sx":float(cx),"sy":float(cy)+i*12,
                "ex":float(tcx),"ey":float(tcy)+i*12,
                "born":now+i*0.09,"life":0.38,"col":col,"size":5,"shadow_y":3})

    def _poison(n=22, col=(80,220,40)):
        for _ in range(n):
            ability_effects.append({"type":"poison_drop",
                "cx":float(tcx)+random.randint(-45,45),
                "cy":float(tcy)+random.randint(-45,45),
                "dx":random.uniform(-1,1),"dy":random.uniform(-3,0),
                "size":random.randint(5,13),"born":now,
                "life":random.uniform(0.6,1.3),"col":col,"shadow_y":4})

    def _meteor(col=(255,110,20), size=24):
        ability_effects.append({"type":"meteor",
            "cx":float(tcx)-55,"cy":float(tcy)-130,
            "tx":float(tcx),"ty":float(tcy),
            "born":now,"life":0.52,"col":col,"size":size,"trail":[]})
        _shockwave(col=(255,200,60), delay=0.52, max_r=115)
        _shards(18, col=col, born_off=0.52)

    def _slash(n=5, col=(80,0,140)):
        for i in range(n):
            ability_effects.append({"type":"shadow_slash",
                "cx":float(tcx)+i*14,"cy":float(tcy),
                "born":now+i*0.045,"life":0.42,
                "col":col,"size":42-i*6})

    def _wave(n=3, col=(60,140,255)):
        for i in range(n):
            ability_effects.append({"type":"wave",
                "cx":float(cx),"cy":float(cy),
                "tx":float(tcx),"ty":float(tcy),
                "born":now+i*0.13,"life":0.55,"col":col,"width":6-i*2})

    # ═══════════════════════════════════════════════════════════
    # УНІКАЛЬНІ ЕФЕКТИ ДЛЯ КОЖНОЇ ЗДІБНОСТІ
    # ═══════════════════════════════════════════════════════════

    # ── АТАКУЮЧІ ──────────────────────────────────────────────
    if ability_name == "Удар Вогню":
        _fireball(26, col=(255,80,0), size_range=(10,26))
        _shockwave(col=(255,120,0), delay=0.3, max_r=70)

    elif ability_name == "Хвиля Води":
        _wave(4, col=(40,120,255))
        _ring(col=(80,160,255), max_r=65)

    elif ability_name == "Блискавка":
        _bolt(col=(255,255,60))
        # додатковий розряд
        for _ in range(3):
            ability_effects.append({"type":"bolt_seg",
                "x1":float(tcx)+random.randint(-30,30),"y1":float(tcy)-60,
                "x2":float(tcx),"y2":float(tcy),
                "born":now+random.uniform(0,0.1),"life":0.22,
                "col":(200,220,255),"thick":2})

    elif ability_name == "Телекінез":
        # Фіолетові кільця що стискаються навколо цілі
        for i in range(4):
            ability_effects.append({"type":"shockwave",
                "cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.08,"life":0.4,"max_r":float(90-i*15),
                "col":(180,80,255)})
        _sparks(12, cols=[(200,80,255),(140,40,200)])

    elif ability_name == "Подвійний Удар":
        for i in range(2):
            _shards(14, col=(255,180,40), born_off=i*0.15)
            ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.15,"life":0.2,"max_r":45.0,"col":(255,220,80)})

    elif ability_name == "Вампіризм":
        # Темно-червоні кулі летять від цілі до героя
        for i in range(12):
            ang = random.uniform(0, 2*math.pi)
            spd = random.uniform(5, 10)
            ability_effects.append({"type":"heal_orb",
                "cx":float(tcx),"cy":float(tcy),
                "dx":(cx-tcx)/max(1,abs(cx-tcx))*spd+math.cos(ang),
                "dy":(cy-tcy)/max(1,abs(cy-tcy)+0.001)*spd+math.sin(ang),
                "size":random.randint(7,16),"born":now,
                "life":random.uniform(0.5,0.9),"col":(180,0,40),"shadow_y":4})

    elif ability_name == "Вибух":
        _shards(30, col=(255,140,0))
        _shockwave(col=(255,100,0), max_r=130)
        _fireball(15, col=(255,60,0), size_range=(12,28))

    elif ability_name == "Метеорит":
        _meteor(col=(255,120,20), size=26)

    elif ability_name == "Третє Око":
        # Золоте сяйво + промені
        _sparks(20, cols=[(255,215,0),(255,200,80),(255,255,160)])
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.4,"max_r":55.0,"col":(255,215,0)})
        for ang in [0, math.pi/4, math.pi/2, 3*math.pi/4, math.pi,
                    5*math.pi/4, 3*math.pi/2, 7*math.pi/4]:
            ability_effects.append({"type":"bolt_seg",
                "x1":float(tcx),"y1":float(tcy),
                "x2":float(tcx)+math.cos(ang)*55,"y2":float(tcy)+math.sin(ang)*55,
                "born":now,"life":0.28,"col":(255,215,0),"thick":3})

    elif ability_name == "Тіньовий Клинок":
        _slash(6, col=(60,0,120))
        for _ in range(8):
            ability_effects.append({"type":"shard",
                "cx":float(tcx)+random.randint(-20,20),
                "cy":float(tcy)+random.randint(-20,20),
                "dx":random.uniform(-5,5),"dy":random.uniform(-8,-2),
                "size":random.randint(4,10),"born":now,
                "life":random.uniform(0.3,0.6),"col":(100,20,200),"shadow_y":3})

    elif ability_name == "Зграя Вовків":
        # 3 хвилі стріл (зграя)
        for i in range(3):
            _arrows(1, col=(180,140,80))
            for _ in range(6):
                ability_effects.append({"type":"shard",
                    "cx":float(tcx)+random.randint(-30,30),
                    "cy":float(tcy)+random.randint(-20,20),
                    "dx":random.uniform(-4,4),"dy":random.uniform(-5,-1),
                    "size":random.randint(4,9),"born":now+i*0.12,
                    "life":random.uniform(0.3,0.6),"col":(150,100,50),"shadow_y":3})

    elif ability_name == "Укус Дракона":
        # Два «зубця» — двокольорові спалахи
        for i in range(2):
            ability_effects.append({"type":"flash",
                "cx":float(tcx)+(i*2-1)*18,"cy":float(tcy),
                "born":now+i*0.1,"life":0.3,"max_r":40.0,"col":(255,80,0)})
        _shards(20, col=(200,50,0))
        _fireball(10, col=(255,40,0), size_range=(8,18))

    elif ability_name == "Стріла Долі":
        _arrows(5, col=(255,215,0))
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now+0.35,"life":0.3,"max_r":40.0,"col":(255,255,180)})

    elif ability_name == "Лавовий Потік":
        _fireball(30, col=(255,60,0), size_range=(12,30), spread=0.8)
        _shockwave(col=(200,60,0), max_r=100)
        for _ in range(10):
            ability_effects.append({"type":"shard",
                "cx":float(tcx)+random.randint(-50,50),
                "cy":float(tcy)+random.randint(-10,10),
                "dx":random.uniform(-3,3),"dy":random.uniform(-2,2),
                "size":random.randint(6,16),"born":now+random.uniform(0,0.3),
                "life":random.uniform(0.5,1.0),"col":(80,30,0),"shadow_y":5})

    elif ability_name == "Тризуб Посейдона":
        # 3 хвилі зсунуті
        for off in [-20, 0, 20]:
            ability_effects.append({"type":"wave",
                "cx":float(cx),"cy":float(cy)+off,
                "tx":float(tcx),"ty":float(tcy)+off,
                "born":now,"life":0.5,"col":(40,140,255),"width":5})
        _ring(col=(80,180,255), max_r=75)

    # ── МАСОВІ ────────────────────────────────────────────────
    elif ability_name == "Землетрус":
        _shockwave(col=(180,120,40), max_r=150)
        _shockwave(col=(140,90,20), max_r=100, delay=0.15)
        _shards(28, col=(140,100,50))
        for _ in range(12):
            ability_effects.append({"type":"shard",
                "cx":float(tcx)+random.randint(-80,80),
                "cy":float(tcy)+random.randint(-20,20),
                "dx":random.uniform(-3,3),"dy":random.uniform(-6,-1),
                "size":random.randint(8,20),"born":now+random.uniform(0,0.2),
                "life":random.uniform(0.5,1.0),"col":(100,70,30),"shadow_y":6})

    elif ability_name == "Масове Зцілення":
        _orbs(20, col=(80,255,120), on_hero=True)
        _ring(col=(80,255,150), max_r=90)

    elif ability_name == "Чума":
        _poison(30, col=(60,200,0))
        _ring(col=(80,180,0), max_r=80)
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.35,"max_r":60.0,"col":(80,200,0)})

    elif ability_name == "Зоряний Вибух":
        _sparks(40, cols=[(255,220,40),(255,80,0),(255,255,180),(200,80,255),(80,200,255)])
        _shockwave(col=(255,200,100), max_r=130)
        _shards(20, col=(255,180,0))

    elif ability_name == "Торнадо":
        _tornado(col=(180,210,255))
        _ring(col=(200,220,255), max_r=70)

    elif ability_name == "Подих Дракона":
        _fireball(35, col=(200,50,0), size_range=(10,28), spread=0.9)
        _fireball(15, col=(255,140,0), size_range=(6,16), spread=0.6)
        _shockwave(col=(255,80,0), max_r=120)

    elif ability_name == "Крижаний Шторм":
        _ring(col=(140,210,255), max_r=100)
        _ring(col=(200,235,255), max_r=70, delay=0.12)
        _shards(25, col=(180,230,255))
        for _ in range(15):
            ability_effects.append({"type":"shard",
                "cx":float(tcx)+random.randint(-70,70),
                "cy":float(tcy)+random.randint(-50,50),
                "dx":random.uniform(-5,5),"dy":random.uniform(-8,-2),
                "size":random.randint(4,10),"born":now+random.uniform(0,0.2),
                "life":random.uniform(0.4,0.8),"col":(200,240,255),"shadow_y":2})

    elif ability_name == "Магнетизм":
        # Концентричні кільця що звужуються
        for i in range(5):
            ability_effects.append({"type":"shockwave",
                "cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.07,"life":0.5,
                "max_r":float(120-i*20),"col":(80,180,255)})
        _sparks(16, cols=[(80,160,255),(40,100,200)])

    elif ability_name == "Ядерна Хвиля":
        _shockwave(col=(200,255,80), max_r=160)
        _shockwave(col=(255,255,100), max_r=110, delay=0.1)
        _shards(35, col=(200,255,80))
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.4,"max_r":80.0,"col":(220,255,120)})
        _fireball(18, col=(180,255,0), size_range=(8,20))

    elif ability_name == "Буря Блискавок":
        for i in range(5):
            tcx2 = tcx + random.randint(-60,60)
            tcy2 = tcy + random.randint(-30,30)
            sx2,sy2 = float(cx),float(cy)
            for j in range(10):
                t_ = j/10
                lx = sx2+(tcx2-cx)*t_+random.uniform(-18,18)
                ly = sy2+(tcy2-cy)*t_+random.uniform(-10,10)
                ability_effects.append({"type":"bolt_seg",
                    "x1":sx2,"y1":sy2,"x2":lx,"y2":ly,
                    "born":now+i*0.06,"life":0.28,"col":(255,240,80),"thick":random.randint(1,4)})
                sx2,sy2 = lx,ly

    # ── ДЕБАФИ ────────────────────────────────────────────────
    elif ability_name == "Заморозка":
        _ring(col=(140,210,255), max_r=75)
        _shards(18, col=(180,230,255))
        # Крижані кристали
        for _ in range(10):
            ability_effects.append({"type":"shard",
                "cx":float(tcx)+random.randint(-35,35),
                "cy":float(tcy)+random.randint(-35,35),
                "dx":0,"dy":random.uniform(-2,0),
                "size":random.randint(6,14),"born":now,
                "life":random.uniform(0.7,1.2),"col":(200,240,255),"shadow_y":2})

    elif ability_name == "Яд":
        _poison(28, col=(60,200,0))
        _ring(col=(40,180,0), max_r=65)

    elif ability_name == "Летаргія":
        # Синьо-фіолетові спіральні кільця
        _tornado(col=(120,80,200))
        _sparks(14, cols=[(100,60,180),(140,100,220),(60,40,140)])

    elif ability_name == "Павутиння":
        # Сіре павутиння
        for i in range(8):
            ang = i*math.pi/4
            for j in range(4):
                r = 15+j*15
                ability_effects.append({"type":"shard",
                    "cx":float(tcx)+math.cos(ang)*r,
                    "cy":float(tcy)+math.sin(ang)*r*0.5,
                    "dx":0,"dy":0,"size":4,"born":now,
                    "life":0.9,"col":(160,160,160),"shadow_y":1})
        _ring(col=(140,140,140), max_r=65)

    elif ability_name == "Сповільнення Серця":
        # Пульсуючі червоно-сині кола
        for i in range(3):
            ability_effects.append({"type":"shockwave",
                "cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.2,"life":0.5,"max_r":float(50+i*20),
                "col":(180,20,40)})
        _sparks(10, cols=[(180,20,40),(80,20,140)])

    elif ability_name == "Туман Невизначеності":
        # Сірий туман — багато розмитих кіл
        for _ in range(30):
            ability_effects.append({"type":"heal_orb",
                "cx":float(tcx)+random.randint(-50,50),
                "cy":float(tcy)+random.randint(-50,50),
                "dx":random.uniform(-1,1),"dy":random.uniform(-1,0.5),
                "size":random.randint(10,25),"born":now,
                "life":random.uniform(0.7,1.4),"col":(140,140,160),"shadow_y":4})

    elif ability_name == "Прокляття":
        # Чорні кістки + фіолетова аура
        _slash(4, col=(80,0,100))
        _ring(col=(100,0,140), max_r=70)
        _sparks(14, cols=[(100,0,160),(60,0,100),(180,0,200)])

    elif ability_name == "Ослаблення Кісток":
        _shards(20, col=(180,160,120))
        _shockwave(col=(150,130,100), max_r=80)
        # Тріщини — косі лінії
        for _ in range(6):
            x1 = tcx+random.randint(-40,40); y1 = tcy+random.randint(-40,40)
            ability_effects.append({"type":"bolt_seg",
                "x1":float(x1),"y1":float(y1),
                "x2":float(x1+random.randint(-25,25)),
                "y2":float(y1+random.randint(-25,25)),
                "born":now,"life":0.5,"col":(200,180,140),"thick":2})

    # ── ЛІКУВАЛЬНІ ────────────────────────────────────────────
    elif ability_name == "Сила Трави":
        _orbs(16, col=(60,220,80), on_hero=True)
        _sparks(12, cols=[(60,200,60),(120,255,100),(40,160,40)])

    elif ability_name == "Квіткове Відродження":
        # Рожеві пелюстки
        _sparks(20, cols=[(255,140,180),(255,100,140),(200,80,120),(255,180,200)])
        _orbs(10, col=(255,120,160), on_hero=True)
        _ring(col=(255,160,200), max_r=60)

    elif ability_name == "Кристальна Роса":
        # Блакитні краплі зверху
        for _ in range(20):
            ability_effects.append({"type":"heal_orb",
                "cx":float(cx)+random.randint(-40,40),
                "cy":float(cy)-random.randint(20,70),
                "dx":random.uniform(-0.5,0.5),"dy":random.uniform(4,8),
                "size":random.randint(5,12),"born":now,
                "life":random.uniform(0.4,0.8),"col":(140,220,255),"shadow_y":3})
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now+0.4,"life":0.25,"max_r":50.0,"col":(180,240,255)})

    elif ability_name == "Регенерація":
        _orbs(18, col=(40,255,100), on_hero=True)
        _ring(col=(60,220,80), max_r=55)
        for _ in range(8):
            ability_effects.append({"type":"shard",
                "cx":float(cx)+random.randint(-30,30),
                "cy":float(cy),"dx":0,
                "dy":random.uniform(-5,-2),
                "size":random.randint(4,9),"born":now+random.uniform(0,0.3),
                "life":random.uniform(0.4,0.8),"col":(80,255,120),"shadow_y":2})

    # ── ОСОБЛИВІ ──────────────────────────────────────────────
    elif ability_name == "Воскресіння":
        _sparks(28, cols=[(255,255,255),(220,200,255),(180,160,255),(140,120,200)])
        _ring(col=(200,180,255), max_r=80)
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now,"life":0.5,"max_r":70.0,"col":(220,200,255)})

    elif ability_name == "Відродження":
        _sparks(24, cols=[(255,215,0),(255,180,0),(200,140,0)])
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now,"life":0.45,"max_r":65.0,"col":(255,220,80)})
        _ring(col=(255,200,0), max_r=75)

    elif ability_name == "Темна Діра":
        # Чорна діра що всмоктує — кільця що звужуються + темні частки
        for i in range(6):
            ability_effects.append({"type":"shockwave",
                "cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.06,"life":0.45,
                "max_r":float(100-i*12),"col":(40,0,60)})
        _sparks(20, cols=[(60,0,80),(100,0,120),(140,0,160),(20,0,40)])

    elif ability_name == "Непробивний Щит":
        _shield_hex(col=(80,180,255))
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now,"life":0.35,"max_r":60.0,"col":(120,200,255)})

    elif ability_name == "Крило Орла":
        # Білі пера + золотий спалах
        _sparks(18, cols=[(255,255,255),(200,240,255),(255,215,0)])
        _shield_hex(col=(255,215,0))
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now,"life":0.3,"max_r":55.0,"col":(255,255,200)})

    elif ability_name == "Удача":
        _sparks(30, cols=[(255,215,0),(80,255,80),(255,80,200),(80,200,255),(255,255,80)])
        _ring(col=(255,215,0), max_r=80)

    elif ability_name == "Веселкова Аура":
        rainbow = [(255,0,0),(255,120,0),(255,255,0),(0,255,0),(0,120,255),(120,0,255)]
        for i,col in enumerate(rainbow):
            ability_effects.append({"type":"ice_ring",
                "cx":float(cx),"cy":float(cy),
                "r":float(10+i*8),"max_r":float(50+i*10),
                "born":now+i*0.06,"life":0.8,"angle_off":i*0.4,"col":col})

    elif ability_name == "Портал":
        # Фіолетова діра + вихід з іншого боку
        _tornado(col=(140,40,200))
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now,"life":0.35,"max_r":55.0,"col":(160,60,220)})
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now+0.2,"life":0.35,"max_r":55.0,"col":(200,100,255)})

    elif ability_name == "Зоряна Сила":
        _sparks(35, cols=[(255,255,180),(255,220,80),(255,180,0),(200,140,0)])
        _shockwave(col=(255,200,40), max_r=100)
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.4,"max_r":65.0,"col":(255,220,100)})

    elif ability_name == "Зупинка Часу":
        # Синьо-білі кільця що розходяться, потім все завмирає
        for i in range(4):
            ability_effects.append({"type":"shockwave",
                "cx":float(tcx),"cy":float(tcy),
                "born":now+i*0.1,"life":0.5,
                "max_r":float(40+i*30),"col":(180,220,255)})
        _sparks(16, cols=[(200,230,255),(255,255,255),(140,180,220)])

    elif ability_name == "Відбиття":
        _shield_hex(col=(255,140,0))
        # Ефект відбиття назад
        _shards(16, col=(255,160,40))
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),
            "born":now+0.3,"life":0.3,"max_r":50.0,"col":(255,180,80)})

    elif ability_name == "Рев Лева":
        # Оранжева ударна хвиля + золоті іскри
        _shockwave(col=(255,140,0), max_r=140)
        _shockwave(col=(200,100,0), max_r=90, delay=0.1)
        _sparks(22, cols=[(255,140,0),(255,200,80),(200,100,0)])

    elif ability_name == "Ключ Долі":
        _sparks(20, cols=[(255,215,0),(255,180,40),(200,140,0)])
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now,"life":0.45,"max_r":60.0,"col":(255,215,0)})
        _ring(col=(255,200,0), max_r=70)

    elif ability_name == "Місячна Тінь":
        # Срібний + темно-синій
        _slash(4, col=(30,30,80))
        _sparks(16, cols=[(180,200,255),(140,160,220),(80,80,160)])
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),
            "born":now+0.15,"life":0.3,"max_r":50.0,"col":(200,220,255)})
        _slash(4, col=(50,50,120))

    # ── Deck Heroes — Атакуючі ────────────────────────────────────────────
    elif ability_name == "Елементальний Сплеск":
        _t = random.choice(["fire","ice","bolt"])
        if _t=="fire": _fireball(22,col=(255,80,0))
        elif _t=="ice": _ring(col=(140,220,255),max_r=75); _shards(14,col=(180,235,255))
        else: _bolt(col=(200,255,80))
    elif ability_name == "Вихр Сталі":
        for ang in [i*math.pi/3 for i in range(6)]:
            for r in [20,40,60]:
                ability_effects.append({"type":"shard","cx":float(tcx)+math.cos(ang)*r,
                    "cy":float(tcy)+math.sin(ang)*r*0.5,"dx":math.cos(ang+math.pi/2)*5,
                    "dy":math.sin(ang+math.pi/2)*3-2,"size":random.randint(6,14),"born":now,
                    "life":random.uniform(0.4,0.8),"col":(200,210,230),"shadow_y":3})
        _shockwave(col=(180,200,220),max_r=80)
    elif ability_name == "Удар Милосердя":
        _shards(22,col=(255,80,80)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":70.0,"col":(255,120,120)})
        for _ in range(8): ability_effects.append({"type":"bolt_seg","x1":float(tcx)+random.randint(-20,20),"y1":float(tcy)-50,"x2":float(tcx),"y2":float(tcy),"born":now+random.uniform(0,0.1),"life":0.2,"col":(255,100,100),"thick":3})
    elif ability_name == "Розколювач Щитів":
        _shards(24,col=(180,160,120)); _shockwave(col=(220,200,150),max_r=90)
        for _ in range(5): ability_effects.append({"type":"bolt_seg","x1":float(tcx)+random.randint(-40,40),"y1":float(tcy)+random.randint(-40,40),"x2":float(tcx),"y2":float(tcy),"born":now,"life":0.3,"col":(255,200,100),"thick":4})
    elif ability_name == "Дзеркальний Випад":
        _slash(4,col=(100,200,255)); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.3,"max_r":50.0,"col":(150,220,255)}); _arrows(2,col=(100,200,255))
    elif ability_name in ("Жага Крові","Спрага Бою"):
        for i in range(6): ability_effects.append({"type":"fireball","cx":float(cx),"cy":float(cy),"dx":random.uniform(-3,3),"dy":random.uniform(-8,-2),"size":random.randint(6,14),"born":now+i*0.06,"life":random.uniform(0.4,0.7),"col":(200,0,30),"shadow_y":4})
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.3,"max_r":45.0,"col":(200,0,40)})
    elif ability_name == "Ефект Несподіванки":
        _shards(30,col=(255,200,0)); _shockwave(col=(255,215,0),max_r=120)
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":80.0,"col":(255,220,80)}); _sparks(20,cols=[(255,200,0),(255,150,0),(255,255,100)])
    elif ability_name == "Проникаюче Лезо":
        _slash(5,col=(200,220,255))
        for i in range(3): ability_effects.append({"type":"bolt_seg","x1":float(cx),"y1":float(cy),"x2":float(tcx)+(i-1)*30,"y2":float(tcy),"born":now+i*0.06,"life":0.3,"col":(180,210,255),"thick":3})
    elif ability_name == "Грозовий Розряд": _bolt(col=(180,220,255)); _ring(col=(200,230,255),max_r=50)
    elif ability_name == "Сфера Попелу":
        _fireball(18,col=(140,100,60),size_range=(8,18))
        if random.random()<0.4: _fireball(10,col=(255,80,0),size_range=(6,14))
    elif ability_name == "Кристальний Шип":
        _shards(20,col=(180,235,255)); _ring(col=(140,210,255),max_r=60)
        if random.random()<0.4: _ring(col=(200,240,255),max_r=40,delay=0.1)
    elif ability_name == "Круговий Замах": _shockwave(col=(200,200,200),max_r=100); _shards(20,col=(200,210,230))
    elif ability_name == "Наскрізний Прокол":
        _slash(3,col=(200,200,255)); ability_effects.append({"type":"bolt_seg","x1":float(cx),"y1":float(cy),"x2":float(tcx)+80,"y2":float(tcy),"born":now,"life":0.35,"col":(200,200,255),"thick":5})
    elif ability_name == "Сліпа Лють":
        _shards(28,col=(255,50,50)); _shockwave(col=(200,0,0),max_r=100); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":75.0,"col":(255,80,80)})
    elif ability_name == "Інфернальна Буря": _fireball(35,col=(200,40,0),size_range=(10,26),spread=1.2); _shockwave(col=(255,60,0),max_r=160)
    elif ability_name == "Вивірений Постріл": _arrows(7,col=(255,215,0)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+0.4,"life":0.3,"max_r":50.0,"col":(255,255,180)})
    elif ability_name == "Шквал Клинків":
        for i in range(4): _slash(3,col=(200+i*10,200+i*5,220))
        _shards(18,col=(210,215,230))
    elif ability_name == "Ослабляючий Розріз": _slash(3,col=(120,80,160)); _poison(10,col=(120,80,160))
    elif ability_name == "Фантомний Кидок":
        _sparks(16,cols=[(200,200,255),(150,150,220),(100,100,200)]); ability_effects.append({"type":"flash","cx":float(tcx)+100,"cy":float(tcy),"born":now,"life":0.3,"max_r":50.0,"col":(180,180,255)})
    elif ability_name == "Посмертна Детонація": _shockwave(col=(200,40,0),max_r=150); _shards(30,col=(200,80,0)); _fireball(20,col=(200,60,0))
    elif ability_name == "Вбивство Слабких": _shards(18,col=(220,80,40)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.3,"max_r":55.0,"col":(255,100,60)})
    elif ability_name == "Дугова Каскад":
        for i in range(3):
            sx2,sy2=float(cx),float(cy); ex2=float(tcx)+random.randint(-60,60); ey2=float(tcy)+random.randint(-30,30)
            for j in range(8):
                t_=j/8; lx=sx2+(ex2-cx)*t_+random.uniform(-15,15); ly=sy2+(ey2-cy)*t_+random.uniform(-10,10)
                ability_effects.append({"type":"bolt_seg","x1":sx2,"y1":sy2,"x2":lx,"y2":ly,"born":now+i*0.08,"life":0.28,"col":(255,220,60),"thick":2}); sx2,sy2=lx,ly
    elif ability_name == "Зеркальний Біль":
        for i in range(5): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.07,"life":0.4,"max_r":float(30+i*20),"col":(180,80,200)})
    elif ability_name == "Праведна Помста": _sparks(18,cols=[(255,100,0),(255,150,50),(200,80,0)]); _fireball(10,col=(255,120,0),size_range=(8,16))

    # ── Deck Heroes — Контроль ──────────────────────────────────────────
    elif ability_name == "Крiостазис":
        _ring(col=(100,200,255),max_r=90); _ring(col=(140,220,255),max_r=60,delay=0.1); _shards(22,col=(180,235,255))
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":65.0,"col":(200,240,255)})
    elif ability_name == "Нейрошок": _bolt(col=(255,200,60)); _ring(col=(255,220,80),max_r=55)
    elif ability_name == "Ментальний Блок":
        for i in range(4): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.08,"life":0.45,"max_r":float(20+i*18),"col":(80,80,180)})
        _sparks(12,cols=[(80,80,180),(100,100,220),(60,60,140)])
    elif ability_name == "Часова Петля": _tornado(col=(200,180,255)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":60.0,"col":(210,190,255)})
    elif ability_name == "Заклик Безумства": _sparks(20,cols=[(200,0,200),(150,0,180),(100,0,140)]); _tornado(col=(180,0,200)); _ring(col=(200,0,220),max_r=65)
    elif ability_name == "Кандали Приречених":
        for ang in range(0,360,45): ability_effects.append({"type":"shard","cx":float(tcx)+math.cos(math.radians(ang))*45,"cy":float(tcy)+math.sin(math.radians(ang))*22,"dx":0,"dy":0,"size":6,"born":now,"life":0.8,"col":(60,40,20),"shadow_y":2})
        _ring(col=(80,60,30),max_r=55)
    elif ability_name == "Тліюче Пепелище": _fireball(15,col=(120,80,30),size_range=(6,14)); _poison(10,col=(180,100,30))
    elif ability_name == "Просторова Кишеня":
        _sparks(20,cols=[(160,0,200),(120,0,160),(80,0,120)]); ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":80.0,"col":(140,0,180)})
    elif ability_name == "Атрофія Сил": _poison(14,col=(120,100,40)); _ring(col=(140,120,40),max_r=55)
    elif ability_name == "Печать Тиші":
        for i in range(6): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.06,"life":0.4,"max_r":float(60-i*8),"col":(80,80,80)})
    elif ability_name == "Погляд Горгони": _ring(col=(160,120,20),max_r=70); _sparks(14,cols=[(200,160,0),(160,120,0),(100,80,0)]); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":60.0,"col":(200,160,20)})
    elif ability_name == "Некроз Тканин": _poison(18,col=(60,100,20)); _ring(col=(50,80,10),max_r=55)
    elif ability_name == "Нитки Ляльковода":
        for i in range(8): ability_effects.append({"type":"bolt_seg","x1":float(tcx),"y1":float(tcy),"x2":float(tcx)+math.cos(i*math.pi/4)*50,"y2":float(tcy)+math.sin(i*math.pi/4)*25,"born":now+i*0.04,"life":0.6,"col":(200,100,200),"thick":2})
    elif ability_name == "Рулетка Долі":
        for i in range(20): ability_effects.append({"type":"star_spark","cx":float(tcx)+random.randint(-50,50),"cy":float(tcy)+random.randint(-50,50),"dx":random.uniform(-2,2),"dy":random.uniform(-3,-0.5),"size":random.randint(5,12),"born":now+i*0.03,"life":random.uniform(0.4,0.9),"col":random.choice([(255,0,0),(0,255,0),(0,0,255),(255,255,0),(255,0,255)])})
    elif ability_name == "Едка Іржа": _shards(18,col=(120,80,30)); _poison(8,col=(140,100,40))
    elif ability_name == "Глибока Летаргія": _tornado(col=(120,100,180)); _sparks(12,cols=[(120,100,180),(100,80,160),(80,60,140)])

    # ── Deck Heroes — Зцілення/Баффи ──────────────────────────────────
    elif ability_name == "Благодать": _orbs(14,col=(255,255,180),on_hero=True); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":55.0,"col":(255,255,200)})
    elif ability_name == "Очисне Сяйво": _ring(col=(255,255,200),max_r=100); _orbs(18,col=(255,255,160),on_hero=True); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.5,"max_r":70.0,"col":(255,255,180)})
    elif ability_name == "Причастя": _orbs(12,col=(200,180,255),on_hero=True); _ring(col=(220,200,255),max_r=60)
    elif ability_name == "Бойовий Дух": _sparks(22,cols=[(255,140,0),(255,100,0),(200,80,0)]); _shockwave(col=(255,120,0),max_r=80)
    elif ability_name == "Останній Іспит": _shards(25,col=(255,200,0)); _sparks(20,cols=[(255,215,0),(255,180,0),(200,140,0)]); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.5,"max_r":75.0,"col":(255,220,80)})
    elif ability_name == "Поклик Предків": _sparks(20,cols=[(255,255,255),(200,200,255),(150,150,220)]); _ring(col=(200,200,255),max_r=70)
    elif ability_name == "Дух Фенікса": _fireball(20,col=(255,100,0),size_range=(10,22)); _orbs(12,col=(255,150,50),on_hero=True); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.5,"max_r":65.0,"col":(255,160,80)})
    elif ability_name == "Сигнал Авангарду": _sparks(18,cols=[(255,180,0),(255,200,80),(200,140,0)]); _ring(col=(255,180,0),max_r=60)
    elif ability_name == "Кругова Порука": _shield_hex(col=(100,180,100)); _ring(col=(80,200,80),max_r=70)
    elif ability_name == "Жага Помсти": _fireball(15,col=(180,0,40),size_range=(8,18)); _ring(col=(200,0,60),max_r=65)
    elif ability_name == "Сльози Богині":
        for _ in range(15): ability_effects.append({"type":"heal_orb","cx":float(cx)+random.randint(-40,40),"cy":float(cy)-random.randint(20,60),"dx":random.uniform(-0.5,0.5),"dy":random.uniform(3,6),"size":random.randint(5,11),"born":now,"life":random.uniform(0.4,0.9),"col":(180,200,255),"shadow_y":3})
    elif ability_name == "Левине Серце": _shockwave(col=(255,150,0),max_r=80); _sparks(16,cols=[(255,150,0),(200,100,0),(255,180,50)])
    elif ability_name == "Морозна Мантія": _ring(col=(140,210,255),max_r=70); _shield_hex(col=(140,210,255))
    elif ability_name == "Абсолютна Чистота": _sparks(25,cols=[(255,255,255),(220,255,220),(200,240,255)]); _ring(col=(255,255,255),max_r=85); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":65.0,"col":(255,255,255)})
    elif ability_name == "Фокус Чаклуна": _bolt(col=(180,100,255)); _sparks(14,cols=[(180,100,255),(140,80,220),(100,60,180)])
    elif ability_name == "Кривавий Пакт": _fireball(12,col=(200,0,50),size_range=(8,16)); _orbs(10,col=(200,0,50),on_hero=True)
    elif ability_name == "Бойова Синергія": _orbs(12,col=(80,220,180),on_hero=True); _ring(col=(80,200,160),max_r=55)

    # ── Deck Heroes — Особливі/Рідкісні ──────────────────────────────
    elif ability_name == "Безодня":
        for i in range(8): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.05,"life":0.5,"max_r":float(20+i*15),"col":(20,0,40)})
        _sparks(20,cols=[(60,0,80),(100,0,120),(140,0,160)]); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+0.4,"life":0.4,"max_r":80.0,"col":(0,0,0)})
    elif ability_name == "Астральна Висилка": _tornado(col=(100,80,200)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.45,"max_r":70.0,"col":(140,120,220)})
    elif ability_name == "Кривава Жатва": _ring(col=(200,0,60),max_r=120); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":80.0,"col":(200,0,40)}); _shards(25,col=(180,0,40))
    elif ability_name == "Небесний Гнів":
        _bolt(col=(255,255,255)); _shockwave(col=(255,255,180),max_r=150)
        _sparks(30,cols=[(255,255,180),(255,255,100),(200,200,80)]); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":90.0,"col":(255,255,200)})
    elif ability_name == "Дзеркальне Втілення": _ring(col=(140,220,255),max_r=70); _ring(col=(200,240,255),max_r=50,delay=0.1); _sparks(18,cols=[(140,220,255),(200,240,255),(100,180,220)])
    elif ability_name == "Чорна Діра":
        for i in range(10): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.04,"life":0.5,"max_r":float(10+i*10),"col":(0,0,0)})
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+0.4,"life":0.4,"max_r":85.0,"col":(20,0,30)})
    elif ability_name == "Дотик Жнеця": _slash(6,col=(0,0,0)); _sparks(12,cols=[(60,0,60),(80,0,80),(100,0,100)])
    elif ability_name == "Темна Жертва": _shards(28,col=(180,0,40)); _shockwave(col=(200,0,60),max_r=130); _orbs(10,col=(180,0,40),on_hero=True)
    elif ability_name == "Глобальний Колапс": _shockwave(col=(255,255,255),max_r=200); _shards(40,col=(200,200,200)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.6,"max_r":100.0,"col":(255,255,255)})
    elif ability_name == "Безтілесний Дух": _sparks(20,cols=[(200,200,255),(150,150,220),(100,100,180)]); _shield_hex(col=(180,180,255))
    elif ability_name == "Викривлення Розуму": _tornado(col=(200,80,200)); _ring(col=(180,60,180),max_r=70)
    elif ability_name == "Деградація": _poison(20,col=(40,180,40)); _ring(col=(60,160,60),max_r=65)
    elif ability_name == "Інверсія Параметрів":
        for i in range(5): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.06,"life":0.45,"max_r":float(80-i*12),"col":(80,200,200)})
        _sparks(14,cols=[(80,200,200),(60,160,160),(40,120,120)])
    elif ability_name == "Смертельні Узи":
        for i in range(2): ability_effects.append({"type":"bolt_seg","x1":float(tcx),"y1":float(tcy),"x2":float(tcx)+random.randint(-80,80),"y2":float(tcy)+random.randint(-40,40),"born":now+i*0.1,"life":0.5,"col":(180,0,120),"thick":4})
        _ring(col=(180,0,120),max_r=65)
    elif ability_name == "Сліпий Шанс":
        _sparks(30,cols=[(255,0,0),(0,255,0),(0,0,255),(255,255,0),(255,0,255),(0,255,255)]); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":80.0,"col":(random.randint(100,255),random.randint(100,255),random.randint(100,255))})
    elif ability_name == "Вища Форма":
        _sparks(40,cols=[(255,215,0),(255,255,255),(200,180,255),(80,255,200),(255,140,0)]); _shockwave(col=(255,215,0),max_r=140); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.6,"max_r":90.0,"col":(255,240,200)})
    elif ability_name == "Мертва Тиша":
        for i in range(6): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.07,"life":0.5,"max_r":float(30+i*25),"col":(20,20,20)})

    # ── ЗАГАЛЬНИЙ FALLBACK ─────────────────────────────────────
    elif ability_name == "Генна Мутація":
        _sparks(16, cols=[(0,255,120),(0,200,80),(80,255,150)]); _ring(col=(0,220,100),max_r=55)
    elif ability_name == "Телепатичний Удар":
        _bolt(col=(180,100,255)); _ring(col=(160,80,220),max_r=50)
    elif ability_name == "Сироватка Сили":
        _orbs(12,col=(0,200,255),on_hero=True); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":55.0,"col":(0,220,255)})
    elif ability_name == "Режим Невидимки":
        _sparks(20,cols=[(200,200,200),(150,150,150),(100,100,100)]); _shield_hex(col=(180,180,180))
    elif ability_name == "Кіберудар":
        _shards(22,col=(80,200,255)); _slash(4,col=(80,180,220)); _shockwave(col=(60,160,200),max_r=75)
    elif ability_name == "Вихор Наночасток":
        _tornado(col=(0,220,180)); _ring(col=(0,200,160),max_r=80)
    elif ability_name == "Енергетичний Щит":
        _shield_hex(col=(0,200,255)); ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":60.0,"col":(0,220,255)})
    elif ability_name == "Кислотний Плевок":
        _poison(20,col=(180,255,0)); _ring(col=(160,220,0),max_r=55)
    elif ability_name == "ЕМП Розряд":
        _bolt(col=(255,200,0)); _ring(col=(220,180,0),max_r=65)
        for i in range(3): ability_effects.append({"type":"bolt_seg","x1":float(tcx)+random.randint(-40,40),"y1":float(tcy)-60,"x2":float(tcx),"y2":float(tcy),"born":now+i*0.06,"life":0.2,"col":(255,220,80),"thick":3})
    elif ability_name == "Нано-Зцілення":
        _orbs(14,col=(0,255,180),on_hero=True); _ring(col=(0,220,160),max_r=50)
    elif ability_name == "Точний Постріл":
        _arrows(5,col=(255,80,80)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+0.35,"life":0.3,"max_r":45.0,"col":(255,120,120)})
    elif ability_name == "Ясновидіння":
        _sparks(18,cols=[(255,255,200),(255,220,80),(200,180,255)]); _ring(col=(255,215,0),max_r=65)
    elif ability_name == "Надшвидкість":
        for i in range(3):
            _shards(8,col=(200,240,255),born_off=i*0.08)
            ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+i*0.1,"life":0.18,"max_r":35.0,"col":(200,240,255)})
    elif ability_name == "Плазмовий Струмінь":
        _fireball(30,col=(100,200,255),size_range=(8,22)); _shockwave(col=(80,180,220),max_r=110)
    elif ability_name == "Кріо-Заморозка":
        _ring(col=(180,240,255),max_r=90); _ring(col=(140,210,255),max_r=60,delay=0.08)
        _shards(24,col=(200,245,255)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":70.0,"col":(200,245,255)})
    elif ability_name == "Тіньовий Клон":
        _sparks(18,cols=[(80,80,80),(120,120,120),(160,160,160)]); _shield_hex(col=(100,100,100))
    elif ability_name == "Магнітна Пастка":
        for i in range(5): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.07,"life":0.4,"max_r":float(100-i*15),"col":(80,180,255)})
        _sparks(14,cols=[(80,160,255),(60,120,200)])
    elif ability_name == "Термоядерний Удар":
        _shockwave(col=(255,200,0),max_r=160); _shockwave(col=(255,120,0),max_r=100,delay=0.1)
        _fireball(30,col=(255,80,0),size_range=(12,28)); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":90.0,"col":(255,220,100)})
    elif ability_name == "Гідро-Волна":
        _wave(4,col=(40,160,255)); _ring(col=(80,180,255),max_r=80)
    elif ability_name == "Торнадо-Копія":
        _tornado(col=(160,220,255)); _ring(col=(180,230,255),max_r=70)
        _shards(14,col=(200,235,255))
    elif ability_name == "Адамантова Шкіра":
        _shield_hex(col=(180,180,200)); _ring(col=(160,160,180),max_r=60)
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":55.0,"col":(200,200,220)})
    elif ability_name == "Уповільнення Часу":
        for i in range(4): ability_effects.append({"type":"shockwave","cx":float(tcx),"cy":float(tcy),"born":now+i*0.1,"life":0.45,"max_r":float(40+i*25),"col":(180,220,255)})
        _sparks(12,cols=[(200,230,255),(255,255,255),(140,180,220)])
    elif ability_name == "Просторовий Стрибок":
        _sparks(16,cols=[(160,0,200),(120,0,160),(80,0,120)]); _tornado(col=(140,40,200))
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now+0.2,"life":0.35,"max_r":55.0,"col":(180,80,255)})
    elif ability_name == "Мегаудар":
        _shards(28,col=(255,100,0)); _shockwave(col=(255,80,0),max_r=120)
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.5,"max_r":80.0,"col":(255,140,40)})
    elif ability_name == "Кров'яний Пакт":
        _fireball(14,col=(180,0,40),size_range=(8,18)); _orbs(10,col=(180,0,40),on_hero=True)
    elif ability_name == "Зоряний Вибух ЕX":
        _sparks(40,cols=[(255,220,40),(255,80,200),(80,200,255),(255,80,0),(200,80,255)])
        _shockwave(col=(255,200,60),max_r=170); ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.6,"max_r":100.0,"col":(255,220,100)})
    elif ability_name == "Нано-Рій":
        for i in range(20): ability_effects.append({"type":"star_spark","cx":float(tcx)+random.randint(-50,50),"cy":float(tcy)+random.randint(-50,50),"dx":random.uniform(-2,2),"dy":random.uniform(-3,-0.5),"size":random.randint(3,7),"born":now+i*0.04,"life":random.uniform(0.3,0.7),"col":(0,220,180)})
    elif ability_name == "Маска Хаосу":
        _sparks(22,cols=[(255,0,0),(0,255,0),(0,0,255),(255,255,0),(255,0,255)])
        ability_effects.append({"type":"flash","cx":float(tcx),"cy":float(tcy),"born":now,"life":0.4,"max_r":65.0,"col":(random.randint(100,255),random.randint(100,255),random.randint(100,255))})
    elif ability_name == "Гіпер-Регенерація":
        _orbs(16,col=(0,255,150),on_hero=True); _ring(col=(0,220,130),max_r=60)
        ability_effects.append({"type":"flash","cx":float(cx),"cy":float(cy),"born":now,"life":0.4,"max_r":55.0,"col":(80,255,180)})
    elif ability_name == "Спектральний Щит":
        rainbow2=[(255,0,0),(255,140,0),(255,255,0),(0,255,0),(0,120,255),(120,0,255)]
        for i,col_r in enumerate(rainbow2): ability_effects.append({"type":"ice_ring","cx":float(cx),"cy":float(cy),"r":float(10+i*7),"max_r":float(45+i*8),"born":now+i*0.05,"life":0.8,"angle_off":i*0.4,"col":col_r})
    else:
        _shards(18, col=(200,200,80))


def draw_ability_effects_3d(surface, dt=0.016):
    """Малює та оновлює псевдо-3D (2.5D) ефекти здібностей."""
    global ability_effects
    now = time.time()
    alive = []
    for ef in ability_effects:
        age  = now - ef["born"]
        life = ef["life"]
        if age < 0:          alive.append(ef); continue   # ще не народився
        if age >= life:      continue                       # мертвий
        progress = age / life          # 0..1
        alpha_f  = max(0.0, 1.0 - progress)   # плавне зникнення
        t_ef = ef["type"]

        # ── fireball ──────────────────────────────────────────────────────
        if t_ef == "fireball":
            px = ef["cx"] + ef["dx"] * age * 60
            py = ef["cy"] + ef["dy"] * age * 60
            sz = max(2, int(ef["size"] * (1 - progress * 0.5)))
            col = ef["col"]
            # Тінь (2.5D)
            sh_y = ef.get("shadow_y", 6)
            sh_col = (max(0,col[0]-80), 0, 0)
            shadow_surf = pygame.Surface((sz*2+4, sz+sh_y+4), pygame.SRCALPHA)
            pygame.draw.ellipse(shadow_surf, (*sh_col, int(80*alpha_f)),
                (2, sh_y+2, sz*2, sz))
            surface.blit(shadow_surf, (int(px)-sz, int(py)+sz//2))
            # Тіло
            fire_surf = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
            pygame.draw.circle(fire_surf, (*col, int(220*alpha_f)), (sz+2, sz+2), sz)
            # Відблиск зверху-зліва
            hl_col = (min(255,col[0]+40), min(255,col[1]+60), 80)
            pygame.draw.circle(fire_surf, (*hl_col, int(150*alpha_f)), (sz, sz), sz//2)
            surface.blit(fire_surf, (int(px)-sz, int(py)-sz))

        # ── bolt_seg ──────────────────────────────────────────────────────
        elif t_ef == "bolt_seg":
            col = ef["col"]
            thick = ef.get("thick", 2)
            a_val = int(240 * alpha_f)
            if a_val > 0:
                # Зовнішнє біле сяйво
                pygame.draw.line(surface, (255,255,255),
                    (int(ef["x1"]), int(ef["y1"])), (int(ef["x2"]), int(ef["y2"])), thick+2)
                pygame.draw.line(surface, col,
                    (int(ef["x1"]), int(ef["y1"])), (int(ef["x2"]), int(ef["y2"])), thick)

        # ── flash ─────────────────────────────────────────────────────────
        elif t_ef == "flash":
            r = int(ef["max_r"] * progress)
            col = ef["col"]
            a_val = int(180 * alpha_f)
            if r > 0 and a_val > 0:
                fl = pygame.Surface((r*2+4, r*2+4), pygame.SRCALPHA)
                pygame.draw.circle(fl, (*col, a_val), (r+2, r+2), r)
                surface.blit(fl, (int(ef["cx"])-r, int(ef["cy"])-r))

        # ── shockwave ─────────────────────────────────────────────────────
        elif t_ef == "shockwave":
            r = int(ef["max_r"] * progress)
            col = ef["col"]
            a_val = int(200 * alpha_f)
            if r > 3 and a_val > 0:
                sw = pygame.Surface((r*2+8, r*2+8), pygame.SRCALPHA)
                pygame.draw.circle(sw, (*col, a_val), (r+4, r+4), r, 4)
                surface.blit(sw, (int(ef["cx"])-r-4, int(ef["cy"])-r-4))

        # ── ice_ring ──────────────────────────────────────────────────────
        elif t_ef == "ice_ring":
            r = int(ef["r"] + (ef["max_r"] - ef["r"]) * progress)
            col = ef["col"]
            a_val = int(180 * alpha_f)
            if r > 0 and a_val > 0:
                ir = pygame.Surface((r*2+4, r*2+4), pygame.SRCALPHA)
                pygame.draw.circle(ir, (*col, a_val), (r+2, r+2), r, 2)
                surface.blit(ir, (int(ef["cx"])-r, int(ef["cy"])-r))

        # ── shard ─────────────────────────────────────────────────────────
        elif t_ef == "shard":
            px = ef["cx"] + ef["dx"] * age * 60
            py = ef["cy"] + ef["dy"] * age * 60 + 0.5 * 9.8 * (age**2) * 20
            sz = max(2, int(ef["size"] * (1 - progress * 0.6)))
            col = ef["col"]
            a_val = int(200 * alpha_f)
            sh_y = ef.get("shadow_y", 4)
            if a_val > 0:
                # Тінь (2.5D ефект)
                sh = pygame.Surface((sz*2+4, sz+sh_y+4), pygame.SRCALPHA)
                pygame.draw.ellipse(sh, (0,0,0, int(60*alpha_f)),
                    (0, sh_y, sz*2+2, sz+2))
                surface.blit(sh, (int(px)-sz, int(py)+sz//2))
                # Сам осколок
                pts = [(int(px), int(py)-sz), (int(px)+sz, int(py)+sz//2),
                       (int(px), int(py)+sz), (int(px)-sz, int(py)+sz//2)]
                sd = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                _pts2 = [(p[0]-int(px)+sz+2, p[1]-int(py)+sz+2) for p in pts]
                pygame.draw.polygon(sd, (*col, a_val), _pts2)
                # Відблиск
                hl = (min(255,col[0]+60), min(255,col[1]+60), min(255,col[2]+60))
                _hl_mid = ((_pts2[0][0]+_pts2[1][0])//2, (_pts2[0][1]+_pts2[1][1])//2)
                pygame.draw.polygon(sd, (*hl, int(100*alpha_f)),
                    [_pts2[0], _hl_mid, _pts2[3]])
                surface.blit(sd, (int(px)-sz, int(py)-sz))

        # ── wave ──────────────────────────────────────────────────────────
        elif t_ef == "wave":
            px = ef["cx"] + (ef["tx"] - ef["cx"]) * progress
            py = ef["cy"] + (ef["ty"] - ef["cy"]) * progress
            col = ef["col"]
            w_val = ef.get("width", 4)
            a_val = int(200 * alpha_f)
            if a_val > 0:
                wv = pygame.Surface((60, 30), pygame.SRCALPHA)
                for wi in range(3):
                    pygame.draw.arc(wv, (*col, a_val),
                        (wi*8, 5, 50-wi*8, 20), 0, math.pi, w_val)
                surface.blit(wv, (int(px)-30, int(py)-15))

        # ── shadow_slash ──────────────────────────────────────────────────
        elif t_ef == "shadow_slash":
            sz = ef["size"]
            col = ef["col"]
            a_val = int(200 * alpha_f)
            if a_val > 0:
                sl = pygame.Surface((sz+4, sz+4), pygame.SRCALPHA)
                pygame.draw.line(sl, (*col, a_val),
                    (0, sz+2), (sz+2, 0), 5)
                pygame.draw.line(sl, (200, 100, 255, int(120*alpha_f)),
                    (2, sz), (sz, 2), 2)
                surface.blit(sl, (int(ef["cx"])-sz//2, int(ef["cy"])-sz//2))

        # ── meteor ────────────────────────────────────────────────────────
        elif t_ef == "meteor":
            px = ef["cx"] + (ef["tx"] - ef["cx"]) * progress
            py = ef["cy"] + (ef["ty"] - ef["cy"]) * progress
            sz = ef["size"]
            col = ef["col"]
            a_val = int(220 * alpha_f)
            if a_val > 0:
                # Хвіст
                for ti in range(6):
                    tail_prog = max(0, progress - ti * 0.04)
                    tx_ = ef["cx"] + (ef["tx"] - ef["cx"]) * tail_prog
                    ty_ = ef["cy"] + (ef["ty"] - ef["cy"]) * tail_prog
                    ts = max(2, sz - ti * 3)
                    tc = (min(255,col[0]), max(0,col[1]-ti*25), 0)
                    ta = max(0, a_val - ti*30)
                    tsurf = pygame.Surface((ts*2+2, ts*2+2), pygame.SRCALPHA)
                    pygame.draw.circle(tsurf, (*tc, ta), (ts+1,ts+1), ts)
                    surface.blit(tsurf, (int(tx_)-ts, int(ty_)-ts))
                # Тіло метеорита
                ms = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                pygame.draw.circle(ms, (80,40,20,a_val), (sz+2,sz+2), sz)
                pygame.draw.circle(ms, (*col, a_val), (sz+2,sz+2), int(sz*0.7))
                pygame.draw.circle(ms, (255,255,200,int(180*alpha_f)), (sz,sz), sz//3)
                surface.blit(ms, (int(px)-sz, int(py)-sz))

        # ── heal_orb ──────────────────────────────────────────────────────
        elif t_ef == "heal_orb":
            px = ef["cx"] + ef["dx"] * age * 60
            py = ef["cy"] + ef["dy"] * age * 60 + 0.5 * 5 * (age**2) * 20
            sz = max(2, int(ef["size"] * (1 - progress * 0.4)))
            col = ef["col"]
            a_val = int(200 * alpha_f)
            if a_val > 0:
                ob = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                pygame.draw.circle(ob, (*col, a_val), (sz+2,sz+2), sz)
                hl = (min(255,col[0]+50), min(255,col[1]+50), min(255,col[2]+50))
                pygame.draw.circle(ob, (*hl, int(140*alpha_f)), (sz, sz), sz//2)
                # Тінь
                sh_y = ef.get("shadow_y", 5)
                sh = pygame.Surface((sz*2+2, sz+sh_y+2), pygame.SRCALPHA)
                pygame.draw.ellipse(sh, (0,60,0, int(50*alpha_f)),
                    (0, sh_y, sz*2, sz))
                surface.blit(sh, (int(px)-sz, int(py)+sz//2))
                surface.blit(ob, (int(px)-sz, int(py)-sz))

        # ── shield_hex ────────────────────────────────────────────────────
        elif t_ef == "shield_hex":
            r = ef["r"] * (0.6 + progress * 0.6)
            col = ef["col"]
            a_val = int(180 * alpha_f)
            angle = ef["angle"] + progress * 0.5
            if a_val > 0:
                px = ef["cx"] + math.cos(angle) * r
                py = ef["cy"] + math.sin(angle) * r * 0.5
                sz = 7
                hx = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                pts_h = [(int(sz+2 + sz*math.cos(math.pi/3*k)),
                          int(sz+2 + sz*math.sin(math.pi/3*k)*0.6)) for k in range(6)]
                pygame.draw.polygon(hx, (*col, a_val), pts_h)
                pygame.draw.polygon(hx, (255,255,255,int(80*alpha_f)), pts_h, 1)
                surface.blit(hx, (int(px)-sz, int(py)-sz))

        # ── poison_drop ───────────────────────────────────────────────────
        elif t_ef == "poison_drop":
            px = ef["cx"] + ef["dx"] * age * 40
            py = ef["cy"] + ef["dy"] * age * 40 + 0.3 * 9 * (age**2) * 20
            sz = max(2, int(ef["size"] * (1 - progress * 0.5)))
            col = ef["col"]
            a_val = int(200 * alpha_f)
            if a_val > 0:
                pd = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                pygame.draw.circle(pd, (*col, a_val), (sz+2, sz+2), sz)
                pygame.draw.polygon(pd, (*col, int(160*alpha_f)),
                    [(sz+2, 2), (sz, sz+2), (sz+4, sz+2)])
                surface.blit(pd, (int(px)-sz, int(py)-sz))

        # ── arrow_proj ────────────────────────────────────────────────────
        elif t_ef == "arrow_proj":
            px = ef["sx"] + (ef["ex"] - ef["sx"]) * progress
            py = ef["sy"] + (ef["ey"] - ef["sy"]) * progress
            col = ef["col"]
            a_val = int(220 * alpha_f)
            angle = math.atan2(ef["ey"]-ef["sy"], ef["ex"]-ef["sx"])
            sz = ef.get("size", 5)
            if a_val > 0:
                # Тінь (2.5D)
                sh = pygame.Surface((30+sz, 12+sz), pygame.SRCALPHA)
                pygame.draw.ellipse(sh, (0,0,0, int(50*alpha_f)), (0, 6, 30, 6))
                surface.blit(sh, (int(px)-15, int(py)+sz+2))
                # Стрілка
                arr = pygame.Surface((30+sz*2, 14+sz*2), pygame.SRCALPHA)
                tip = (28+sz, 7+sz)
                tail= (2, 7+sz)
                pygame.draw.line(arr, (*col, a_val), tail, tip, sz)
                pygame.draw.polygon(arr, (*col, a_val),
                    [tip, (tip[0]-8, tip[1]-5), (tip[0]-8, tip[1]+5)])
                # Перо
                pygame.draw.line(arr, (255,240,180,int(160*alpha_f)),
                    (tail[0]+4, tail[1]-3), (tail[0]+12, tail[1]+3), 2)
                rotated = pygame.transform.rotate(arr, -math.degrees(angle))
                surface.blit(rotated, (int(px)-rotated.get_width()//2, int(py)-rotated.get_height()//2))

        # ── tornado_seg ───────────────────────────────────────────────────
        elif t_ef == "tornado_seg":
            col = ef["col"]
            sz = ef.get("size", 8)
            a_val = int(180 * alpha_f)
            if a_val > 0:
                ts = pygame.Surface((sz*2+4, sz*2+4), pygame.SRCALPHA)
                pygame.draw.circle(ts, (*col, a_val), (sz+2,sz+2), sz)
                surface.blit(ts, (int(ef["cx"])-sz, int(ef["cy"])-sz))

        # ── star_spark ────────────────────────────────────────────────────
        elif t_ef == "star_spark":
            px = ef["cx"] + ef.get("dx",0) * age * 40
            py = ef["cy"] + ef.get("dy",0) * age * 40
            sz = max(1, int(ef["size"] * (1 - progress*0.6)))
            col = ef["col"]
            a_val = int(200 * alpha_f)
            if a_val > 0 and sz > 0:
                # 5-кутна зірочка
                sp = pygame.Surface((sz*4+4, sz*4+4), pygame.SRCALPHA)
                cx2, cy2 = sz*2+2, sz*2+2
                pts_s = []
                for k in range(5):
                    a_out = math.pi/2 + k * 2*math.pi/5
                    a_in  = a_out + math.pi/5
                    pts_s.append((cx2+int(sz*2*math.cos(a_out)), cy2-int(sz*2*math.sin(a_out))))
                    pts_s.append((cx2+int(sz*math.cos(a_in)),   cy2-int(sz*math.sin(a_in))))
                if len(pts_s) >= 3:
                    pygame.draw.polygon(sp, (*col, a_val), pts_s)
                surface.blit(sp, (int(px)-sz*2, int(py)-sz*2))

        alive.append(ef)
    ability_effects = alive

def apply_titan_ability(game, ability_key, level):
    """Застосовує здібність Титана залежно від ключа та рівня."""
    bonus = level - 1
    living_e = [e for e in game.enemies if e.is_alive]
    living_h = [h for h in game.army   if h.is_alive]
    phrase = ""

    # Середня позиція ворогів та героїв для ефектів
    def _ecx(): return int(sum(e.rect.centerx for e in living_e)/max(1,len(living_e))) if living_e else 500
    def _ecy(): return int(sum(e.rect.centery for e in living_e)/max(1,len(living_e))) if living_e else 300
    def _hcx(): return int(sum(h.rect.centerx for h in living_h)/max(1,len(living_h))) if living_h else 200
    def _hcy(): return int(sum(h.rect.centery for h in living_h)/max(1,len(living_h))) if living_h else 300

    if ability_key == "clone" and living_h:
        from copy import copy
        src = random.choice(living_h)
        clone = Card(src.original_name, src.level)
        hp_b, atk_b = get_titan_clone_bonus(level)
        clone.max_hp = max(1, src.max_hp + hp_b)
        clone.hp     = clone.max_hp
        clone.attack = max(1, src.attack + atk_b)
        clone.name   = f"Тінь:{src.name}"
        max_c = get_titan_max_cards(level)
        if len(game.army) < max_c:
            game.army.append(clone)
        # ── Ефект: срібний спалах + дзеркальні частки
        spawn_ability_effect("Воскресіння", _hcx(), _hcy(), _hcx()+60, _hcy()-30)
        for _ in range(20):
            ability_effects.append({"type":"shard",
                "cx":float(_hcx()),"cy":float(_hcy()),
                "dx":random.uniform(-6,6),"dy":random.uniform(-9,-2),
                "size":random.randint(5,13),"born":time.time(),
                "life":random.uniform(0.4,0.8),"col":(180,200,255),"shadow_y":3})
        phrase = f"⚔️ Клон {src.name} призваний!"

    elif ability_key == "fire_strike" and living_e:
        dmg = 200 + bonus * 10
        target = max(living_e, key=lambda c: c.max_hp)
        target.hp = max(0, target.hp - dmg)
        if target.hp <= 0: target.is_alive = False; spawn_death_effect(int(target.rect.centerx), int(target.rect.centery), 4)
        fire_card_effects.append({"card": target, "end_time": time.time()+4, "particles": []})
        # ── Ефект: великий метеорит вогню
        spawn_ability_effect("Метеорит", _hcx(), _hcy(), _ecx(), _ecy())
        spawn_ability_effect("Лавовий Потік", _hcx(), _hcy(), _ecx(), _ecy())
        phrase = f"🔥 Вогняний Удар: -{dmg} до {target.name}!"

    elif ability_key == "freeze_all" and living_e:
        dur = 2 + bonus // 5
        for e in living_e:
            e._frozen_until = time.time() + dur * 2
            e.attack = max(0, int(e.attack * 0.3))
            ice_effects.append({"card": e, "end_time": time.time() + dur * 2})
        # ── Ефект: крижаний шторм по всій арені
        for e in living_e:
            spawn_ability_effect("Крижаний Шторм", _hcx(), _hcy(),
                int(e.rect.centerx), int(e.rect.centery))
        phrase = f"❄️ Заморозка {len(living_e)} ворогів на {dur} ходи!"

    elif ability_key == "mega_freeze" and living_e:
        dur = 3 + bonus // 4
        for e in living_e:
            e._frozen_until = time.time() + dur * 2
            e.attack = max(0, int(e.attack * 0.15))
            ice_effects.append({"card": e, "end_time": time.time() + dur * 2})
        # ── Ефект: МЕГА крига — кільця + шторм
        spawn_ability_effect("Заморозка", _hcx(), _hcy(), _ecx(), _ecy())
        spawn_ability_effect("Крижаний Шторм", _hcx(), _hcy(), _ecx(), _ecy())
        for _ in range(25):
            ability_effects.append({"type":"shard",
                "cx":float(_ecx())+random.randint(-80,80),
                "cy":float(_ecy())+random.randint(-60,60),
                "dx":random.uniform(-4,4),"dy":random.uniform(-6,-1),
                "size":random.randint(6,16),"born":time.time(),
                "life":random.uniform(0.5,1.0),"col":(200,240,255),"shadow_y":2})
        phrase = f"🧊 МЕГА-заморозка всіх ворогів на {dur} ходи!"

    elif ability_key == "shadow_hit" and living_e:
        cnt = min(2 + bonus // 8, len(living_e))
        targets = random.sample(living_e, cnt)
        dmg = 80 + bonus * 5
        for t in targets:
            t.hp = max(0, t.hp - dmg)
            if t.hp <= 0: t.is_alive = False; spawn_death_effect(int(t.rect.centerx), int(t.rect.centery), 2)
            # ── Ефект: тіньовий клинок до кожного
            spawn_ability_effect("Тіньовий Клинок", _hcx(), _hcy(),
                int(t.rect.centerx), int(t.rect.centery))
        phrase = f"🌑 Тіньовий Удар: -{dmg} по {cnt} ворогах!"

    elif ability_key == "shield" and living_h:
        dur = 3 + bonus // 6
        hero = random.choice(living_h)
        hero._shielded_until = time.time() + dur * 3
        shield_effects.append({"card": hero, "end_time": time.time() + dur * 3})
        # ── Ефект: золотий щит + відблиск
        spawn_ability_effect("Непробивний Щит", int(hero.rect.centerx), int(hero.rect.centery))
        spawn_ability_effect("Крило Орла", int(hero.rect.centerx), int(hero.rect.centery))
        phrase = f"⚡ Щит навколо {hero.name} на {dur} ходи!"

    elif ability_key == "aoe_hit" and living_e:
        dmg = 60 + bonus * 4
        for e in living_e:
            e.hp = max(0, e.hp - dmg)
            if e.hp <= 0: e.is_alive = False; spawn_death_effect(int(e.rect.centerx), int(e.rect.centery), 3)
        # ── Ефект: землетрус
        spawn_ability_effect("Землетрус", _hcx(), _hcy(), _ecx(), _ecy())
        phrase = f"🪨 Земний Удар: -{dmg} всім {len(living_e)} ворогам!"

    elif ability_key == "mass_heal" and living_h:
        heal = 40 + bonus * 3
        for h in living_h:
            h.hp = min(h.max_hp, h.hp + heal)
        grass_effects.extend([{"card": h, "end_time": time.time() + 3} for h in living_h])
        # ── Ефект: масове зцілення + регенерація
        spawn_ability_effect("Масове Зцілення", _hcx(), _hcy(), _hcx(), _hcy())
        spawn_ability_effect("Регенерація", _hcx(), _hcy(), _hcx(), _hcy())
        phrase = f"🌊 Зцілення: +{heal} HP всім {len(living_h)} героям!"

    elif ability_key == "chain" and living_e:
        cnt = min(3 + bonus // 7, len(living_e))
        targets = random.sample(living_e, cnt)
        dmg = 120 + bonus * 6
        for t in targets:
            t.hp = max(0, t.hp - dmg * 2)
            if t.hp <= 0: t.is_alive = False; spawn_death_effect(int(t.rect.centerx), int(t.rect.centery), 3)
            chain_effects.append({"card": t, "end_time": time.time() + 2})
            # ── Ефект: блискавка до кожного ланцюга
            spawn_ability_effect("Блискавка", _hcx(), _hcy(),
                int(t.rect.centerx), int(t.rect.centery))
        phrase = f"⛓️ Ланцюги: -{dmg*2} по {cnt} ворогах!"

    elif ability_key == "vamp" and game.selected_hero and game.selected_monster:
        heal = max(1, int(game.selected_hero.attack * (0.3 + bonus * 0.02)))
        game.selected_hero.hp = min(game.selected_hero.max_hp, game.selected_hero.hp + heal)
        grass_effects.append({"card": game.selected_hero, "end_time": time.time() + 2})
        # ── Ефект: вампіризм
        spawn_ability_effect("Вампіризм",
            int(game.selected_hero.rect.centerx), int(game.selected_hero.rect.centery),
            int(game.selected_monster.rect.centerx), int(game.selected_monster.rect.centery))
        phrase = f"🌺 Вампіризм: +{heal} HP герою {game.selected_hero.name}!"

    elif ability_key == "revive" and game.graveyard:
        revived = game.graveyard[-1]
        revived.is_alive = True
        revived.hp = max(1, int(revived.max_hp * (0.3 + bonus * 0.03)))
        max_c = get_titan_max_cards(level)
        if len(game.army) < max_c:
            game.army.append(revived)
            game.all_heroes.append(revived)
            game.graveyard.remove(revived)
        # ── Ефект: воскресіння + золотий спалах
        spawn_ability_effect("Воскресіння", _hcx(), _hcy(), _hcx(), _hcy())
        spawn_ability_effect("Відродження", _hcx(), _hcy(), _hcx(), _hcy())
        phrase = f"🌿 Відродження: {revived.name} повернувся до бою!"

    elif ability_key == "strip" and living_e:
        dur = 3 + bonus // 5
        for e in living_e:
            e._stripped_until = time.time() + dur * 2
        # ── Ефект: сонячний промінь зверху
        spawn_ability_effect("Зупинка Часу", _hcx(), _hcy(), _ecx(), _ecy())
        for e in living_e:
            ability_effects.append({"type":"bolt_seg",
                "x1":float(e.rect.centerx),"y1":float(e.rect.top)-80,
                "x2":float(e.rect.centerx),"y2":float(e.rect.centery),
                "born":time.time(),"life":0.5,"col":(255,255,180),"thick":5})
        phrase = f"☀️ Здібності знято з {len(living_e)} ворогів на {dur} ходи!"

    elif ability_key == "steal_xp" and living_e and living_h:
        hero = random.choice(living_h)
        target = random.choice(living_e)
        stolen = max(1, int(target.xp * (0.4 + bonus * 0.02)) + bonus)
        hero.xp = min(hero.max_xp - 1, hero.xp + stolen)
        # ── Ефект: червоні кулі від монстра до героя
        for i in range(10):
            ability_effects.append({"type":"heal_orb",
                "cx":float(target.rect.centerx),
                "cy":float(target.rect.centery),
                "dx":(hero.rect.centerx-target.rect.centerx)/max(1,abs(hero.rect.centerx-target.rect.centerx))*random.uniform(4,8),
                "dy":(hero.rect.centery-target.rect.centery)/max(1,abs(hero.rect.centery-target.rect.centery)+0.001)*random.uniform(3,6),
                "size":random.randint(6,12),"born":time.time()+i*0.05,
                "life":random.uniform(0.5,0.9),"col":(180,0,60),"shadow_y":4})
        phrase = f"🩸 Вкрадено {stolen} XP у {target.name} → {hero.name}!"

    elif ability_key == "dodge" and living_h:
        dur = 3 + bonus // 5
        hero = random.choice(living_h)
        hero._dodge_until = time.time() + dur * 3
        # ── Ефект: уворот — срібні іскри навколо героя
        spawn_ability_effect("Крило Орла",
            int(hero.rect.centerx), int(hero.rect.centery))
        for _ in range(14):
            ability_effects.append({"type":"star_spark",
                "cx":float(hero.rect.centerx)+random.randint(-35,35),
                "cy":float(hero.rect.centery)+random.randint(-35,35),
                "dx":random.uniform(-2,2),"dy":random.uniform(-3,-0.5),
                "size":random.randint(4,9),"born":time.time(),
                "life":random.uniform(0.4,0.8),"col":(200,220,255)})
        phrase = f"👁️ Уворот: {hero.name} уникатиме ударів {dur} ходи!"

    elif ability_key == "shadow_card" and living_h:
        src = random.choice(living_h)
        shadow = Card(src.original_name, min(100, src.level + 5 + bonus))
        shadow.name = f"Дракон:{src.name}"
        shadow.attack = int(src.attack * (1.5 + bonus * 0.05))
        shadow.max_hp = int(src.max_hp * (1.3 + bonus * 0.03))
        shadow.hp = shadow.max_hp
        max_c = get_titan_max_cards(level)
        if len(game.army) < max_c: game.army.append(shadow)
        # ── Ефект: дракон-тінь — темно-зелений + вогонь
        spawn_ability_effect("Подих Дракона", _hcx(), _hcy(), _hcx()+80, _hcy()-40)
        spawn_ability_effect("Тіньовий Клинок", _hcx(), _hcy(), _hcx(), _hcy())
        phrase = f"🐉 Дракон-тінь {src.name} призваний!"

    elif ability_key == "volcano" and living_e:
        cnt = min(4 + bonus // 6, len(living_e))
        targets = random.sample(living_e, cnt)
        dmg = 100 + bonus * 8
        for t in targets:
            t.hp = max(0, t.hp - dmg)
            if t.hp <= 0: t.is_alive = False; spawn_death_effect(int(t.rect.centerx), int(t.rect.centery), 4)
            fire_card_effects.append({"card": t, "end_time": time.time() + 3, "particles": []})
            # ── Ефект: метеорит на кожну ціль
            spawn_ability_effect("Лавовий Потік", _hcx(), _hcy(),
                int(t.rect.centerx), int(t.rect.centery))
        phrase = f"🌋 Виверження: -{dmg} по {cnt} ворогах!"

    elif ability_key == "steal_card" and living_e and len(living_e) >= 2:
        stolen = random.sample(living_e, min(2, len(living_e)))
        for s in stolen:
            s.is_alive = False
            s.is_monster = False
            s.name = f"Захоп:{s.name}"
            max_c = get_titan_max_cards(level)
            if len(game.army) < max_c: game.army.append(s)
        # ── Ефект: квантовий вихор
        spawn_ability_effect("Портал", _hcx(), _hcy(), _ecx(), _ecy())
        spawn_ability_effect("Телекінез", _hcx(), _hcy(), _ecx(), _ecy())
        phrase = f"⚛️ Вкрадено {len(stolen)} карт монстрів у свою колоду!"

    elif ability_key == "arrows" and living_e:
        cnt = min(5, len(living_e))
        targets = random.sample(living_e, cnt)
        dmg = 50 + bonus * 4
        for t in targets:
            t.hp = max(0, t.hp - dmg)
            if t.hp <= 0: t.is_alive = False; spawn_death_effect(int(t.rect.centerx), int(t.rect.centery), 2)
            arrow_effects.append({"card": t, "end_time": time.time() + 1.5})
            # ── Ефект: стріла до кожної цілі
            spawn_ability_effect("Стріла Долі", _hcx(), _hcy(),
                int(t.rect.centerx), int(t.rect.centery))
        phrase = f"🌠 {cnt} Зоряних Стріл: -{dmg} кожному!"

    elif ability_key == "ultimate":
        heal_u = 60 + bonus * 5
        for h in living_h: h.hp = min(h.max_hp, h.hp + heal_u)
        dmg_u = 150 + bonus * 10
        for e in living_e:
            e.hp = max(0, e.hp - dmg_u)
            if e.hp <= 0: e.is_alive = False; spawn_death_effect(int(e.rect.centerx), int(e.rect.centery), 5)
        if living_h:
            src_u = random.choice(living_h)
            clone_u = Card(src_u.original_name, src_u.level)
            clone_u.name = f"Абс:{src_u.name}"
            max_c = get_titan_max_cards(level)
            if len(game.army) < max_c: game.army.append(clone_u)
        # ── Ефект: АБСОЛЮТНА СИЛА — все одразу
        spawn_ability_effect("Зоряна Сила", _hcx(), _hcy(), _ecx(), _ecy())
        spawn_ability_effect("Ядерна Хвиля", _hcx(), _hcy(), _ecx(), _ecy())
        spawn_ability_effect("Масове Зцілення", _hcx(), _hcy(), _hcx(), _hcy())
        spawn_ability_effect("Веселкова Аура", _hcx(), _hcy(), _hcx(), _hcy())
        for _ in range(30):
            ability_effects.append({"type":"shard",
                "cx":float(_ecx())+random.randint(-100,100),
                "cy":float(_ecy())+random.randint(-80,80),
                "dx":random.uniform(-8,8),"dy":random.uniform(-12,-2),
                "size":random.randint(6,18),"born":time.time()+random.uniform(0,0.3),
                "life":random.uniform(0.5,1.0),
                "col":random.choice([(255,215,0),(255,80,0),(80,200,255),(255,80,200)]),"shadow_y":5})
        phrase = f"💫 АБСОЛЮТНА СИЛА: +{heal_u}HP армії, -{dmg_u} ворогам, призваний клон!"

    return phrase


class Game:
    def __init__(self):
        self.reset_to_defaults()
        self.load_game()

    def reset_to_defaults(self):
        """Ініціалізація або повне скидання всіх параметрів гри"""
        self.state = MENU
        self.total_hits = 0
        self.max_hits = 100
        self.gold = 1000
        self.diamonds = 100
        self.ore = 500
        self.keys = 10
        self.all_heroes = [Card(f"Герой {i}", i * 5 if i > 0 else 1) for i in range(5)]
        self.graveyard = []
        self.army = []
        self.enemies = []
        self.selected_world = 1
        self.selected_location = 1
        self.completed_levels = {}
        self.sh_selected_world = 1
        self.sh_selected_location = 1
        self.sh_is_battle = False   # прапор: поточна битва — SH режим
        self.sh_completed_levels = {}  # окремий прогрес для СУПЕР ЛЮДЕЙ
        self.ad_completed_levels = {}  # окремий прогрес для АНГЕЛИ ТА ДЕМОНИ
        # ── Вдача Героїв ─────────────────────────────────────────
        self.lucky_free_tries   = 3    # безплатні спроби Монополії
        self.lucky_gold_tries   = 3    # спроби за золото Монополії
        self.fort_free_tries    = 3    # безплатні спроби Фортуни
        self.fort_ore_tries     = 3    # спроби за руду Фортуни
        self.mono_pos           = 0    # позиція фішки (0-15)
        self.mono_skip          = False
        self.wheel_angle        = 0.0  # поточний кут колеса
        self.wheel_spinning     = False
        self.lucky_prize_pending = None  # приз який чекає на "ЗАБРАТИ"
        self.lucky_last_reset   = 0    # день останнього відновлення спроб
        self.enc_section        = 0    # поточний розділ Енциклопедії
        self.enc_abil_page      = 0    # сторінка здібностей
        self.ad_selected_world    = 1
        self.ad_selected_location = 1
        self.ad_is_battle         = False
        self.diamond_cards = self.generate_shop_cards("Діамант.")
        self.gold_cards = self.generate_shop_cards("Золотий")
        self.ore_cards = self.generate_shop_cards("Рудний")
        self.shop_selected_card = None
        self.selected_hero = None
        self.selected_monster = None
        self.dead_selected_card = None
        self.winner_text = ""
        self.reward_text = ""
        self._pending_enemy_attack = None   # відкладена контратака ворога

        # --- АЛТАРЬ ГЕРОЇВ ---
        self.altar_last_draw_time = 0   # unix timestamp останнього витягування
        self.altar_drawn_card = None    # картка яку щойно витягнули
        self.altar_message = ""         # повідомлення гравцю
        self.altar_key_draw_card = None  # карта витягнута за ключ
        self.altar_key_message = ""      # повідомлення для витягування за ключ
        # --- ПЕРЕМОГА: випадкова карта ---
        self.victory_bonus_card = None   # карта що випала при перемозі
        self.victory_location_wins = {}  # скільки разів пройдено кожну локацію

        # Ректи кнопок
        self.btn_army = pygame.Rect(0,0,0,0)
        self.btn_altar = pygame.Rect(0,0,0,0)
        self.btn_worlds = pygame.Rect(0,0,0,0)
        self.btn_graveyard = pygame.Rect(0,0,0,0)
        self.btn_resurrect = pygame.Rect(0,0,0,0)
        self.btn_bury      = pygame.Rect(0,0,0,0)
        self.btn_settings = pygame.Rect(0,0,0,0)
        self.btn_reset = pygame.Rect(0,0,0,0)
        self.btn_cards_diamond = pygame.Rect(0,0,0,0)
        self.btn_cards_gold = pygame.Rect(0,0,0,0)
        self.btn_cards_ore = pygame.Rect(0,0,0,0)
        self.btn_market = pygame.Rect(0,0,0,0)
        self.btn_save_exit = pygame.Rect(0,0,0,0)
        self.btn_back = pygame.Rect(0,0,0,0)
        self.btn_battle = pygame.Rect(0,0,0,0)
        self.btn_refresh = pygame.Rect(0,0,0,0)
        self.btn_buy = pygame.Rect(0,0,0,0)
        self.btn_altar_draw = pygame.Rect(0,0,0,0)
        self.btn_altar_buy = pygame.Rect(0,0,0,0)
        self.btn_altar_key_draw = pygame.Rect(0,0,0,0)
        self.btn_live_battle   = pygame.Rect(0,0,0,0)
        self.btn_battle_hub    = pygame.Rect(0,0,0,0)
        self.btn_arena         = pygame.Rect(0,0,0,0)
        self.btn_tournament    = pygame.Rect(0,0,0,0)
        # --- БИТВА В ЖИВУ ---
        # self.lb_tab вже встановлено вище
        self.lb_sim_rooms     = []          # згенеровані кімнати-боти
        self.lb_selected_room = None        # вибрана кімната
        self.lb_lobby_countdown = 0         # таймер відліку в лобі
        self.lb_lobby_start   = 0           # час початку лобі
        self.lb_battle_enemy_deck = []      # карти противника (боти)
        self.lb_battle_result = None        # "win"/"lose"/None
        self.lb_lan_ip        = ""          # IP для LAN
        self.lb_lan_status    = ""          # статус підключення LAN
        self.lb_lan_socket    = None        # сокет LAN
        self.lb_lan_role      = None        # "host"/"client"
        self.lb_lan_connected = False
        self.lb_lan_opponent_ready = False
        self.lb_lan_self_ready = False
        self.lb_lan_enemy_deck = []
        self._lb_lan_opp_hit     = False
        self.lb_lan_my_turn       = True   # True = мій хід
        self._lb_is_lan_pvp       = False   # True = LAN PvP
        self.lb_turn_timer_start      = 0.0    # час початку поточного ходу
        self._lb_online_result_shown_at = 0     # час появи результату онлайн бою
        self.lb_turn_timeout      = 38     # скільки секунд на хід
        self.lb_ip_input      = ""          # поточний ввід IP
        self.lb_ip_active     = False       # чи активне поле вводу
        # --- Онлайн через сервер ---
        self.lb_tab           = "online"   # за замовчуванням — онлайн
        self.online_pid       = ""          # унікальний ID гравця
        self.online_room_id   = ""          # ID кімнати
        self.online_role      = ""          # "player1"/"player2"
        self.online_status    = ""          # статус пошуку/бою
        self.online_searching = False       # чи шукаємо матч
        self.online_last_poll = 0.0         # час останнього запиту стану
        self.online_whose_turn = ""         # "player1"/"player2"
        self.online_deadline  = 0.0         # deadline поточного ходу
        self.online_opp_army  = []          # армія суперника (як ми її бачимо)
        self.online_my_army   = []          # наша армія (з сервера)
        self.online_turn      = 0           # номер ходу
        self.online_result    = None        # результат ("win"/"lose")
        self.online_log       = []          # останні удари
        self.online_my_name   = ""          # ім'я гравця
        self.world_rects = []
        self.loc_rects = []
        self.tab_img_rects = {}
        self.music_enabled = True
        self.scroll_offset = {"my_army": 0, "graveyard": 0, "worlds": 0,
                              "titans": 0, "titan_monsters": 0, "my_shards": 0,
                              "my_runes": 0, "rune_evolution": 0}
        self.scroll_arrows_rects = {}   # {key: (up_rect, down_rect)}
        # --- Титан ---
        self.titan_level    = 1          # поточний рівень активного Титана
        self.titan_levels   = {"titan_1": 1}  # рівні кожного Титана окремо
        self.titan_xp       = 0          # досвід Титана
        self.titan_id       = "titan_1"  # поточний активний Титан
        self.owned_titans   = ["titan_1"]# куплені титани
        self.titan_gif_frame= 0          # кадр GIF анімації
        self.titan_clone_done = False    # чи вже призваний клон у цьому бою
        self.remove_rune_selected_card = None  # карта вибрана для зняття руни
        self.btn_altar_hub = pygame.Rect(0,0,0,0)
        self.btn_hub_altar = pygame.Rect(0,0,0,0)
        self.btn_hub_rune_altar = pygame.Rect(0,0,0,0)
        self.btn_strip_rune = pygame.Rect(0,0,0,0)
        self.btn_remove_rune_mode = pygame.Rect(0,0,0,0)
        self.titan_used_hits  = set()   # які ходи вже спрацювала здібність Титана
        self.titan_shards = {}           # {tid: кількість осколків}
        self.titan_monster_hp = {}       # {tid: {loc: [hp список]}} — збережений HP монстрів
        self.selected_titan_monster = None  # поточний Титан-монстр для локацій
        self.btn_my_shards = pygame.Rect(0,0,0,0)
        self.btn_titan_monsters = pygame.Rect(0,0,0,0)
        self.tm_loc_rects = []
        self.tm_titan_rects = {}
        self.is_titan_monster_battle = False
        self.current_titan_monster_loc = 1
        self.selected_titan_monster_loc = 1
        self.btn_tm_refresh = pygame.Rect(0,0,0,0)
        self.titan_refresh_time = {}  # {tid: unix timestamp останнього ОНОВИТИ}
        self.titan_gif_timer= 0.0
        self.btn_titan      = pygame.Rect(0,0,0,0)
        self.btn_titans     = pygame.Rect(0,0,0,0)
        self.titan_buy_rects= {}         # ректи кнопок купівлі в TITANS
        self.sounds_enabled = True
        # --- РУНИ ---
        self.my_runes        = []
        self.my_essences     = []
        self.essence_last_draw = 0
        self.altar_ess_msg  = ""
        self.abil_card_idx  = None
        self.abil_sel_idx   = None
        self.abil_msg       = ""
        self.sel_ess_idx    = None
        self.ess_page       = 0
        self.rune_page      = 0
        # Супер Люди — осколки {orig_name: count} та розблоковані
        self.super_human_shards   = {sh[0]: 0 for sh in SUPER_HUMAN_CARDS}
        self.unlocked_super_humans = []   # список original_name розблокованих карт
        self.arch_shards   = {ar[0]: 0 for ar in ARCH_CARDS}
        self.unlocked_arch = []   # розблоковані Архангели та Архдемони
        self.arch_sel      = None
        self.arch_pay_msg  = ""
        # Pending платежі для Super Human (покупка за гривні)
        self._sh_pay_pending_invoice = ""
        self._sh_pay_pending_card    = None
        self._sh_pay_ref             = ""
        self.sh_sel                  = None
        self.sh_pay_msg              = ""
        self._last_shard_reward      = ""
        self.supreme_evo_sel = None
        self.supreme_msg     = ""
        self.supreme_lvl_msg = ""
        self.sl_target_idx   = None
        self.sl_feed_idxs    = set()
        self._pending_supreme_card    = None
        self._pending_supreme_invoice = ""
        self._supreme_pay_ref         = ""
        self.rune_altar_tab = "dia"
        self.my_runes_tab = "hero"
        self.rune_selected_idx = None
        self.rune_evo_target_idx = None
        self.rune_evo_feed_idxs = set()
        self.rune_evo_progress = 0
        self.rune_msg = ""
        self.rune_evo_scroll = 0  # зміщення прокрутки (рядки)
        self.btn_my_runes = pygame.Rect(0,0,0,0)
        self.btn_rune_altar = pygame.Rect(0,0,0,0)
        self.btn_evolution = pygame.Rect(0,0,0,0)
        self.rune_rects = []
        self.rune_altar_btn_rects = {}
        self.evo_rune_rects = []
        # Сундуки: last_open_time і open_anim_timer для кожного
        self.chest_last_open = {c[0]: 0 for c in CHESTS}
        self.chest_anim     = {c[0]: 0 for c in CHESTS}   # таймштамп відкриття (для анімації)
        self.chest_reward   = {c[0]: "" for c in CHESTS}  # текст нагороди
        self.chest_rects    = {}

    def reset_progress(self):
        """Функція для повного очищення прогресу та файлу збереження"""
        if os.path.exists("savegame.json"):
            os.remove("savegame.json")
        self.reset_to_defaults()
        self.state = MENU

    def _do_supreme_payment(self, card):
        """
        Створює інвойс через Monobank Acquiring API (еквайринговий токен).
        POST https://api.monobank.ua/api/merchant/invoice/create
        Відкриває платіжну сторінку у браузері.
        """
        import threading, urllib.request, urllib.error, json as _json, random as _rand

        token = MONOBANK_MERCHANT_TOKEN.strip()
        if not token:
            self.supreme_msg = "❌ Немає еквайрингового токена!"; return

        # Унікальний reference для цього платежу
        ref = f"EvII-{card.original_name[:10]}-{_rand.randint(10000,99999)}"
        self._supreme_pay_ref  = ref
        self._pending_supreme_card = card
        self._pending_supreme_invoice = ""
        self.supreme_msg = "⏳ Створюємо платіжний рахунок..."

        payload = _json.dumps({
            "amount":   SUPREME_PRICE_UAH * 100,   # копійки
            "ccy":      980,                         # UAH
            "merchantPaymInfo": {
                "reference":   ref,
                "destination": f"Верховна Еволюція карти {card.name} (EvII)"
            },
            "redirectUrl": "https://monobank.ua",
            "validity":    3600                      # рахунок дійсний 1 годину
        }).encode("utf-8")

        def _thread():
            try:
                req = urllib.request.Request(
                    "https://api.monobank.ua/api/merchant/invoice/create",
                    data    = payload,
                    headers = {
                        "X-Token":      token,
                        "Content-Type": "application/json"
                    },
                    method  = "POST"
                )
                with urllib.request.urlopen(req, timeout=15) as resp:
                    data = _json.loads(resp.read().decode())

                page_url   = data.get("pageUrl", "")
                invoice_id = data.get("invoiceId", "")

                if page_url and invoice_id:
                    self._pending_supreme_invoice = invoice_id
                    _open_url(page_url)
                    self.supreme_msg = (
                        f"✅ Платіжну сторінку відкрито у браузері!\n"
                        f"Сума: {SUPREME_PRICE_UAH} грн · Ref: {ref}\n"
                        f"Після оплати натисни  🔍 ПЕРЕВІРИТИ"
                    )
                else:
                    err = data.get("errText", str(data))
                    self.supreme_msg = f"❌ Monobank: {err}"

            except urllib.error.HTTPError as e:
                try:    body = _json.loads(e.read().decode())
                except: body = {}
                err = body.get("errText", f"HTTP {e.code}")
                self.supreme_msg = f"❌ Помилка {e.code}: {err}"
            except Exception as e:
                self.supreme_msg = f"❌ {str(e)[:80]}"

        threading.Thread(target=_thread, daemon=True).start()

    def _check_supreme_payment(self):
        """
        Перевіряє статус інвойса через Monobank Acquiring API.
        GET https://api.monobank.ua/api/merchant/invoice/status?invoiceId=...
        Можливі статуси: created | processing | hold | success | failure | reversed | expired
        """
        import threading, urllib.request, urllib.error, json as _json

        token      = MONOBANK_MERCHANT_TOKEN.strip()
        invoice_id = getattr(self, "_pending_supreme_invoice", "")
        card       = getattr(self, "_pending_supreme_card", None)

        if not card:
            self.supreme_msg = "❌ Спочатку натисни кнопку оплати!"; return
        if not token:
            self.supreme_msg = "❌ Немає еквайрингового токена!"; return
        if not invoice_id:
            self.supreme_msg = "❌ Рахунок ще не створено. Натисни оплату ще раз!"; return

        self.supreme_msg = "⏳ Перевіряємо статус оплати..."

        def _thread():
            try:
                url = f"https://api.monobank.ua/api/merchant/invoice/status?invoiceId={invoice_id}"
                req = urllib.request.Request(
                    url,
                    headers = {"X-Token": token},
                    method  = "GET"
                )
                with urllib.request.urlopen(req, timeout=12) as resp:
                    data = _json.loads(resp.read().decode())

                status = data.get("status", "")
                amount = data.get("amount", 0)   # копійки

                if status == "success":
                    # Гроші на рахунку — активуємо EvII
                    card.evo_tier   = 2
                    card.evolved    = True
                    card.divine_xp  = 0
                    card._update_stats()
                    card.hp = card.max_hp
                    self._pending_supreme_card    = None
                    self._pending_supreme_invoice = ""
                    self._supreme_pay_ref         = ""
                    self.supreme_evo_sel          = None
                    self.supreme_msg = (
                        f"✅ Оплата {amount//100} грн підтверджена!\n"
                        f"{card.name} → EvII 👑 (рівні 111-120 розблоковано)"
                    )
                    self.save_game()

                elif status == "processing":
                    self.supreme_msg = "⏳ Оплата обробляється банком… Спробуй ще раз через хвилину."

                elif status == "hold":
                    self.supreme_msg = "⏳ Кошти утримуються. Підтвердження незабаром."

                elif status == "created":
                    self.supreme_msg = (
                        f"⏳ Рахунок створено, але оплата ще не надійшла.\n"
                        f"Ref: {getattr(self,'_supreme_pay_ref','')}"
                    )

                elif status == "failure":
                    self.supreme_msg = "❌ Оплата відхилена банком. Спробуй іншу картку."

                elif status == "reversed":
                    self.supreme_msg = "❌ Оплату скасовано (reversed)."

                elif status == "expired":
                    self.supreme_msg = "❌ Рахунок прострочений. Натисни кнопку оплати ще раз."
                    self._pending_supreme_invoice = ""

                else:
                    self.supreme_msg = f"❓ Невідомий статус: {status}"

            except urllib.error.HTTPError as e:
                try:    body = _json.loads(e.read().decode())
                except: body = {}
                err = body.get("errText", f"HTTP {e.code}")
                if e.code == 429:
                    self.supreme_msg = "⏳ Занадто багато запитів — зачекай 1 хв."
                else:
                    self.supreme_msg = f"❌ {err}"
            except Exception as e:
                self.supreme_msg = f"❌ {str(e)[:80]}"

        threading.Thread(target=_thread, daemon=True).start()

    def _sh_buy_payment(self, sh_data):
        """Monobank платіж за карту Супер Людей."""
        import threading, urllib.request, urllib.error, json as _json, random as _rand
        token = MONOBANK_MERCHANT_TOKEN.strip()
        if not token: self.sh_pay_msg = "❌ Немає токена!"; return
        orig, name, lvl, shard_cost, uah_price = sh_data
        ref = f"SH-{orig[:10]}-{_rand.randint(10000,99999)}"
        self._sh_pay_ref = ref; self._sh_pay_pending_card = sh_data; self._sh_pay_pending_invoice = ""
        self.sh_pay_msg = "⏳ Створюємо рахунок..."
        payload = _json.dumps({
            "amount": uah_price*100, "ccy": 980,
            "merchantPaymInfo": {"reference": ref, "destination": f"Карта {name} (Супер Люди)"},
            "redirectUrl": "https://monobank.ua", "validity": 3600
        }).encode("utf-8")
        def _t():
            try:
                req = urllib.request.Request(
                    "https://api.monobank.ua/api/merchant/invoice/create",
                    data=payload, method="POST",
                    headers={"X-Token": token, "Content-Type": "application/json"})
                with urllib.request.urlopen(req, timeout=15) as resp:
                    data = _json.loads(resp.read().decode())
                page_url = data.get("pageUrl",""); inv_id = data.get("invoiceId","")
                if page_url:
                    self._sh_pay_pending_invoice = inv_id
                    _open_url(page_url)
                    self.sh_pay_msg = f"✅ Браузер відкрито!\nСума: {uah_price} грн · {name}\nНатисни ПЕРЕВІРИТИ після оплати"
                else:
                    self.sh_pay_msg = f"❌ {data.get('errText','Помилка')}"
            except urllib.error.HTTPError as e:
                try: body=_json.loads(e.read().decode())
                except: body={}
                self.sh_pay_msg = f"❌ HTTP {e.code}: {body.get('errText',str(e))}"
            except Exception as e: self.sh_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()

    def _sh_check_payment(self):
        """Перевіряє статус оплати за карту Супер Людей."""
        import threading, urllib.request, urllib.error, json as _json
        token = MONOBANK_MERCHANT_TOKEN.strip()
        inv_id = getattr(self,'_sh_pay_pending_invoice','')
        sh_data = getattr(self,'_sh_pay_pending_card',None)
        if not inv_id or not sh_data:
            self.sh_pay_msg = "❌ Немає активного рахунку. Натисни КУПИТИ."; return
        self.sh_pay_msg = "⏳ Перевіряємо..."
        def _t():
            try:
                req = urllib.request.Request(
                    f"https://api.monobank.ua/api/merchant/invoice/status?invoiceId={inv_id}",
                    headers={"X-Token": token}, method="GET")
                with urllib.request.urlopen(req, timeout=12) as resp:
                    data = _json.loads(resp.read().decode())
                status = data.get("status","")
                orig4,name4,*_ = sh_data
                if status == "success":
                    if orig4 not in self.unlocked_super_humans:
                        self.unlocked_super_humans.append(orig4)
                        _paid_card = create_super_human_card(sh_data)
                        self.all_heroes.append(_paid_card)
                    self._sh_pay_pending_invoice = ""; self._sh_pay_pending_card = None
                    self.sh_sel = None
                    self.sh_pay_msg = (f"✅ Оплата підтверджена! {name4} у МОЯ АРМІЯ! 🦸\n"
                                       f"Карта Lv1. Здібності відкриються з Lv20.")
                    self.save_game()
                elif status in ("created","processing","hold"):
                    self.sh_pay_msg = f"⏳ Статус: {status}. Зачекай або оплати."
                elif status == "failure": self.sh_pay_msg = "❌ Оплату відхилено."
                elif status == "expired":
                    self.sh_pay_msg = "❌ Прострочено. Натисни КУПИТИ знову."
                    self._sh_pay_pending_invoice = ""
                else: self.sh_pay_msg = f"❓ {status}"
            except urllib.error.HTTPError as e:
                self.sh_pay_msg = f"❌ HTTP {e.code}" + (" (rate limit)" if e.code==429 else "")
            except Exception as e: self.sh_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()


    def _arch_buy_payment(self, ar_data):
        """Monobank платіж за карту Архангели та Архдемони."""
        import threading, urllib.request, urllib.error, json as _json, random as _rand
        token = MONOBANK_MERCHANT_TOKEN.strip()
        if not token: self.arch_pay_msg = "❌ Немає токена!"; return
        orig, name, lvl, shard_cost, uah_price = ar_data
        ref = f"AR-{orig[:10]}-{_rand.randint(10000,99999)}"
        self._arch_pay_ref = ref; self._arch_pay_pending_card = ar_data
        self._arch_pay_pending_invoice = ""
        self.arch_pay_msg = "⏳ Створюємо рахунок..."
        payload = _json.dumps({
            "amount": uah_price * 100, "ccy": 980,
            "merchantPaymInfo": {"reference": ref,
                                  "destination": f"Карта {name} (Архангели та Архдемони)"},
            "redirectUrl": "https://monobank.ua", "validity": 3600
        }).encode("utf-8")
        def _t():
            try:
                req = urllib.request.Request(
                    "https://api.monobank.ua/api/merchant/invoice/create",
                    data=payload, method="POST",
                    headers={"X-Token": token, "Content-Type": "application/json"})
                with urllib.request.urlopen(req, timeout=15) as resp:
                    data = _json.loads(resp.read().decode())
                page_url = data.get("pageUrl", ""); inv_id = data.get("invoiceId", "")
                if page_url:
                    self._arch_pay_pending_invoice = inv_id
                    _open_url(page_url)
                    self.arch_pay_msg = (f"✅ Браузер відкрито!\nСума: {uah_price} грн · {name}\nНатисни ПЕРЕВІРИТИ після оплати")
                else:
                    self.arch_pay_msg = f"❌ {data.get('errText','Помилка')}"
            except urllib.error.HTTPError as e:
                try: body = _json.loads(e.read().decode())
                except: body = {}
                self.arch_pay_msg = f"❌ HTTP {e.code}: {body.get('errText', str(e))}"
            except Exception as e:
                self.arch_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()

    def _arch_check_payment(self):
        """Перевіряє статус оплати за карту Архангели та Архдемони."""
        import threading, urllib.request, urllib.error, json as _json
        token = MONOBANK_MERCHANT_TOKEN.strip()
        inv_id  = getattr(self, '_arch_pay_pending_invoice', '')
        ar_data = getattr(self, '_arch_pay_pending_card', None)
        if not inv_id or not ar_data:
            self.arch_pay_msg = "❌ Немає активного рахунку. Натисни КУПИТИ."; return
        self.arch_pay_msg = "⏳ Перевіряємо..."
        def _t():
            try:
                req = urllib.request.Request(
                    f"https://api.monobank.ua/api/merchant/invoice/status?invoiceId={inv_id}",
                    headers={"X-Token": token}, method="GET")
                with urllib.request.urlopen(req, timeout=12) as resp:
                    data = _json.loads(resp.read().decode())
                status = data.get("status", "")
                orig_a, name_a, *_ = ar_data
                if status == "success":
                    if orig_a not in self.unlocked_arch:
                        self.unlocked_arch.append(orig_a)
                        _paid_ar = create_arch_card(ar_data)
                        self.all_heroes.append(_paid_ar)
                    self._arch_pay_pending_invoice = ""; self._arch_pay_pending_card = None
                    self.arch_sel = None
                    self.arch_pay_msg = (f"✅ Оплата підтверджена! {name_a} у МОЯ АРМІЯ! 😇\n"
                                         f"Карта Lv1. Прокачка у ЕВОЛЮЦІЯ РІВНЯ БОЖЕСТВ.")
                    self.save_game()
                elif status in ("created", "processing", "hold"):
                    self.arch_pay_msg = f"⏳ Статус: {status}. Зачекай або оплати."
                elif status == "failure":
                    self.arch_pay_msg = "❌ Оплату відхилено."
                elif status == "expired":
                    self.arch_pay_msg = "❌ Прострочено. Натисни КУПИТИ знову."
                    self._arch_pay_pending_invoice = ""
                else:
                    self.arch_pay_msg = f"❓ {status}"
            except urllib.error.HTTPError as e:
                self.arch_pay_msg = f"❌ HTTP {e.code}" + (" (rate limit)" if e.code == 429 else "")
            except Exception as e:
                self.arch_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()

    def _arch_buy_payment(self, ar_data):
        """Monobank платіж за карту Архангели та Архдемони."""
        import threading, urllib.request, urllib.error, json as _json, random as _rand
        token = MONOBANK_MERCHANT_TOKEN.strip()
        if not token: self.arch_pay_msg = "❌ Немає токена!"; return
        orig, name, lvl, shard_cost, uah_price = ar_data
        ref = f"AR-{orig[:10]}-{_rand.randint(10000,99999)}"
        self._arch_pay_ref = ref; self._arch_pay_pending_card = ar_data
        self._arch_pay_pending_invoice = ""
        self.arch_pay_msg = "⏳ Створюємо рахунок..."
        payload = _json.dumps({
            "amount": uah_price * 100, "ccy": 980,
            "merchantPaymInfo": {"reference": ref,
                                 "destination": f"Карта {name} (Архангели та Архдемони)"},
            "redirectUrl": "https://monobank.ua", "validity": 3600
        }).encode("utf-8")
        def _t():
            try:
                req = urllib.request.Request(
                    "https://api.monobank.ua/api/merchant/invoice/create",
                    data=payload, method="POST",
                    headers={"X-Token": token, "Content-Type": "application/json"})
                with urllib.request.urlopen(req, timeout=15) as resp:
                    data = _json.loads(resp.read().decode())
                page_url = data.get("pageUrl", ""); inv_id = data.get("invoiceId", "")
                if page_url:
                    self._arch_pay_pending_invoice = inv_id
                    _open_url(page_url)
                    self.arch_pay_msg = (f"✅ Браузер відкрито!\nСума: {uah_price} грн · {name}\nНатисни ПЕРЕВІРИТИ після оплати")
                else:
                    self.arch_pay_msg = f"❌ {data.get('errText','Помилка')}"
            except urllib.error.HTTPError as e:
                try: body = _json.loads(e.read().decode())
                except: body = {}
                self.arch_pay_msg = f"❌ HTTP {e.code}: {body.get('errText', str(e))}"
            except Exception as e: self.arch_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()

    def _arch_check_payment(self):
        """Перевіряє статус оплати за карту Архангели та Архдемони."""
        import threading, urllib.request, urllib.error, json as _json
        token = MONOBANK_MERCHANT_TOKEN.strip()
        inv_id = getattr(self, '_arch_pay_pending_invoice', '')
        ar_data = getattr(self, '_arch_pay_pending_card', None)
        if not inv_id or not ar_data:
            self.arch_pay_msg = "❌ Немає активного рахунку. Натисни КУПИТИ."; return
        self.arch_pay_msg = "⏳ Перевіряємо..."
        def _t():
            try:
                req = urllib.request.Request(
                    f"https://api.monobank.ua/api/merchant/invoice/status?invoiceId={inv_id}",
                    headers={"X-Token": token}, method="GET")
                with urllib.request.urlopen(req, timeout=12) as resp:
                    data = _json.loads(resp.read().decode())
                status = data.get("status", "")
                orig_a, name_a, *_ = ar_data
                if status == "success":
                    if orig_a not in self.unlocked_arch:
                        self.unlocked_arch.append(orig_a)
                        _paid = create_arch_card(ar_data)
                        self.all_heroes.append(_paid)
                    self._arch_pay_pending_invoice = ""; self._arch_pay_pending_card = None
                    self.arch_sel = None
                    self.arch_pay_msg = (f"✅ Оплата підтверджена! {name_a} у МОЯ АРМІЯ! 😇\n"
                                         f"Карта Lv1. Здібності відкриються з Lv5.")
                    self.save_game()
                elif status in ("created", "processing", "hold"):
                    self.arch_pay_msg = f"⏳ Статус: {status}. Зачекай або оплати."
                elif status == "failure":
                    self.arch_pay_msg = "❌ Оплату відхилено."
                elif status == "expired":
                    self.arch_pay_msg = "❌ Прострочено. Натисни КУПИТИ знову."
                    self._arch_pay_pending_invoice = ""
                else:
                    self.arch_pay_msg = f"❓ {status}"
            except urllib.error.HTTPError as e:
                self.arch_pay_msg = f"❌ HTTP {e.code}" + (" (rate limit)" if e.code == 429 else "")
            except Exception as e: self.arch_pay_msg = f"❌ {str(e)[:70]}"
        threading.Thread(target=_t, daemon=True).start()

    def _lucky_refresh_if_needed(self):
        """Оновлює спроби Монополії та Фортуни раз у 12 годин."""
        import time as _t
        _now = _t.time()
        _last = getattr(self, 'lucky_last_reset', 0)
        _INTERVAL = 12 * 3600  # 12 годин у секундах
        if _now - _last >= _INTERVAL:
            self.lucky_free_tries  = 3
            self.lucky_gold_tries  = 3
            self.fort_free_tries   = 3
            self.fort_ore_tries    = 3
            self.lucky_last_reset  = _now
            self.save_game()
        return _now - _last, _INTERVAL  # (пройшло, інтервал)

    def save_game(self):
        save_data = {
            "gold": self.gold, "diamonds": self.diamonds, "ore": self.ore, "keys": self.keys,
            "selected_world": self.selected_world, "selected_location": self.selected_location,
            "completed_levels": self.completed_levels,
            "altar_last_draw_time": self.altar_last_draw_time,
            "chest_last_open": self.chest_last_open,
            "titan_level": self.titan_level, "titan_xp": self.titan_xp, "titan_levels": self.titan_levels,
            "titan_id": self.titan_id, "owned_titans": self.owned_titans,
            "titan_shards": self.titan_shards,
            "titan_refresh_time": self.titan_refresh_time,
            "titan_monster_hp": self.titan_monster_hp,
            "all_heroes": [{"name": h.original_name, "level": h.level, "xp": h.xp,
                             "runes": getattr(h, "runes", []),
                             "evolved": getattr(h, "evolved", False),
                             "evo_tier": getattr(h, "evo_tier", 0),
                             "divine_xp": getattr(h, "divine_xp", 0),
                             "ability_levels": getattr(h, "ability_levels", {})}
                            for h in self.all_heroes if not getattr(h, '_super_human', False)],
            "graveyard": [{"name": h.original_name, "level": h.level, "xp": h.xp} for h in self.graveyard],
            "my_runes": self.my_runes,
            "titan_runes": getattr(self, "titan_runes", {}),
            "my_essences": getattr(self, "my_essences", []),
            "essence_last_draw": getattr(self, "essence_last_draw", 0),
            "super_human_shards":    getattr(self, "super_human_shards", {}),
            "unlocked_super_humans": getattr(self, "unlocked_super_humans", []),
            "arch_shards":   getattr(self, "arch_shards", {}),
            "unlocked_arch": getattr(self, "unlocked_arch", []),
            "arch_levels": {
                getattr(h, '_arch_orig', h.original_name): {
                    "level": h.level, "xp": getattr(h, '_arch_xp', 0)
                }
                for h in self.all_heroes if getattr(h, '_arch_card', False)
            },
            "sh_completed_levels": getattr(self, "sh_completed_levels", {}),
            "ad_completed_levels": getattr(self, "ad_completed_levels", {}),
            "lucky_free_tries":  getattr(self, "lucky_free_tries", 3),
            "lucky_gold_tries":  getattr(self, "lucky_gold_tries", 3),
            "fort_free_tries":   getattr(self, "fort_free_tries", 3),
            "fort_ore_tries":    getattr(self, "fort_ore_tries", 3),
            "mono_pos":          getattr(self, "mono_pos", 0),
            "wheel_angle":       getattr(self, "wheel_angle", 0.0),
            "lucky_last_reset":  getattr(self, "lucky_last_reset", 0),
            "super_human_levels": {
                getattr(h, '_super_human_orig', h.original_name): {
                    "level": h.level, "xp": getattr(h, '_super_human_xp', 0)
                }
                for h in self.all_heroes if getattr(h, '_super_human', False)
            },
        }
        # Визначаємо безпечну директорію для збереження
        _save_dir = os.path.dirname(os.path.abspath(__file__))
        _save_path = os.path.join(_save_dir, "savegame.json")
        try:
            with open(_save_path, "w", encoding="utf-8") as f:
                json.dump(save_data, f, ensure_ascii=False, indent=4)
        except PermissionError:
            # Якщо немає дозволу в директорії скрипту — зберігаємо у домашню директорію
            import pathlib
            _home_save = os.path.join(str(pathlib.Path.home()), "savegame_arena.json")
            try:
                with open(_home_save, "w", encoding="utf-8") as f:
                    json.dump(save_data, f, ensure_ascii=False, indent=4)
            except Exception:
                pass  # Якщо і там не вдалося — мовчки пропускаємо
        except Exception:
            pass

    def load_game(self):
        # Визначаємо шлях до файлу збереження (той самий що в save_game)
        _save_dir = os.path.dirname(os.path.abspath(__file__))
        _save_path = os.path.join(_save_dir, "savegame.json")
        if not os.path.exists(_save_path):
            import pathlib
            _home_save = os.path.join(str(pathlib.Path.home()), "savegame_arena.json")
            if os.path.exists(_home_save):
                _save_path = _home_save
        if os.path.exists(_save_path):
            try:
                with open(_save_path, "r", encoding="utf-8") as f:
                    data = json.load(f)
                    self.gold = data.get("gold", 1000)
                    self.diamonds = data.get("diamonds", 100)
                    self.ore = data.get("ore", 500)
                    self.keys = data.get("keys", 10)
                    self.selected_world = data.get("selected_world", 1)
                    self.selected_location = data.get("selected_location", 1)
                    self.altar_last_draw_time = data.get("altar_last_draw_time", 0)
                    self.chest_last_open = data.get("chest_last_open", {c[0]: 0 for c in CHESTS})
                    self.titan_level  = data.get("titan_level", 1)
                    self.titan_xp     = data.get("titan_xp", 0)
                    self.titan_levels = data.get("titan_levels", {"titan_1": 1})
                    self.titan_id     = data.get("titan_id", "titan_1")
                    self.owned_titans = data.get("owned_titans", ["titan_1"])
                    self.titan_shards = data.get("titan_shards", {})
                    self.titan_monster_hp = data.get("titan_monster_hp", {})
                    self.titan_refresh_time = data.get("titan_refresh_time", {})
                    raw_completed = data.get("completed_levels", {})
                    self.completed_levels = {int(k): v for k, v in raw_completed.items()}
                    raw_sh_completed = data.get("sh_completed_levels", {})
                    self.sh_completed_levels = {int(k): v for k, v in raw_sh_completed.items()}
                    raw_ad_completed = data.get("ad_completed_levels", {})
                    self.ad_completed_levels = {int(k): v for k, v in raw_ad_completed.items()}
                    self.lucky_free_tries  = data.get("lucky_free_tries", 3)
                    self.lucky_gold_tries  = data.get("lucky_gold_tries", 3)
                    self.fort_free_tries   = data.get("fort_free_tries", 3)
                    self.fort_ore_tries    = data.get("fort_ore_tries", 3)
                    self.mono_pos          = data.get("mono_pos", 0)
                    self.wheel_angle       = data.get("wheel_angle", 0.0)
                    self.lucky_last_reset  = data.get("lucky_last_reset", 0)
                    # ── Архангели та Архдемони ──────────────────────────────────
                    saved_ar = data.get("arch_shards", {})
                    self.arch_shards   = {ar[0]: saved_ar.get(ar[0], 0) for ar in ARCH_CARDS}
                    self.unlocked_arch = data.get("unlocked_arch", [])
                    saved_ar_levels = data.get("arch_levels", {})
                    for orig_ar in self.unlocked_arch:
                        ar_d = next((a for a in ARCH_CARDS if a[0] == orig_ar), None)
                        if ar_d:
                            c_ar = create_arch_card(ar_d)
                            ar_saved = saved_ar_levels.get(orig_ar, {})
                            if ar_saved.get("level", 1) > 1:
                                c_ar.level = ar_saved["level"]
                                c_ar.max_hp, c_ar.attack = get_arch_stats(c_ar.level)
                                c_ar.hp = c_ar.max_hp
                            c_ar._arch_xp = ar_saved.get("xp", 0)
                            self.all_heroes.append(c_ar)

                    self.all_heroes = []
                    for h in data.get("all_heroes", []):
                        c = Card(h["name"], h["level"])
                        c.xp        = h.get("xp", 0)
                        c.runes     = h.get("runes", [])
                        c.evolved   = h.get("evolved", False)
                        c.evo_tier  = h.get("evo_tier", 0)
                        c.divine_xp = h.get("divine_xp", 0)
                        c.ability_levels = h.get("ability_levels", {})
                        if c.evolved: c._update_stats()
                        self.all_heroes.append(c)

                    self.graveyard = []
                    for h in data.get("graveyard", []):
                        c = Card(h["name"], h["level"])
                        c.hp = 0
                        c.is_alive = False
                        c.xp = h.get("xp", 0)
                        self.graveyard.append(c)
                    self.my_runes = data.get("my_runes", [])
                    self.titan_runes = data.get("titan_runes", {})
                    self.my_essences = data.get("my_essences", [])
                    self.essence_last_draw = data.get("essence_last_draw", 0)
                    saved_sh = data.get("super_human_shards", {})
                    self.super_human_shards = {sh[0]: saved_sh.get(sh[0], 0) for sh in SUPER_HUMAN_CARDS}
                    self.unlocked_super_humans = data.get("unlocked_super_humans", [])
                    saved_sh_levels = data.get("super_human_levels", {})
                    # Відновлюємо Супер Людей у all_heroes — рівно по одній на кожен orig
                    # (вони НЕ зберігаються в all_heroes, тому дублікатів немає)
                    for orig in self.unlocked_super_humans:
                        sh_d = next((s for s in SUPER_HUMAN_CARDS if s[0] == orig), None)
                        if sh_d:
                            c_sh = create_super_human_card(sh_d)
                            sh_saved = saved_sh_levels.get(orig, {})
                            if sh_saved.get("level", 1) > 1:
                                c_sh.level = sh_saved["level"]
                                c_sh.max_hp, c_sh.attack = get_super_human_stats(c_sh.level)
                                c_sh.hp = c_sh.max_hp
                            c_sh._super_human_xp = sh_saved.get("xp", 0)
                            self.all_heroes.append(c_sh)
            except Exception as e:
                print(f"Помилка завантаження: {e}")

    def generate_shop_cards(self, prefix):
        """Генерує 10 унікальних карт з фіксованого пулу (щоб були іменовані картинки)."""
        if "Діамант" in prefix:
            pool = SHOP_DIAMOND_NAMES
        elif "Золотий" in prefix:
            pool = SHOP_GOLD_NAMES
        else:
            pool = SHOP_ORE_NAMES
        chosen = random.sample(pool, min(10, len(pool)))
        return [Card(name, random.randint(1, 50)) for name in chosen]

    def draw_scroll_arrows(self, key, content_h, visible_h, scroll_y, ax, top_y, bot_y, size=36):
        """Малює дві стрілки прокрутки (вгору/вниз). Повертає (up_rect, down_rect)."""
        can_up   = scroll_y > 0
        can_down = scroll_y < content_h - visible_h
        def arrow(cx, cy, direction, active):
            col  = (200, 200, 255) if active else (70, 70, 90)
            bg   = (50, 50, 75)   if active else (30, 30, 45)
            rect = pygame.Rect(cx - size//2, cy - size//2, size, size)
            pygame.draw.rect(screen, bg,  rect, border_radius=8)
            pygame.draw.rect(screen, col, rect, 2, border_radius=8)
            # трикутник
            m = size // 2 - 6
            if direction == 'up':
                pts = [(cx, cy - m), (cx - m, cy + m//2), (cx + m, cy + m//2)]
            else:
                pts = [(cx, cy + m), (cx - m, cy - m//2), (cx + m, cy - m//2)]
            pygame.draw.polygon(screen, col, pts)
            return rect
        up_rect   = arrow(ax, top_y, 'up',   can_up)
        down_rect = arrow(ax, bot_y, 'down', can_down)
        return up_rect, down_rect

    def draw_button(self, text, x, y, w, h, color, active=True):
        rect = pygame.Rect(x, y, w, h)
        draw_color = color if active else GRAY
        pygame.draw.rect(screen, draw_color, rect, border_radius=10)
        txt = font.render(text, True, WHITE)
        screen.blit(txt, (x + (w - txt.get_width()) // 2, y + (h - txt.get_height()) // 2))
        return rect

    def draw_button_with_icons(self, text, x, y, w, h, color, icons, active=True):
        """Малює кнопку з іконками. icons = список (pygame.Surface або None) зліва від тексту."""
        rect = pygame.Rect(x, y, w, h)
        draw_color = color if active else GRAY
        pygame.draw.rect(screen, draw_color, rect, border_radius=10)
        icon_size = h - 10
        valid_icons = [ic for ic in icons if ic is not None]
        total_icon_w = sum(icon_size + 3 for _ in valid_icons)
        txt = font.render(text, True, WHITE)
        total_content_w = total_icon_w + txt.get_width()
        cx = x + (w - total_content_w) // 2
        cy_center = y + h // 2
        for ic in valid_icons:
            scaled = pygame.transform.smoothscale(ic, (icon_size, icon_size))
            screen.blit(scaled, (cx, cy_center - icon_size // 2))
            cx += icon_size + 3
        screen.blit(txt, (cx, cy_center - txt.get_height() // 2))
        return rect

    def draw_wallet(self):
        # Розмір іконок та відступи
        icon_size = 24
        pad = 8
        gap = 6
        items = [
            (ICON_DIAMOND, "💎", str(self.diamonds)),
            (ICON_GOLD,    "🪙", str(self.gold)),
            (ICON_ORE,     "⛏️", str(self.ore)),
            (ICON_KEY,     "🔑", str(self.keys)),
        ]
        x_cursor = 20
        y_base = 12
        total_w = 0
        rendered = []
        for icon, emoji, val in items:
            label = f"{emoji}: {val}" if icon is None else val
            surf = font.render(label, True, WHITE)
            rendered.append((icon, surf))
            total_w += (icon_size + gap if icon else 0) + surf.get_width() + pad * 2
        pygame.draw.rect(screen, (30, 30, 40), (10, 10, total_w + 10, 40), border_radius=8)
        for icon, surf in rendered:
            if icon:
                screen.blit(icon, (x_cursor, y_base + (icon_size - icon.get_height()) // 2))
                x_cursor += icon_size + gap
            screen.blit(surf, (x_cursor, y_base + (icon_size - surf.get_height()) // 2))
            x_cursor += surf.get_width() + pad * 2

    def buy_card(self):
        if not self.shop_selected_card: return
        lvl = self.shop_selected_card.level
        cost = lvl * 1 if self.state == CARDS_DIAMOND else lvl * 100 if self.state == CARDS_GOLD else lvl * 30

        can_buy = False
        if self.state == CARDS_DIAMOND and self.diamonds >= cost: self.diamonds -= cost; can_buy = True
        elif self.state == CARDS_GOLD and self.gold >= cost: self.gold -= cost; can_buy = True
        elif self.state == CARDS_ORE and self.ore >= cost: self.ore -= cost; can_buy = True

        if can_buy:
            new_card = Card(self.shop_selected_card.original_name, self.shop_selected_card.level)
            self.all_heroes.append(new_card)
            if self.state == CARDS_DIAMOND: self.diamond_cards.remove(self.shop_selected_card)
            elif self.state == CARDS_GOLD: self.gold_cards.remove(self.shop_selected_card)
            elif self.state == CARDS_ORE: self.ore_cards.remove(self.shop_selected_card)
            self.shop_selected_card = None

    def resurrect_hero(self):
        if not self.dead_selected_card: return
        lvl = self.dead_selected_card.level
        g_cost, o_cost, d_cost = lvl * 10, lvl * 5, lvl * 1

        if self.keys >= 1 and self.gold >= g_cost and self.ore >= o_cost and self.diamonds >= d_cost:
            self.keys -= 1
            self.gold -= g_cost
            self.ore -= o_cost
            self.diamonds -= d_cost

            hero = self.dead_selected_card
            hero.is_alive = True
            hero.hp = hero.max_hp
            hero.selected = False
            self.all_heroes.append(hero)
            self.graveyard.remove(hero)
            self.dead_selected_card = None

    # --- АЛТАРЬ: визначення рівня картки за прогресом ---
    def altar_get_card_level_range(self):
        """Рівень карти залежить від найвищого відкритого світу.
        Світ 1 -> рівні 1-5, Світ 2 -> 1-10, ..., Світ 20 -> 1-100"""
        max_world = max(self.selected_world, 1)
        for w_id, locs in self.completed_levels.items():
            if len(locs) > 0:
                max_world = max(max_world, w_id)
        max_level = min(100, max_world * 5)
        return 1, max_level

    def altar_draw_card(self):
        """Витягує нову карту героя (показує — без покупки)"""
        now = time.time()
        elapsed = now - self.altar_last_draw_time
        if elapsed < ALTAR_COOLDOWN:
            remaining = int(ALTAR_COOLDOWN - elapsed)
            mins = remaining // 60
            secs = remaining % 60
            self.altar_message = f"Зачекайте ще {mins}хв {secs}с"
            return
        lvl_min, lvl_max = self.altar_get_card_level_range()
        level = random.randint(lvl_min, lvl_max)
        hero_num = random.randint(1, 5)
        self.altar_drawn_card = Card(f"Герой {hero_num}", level)
        self.altar_last_draw_time = now
        self.altar_message = "Картку витягнуто! Купи за 1 🪙"
        rank_stars_check = {"НОВАЧОК": 0, "ВЕТЕРАН": 1, "МАЙСТЕР": 2, "ГЕРОЙ": 3, "ЛЕГЕНДА": 4, "БОЖЕСТВО": 5}
        if rank_stars_check.get(self.altar_drawn_card.rank, 0) > 0:
            self.play_snd(SND_RAMKA)

    def altar_buy_drawn_card(self):
        """Купує вже витягнуту картку за 1 золото"""
        if not self.altar_drawn_card:
            self.altar_message = "Спочатку витягни картку!"
            return
        if self.gold < 1:
            self.altar_message = "Недостатньо золота! Потрібно 1 🪙"
            return
        self.gold -= 1
        new_hero = Card(self.altar_drawn_card.original_name, self.altar_drawn_card.level)
        self.all_heroes.append(new_hero)
        self.altar_drawn_card = None
        self.altar_message = "Герой відправлений до Моєї Армії!"
        self.save_game()

    # ─── БИТВА В ЖИВУ: допоміжні методи ─────────────────────────
    def lb_generate_sim_rooms(self):
        """Генерує 10 симульованих онлайн-кімнат з випадковими колодами."""
        import hashlib
        names = [
            "DragonSlayer_UA","IronWolf88","ShadowMaster","NightHunter",
            "CrystalBlade","ThunderStrike","DarkPhoenix","StormBreaker",
            "BloodRaven","FrostBite99"
        ]
        rooms = []
        for i, name in enumerate(names):
            loc = random.randint(1, 10)
            world = random.randint(1, max(1, len(self.completed_levels)))
            lvl_min, lvl_max = LOCATION_LEVELS.get(loc, (1, 5))
            wb = (world - 1) * 3
            deck = []
            count = random.randint(4, 8)
            for j in range(count):
                lvl = random.randint(lvl_min + wb, lvl_max + wb)
                nm = get_monster_name(loc, j)
                c = Card(nm, lvl, True)
                deck.append(c)
            total_hp  = sum(c.max_hp for c in deck)
            total_atk = sum(c.attack for c in deck)
            max_atk   = max(c.attack for c in deck)
            rooms.append({
                "name":      name,
                "deck":      deck,
                "cards":     count,
                "total_hp":  total_hp,
                "total_atk": total_atk,
                "max_atk":   max_atk,
                "loc":       loc,
                "world":     world,
                "ping":      random.randint(12, 95),
                "status":    random.choice(["Чекає", "Чекає", "Чекає", "В бою"]),
            })
        return rooms

    def lb_start_sim_battle(self, room):
        """Запускає бій у режимі симуляції: армія гравця vs колода кімнати."""
        if not self.army:
            return
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        self.enemies = []
        for c in room["deck"]:
            ec = Card(c.original_name, c.level, True)
            ec.hp = ec.max_hp
            self.enemies.append(ec)
        self.total_hits = 0
        self.titan_clone_done = False
        self.titan_used_hits = set()
        self.is_titan_monster_battle = False
        self.lb_battle_result = None
        self.selected_hero = None
        self.selected_monster = None
        self._lb_opp_name = room.get("name", "Противник")
        self.state = LB_BATTLE

    def lb_get_turn_timeout(self):
        """Повертає ліміт секунд для поточного ходу."""
        h = self.total_hits + 1   # номер наступного ходу
        if h <= 2:   return 38
        if h <= 10:  return 28
        return 19

    def lb_start_lan_battle(self, opp_deck_raw, opp_name='LAN Гравець'):
        """Справжній LAN PvP: армія гравця vs армія суперника.
        Клони ЗАБОРОНЕНІ. Хост ходить першим.
        """
        if not self.army: return
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        self.enemies = []
        for ed in opp_deck_raw:
            ec = Card(ed.get('name','Герой 0'), ed.get('level',1), False)
            ec.hp = ec.max_hp
            self.enemies.append(ec)
        if not self.enemies: return
        self.total_hits           = 0
        self.titan_clone_done     = True   # заборонити клонів у LAN PvP
        self.titan_used_hits      = set()
        self.is_titan_monster_battle = False
        self.lb_battle_result     = None
        self.selected_hero        = None
        self.selected_monster     = None
        self._lb_opp_name         = opp_name
        self._lb_is_lan_pvp       = True
        # Хост ходить першим
        self.lb_lan_my_turn       = (self.lb_lan_role == 'host')
        self.lb_turn_timer_start  = time.time()
        self.lb_turn_timeout      = self.lb_get_turn_timeout()
        self.state = LB_BATTLE

    def lb_check_end(self):
        """Перевіряє кінець LB_BATTLE."""
        living_h = [h for h in self.army if h.is_alive]
        living_e = [e for e in self.enemies if e.is_alive]
        if self.total_hits >= self.max_hits or not living_h or not living_e:
            # Визначаємо чи це ТУРНІР ГЕРОЇВ (PvP) а не АРЕНА (sim-боти)
            _is_tournament = (
                getattr(self, '_lb_is_online_pvp', False) or
                getattr(self, '_lb_is_lan_pvp',    False)
            )
            if living_h:
                self.lb_battle_result = 'win'
                self.diamonds += 100
                self.keys += 3
                # Осколки тільки за ТУРНІР ГЕРОЇВ
                if _is_tournament:
                    _sh_rand1 = random.choice(SUPER_HUMAN_CARDS)
                    _sh_rand2 = random.choice(SUPER_HUMAN_CARDS)
                    self.super_human_shards[_sh_rand1[0]] = self.super_human_shards.get(_sh_rand1[0], 0) + 1
                    self.super_human_shards[_sh_rand2[0]] = self.super_human_shards.get(_sh_rand2[0], 0) + 1
                    # Архангели: +2 осколки за перемогу
                    _ar_w1 = random.choice(ARCH_CARDS); _ar_w2 = random.choice(ARCH_CARDS)
                    self.arch_shards[_ar_w1[0]] = self.arch_shards.get(_ar_w1[0], 0) + 1
                    self.arch_shards[_ar_w2[0]] = self.arch_shards.get(_ar_w2[0], 0) + 1
                    for ar in ARCH_CARDS:
                        if (self.arch_shards.get(ar[0], 0) >= ar[3] and ar[0] not in self.unlocked_arch):
                            self.unlocked_arch.append(ar[0])
                            if not any(getattr(c,'_arch_orig',None)==ar[0] for c in self.all_heroes):
                                self.all_heroes.append(create_arch_card(ar))
                    self._last_shard_reward = f"🏆 +2 осколки: {_sh_rand1[1]} та {_sh_rand2[1]}"
                    # Перевіряємо чи зібрали достатньо осколків
                    for sh in SUPER_HUMAN_CARDS:
                        if (self.super_human_shards.get(sh[0], 0) >= sh[3]
                                and sh[0] not in self.unlocked_super_humans):
                            self.unlocked_super_humans.append(sh[0])
                            if not any(getattr(c, '_super_human_orig', None) == sh[0] for c in self.all_heroes):
                                nc = create_super_human_card(sh)
                                self.all_heroes.append(nc)
                self.play_snd(SND_WIN)
            else:
                self.lb_battle_result = 'lose'
                self.diamonds += 10
                self.keys += 1
                # Осколки тільки за ТУРНІР ГЕРОЇВ
                if _is_tournament:
                    _sh_rand = random.choice(SUPER_HUMAN_CARDS)
                    self.super_human_shards[_sh_rand[0]] = self.super_human_shards.get(_sh_rand[0], 0) + 1
                    self._last_shard_reward = f"💀 +1 осколок: {_sh_rand[1]}"
                    for sh in SUPER_HUMAN_CARDS:
                        if (self.super_human_shards.get(sh[0], 0) >= sh[3]
                                and sh[0] not in self.unlocked_super_humans):
                            self.unlocked_super_humans.append(sh[0])
                            if not any(getattr(c, '_super_human_orig', None) == sh[0] for c in self.all_heroes):
                                nc = create_super_human_card(sh)
                                self.all_heroes.append(nc)
                    # Архангели: +1 осколок за поразку
                    _ar_lose = random.choice(ARCH_CARDS)
                    self.arch_shards[_ar_lose[0]] = self.arch_shards.get(_ar_lose[0], 0) + 1
                    for ar in ARCH_CARDS:
                        if (self.arch_shards.get(ar[0], 0) >= ar[3] and ar[0] not in self.unlocked_arch):
                            self.unlocked_arch.append(ar[0])
                            if not any(getattr(c,'_arch_orig',None)==ar[0] for c in self.all_heroes):
                                self.all_heroes.append(create_arch_card(ar))
                self.play_snd(SND_LOSE)
            self.save_game()

    def lb_lan_try_host(self):
        """Запускає LAN-сервер. Чекає клієнта у фоновому потоці."""
        import socket, threading
        # Скидаємо стан
        self.lb_lan_connected      = False
        self.lb_lan_self_ready     = False
        self.lb_lan_opponent_ready = False
        self.lb_lan_role           = "host"
        self.lb_lan_conn           = None
        self._lb_lan_battle_start  = False
        try:
            if self.lb_lan_socket:
                try: self.lb_lan_socket.close()
                except: pass
            s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            s.bind(("0.0.0.0", 55555))
            s.listen(1)
            self.lb_lan_socket = s
            try:
                hostname = socket.gethostname()
                local_ip = socket.gethostbyname(hostname)
            except:
                local_ip = "127.0.0.1"
            self.lb_lan_status = f"Ваш IP: {local_ip} | Порт: 55555 | Чекаємо клієнта..."

            def _host_thread():
                try:
                    s.settimeout(120)
                    conn, addr = s.accept()
                    conn.settimeout(None)
                    self.lb_lan_conn      = conn
                    self.lb_lan_connected = True
                    self.lb_lan_status    = f"Клієнт {addr[0]} підключився! Натисни ГОТОВО"
                    # Слухаємо повідомлення від клієнта
                    buf = b""
                    while True:
                        try:
                            chunk = conn.recv(256)
                            if not chunk:
                                break
                            buf += chunk
                            while b"\n" in buf:
                                line, buf = buf.split(b"\n", 1)
                                msg = line.decode().strip()
                                if msg.startswith("READY"):
                                    self.lb_lan_opponent_ready = True
                                    self.lb_lan_status = "Клієнт готовий! Натисни ГОТОВО ДО БОЮ"
                                    if ":" in msg:
                                        import json as _j
                                        try:
                                            deck_raw = _j.loads(msg.split(":",1)[1])
                                            _lan_msg_queue.put(("DECK", deck_raw))
                                        except: pass
                                elif msg == "START":
                                    self._lb_lan_battle_start = True
                                elif msg == "TURN":
                                    _lan_msg_queue.put(("TURN", {}))
                                elif msg == "SURRENDER":
                                    _lan_msg_queue.put(("SURRENDER", {}))
                                elif msg.startswith("HIT:"):
                                    import json as _j
                                    try:
                                        hdata = _j.loads(msg[4:])
                                        _lan_msg_queue.put(("HIT", hdata))
                                    except: pass
                        except:
                            break
                except:
                    if self.lb_lan_status.startswith("Ваш"):
                        self.lb_lan_status = "Очікування скасовано"
            threading.Thread(target=_host_thread, daemon=True).start()
        except Exception as e:
            self.lb_lan_status = f"Помилка хоста: {e}"

    def lb_lan_try_join(self, ip):
        """Підключається до LAN-хоста."""
        import socket, threading
        self.lb_lan_connected      = False
        self.lb_lan_self_ready     = False
        self.lb_lan_opponent_ready = False
        self.lb_lan_role           = "client"
        self.lb_lan_conn           = None
        self._lb_lan_battle_start  = False
        ip = ip.strip()
        if not ip:
            self.lb_lan_status = "Введіть IP адресу хоста!"; return
        try:
            s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            s.settimeout(8)
            s.connect((ip, 55555))
            s.settimeout(None)
            self.lb_lan_conn      = s
            self.lb_lan_connected = True
            self.lb_lan_status    = f"Підключено до {ip}! Натисни ГОТОВО"

            def _client_thread():
                buf = b""
                while True:
                    try:
                        chunk = s.recv(256)
                        if not chunk:
                            break
                        buf += chunk
                        while b"\n" in buf:
                            line, buf = buf.split(b"\n", 1)
                            msg = line.decode().strip()
                            if msg.startswith("READY"):
                                self.lb_lan_opponent_ready = True
                                self.lb_lan_status = "Хост готовий! Натисни ГОТОВО ДО БОЮ"
                                if ":" in msg:
                                    import json as _j
                                    try:
                                        deck_raw = _j.loads(msg.split(":",1)[1])
                                        _lan_msg_queue.put(("DECK", deck_raw))
                                    except: pass
                            elif msg == "START":
                                self._lb_lan_battle_start = True
                            elif msg == "TURN":
                                _lan_msg_queue.put(("TURN", {}))
                            elif msg == "SURRENDER":
                                _lan_msg_queue.put(("SURRENDER", {}))
                            elif msg.startswith("HIT:"):
                                import json as _j
                                try:
                                    hdata = _j.loads(msg[4:])
                                    _lan_msg_queue.put(("HIT", hdata))
                                except: pass
                    except:
                        break
            threading.Thread(target=_client_thread, daemon=True).start()
        except Exception as e:
            self.lb_lan_status = f"Не вдалось підключитись до {ip}: {e}"

    def lb_lan_send_ready(self):
        """Надсилає READY+колоду партнеру і позначає себе готовим."""
        import json as _json
        try:
            deck_data = [{"name": c.original_name, "level": c.level} for c in self.army]
            msg = "READY:" + _json.dumps(deck_data, ensure_ascii=False) + "\n"
            self.lb_lan_conn.send(msg.encode("utf-8"))
            self.lb_lan_self_ready = True
        except Exception as e:
            self.lb_lan_status = f"Помилка відправки: {e}"

    def lb_lan_send_start(self):
        """Надсилає START партнеру — запускає бій на обох сторонах."""
        try:
            self.lb_lan_conn.send(b"START\n")
        except: pass

    def lb_lan_send_hit(self, hero_name, monster_name, dmg):
        """Надсилає удар партнеру через LAN і передає хід."""
        import json as _json
        try:
            msg = "HIT:" + _json.dumps({"h": hero_name, "m": monster_name, "d": dmg},
                                        ensure_ascii=False) + "\n"
            self.lb_lan_conn.send(msg.encode("utf-8"))
            self.lb_lan_conn.send(b"TURN\n")  # передаємо хід партнеру
            self.lb_lan_my_turn = False
        except: pass

    def lb_lan_send_surrender(self):
        """Здатись — повідомляємо суперника."""
        try:
            self.lb_lan_conn.send(b"SURRENDER\n")
        except: pass

    def lb_lan_cleanup(self):
        """Закриває всі LAN з'єднання."""
        # Очищаємо чергу повідомлень
        while not _lan_msg_queue.empty():
            try: _lan_msg_queue.get_nowait()
            except: break
        for attr in ("lb_lan_conn", "lb_lan_socket"):
            obj = getattr(self, attr, None)
            if obj:
                try: obj.close()
                except: pass
                setattr(self, attr, None)
        self.lb_lan_connected      = False
        self.lb_lan_self_ready     = False
        self.lb_lan_opponent_ready = False
        self._lb_lan_battle_start  = False

    # ──────────────────── ОНЛАЙН ЧЕРЕЗ СЕРВЕР ────────────────────

    def _online_req(self, method, path, data=None, timeout=5):
        """HTTP запит до сервера. Повертає dict або None."""
        import urllib.request, urllib.error, json as _j
        url = ONLINE_SERVER + path
        try:
            if method == "GET":
                req = urllib.request.Request(url)
                with urllib.request.urlopen(req, timeout=timeout) as r:
                    return _j.loads(r.read())
            else:
                body = _j.dumps(data, ensure_ascii=False).encode("utf-8")
                req  = urllib.request.Request(url, data=body,
                       headers={"Content-Type":"application/json"})
                with urllib.request.urlopen(req, timeout=timeout) as r:
                    return _j.loads(r.read())
        except Exception as e:
            self.online_status = f"Помилка мережі: {e}"
            return None

    def _online_ensure_pid(self):
        """Генерує унікальний PID якщо ще немає."""
        if not self.online_pid:
            import uuid
            self.online_pid = str(uuid.uuid4())[:12]
        if not self.online_my_name:
            self.online_my_name = f"Гравець_{self.online_pid[:4]}"

    def online_find_match(self):
        """Шукає суперника через сервер."""
        import threading
        self._online_ensure_pid()
        if not self.army:
            self.online_status = "⚠ Армія порожня! Додай героїв у Моя Армія"; return
        self.online_searching = True
        self.online_status    = "🔍 Шукаємо суперника..."
        self.online_result    = None
        deck = [{"name": c.original_name, "level": c.level,
                 "attack": c.attack, "hp": c.max_hp, "max_hp": c.max_hp}
                for c in self.army if c.is_alive]
        def _search():
            resp = self._online_req("POST", "/matchmake",
                                    {"pid": self.online_pid, "deck": deck})
            if resp is None: self.online_searching = False; return
            if resp.get("matched"):
                self.online_room_id = resp["room_id"]
                self.online_role    = resp["role"]
                self.online_status  = f"✅ Матч знайдено! Кімната {self.online_room_id}"
                self.online_last_poll = 0  # одразу опитати стан
            elif resp.get("waiting"):
                self.online_status = "⏳ Чекаємо суперника... (автоматично)"
                # продовжуємо шукати у фоні
                import time as _t; _t.sleep(3)
                if self.online_searching: self.online_find_match()
            else:
                self.online_searching = False
                self.online_status = f"Помилка: {resp}"
        threading.Thread(target=_search, daemon=True).start()

    def online_cancel_search(self):
        self.online_searching = False
        self.online_room_id   = ""
        self.online_role      = ""
        self.online_status    = "Пошук скасовано"

    def online_poll_state(self):
        """Опитує стан кімнати з сервера (викликається щокадру якщо час)."""
        import threading, time as _t
        if not self.online_room_id: return
        now = _t.time()
        if now - self.online_last_poll < ONLINE_POLL_INTERVAL: return
        self.online_last_poll = now
        pid = self.online_pid
        rid = self.online_room_id
        def _poll():
            resp = self._online_req("GET", f"/state/{rid}?pid={pid}")
            if not resp: return
            _prev_turn = self.online_whose_turn
            self.online_whose_turn = resp.get("whose_turn","")
            self.online_deadline   = resp.get("turn_deadline", 0)
            _srv_turn_num = resp.get("turn", 0)
            # Оновлюємо лічильник ходів з сервера
            if _srv_turn_num > 0:
                self.total_hits = _srv_turn_num - 1
            self.online_turn = _srv_turn_num
            self.online_log        = resp.get("log", [])
            self.online_result     = resp.get("result")
            # Якщо хід змінився — скидаємо локальний таймер
            if self.online_whose_turn != _prev_turn or self.lb_turn_timer_start == 0:
                self.lb_turn_timer_start = time.time()
                self.lb_turn_timeout     = self.lb_get_turn_timeout()
            # Синхронізувати HP карт з сервером
            srv_my  = resp.get("my_army",  [])
            srv_opp = resp.get("opp_army", [])
            # Оновити HP нашої армії
            for srv_c in srv_my:
                for c in self.army:
                    if c.original_name == srv_c.get("name","") or c.name == srv_c.get("name",""):
                        c.hp     = srv_c.get("hp", c.hp)
                        c.is_alive = c.hp > 0
                        break
            # Оновити HP армії суперника
            self.online_opp_army = srv_opp
            for srv_c in srv_opp:
                for e in self.enemies:
                    if e.original_name == srv_c.get("name","") or e.name == srv_c.get("name",""):
                        e.hp     = srv_c.get("hp", e.hp)
                        e.is_alive = e.hp > 0
                        break
            # Якщо ще у пошуку — перевірити чи знайшли
            if self.online_searching and resp.get("status") == "battle":
                self.online_searching = False
                self.online_status    = "✅ Суперника знайдено! Бій починається!"
            # Автозапуск бою якщо ще не в LB_BATTLE
            if resp.get("status") == "battle" and self.state not in (LB_BATTLE,):
                _lan_msg_queue.put(("ONLINE_START", resp))
        threading.Thread(target=_poll, daemon=True).start()

    def online_hit(self, hero, monster):
        """Надсилає удар на сервер."""
        import threading
        def _hit():
            resp = self._online_req("POST", "/hit", {
                "room_id": self.online_room_id,
                "pid":     self.online_pid,
                "hero":    hero.original_name,
                "monster": monster.original_name,
            })
            if resp and resp.get("ok"):
                self.online_last_poll = 0  # одразу оновити стан
            elif resp and resp.get("error") == "not your turn":
                self.set_battle_phrase("⛔ Зачекай свого ходу!")
        threading.Thread(target=_hit, daemon=True).start()

    def online_surrender(self):
        """Здатись."""
        import threading
        def _surr():
            self._online_req("POST", "/surrender",
                             {"room_id": self.online_room_id, "pid": self.online_pid})
        threading.Thread(target=_surr, daemon=True).start()

    def online_full_reset(self):
        """Повний скид стану онлайн-бою. Генерує новий PID щоб уникнути 400 BAD REQUEST."""
        import uuid
        self.online_pid               = str(uuid.uuid4())[:12]
        self.online_room_id           = ""
        self.online_role              = ""
        self.online_searching         = False
        self.online_whose_turn        = ""
        self.online_deadline          = 0.0
        self.online_last_poll         = 0.0
        self.online_result            = None
        self.online_log               = []
        self.online_turn              = 0
        self.online_status            = ""
        self._lb_is_online_pvp        = False
        self._online_auto_fired       = False
        self._lb_online_result_shown_at = 0
        self.lb_battle_result         = None
        self.lb_turn_timer_start      = 0.0
        self.lb_turn_timeout          = 38
        self.enemies                  = []
        self.selected_hero            = None
        self.selected_monster         = None
        for h in self.army:
            h.hp = h.max_hp; h.is_alive = True; h.selected = False

    def online_start_battle_from_state(self, state_resp):
        """Починає LB_BATTLE після отримання стану кімнати.
        Гарантує чистий стан — усі лишки від попереднього бою очищені.
        """
        if not self.army: return
        # Якщо вже у бою з ТІЄЮ САМОЮ кімнатою — не перезапускати
        if self.state == LB_BATTLE and self._lb_is_online_pvp: return
        # Скидаємо прапори результату попереднього бою
        self.lb_battle_result           = None
        self._lb_online_result_shown_at = 0
        self.online_result              = None
        self._online_auto_fired         = False
        # Відновлюємо армію
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        # Будуємо армію ворога
        self.enemies = []
        for ed in state_resp.get("opp_army", []):
            ec = Card(ed.get("name", "Герой 0"), ed.get("level", 1), False)
            ec.max_hp   = ed.get("max_hp", ec.max_hp)
            ec.hp       = ed.get("hp", ec.max_hp)
            ec.attack   = ed.get("attack", ec.attack)
            ec.is_alive = ec.hp > 0
            self.enemies.append(ec)
        if not self.enemies: return
        self.total_hits              = 0
        self.titan_clone_done        = True
        self.titan_used_hits         = set()
        self.is_titan_monster_battle = False
        self.selected_hero           = None
        self.selected_monster        = None
        self._lb_opp_name            = state_resp.get("opp_name", "🌐 Онлайн Гравець")
        self._lb_is_lan_pvp          = False
        self._lb_is_online_pvp       = True
        self.online_whose_turn       = state_resp.get("whose_turn", "player1")
        self.online_deadline         = state_resp.get("turn_deadline", 0)
        self.online_turn             = state_resp.get("turn", 1)
        self.lb_turn_timer_start     = time.time()
        self.lb_turn_timeout         = self.lb_get_turn_timeout()
        self.online_last_poll        = 0
        self.state = LB_BATTLE

    def altar_draw_card_by_key(self):
        """Витягує нову карту героя за 1 ключ (без кулдауну)"""
        if self.keys < 1:
            self.altar_key_message = "Недостатньо ключів! Потрібен 1 🔑"
            return
        self.keys -= 1
        lvl_min, lvl_max = self.altar_get_card_level_range()
        level = random.randint(lvl_min, lvl_max)
        # З шансом 30% — карта монстра, 70% — герой
        if random.random() < 0.3:
            from_pool = [n for n in RENAME_DICT.values() if not n.startswith("Герой")]
            nm = random.choice(from_pool) if from_pool else f"Герой {random.randint(1,5)}"
            self.altar_key_draw_card = Card(nm, level, is_monster=False)
        else:
            hero_num = random.randint(1, 5)
            self.altar_key_draw_card = Card(f"Герой {hero_num}", level)
        # Відразу додаємо до армії
        self.all_heroes.append(self.altar_key_draw_card)
        self.altar_key_message = f"🔑 Витягнуто: {self.altar_key_draw_card.name} (Lv{self.altar_key_draw_card.level}) → додано до Моєї Армії!"
        rank_stars_check = {"НОВАЧОК": 0, "ВЕТЕРАН": 1, "МАЙСТЕР": 2, "ГЕРОЙ": 3, "ЛЕГЕНДА": 4, "БОЖЕСТВО": 5}
        if rank_stars_check.get(self.altar_key_draw_card.rank, 0) > 0:
            self.play_snd(SND_RAMKA)
        self.save_game()

    def handle_click(self, pos):
        # --- Стрілки прокрутки ---
        SCROLL_STEP = 80
        state_map = {"my_army": MY_ARMY, "graveyard": GRAVEYARD, "worlds": WORLDS,
                     "titans": TITANS_STATE, "titan_monsters": TITAN_MONSTERS,
                     "my_shards": MY_SHARDS, "abil_card": ABILITY_EVO,
                     "super_human_shop": SUPER_HUMAN_SHOP,
                     "sh_worlds": SH_WORLDS,
                     "ad_worlds": AD_WORLDS,
                     "encyclopedia": ENCYCLOPEDIA,
                     "arch_shop": ARCH_SHOP}
        for key, (up_r, dn_r) in getattr(self, "scroll_arrows_rects", {}).items():
            if self.state != state_map.get(key):
                continue
            if up_r.collidepoint(pos):
                self.scroll_offset[key] = max(0, self.scroll_offset.get(key,0) - SCROLL_STEP)
                return
            if dn_r.collidepoint(pos):
                content_map = {
                    "my_army":           100 + max(1,(len(self.all_heroes)+4)//5)*260,
                    "graveyard":         100 + max(1,(len(self.graveyard)+4)//5)*260,
                    "worlds":            120 + 4*140,
                    "titans":            60  + 5*(270+48),
                    "titan_monsters":    60  + 5*(240+46),
                    "my_shards":         65  + 5*(220+28),
                    "abil_card":         55  + max(1,(len(self.all_heroes)+1)//2)*210,
                    "super_human_shop":  60  + max(1,(len(SUPER_HUMAN_CARDS)+4)//5)*290,
                    "sh_worlds":         80  + ((20+4)//5)*120,
                    "ad_worlds":         80  + ((20+4)//5)*120,
                    "encyclopedia":   200,
                    "arch_shop":         60  + max(1,(len(ARCH_CARDS)+4)//5)*290,
                }
                vis_h = screen.get_height() - 160
                max_scroll = max(0, content_map.get(key, vis_h+1) - vis_h)
                self.scroll_offset[key] = min(max_scroll, self.scroll_offset.get(key,0) + SCROLL_STEP)
                return
        if self.state == MENU:
            # Клік по сундуках
            for cid, crect in getattr(self, "chest_rects", {}).items():
                if crect.collidepoint(pos):
                    chest_data = next((c for c in CHESTS if c[0] == cid), None)
                    if chest_data:
                        now = time.time()
                        elapsed = now - self.chest_last_open.get(cid, 0)
                        if elapsed >= chest_data[4]:
                            _, _, _, _, _, rtype, rmin, rmax, _ = chest_data
                            amount = random.randint(rmin, rmax)
                            if rtype == "diamonds": self.diamonds += amount
                            elif rtype == "gold":    self.gold     += amount
                            elif rtype == "ore":     self.ore      += amount
                            elif rtype == "keys":    self.keys     += amount
                            self.chest_last_open[cid] = now
                            self.chest_anim[cid]  = now
                            self.chest_reward[cid] = f"+{amount}!"
                            self.play_snd(SND_WIN)
                            self.save_game()
                    break
            # Перевірка кліку по фото вкладок
            img_rects = getattr(self, "tab_img_rects", {})
            def _img_hit(attr):
                r = img_rects.get(attr)
                return r is not None and r.collidepoint(pos)

            if self.btn_army.collidepoint(pos) or _img_hit("btn_army"):
                self.state = MY_ARMY
            elif self.btn_altar.collidepoint(pos) or _img_hit("btn_altar"):
                self.state = ALTAR; self.altar_message = ""
            elif getattr(self, "btn_altar_hub", pygame.Rect(0,0,0,0)).collidepoint(pos) or _img_hit("btn_altar_hub"):
                self.state = ALTAR_HUB
            elif self.btn_worlds.collidepoint(pos) or _img_hit("btn_worlds"):
                self.state = WORLDS
            elif getattr(self,"btn_battle_hub",pygame.Rect(0,0,0,0)).collidepoint(pos) or _img_hit("btn_battle_hub"):
                self.state = BATTLE_HUB
            elif self.btn_graveyard.collidepoint(pos) or _img_hit("btn_graveyard"):
                self.state = GRAVEYARD
            elif self.btn_settings.collidepoint(pos) or _img_hit("btn_settings"):
                self.state = SETTINGS
            elif self.btn_market.collidepoint(pos) or _img_hit("btn_market"):
                self.state = MARKET
            elif self.btn_my_shards.collidepoint(pos) or _img_hit("btn_my_shards"):
                self.state = MY_SHARDS
            elif getattr(self,"btn_lucky_heroes",pygame.Rect(0,0,0,0)).collidepoint(pos) or _img_hit("btn_lucky_heroes"):
                self.state = LUCKY_HEROES
            elif getattr(self,"btn_encyclopedia",pygame.Rect(0,0,0,0)).collidepoint(pos) or _img_hit("btn_encyclopedia"):
                self.state = ENCYCLOPEDIA; self.enc_section = 0; self.enc_abil_page = 0
            elif self.btn_titan_monsters.collidepoint(pos) or _img_hit("btn_titan_monsters"):
                self.state = TITAN_MONSTERS
            elif getattr(self, "titan_menu_rect", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = TITAN_STATE
            # save_exit перенесено у SETTINGS
            elif self.btn_my_runes.collidepoint(pos) or _img_hit("btn_my_runes"):
                self.state = MY_RUNES; self.rune_selected_idx = None; self.rune_msg = ""
            elif self.btn_rune_altar.collidepoint(pos) or _img_hit("btn_rune_altar"):
                self.state = RUNE_ALTAR; self.rune_msg = ""
            elif getattr(self, "btn_live_battle", pygame.Rect(0,0,0,0)).collidepoint(pos) or _img_hit("btn_live_battle"):
                self.state = LIVE_BATTLE
            elif getattr(self, "btn_arena", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = ARENA_STATE
            elif getattr(self, "btn_tournament", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = TOURNAMENT_STATE
            elif self.btn_evolution.collidepoint(pos) or _img_hit("btn_evolution"):
                self.state = EVOLUTION; self.rune_msg = ""

        elif self.state == BATTLE_HUB:
            if self.btn_back.collidepoint(pos):
                self.state = MENU
            elif getattr(self,"btn_worlds",pygame.Rect(0,0,0,0)).collidepoint(pos) or getattr(self,"_bh_btn_worlds",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = WORLDS
            elif getattr(self,"btn_titan_monsters",pygame.Rect(0,0,0,0)).collidepoint(pos) or getattr(self,"_bh_btn_tm",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = TITAN_MONSTERS
            elif getattr(self,"_bh_btn_sh",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = SH_WORLDS
            elif getattr(self,"_bh_btn_ad",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = AD_WORLDS
            elif getattr(self,"_bh_btn_arena",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = ARENA_STATE
                self.lb_tab = "sim"
            elif getattr(self,"_bh_btn_tournament",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = TOURNAMENT_STATE
                self.lb_tab = "lan"

        elif self.state == ARENA_STATE:
            # Арена (СИМ боти) — та сама логіка що була у LIVE_BATTLE/sim
            if self.btn_back.collidepoint(pos):
                self.state = BATTLE_HUB
            elif self.lb_tab == "sim":
                for i, rr in enumerate(getattr(self,"lb_room_rects",[])):
                    if rr.collidepoint(pos):
                        room = self.lb_sim_rooms[i]
                        if room["status"] != "В бою" and self.army:
                            self.lb_selected_room = room
                            self.lb_lobby_start = time.time()
                            self.lb_lobby_countdown = 5
                            self.state = LB_LOBBY_JOIN
                        break
                if getattr(self,"lb_btn_refresh",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self.lb_sim_rooms = self.lb_generate_sim_rooms()
                if getattr(self,"lb_btn_create",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    if self.army:
                        self.lb_selected_room = None
                        self.lb_lobby_start = time.time()
                        self.lb_lobby_countdown = 8
                        self.state = LB_LOBBY_HOST

        elif self.state == TOURNAMENT_STATE:
            # Турнир героїв (онлайн PvP через сервер)
            if self.btn_back.collidepoint(pos):
                self.state = BATTLE_HUB
            elif getattr(self,"lb_btn_online_find",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if not self.online_searching:
                    self.online_full_reset()
                self.online_find_match()
            elif getattr(self,"lb_btn_online_cancel",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.online_cancel_search()
            elif getattr(self,"lb_btn_online_enter",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.online_room_id:
                    resp = self._online_request("GET", f"/room/{self.online_room_id}/state")
                    if resp and resp.get("status") == "battle":
                        self.online_start_battle_from_state(resp)

        elif self.state == LIVE_BATTLE:
            if self.btn_back.collidepoint(pos): self.state = MENU
            # Вкладки Симуляція / LAN
            elif getattr(self,"lb_btn_sim",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.lb_tab = "sim"
                if not self.lb_sim_rooms: self.lb_sim_rooms = self.lb_generate_sim_rooms()
            elif getattr(self,"lb_btn_lan",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.lb_tab = "lan"
            elif self.lb_tab == "sim":
                # Клік по рядку кімнати
                for i, rr in enumerate(getattr(self,"lb_room_rects",[])):
                    if rr.collidepoint(pos):
                        room = self.lb_sim_rooms[i]
                        if room["status"] != "В бою" and self.army:
                            self.lb_selected_room = room
                            self.lb_lobby_start = time.time()
                            self.lb_lobby_countdown = 5
                            self.state = LB_LOBBY_JOIN
                        break
                # Кнопка Оновити
                if getattr(self,"lb_btn_refresh",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self.lb_sim_rooms = self.lb_generate_sim_rooms()
                # Кнопка Створити битву
                if getattr(self,"lb_btn_create",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    if self.army:
                        self.lb_selected_room = None
                        self.lb_lobby_start = time.time()
                        self.lb_lobby_countdown = 8
                        self.state = LB_LOBBY_HOST
            elif self.lb_tab == "lan":
                # Онлайн через PythonAnywhere
                if getattr(self,"lb_btn_online_find",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self.online_find_match()
                elif getattr(self,"lb_btn_online_cancel",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self.online_cancel_search()
                elif getattr(self,"lb_btn_online_enter",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    resp = self._online_req("GET",
                        f"/state/{self.online_room_id}?pid={self.online_pid}")
                    if resp and resp.get("status") == "battle":
                        self.online_start_battle_from_state(resp)
                    else:
                        self.online_status = "Бій ще не почався або кімната не знайдена"

        elif self.state == LB_LOBBY_HOST:
            if getattr(self,"lb_btn_cancel",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.lb_lan_cleanup()
                self._lb_host_fake_room = None
                self.state = LIVE_BATTLE
            elif getattr(self,"lb_btn_ready",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.lb_tab == "sim":
                    _fr = getattr(self, "_lb_host_fake_room", None)
                    if _fr and self.army:
                        self._lb_host_fake_room = None
                        self.lb_start_sim_battle(_fr)
                elif self.lb_tab == "lan" and self.lb_lan_connected:
                    if not self.lb_lan_self_ready:
                        self.lb_lan_send_ready()
                        self.lb_lan_status = "Відправлено READY + колоду. Чекаємо клієнта..."
                    elif self.lb_lan_opponent_ready:
                        opp_raw = getattr(self, 'lb_lan_enemy_deck', [])
                        if opp_raw:
                            self.lb_lan_send_start()
                            self.lb_start_lan_battle(opp_raw, opp_name='LAN Клієнт')
                        else:
                            self.lb_lan_status = "Чекаємо колоду клієнта..."

        elif self.state == LB_LOBBY_JOIN:
            if getattr(self,"lb_btn_cancel",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.lb_lan_cleanup()
                self.state = LIVE_BATTLE
            elif getattr(self,"lb_btn_start_sim",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.lb_selected_room and self.lb_tab == "sim":
                    self.lb_start_sim_battle(self.lb_selected_room)
            elif getattr(self,"lb_btn_ready_lan",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.lb_tab == "lan" and self.lb_lan_connected:
                    if not self.lb_lan_self_ready:
                        self.lb_lan_send_ready()
                        self.lb_lan_status = "Відправлено READY + колоду. Чекаємо START від хоста..."

        elif self.state == LB_BATTLE:
            if getattr(self,"lb_battle_result",None) is not None:
                if getattr(self,"lb_btn_back_res",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self.lb_battle_result = None
                    # Онлайн PvP → BATTLE_HUB, боти → ARENA_STATE, інше → LIVE_BATTLE
                    if getattr(self,"_lb_is_online_pvp",False) or getattr(self,"_lb_is_online",False):
                        self.state = BATTLE_HUB
                        self._lb_is_online_pvp = False
                        self.online_room_id = ""
                        self.online_searching = False
                    else:
                        self.state = ARENA_STATE
                        self.lb_sim_rooms = self.lb_generate_sim_rooms()
                    return
                elif getattr(self,"lb_btn_new_battle",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    # Нова битва з тим самим противником або новим
                    if self.lb_sim_rooms:
                        _nr = random.choice([r for r in self.lb_sim_rooms if r["status"]!="В бою"] or self.lb_sim_rooms)
                        self.lb_start_sim_battle(_nr)
                    return
            if getattr(self,"lb_btn_flee",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if getattr(self,"_lb_is_online_pvp",False):
                    # Онлайн PvP: повідомляємо сервер, нараховуємо програш і повний скид
                    self.online_surrender()
                    self.diamonds += 10
                    self.save_game()
                    import time as _t_flee; _t_flee.sleep(0.4)
                    self.online_full_reset()
                    self.state = BATTLE_HUB
                    return
                elif getattr(self,"_lb_is_lan_pvp",False) and getattr(self,"lb_lan_conn",None):
                    self.lb_lan_send_surrender()
                self.lb_battle_result = "lose"
                self.diamonds += 10
                self.save_game()
                return
            if self.selected_hero and self.selected_monster:
                if getattr(self,"lb_btn_hit",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    if getattr(self, "_lb_is_online_pvp", False):
                        if self.online_whose_turn == self.online_role:
                            # ── Псевдо-3D ефекти здібностей при ударі (Турнір Героїв) ──
                            _h_ab = get_abilities_for_card(self.selected_hero)
                            _hcx_lb, _hcy_lb = _card_center(self.selected_hero)
                            _mcx_lb, _mcy_lb = _card_center(self.selected_monster)
                            for _ab_lb in _h_ab:
                                spawn_ability_effect(_ab_lb[1], _hcx_lb, _hcy_lb, _mcx_lb, _mcy_lb)
                                self.play_snd(ABILITY_SOUNDS.get(_ab_lb[1]))
                            # Базовий ефект удару
                            spawn_ability_effect("Подвійний Удар", _hcx_lb, _hcy_lb, _mcx_lb, _mcy_lb)
                            self.play_snd(SND_HIT)
                            self.online_hit(self.selected_hero, self.selected_monster)
                            self.total_hits += 1
                            self._online_auto_fired = False
                            self.lb_turn_timer_start = time.time()
                            self.lb_turn_timeout = self.lb_get_turn_timeout()
                            self.set_battle_phrase(f"⚔ {self.selected_hero.name} → {self.selected_monster.name}")
                            self.selected_hero.selected = False
                            self.selected_monster.selected = False
                            self.selected_hero = None
                            self.selected_monster = None
                        else:
                            self.set_battle_phrase("⛔ Зачекай свого ходу!")
                    elif getattr(self,"_lb_is_lan_pvp",False) and not getattr(self,"lb_lan_my_turn",True):
                        return  # не мій хід
                    else:
                        # АРЕНА (sim-боти) або LAN — теж додаємо ефекти
                        _h_ab2 = get_abilities_for_card(self.selected_hero)
                        _hcx_lb2, _hcy_lb2 = _card_center(self.selected_hero)
                        _mcx_lb2, _mcy_lb2 = _card_center(self.selected_monster)
                        for _ab_lb2 in _h_ab2:
                            spawn_ability_effect(_ab_lb2[1], _hcx_lb2, _hcy_lb2, _mcx_lb2, _mcy_lb2)
                        self.execute_battle()
                        self.lb_check_end()
                    return
            for e in self.enemies:
                if e.is_alive and e.rect.collidepoint(pos):
                    if self.selected_monster: self.selected_monster.selected = False
                    self.selected_monster = e; e.selected = True
            for h in self.army:
                if h.is_alive and h.rect.collidepoint(pos):
                    if self.selected_hero: self.selected_hero.selected = False
                    self.selected_hero = h; h.selected = True


        elif self.state == ALTAR_HUB:
            if self.btn_back.collidepoint(pos): self.state = MENU
            elif getattr(self, "btn_hub_altar", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = ALTAR; self.altar_message = ""
            elif getattr(self, "btn_hub_rune_altar", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = RUNE_ALTAR
            elif getattr(self, "btn_hub_altar_essence", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = ALTAR_ESSENCE
                self.altar_ess_msg = ""

        elif self.state == REMOVE_RUNE_STATE:
            if self.btn_back.collidepoint(pos): self.state = MY_RUNES
            # Наведення та клік по картах для зняття руни
            for card in self.all_heroes:
                if card.rect.collidepoint(pos) and getattr(card, "runes", []):
                    self.remove_rune_selected_card = card
                    break
            if getattr(self, "btn_strip_rune", pygame.Rect(0,0,0,0)).collidepoint(pos):
                card = getattr(self, "remove_rune_selected_card", None)
                if card and getattr(card, "runes", []):
                    returned = card.runes.pop(0)  # знімаємо першу руну
                    returned.pop("for_titan", None)
                    self.my_runes.append(returned)
                    self.rune_msg = f"Руну {returned["name"]} знято і повернуто до Моїх Рун!"
                    if not card.runes:
                        self.remove_rune_selected_card = None
                    self.save_game()

        elif self.state == ALTAR:
            if self.btn_back.collidepoint(pos):
                self.state = ALTAR_HUB
                self.altar_drawn_card = None
                self.altar_message = ""
                self.altar_key_draw_card = None
                self.altar_key_message = ""
            elif self.btn_altar_draw.collidepoint(pos):
                self.altar_draw_card()
            elif self.btn_altar_buy.collidepoint(pos):
                self.altar_buy_drawn_card()
            elif getattr(self, "btn_altar_key_draw", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.altar_draw_card_by_key()

        elif self.state == SETTINGS:
            if self.btn_back.collidepoint(pos): self.state = MENU
            elif self.btn_reset.collidepoint(pos): self.reset_progress()
            elif getattr(self, "btn_save_exit", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.save_game(); pygame.quit(); sys.exit()
            elif getattr(self, "btn_toggle_music", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.music_enabled = not self.music_enabled
                if self.music_enabled:
                    pygame.mixer.music.set_volume(0.5)
                    if not pygame.mixer.music.get_busy(): start_music()
                else:
                    pygame.mixer.music.set_volume(0.0)
            elif getattr(self, "btn_toggle_sounds", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.sounds_enabled = not self.sounds_enabled

        elif self.state == GRAVEYARD:
            if self.btn_back.collidepoint(pos): self.state = MENU; self.dead_selected_card = None; self.scroll_offset["graveyard"] = 0
            if self.btn_resurrect.collidepoint(pos): self.resurrect_hero()
            if getattr(self,"btn_bury",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.dead_selected_card and self.dead_selected_card in self.graveyard:
                    self.graveyard.remove(self.dead_selected_card)
                    self.diamonds += 1
                    self.ore      += 2
                    self.gold     += 3
                    self.dead_selected_card = None
                    self.save_game()
            for card in self.graveyard:
                if card.rect.collidepoint(pos):
                    if self.dead_selected_card: self.dead_selected_card.selected = False
                    self.dead_selected_card = card
                    card.selected = True

        elif self.state == MY_ARMY:
            if self.btn_back.collidepoint(pos): self.state = MENU; self.scroll_offset["my_army"] = 0
            # Кнопка СКАСУВАТИ ВИБІР
            elif getattr(self,"btn_cancel_army",pygame.Rect(0,0,0,0)).collidepoint(pos):
                for card in self.army: card.selected = False
                self.army.clear()
            else:
                for card in self.all_heroes:
                    if card.rect.collidepoint(pos):
                        if card in self.army:
                            self.army.remove(card); card.selected = False
                        else:
                            max_cards  = get_titan_max_cards(self.titan_level)
                            army_limit = get_titan_army_limit(self.titan_level)
                            cur_pts    = sum(get_titan_card_points(c) for c in self.army)
                            card_pts   = get_titan_card_points(card)
                            if len(self.army) < max_cards and cur_pts + card_pts <= army_limit:
                                self.army.append(card); card.selected = True

        elif self.state in [CARDS_DIAMOND, CARDS_GOLD, CARDS_ORE]:
            if self.btn_back.collidepoint(pos): self.state = MARKET; self.shop_selected_card = None
            elif self.btn_refresh.collidepoint(pos) and self.keys > 0:
                self.keys -= 1; self.shop_selected_card = None
                if self.state == CARDS_DIAMOND: self.diamond_cards = self.generate_shop_cards("Діамант.")
                elif self.state == CARDS_GOLD: self.gold_cards = self.generate_shop_cards("Золотий")
                elif self.state == CARDS_ORE: self.ore_cards = self.generate_shop_cards("Рудний")
            elif self.btn_buy.collidepoint(pos): self.buy_card()

            cards = self.diamond_cards if self.state == CARDS_DIAMOND else self.gold_cards if self.state == CARDS_GOLD else self.ore_cards
            for c in cards:
                if c.rect.collidepoint(pos):
                    if self.shop_selected_card: self.shop_selected_card.selected = False
                    self.shop_selected_card = c; c.selected = True

        elif self.state == MARKET:
            if self.btn_back.collidepoint(pos): self.state = MENU
            for mstate, mrect in getattr(self, "mkt_rects", {}).items():
                if mrect.collidepoint(pos):
                    base = mstate.replace("_img", "")
                    if base in [CARDS_DIAMOND, CARDS_GOLD, CARDS_ORE, TITANS_STATE, SUPER_HUMAN_SHOP, ARCH_SHOP]:
                        self.state = base
                        if base == SUPER_HUMAN_SHOP:
                            self.sh_sel = None; self.sh_pay_msg = ""
                            if "super_human_shop" not in self.scroll_offset:
                                self.scroll_offset["super_human_shop"] = 0
                        elif base == ARCH_SHOP:
                            self.arch_sel = None; self.arch_pay_msg = ""
                            if "arch_shop" not in self.scroll_offset:
                                self.scroll_offset["arch_shop"] = 0
                        break

        elif self.state == WORLDS:
            if self.btn_back.collidepoint(pos): self.state = BATTLE_HUB; self.scroll_offset["worlds"] = 0
            for i, rect in enumerate(getattr(self,"world_rects",[])):
                if rect and rect.collidepoint(pos):
                    w_id = i + 1
                    # Відкритий якщо: світ 1, або попередній повністю пройдений
                    prev_done = (w_id == 1) or (len(self.completed_levels.get(w_id-1, [])) >= 10)
                    is_done   = len(self.completed_levels.get(w_id, [])) >= 10
                    if prev_done and not is_done:
                        self.selected_world = w_id; self.state = LEVELS

        elif self.state == LEVELS:
            if self.btn_back.collidepoint(pos): self.state = WORLDS
            for i, rect in enumerate(getattr(self,"loc_rects",[])):
                if rect and rect.collidepoint(pos):
                    l_id = i + 1
                    # Локація відкрита якщо: лок 1, або попередня пройдена
                    prev_loc_done = (l_id == 1) or ((l_id-1) in self.completed_levels.get(self.selected_world, []))
                    if prev_loc_done and l_id not in self.completed_levels.get(self.selected_world, []):
                        self.selected_location = l_id; self.start_battle()

        elif self.state == SH_WORLDS:
            if self.btn_back.collidepoint(pos): self.state = BATTLE_HUB; self.scroll_offset["sh_worlds"] = 0
            for i, rect in enumerate(getattr(self,"sh_world_rects",[])):
                if rect and rect.collidepoint(pos):
                    w_id = i + 1
                    prev_done = (w_id == 1) or (len(self.sh_completed_levels.get(w_id-1, [])) >= 10)
                    is_done   = len(self.sh_completed_levels.get(w_id, [])) >= 10
                    if prev_done and not is_done:
                        self.sh_selected_world = w_id; self.state = SH_LEVELS

        elif self.state == SH_LEVELS:
            if self.btn_back.collidepoint(pos): self.state = SH_WORLDS
            for i, rect in enumerate(getattr(self,"sh_loc_rects",[])):
                if rect and rect.collidepoint(pos):
                    l_id = i + 1
                    prev_loc_done = (l_id == 1) or ((l_id-1) in self.sh_completed_levels.get(self.sh_selected_world, []))
                    if prev_loc_done and l_id not in self.sh_completed_levels.get(self.sh_selected_world, []):
                        self.sh_selected_location = l_id; self.start_sh_battle()

        elif self.state == ENCYCLOPEDIA:
            if self.btn_back.collidepoint(pos): self.state = MENU; return
            for _ni, _nbr in enumerate(getattr(self,"_enc_nav_btns",[])):
                if _nbr and _nbr.collidepoint(pos):
                    self.enc_section = _ni; self.scroll_offset["encyclopedia"] = 0; self.enc_abil_page = 0; return
            # Кнопки сторінок здібностей
            if getattr(self,"enc_section",0)==3:
                for _pi, _pb in enumerate(getattr(self,"_enc_abil_pbtns",[])):
                    if _pb and _pb.collidepoint(pos):
                        self.enc_abil_page = _pi; self.scroll_offset["encyclopedia"] = 0; return

        elif self.state == LUCKY_HEROES:
            if self.btn_back.collidepoint(pos): self.state = MENU; return
            if getattr(self,"btn_go_mono",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = MONOPOLY_STATE; self.lucky_prize_pending = None
            elif getattr(self,"btn_go_fort",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = FORTUNA_STATE; self.lucky_prize_pending = None

        elif self.state == MONOPOLY_STATE:
            if self.btn_back.collidepoint(pos): self.state = LUCKY_HEROES; return
            # Кнопка "ЗАБРАТИ" приз
            if self.lucky_prize_pending and getattr(self,"btn_claim",pygame.Rect(0,0,0,0)).collidepoint(pos):
                msg = apply_lucky_prize(self, self.lucky_prize_pending)
                self.lucky_msg = msg; self.lucky_prize_pending = None
                self.mono_skip = (self.lucky_prize_pending or {}).get("type") == "skip"
                self.save_game(); return
            if getattr(self,"btn_roll_free",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.lucky_free_tries > 0 and not self.lucky_prize_pending and not self.mono_skip:
                    self.lucky_free_tries -= 1; self._mono_do_roll()
            elif getattr(self,"btn_roll_gold",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.lucky_gold_tries > 0 and not self.lucky_prize_pending and not self.mono_skip:
                    if self.gold >= 3000:
                        self.gold -= 3000; self.lucky_gold_tries -= 1; self._mono_do_roll()
                    else: self.lucky_msg = "❌ Потрібно 3000 золота!"
            elif getattr(self,"btn_skip_use",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.mono_skip = False; self.lucky_msg = "Пропуск витрачений, можна кидати"

        elif self.state == FORTUNA_STATE:
            if self.btn_back.collidepoint(pos): self.state = LUCKY_HEROES; return
            if self.lucky_prize_pending and getattr(self,"btn_claim",pygame.Rect(0,0,0,0)).collidepoint(pos):
                msg = apply_lucky_prize(self, self.lucky_prize_pending)
                self.lucky_msg = msg; self.lucky_prize_pending = None; self.save_game(); return
            if getattr(self,"btn_spin_free",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.fort_free_tries > 0 and not self.lucky_prize_pending:
                    self.fort_free_tries -= 1; self._fort_do_spin()
            elif getattr(self,"btn_spin_ore",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.fort_ore_tries > 0 and not self.lucky_prize_pending:
                    if self.ore >= 3000:
                        self.ore -= 3000; self.fort_ore_tries -= 1; self._fort_do_spin()
                    else: self.lucky_msg = "❌ Потрібно 3000 руди!"
            elif getattr(self,"btn_refresh_prizes",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.keys >= 1:
                    self.keys -= 1
                    self._fort_prizes = random.sample(LUCKY_PRIZES*2, 16)
                    self.lucky_msg = "✅ Призи оновлено за 1 ключ!"
                else: self.lucky_msg = "❌ Потрібен 1 ключ!"

        elif self.state == AD_WORLDS:
            if self.btn_back.collidepoint(pos): self.state = BATTLE_HUB; self.scroll_offset["ad_worlds"] = 0; return
            for i, rect in enumerate(getattr(self,"ad_world_rects",[])):
                if rect and rect.collidepoint(pos):
                    w_id = i + 1
                    prev_done = (w_id == 1) or (len(self.ad_completed_levels.get(w_id-1, [])) >= 10)
                    is_done   = len(self.ad_completed_levels.get(w_id, [])) >= 10
                    if prev_done and not is_done:
                        self.ad_selected_world = w_id; self.state = AD_LEVELS

        elif self.state == AD_LEVELS:
            if self.btn_back.collidepoint(pos): self.state = AD_WORLDS; return
            for i, rect in enumerate(getattr(self,"ad_loc_rects",[])):
                if rect and rect.collidepoint(pos):
                    l_id = i + 1
                    prev_loc_done = (l_id == 1) or ((l_id-1) in self.ad_completed_levels.get(self.ad_selected_world, []))
                    if prev_loc_done and l_id not in self.ad_completed_levels.get(self.ad_selected_world, []):
                        self.ad_selected_location = l_id; self.start_ad_battle()

        elif self.state == BATTLE:
            if self.btn_back.collidepoint(pos):
                if self.is_titan_monster_battle:
                    # Зберегти поточний HP монстрів
                    tid = self.selected_titan_monster
                    loc_key = str(self.current_titan_monster_loc)
                    if tid not in self.titan_monster_hp:
                        self.titan_monster_hp[tid] = {}
                    self.titan_monster_hp[tid][loc_key] = [
                        {"name": e.original_name, "level": e.level,
                         "max_hp": e.max_hp, "hp": max(0, e.hp),
                         "attack": e.attack}
                        for e in self.enemies]
                    self.save_game()
                    self.state = TITAN_MONSTER_LOCS
                else:
                    self.state = MENU
            if self.btn_battle.collidepoint(pos):
                if getattr(self, '_pending_enemy_attack', None):
                    self.set_battle_phrase("⏳ Зачекай — ворог ще атакує!")
                else:
                    self.execute_battle()
            for h in self.army:
                if h.is_alive and h.rect.collidepoint(pos):
                    if self.selected_hero: self.selected_hero.selected = False
                    self.selected_hero = h; h.selected = True
            for m in self.enemies:
                if m.is_alive and m.rect.collidepoint(pos):
                    if self.selected_monster: self.selected_monster.selected = False
                    self.selected_monster = m; m.selected = True

        elif self.state == TITANS_STATE:
            if self.btn_back.collidepoint(pos): self.state = MARKET
            for tid, (brect, _) in getattr(self, "titan_buy_rects", {}).items():
                if brect.collidepoint(pos):
                    tdata = next((t for t in TITANS_DATA if t[0] == tid), None)
                    if tdata and tid not in self.owned_titans:
                        shards = self.titan_shards.get(tid, 0)
                        if shards >= 100:
                            self.titan_shards[tid] = shards - 100
                            self.owned_titans.append(tid)
                            self.titan_levels[tid] = 1  # новий Титан з рівня 1
                            self.save_game()
                    elif tdata and tid in self.owned_titans:
                        # Зберегти рівень поточного Титана
                        self.titan_levels[self.titan_id] = self.titan_level
                        self.titan_id = tid
                        # Відновити рівень нового
                        self.titan_level = self.titan_levels.get(tid, 1)
                        self.titan_xp = 0
                        self.state = TITAN_STATE
                    break

        elif self.state == TITAN_MONSTERS:
            if self.btn_back.collidepoint(pos): self.state = MENU
            for tid, tr in getattr(self, "tm_titan_rects", {}).items():
                if tr.collidepoint(pos):
                    self.selected_titan_monster = tid
                    self.state = TITAN_MONSTER_LOCS
                    break

        elif self.state == TITAN_MONSTER_LOCS:
            if self.btn_back.collidepoint(pos): self.state = TITAN_MONSTERS
            # Кнопка ОНОВИТИ
            if getattr(self, "btn_tm_refresh", pygame.Rect(0,0,0,0)).collidepoint(pos):
                tid = self.selected_titan_monster
                WEEK = 7 * 24 * 3600
                last_ref = self.titan_refresh_time.get(tid, 0)
                if time.time() - last_ref >= WEEK:
                    if tid in self.titan_monster_hp:
                        del self.titan_monster_hp[tid]
                    self.titan_refresh_time[tid] = time.time()
                    self.save_game()
            for i, lr in enumerate(getattr(self, "tm_loc_rects", [])):
                if lr.collidepoint(pos):
                    loc_id = i + 1
                    self.selected_titan_monster_loc = loc_id
                    self._start_titan_monster_battle(loc_id)
                    break


        elif self.state == MY_RUNES:
            if self.btn_back.collidepoint(pos):
                self.state = MENU; self.rune_selected_idx = None; return
            # ── Пагінація ──
            if getattr(self,'_btn_rune_pg_prev',pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.rune_page = max(0, getattr(self,'rune_page',0) - 1); return
            if getattr(self,'_btn_rune_pg_next',pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.rune_page = getattr(self,'rune_page',0) + 1; return
            for _pi, _pbr in getattr(self,'_rune_pg_btn_list',[]):
                if _pbr.collidepoint(pos):
                    self.rune_page = _pi; return
            # ── Вкладки ──
            for tab_key, trect in getattr(self, "my_runes_tab_rects", {}).items():
                if trect.collidepoint(pos):
                    self.my_runes_tab = tab_key; self.rune_selected_idx = None
                    self.rune_page = 0; return
            if getattr(self, "btn_remove_rune_mode", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.remove_rune_selected_card = None
                self.rune_msg = ""
                self.state = REMOVE_RUNE_STATE; return
            # ── Кнопки дій ──
            if getattr(self, "btn_place_rune", pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.rune_selected_idx is not None:
                    rune = self.my_runes[self.rune_selected_idx]
                    if rune.get("for_titan"):
                        self.rune_msg = "Ця руна для Титанів! Перейди до вкладки Титани."
                    else:
                        placed = False
                        for hero in self.all_heroes:
                            abilities = get_abilities_for_card(hero)
                            names = [a[1] for a in abilities]
                            if rune["name"] in names:
                                if not hasattr(hero, "runes"):
                                    hero.runes = []
                                hero.runes.append(rune)
                                self.my_runes.pop(self.rune_selected_idx)
                                self.rune_selected_idx = None
                                self.rune_msg = f"Руна встановлена на {hero.name}!"
                                placed = True
                                self.save_game()
                                break
                        if not placed:
                            self.rune_msg = "Немає героїв з цією здібністю!"
            elif getattr(self, "btn_place_rune_titan", pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.rune_selected_idx is not None:
                    rune = self.my_runes[self.rune_selected_idx]
                    if not rune.get("for_titan"):
                        self.rune_msg = "Ця руна для Героїв/Монстрів!"
                    else:
                        tdata = next((t for t in TITANS_DATA if t[0] == self.titan_id), None)
                        if tdata:
                            if not hasattr(self, "titan_runes"):
                                self.titan_runes = {}
                            if self.titan_id not in self.titan_runes:
                                self.titan_runes[self.titan_id] = []
                            self.titan_runes[self.titan_id].append(rune)
                            self.my_runes.pop(self.rune_selected_idx)
                            self.rune_selected_idx = None
                            self.rune_msg = f"Руна встановлена на {tdata[1]}!"
                            self.save_game()
            elif getattr(self, "btn_remove_rune", pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.rune_selected_idx is not None:
                    self.my_runes.pop(self.rune_selected_idx)
                    self.rune_selected_idx = None
                    self.rune_msg = "Руну видалено!"
                    self.save_game()
            else:
                # Вибір руни — використовуємо _rune_orig_map для правильного orig_i
                orig_map = getattr(self, "_rune_orig_map", [])
                for i, rrect in enumerate(getattr(self, "rune_rects", [])):
                    if rrect.collidepoint(pos):
                        orig_i = orig_map[i] if i < len(orig_map) else i
                        if self.rune_selected_idx == orig_i:
                            self.rune_selected_idx = None   # зняти виділення
                        else:
                            self.rune_selected_idx = orig_i
                        self.rune_msg = ""
                        return
                # Видалення руни з карти героя (кнопка на карті)
                for hero in self.all_heroes:
                    for ri, rrect in enumerate(getattr(hero, "rune_rects_on_card", [])):
                        if rrect.collidepoint(pos):
                            self.my_runes.append(hero.runes[ri])
                            hero.runes.pop(ri)
                            self.rune_msg = f"Руну знято з {hero.name}!"
                            self.save_game()
                            return

        elif self.state == RUNE_ALTAR:
            if self.btn_back.collidepoint(pos): self.state = ALTAR_HUB; self.rune_msg = ""
            for tab_key, trect in getattr(self, "rune_altar_tab_rects", {}).items():
                if trect.collidepoint(pos):
                    self.rune_altar_tab = tab_key; return
            if getattr(self, "btn_rune_pull", pygame.Rect(0,0,0,0)).collidepoint(pos):
                cost_key = self.rune_altar_tab
                cost = RUNE_PULL_COSTS[cost_key]
                if cost_key == "dia" and self.diamonds >= cost:
                    self.diamonds -= cost
                    new_runes = generate_runes(cost_key)
                    self.my_runes.extend(new_runes)
                    self._last_pulled_runes = new_runes
                    self.rune_msg = f"Отримано 5 рун за {cost} 💎!"
                    self.save_game()
                elif cost_key == "ore" and self.ore >= cost:
                    self.ore -= cost
                    new_runes = generate_runes(cost_key)
                    self.my_runes.extend(new_runes)
                    self._last_pulled_runes = new_runes
                    self.rune_msg = f"Отримано 5 рун за {cost} ⛏️!"
                    self.save_game()
                elif cost_key == "gold" and self.gold >= cost:
                    self.gold -= cost
                    new_runes = generate_runes(cost_key)
                    self.my_runes.extend(new_runes)
                    self._last_pulled_runes = new_runes
                    self.rune_msg = f"Отримано 5 рун за {cost} 🪙!"
                    self.save_game()
                else:
                    self.rune_msg = "Недостатньо ресурсів!"

        elif self.state == EVOLUTION:
            if self.btn_back.collidepoint(pos): self.state = MENU
            if getattr(self,"btn_rune_evo",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=RUNE_EVOLUTION; self.rune_evo_target_idx=None; self.rune_evo_feed_idxs=set(); self.rune_evo_progress=0; self.rune_msg=""; self.rune_evo_scroll=0
            if getattr(self,"btn_divine_evo",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=DIVINE_EVO; self.divine_evo_sel=None; self.divine_msg=""
            if getattr(self,"btn_divine_lvl",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=DIVINE_LEVEL_EVO; self.dl_target_idx=None; self.dl_feed_idxs=set(); self.divine_lvl_msg=""
            if getattr(self,"btn_supreme_evo",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=SUPREME_EVO; self.supreme_evo_sel=None; self.supreme_msg=""
            if getattr(self,"btn_supreme_lvl",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=SUPREME_LEVEL_EVO; self.sl_target_idx=None; self.sl_feed_idxs=set(); self.supreme_lvl_msg=""
            if getattr(self,"btn_ability_evo",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state=ABILITY_EVO; self.abil_card_idx=None; self.abil_sel_idx=None; self.abil_msg=""
                self.dl_target_idx = None
                self.dl_feed_idxs  = set()
                self.divine_lvl_msg = ""

        elif self.state == RUNE_EVOLUTION:
            if self.btn_back.collidepoint(pos):
                self.state = EVOLUTION
                self.rune_evo_target_idx = None
                self.rune_evo_feed_idxs = set()
                self.rune_evo_progress = 0
                self.rune_msg = ""
                return
            # Стрілки прокрутки рун
            hero_runes_sc = [r for r in self.my_runes if not r.get("for_titan")]
            R_COLS_SC = 6; R_H_SC = 90; R_GAP_SC = 8
            total_rows_sc = (len(hero_runes_sc) + R_COLS_SC - 1) // R_COLS_SC
            max_scroll_sc = max(0, total_rows_sc - 4)
            if getattr(self, "btn_rune_scroll_up", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.rune_evo_scroll = max(0, getattr(self, "rune_evo_scroll", 0) - 1)
                return
            if getattr(self, "btn_rune_scroll_down", pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.rune_evo_scroll = min(max_scroll_sc, getattr(self, "rune_evo_scroll", 0) + 1)
                return
            # Вибір цілі прокачки
            hero_runes = [r for r in self.my_runes if not r.get("for_titan")]
            for i, rrect in enumerate(getattr(self, "evo_rune_rects", [])):
                if rrect.collidepoint(pos):
                    if self.rune_evo_target_idx is None:
                        self.rune_evo_target_idx = i
                        self.rune_evo_feed_idxs = set()
                        self.rune_evo_progress = 0
                        self.rune_msg = "Вибери руни для скормлення"
                    elif i != self.rune_evo_target_idx:
                        if i in self.rune_evo_feed_idxs:
                            self.rune_evo_feed_idxs.discard(i)
                        else:
                            self.rune_evo_feed_idxs.add(i)
                    return
            if getattr(self, "btn_feed_runes", pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.rune_evo_target_idx is not None and self.rune_evo_feed_idxs:
                    # hero_runes — відфільтрований список (тільки не-титанові руни)
                    # Треба правильно зіставити індекси hero_runes → my_runes
                    hero_rune_real_idxs = [j for j, r in enumerate(self.my_runes) if not r.get("for_titan")]
                    target_real_idx = hero_rune_real_idxs[self.rune_evo_target_idx] if self.rune_evo_target_idx < len(hero_rune_real_idxs) else None
                    if target_real_idx is None:
                        self.rune_msg = "Помилка: ціль не знайдена!"; return
                    target = self.my_runes[target_real_idx]
                    needed = runes_needed_for_upgrade(target["level"])
                    # Рахуємо прогрес по відфільтрованих індексах
                    feed_hero_idxs = sorted(
                        [i for i in self.rune_evo_feed_idxs if i < len(hero_rune_real_idxs)],
                        reverse=True)
                    progress = sum(rune_feed_progress(self.my_runes[hero_rune_real_idxs[i]]["level"])
                                   for i in feed_hero_idxs)
                    if progress >= needed:
                        if target["level"] < 10:
                            # Підвищити рівень ЦІЛІ спочатку (до видалення)
                            self.my_runes[target_real_idx]["level"] = min(10, target["level"] + 1)
                            new_level = self.my_runes[target_real_idx]["level"]
                            # Видалити скормлені руни (реальні індекси, від більшого до меншого)
                            feed_real_idxs = sorted(
                                [hero_rune_real_idxs[i] for i in feed_hero_idxs
                                 if hero_rune_real_idxs[i] != target_real_idx],
                                reverse=True)
                            for ri in feed_real_idxs:
                                self.my_runes.pop(ri)
                            self.rune_msg = f"Руна прокачана до Lv{new_level}!"
                            self.rune_evo_target_idx = None
                            self.rune_evo_feed_idxs = set()
                            self.rune_evo_progress = 0
                            self.save_game()
                        else:
                            self.rune_msg = "Руна вже максимального рівня (10)!"
                    else:
                        self.rune_msg = f"Потрібно {needed} одиниць прогресу, є {progress}. Додай більше рун!"


        elif self.state == SUPER_HUMAN_SHOP:
            if self.btn_back.collidepoint(pos): self.state = MARKET; return
            for _shi, _sr in enumerate(getattr(self,"sh_card_rects",[])):
                if _sr and _sr.collidepoint(pos):
                    self.sh_sel = None if getattr(self,"sh_sel",None)==_shi else _shi
                    self.sh_pay_msg = ""; return
            _sh_sel3 = getattr(self,"sh_sel",None)
            if _sh_sel3 is not None and _sh_sel3 < len(SUPER_HUMAN_CARDS):
                sh_d3 = SUPER_HUMAN_CARDS[_sh_sel3]
                orig3, name3, lvl3, sc3, up3 = sh_d3
                # ── Кнопка РОЗБЛОКУВАТИ (осколки) ──
                if getattr(self,"btn_sh_unlock",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    _cs3 = self.super_human_shards.get(orig3,0)
                    if orig3 in self.unlocked_super_humans:
                        self.sh_pay_msg = "✅ Вже розблоковано і є у МОЯ АРМІЯ!"
                    elif _cs3 >= sc3:
                        # Витрачаємо осколки, додаємо карту (Lv1, без здібностей)
                        self.super_human_shards[orig3] = _cs3 - sc3
                        self.unlocked_super_humans.append(orig3)
                        _new_card = create_super_human_card(sh_d3)
                        self.all_heroes.append(_new_card)
                        self.sh_pay_msg = (f"✅ {name3} розблоковано за {sc3}🧩!\n"
                                           f"Карта Lv1 у МОЯ АРМІЯ. Здібності з Lv20.")
                        self.save_game()
                    else:
                        self.sh_pay_msg = f"❌ Не вистачає {sc3-_cs3}🧩"
                # ── Кнопка КУПИТИ (гривні) ──
                elif getattr(self,"btn_sh_buy",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self._sh_buy_payment(sh_d3)
                # ── Кнопка ПЕРЕВІРИТИ ──
                elif getattr(self,"btn_sh_check",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self._sh_check_payment()


        elif self.state == ARCH_SHOP:
            if self.btn_back.collidepoint(pos): self.state = MARKET; return
            for _ai, _ar2 in enumerate(getattr(self, "arch_card_rects", [])):
                if _ar2 and _ar2.collidepoint(pos):
                    self.arch_sel = None if getattr(self,"arch_sel",None)==_ai else _ai
                    self.arch_pay_msg = ""; return
            _asel = getattr(self, "arch_sel", None)
            if _asel is not None and _asel < len(ARCH_CARDS):
                ar_d = ARCH_CARDS[_asel]
                orig_a, name_a, lvl_a, sc_a, up_a = ar_d
                if getattr(self,"btn_ar_unlock",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    _cs_a = self.arch_shards.get(orig_a, 0)
                    if orig_a in self.unlocked_arch:
                        self.arch_pay_msg = "✅ Вже розблоковано і є у МОЯ АРМІЯ!"
                    elif _cs_a >= sc_a:
                        self.arch_shards[orig_a] = _cs_a - sc_a
                        self.unlocked_arch.append(orig_a)
                        _new_ar = create_arch_card(ar_d)
                        self.all_heroes.append(_new_ar)
                        self.arch_pay_msg = (f"✅ {name_a} розблоковано за {sc_a}🧩!\n"
                                             f"Карта Lv1 у МОЯ АРМІЯ. Здібності з'являться при прокачці.")
                        self.save_game()
                    else:
                        self.arch_pay_msg = f"❌ Не вистачає {sc_a-_cs_a}🧩"
                elif getattr(self,"btn_ar_buy",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self._arch_buy_payment(ar_d)
                elif getattr(self,"btn_ar_check",pygame.Rect(0,0,0,0)).collidepoint(pos):
                    self._arch_check_payment()
        elif self.state == DIVINE_EVO:
            if self.btn_back.collidepoint(pos):
                self.state = EVOLUTION; self.divine_msg = ""; return
            _div_cards = [c for c in self.all_heroes if c.level >= 100 and not getattr(c, 'evolved', False)]
            for _ci, _cr in enumerate(getattr(self, 'divine_evo_card_rects', [])):
                if _cr.collidepoint(pos):
                    self.divine_evo_sel = None if getattr(self,'divine_evo_sel',None)==_ci else _ci
                    return
            _evo_sel = getattr(self, 'divine_evo_sel', None)
            if _evo_sel is not None and _evo_sel < len(_div_cards):
                _sel_card = _div_cards[_evo_sel]
                def _do_evolve(cost_type):
                    if cost_type == "diamonds":
                        if self.diamonds < DIVINE_EVO_COSTS["diamonds"]:
                            self.divine_msg = f"❌ Не вистачає алмазів! Потрібно {DIVINE_EVO_COSTS['diamonds']}💎"; return
                        self.diamonds -= DIVINE_EVO_COSTS["diamonds"]
                    elif cost_type == "ore":
                        if self.ore < DIVINE_EVO_COSTS["ore"]:
                            self.divine_msg = f"❌ Не вистачає руди! Потрібно {DIVINE_EVO_COSTS['ore']}⛏"; return
                        self.ore -= DIVINE_EVO_COSTS["ore"]
                    elif cost_type == "gold":
                        if self.gold < DIVINE_EVO_COSTS["gold"]:
                            self.divine_msg = f"❌ Не вистачає золота! Потрібно {DIVINE_EVO_COSTS['gold']}🪙"; return
                        self.gold -= DIVINE_EVO_COSTS["gold"]
                    _sel_card.evolved  = True
                    _sel_card.evo_tier = 1
                    _sel_card.divine_xp = 0
                    _sel_card._update_stats()
                    _sel_card.hp = _sel_card.max_hp
                    self.divine_evo_sel = None
                    self.divine_msg = f"✅ {_sel_card.name} еволюціонована! Тепер вона БЕЗСМЕРТНА!"
                    self.save_game()
                if getattr(self,'btn_evo_dia', pygame.Rect(0,0,0,0)).collidepoint(pos):  _do_evolve("diamonds")
                if getattr(self,'btn_evo_ore', pygame.Rect(0,0,0,0)).collidepoint(pos):  _do_evolve("ore")
                if getattr(self,'btn_evo_gold',pygame.Rect(0,0,0,0)).collidepoint(pos):  _do_evolve("gold")

        elif self.state == DIVINE_LEVEL_EVO:
            if self.btn_back.collidepoint(pos):
                self.state = EVOLUTION; self.divine_lvl_msg = ""; return
            # Цілі: еволюціоновані + Супер Люди
            _evo_cards  = [c for c in self.all_heroes if
                           (getattr(c,'evolved',False) and getattr(c,'evo_tier',0)==1 and c.level<DIVINE_MAX_LEVEL)
                           or getattr(c,'_super_human',False)
                           or getattr(c,'_arch_card',False)]
            _feed_cards = [c for c in self.all_heroes if
                           not getattr(c,'evolved',False) and not getattr(c,'_super_human',False)
                           and not getattr(c,'_arch_card',False)]
            for _ti, _tr in enumerate(getattr(self,'dl_target_rects',[])):
                if _tr.collidepoint(pos):
                    self.dl_target_idx = _ti; self.dl_feed_idxs = set()
                    self.divine_lvl_msg = "Обери карти для скормлення праворуч"; return
            for _fi, _fr in enumerate(getattr(self,'dl_feed_rects',[])):
                if _fr.collidepoint(pos):
                    if not hasattr(self,'dl_feed_idxs'): self.dl_feed_idxs = set()
                    self.dl_feed_idxs.discard(_fi) if _fi in self.dl_feed_idxs else self.dl_feed_idxs.add(_fi); return
            if getattr(self,'btn_dl_feed',pygame.Rect(0,0,0,0)).collidepoint(pos):
                _dl_t = getattr(self,'dl_target_idx',None); _dl_f = getattr(self,'dl_feed_idxs',set())
                if _dl_t is not None and _dl_f and _dl_t < len(_evo_cards):
                    _tgt = _evo_cards[_dl_t]
                    _is_sh   = getattr(_tgt,'_super_human',False)
                    _is_arch = getattr(_tgt,'_arch_card',False)
                    _feed_sorted = sorted(_dl_f, reverse=True)
                    _total_lvls = sum(_feed_cards[_fi].level for _fi in _feed_sorted if _fi<len(_feed_cards))
                    _to_remove  = [_feed_cards[_fi] for _fi in _feed_sorted if _fi<len(_feed_cards)]
                    for _fc in _to_remove:
                        if _fc in self.all_heroes: self.all_heroes.remove(_fc)
                    _old_lv = _tgt.level
                    if _is_sh:
                        # Супер Люди: max 125, прокачка з будь-якого рівня
                        _tgt.divine_xp = getattr(_tgt,'divine_xp',0) + _total_lvls
                        _leveled = False
                        while _tgt.divine_xp >= 100 and _tgt.level < 125:
                            _tgt.divine_xp -= 100; _tgt.level += 1
                            _tgt.max_hp, _tgt.attack = get_super_human_stats(_tgt.level)
                            _tgt.hp = min(_tgt.hp+300, _tgt.max_hp); _leveled = True
                    elif _is_arch:
                        # Архангели та Архдемони: max 130, прокачка з 1-го рівня
                        _tgt.divine_xp = getattr(_tgt,'divine_xp',0) + _total_lvls
                        _leveled = False
                        while _tgt.divine_xp >= 100 and _tgt.level < 130:
                            _tgt.divine_xp -= 100; _tgt.level += 1
                            _tgt.max_hp, _tgt.attack = get_arch_stats(_tgt.level)
                            _tgt.hp = min(_tgt.hp+350, _tgt.max_hp); _leveled = True
                    else:
                        _tgt.divine_xp += _total_lvls; _leveled = False
                        while _tgt.divine_xp >= 100 and _tgt.level < DIVINE_MAX_LEVEL:
                            _tgt.divine_xp -= 100; _tgt.level += 1
                            _tgt._update_stats(); _tgt.hp=min(_tgt.hp+500,_tgt.max_hp); _leveled=True
                    if _leveled:
                        self.divine_lvl_msg = f"✅ {_tgt.name} Lv{_old_lv}→Lv{_tgt.level}!"
                    else:
                        self.divine_lvl_msg = f"⬆ +{_total_lvls} EVO XP → {_tgt.divine_xp}/100"
                    self.dl_target_idx=None; self.dl_feed_idxs=set(); self.save_game()

        elif self.state == SUPREME_EVO:
            if self.btn_back.collidepoint(pos): self.state=EVOLUTION; self.supreme_msg=""; return
            if getattr(self,'btn_supreme_check',pygame.Rect(0,0,0,0)).collidepoint(pos):
                self._check_supreme_payment(); return
            _sup_cards2=[c for c in self.all_heroes if c.level>=DIVINE_MAX_LEVEL and getattr(c,'evo_tier',0)<2]
            for _ci,_cr in enumerate(getattr(self,'supreme_evo_card_rects',[])):
                if _cr.collidepoint(pos):
                    self.supreme_evo_sel=None if getattr(self,'supreme_evo_sel',None)==_ci else _ci; return
            _sup_sel2=getattr(self,'supreme_evo_sel',None)
            if _sup_sel2 is not None and _sup_sel2<len(_sup_cards2):
                if getattr(self,'btn_supreme_pay',pygame.Rect(0,0,0,0)).collidepoint(pos):
                    _sc2=_sup_cards2[_sup_sel2]
                    self._do_supreme_payment(_sc2)

        elif self.state == SUPREME_LEVEL_EVO:
            if self.btn_back.collidepoint(pos): self.state=EVOLUTION; self.supreme_lvl_msg=""; return
            _sup_evo2=[c for c in self.all_heroes if getattr(c,'evo_tier',0)==2 and c.level<SUPREME_MAX_LEVEL]
            _feed2   =[c for c in self.all_heroes if getattr(c,'evo_tier',0)<2]
            for _ti,_tr in enumerate(getattr(self,'sl_target_rects',[])):
                if _tr.collidepoint(pos):
                    self.sl_target_idx=_ti; self.sl_feed_idxs=set()
                    self.supreme_lvl_msg="Обери карти для скормки праворуч"; return
            for _fi,_fr in enumerate(getattr(self,'sl_feed_rects',[])):
                if _fr.collidepoint(pos):
                    if not hasattr(self,'sl_feed_idxs'): self.sl_feed_idxs=set()
                    self.sl_feed_idxs.discard(_fi) if _fi in self.sl_feed_idxs else self.sl_feed_idxs.add(_fi); return
            if getattr(self,'btn_sl_feed',pygame.Rect(0,0,0,0)).collidepoint(pos):
                _slt=getattr(self,'sl_target_idx',None); _slf=getattr(self,'sl_feed_idxs',set())
                if _slt is not None and _slf and _slt<len(_sup_evo2):
                    _tgt2=_sup_evo2[_slt]
                    _feed_sorted2=sorted(_slf,reverse=True)
                    _total_lvls2=sum(_feed2[_fi].level for _fi in _feed_sorted2 if _fi<len(_feed2))
                    _to_remove2=[_feed2[_fi] for _fi in _feed_sorted2 if _fi<len(_feed2)]
                    for _fc2 in _to_remove2:
                        if _fc2 in self.all_heroes: self.all_heroes.remove(_fc2)
                    _old_lv2=_tgt2.level
                    _tgt2.divine_xp+=_total_lvls2
                    _leveled2=False
                    while _tgt2.divine_xp>=100 and _tgt2.level<SUPREME_MAX_LEVEL:
                        _tgt2.divine_xp-=100; _tgt2.level+=1; _tgt2._update_stats()
                        _tgt2.hp=min(_tgt2.hp+600,_tgt2.max_hp); _leveled2=True
                    self.supreme_lvl_msg=(f"✅ {_tgt2.name} Lv{_old_lv2}→Lv{_tgt2.level}!" if _leveled2
                                          else f"🌟 +{_total_lvls2} EVO XP → {_tgt2.divine_xp}/100")
                    self.sl_target_idx=None; self.sl_feed_idxs=set(); self.save_game()

        elif self.state == ALTAR_ESSENCE:
            if self.btn_back.collidepoint(pos): self.state = ALTAR_HUB; return
            if getattr(self,"btn_my_ess",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.state = MY_ESSENCES; self.ess_page = 0; self.sel_ess_idx = None; return
            # Безкоштовне витягання
            if getattr(self,"btn_ess_free",pygame.Rect(0,0,0,0)).collidepoint(pos):
                _ela = time.time() - getattr(self,'essence_last_draw',0)
                if _ela >= ESSENCE_COOLDOWN:
                    _new = _random_essence()
                    self.my_essences.append(_new)
                    self.essence_last_draw = time.time()
                    self.altar_ess_msg = f"✅ Отримано: {_new['name']} Lv{_new['level']}!"
                    self.save_game()
                else:
                    _rem2 = int(ESSENCE_COOLDOWN - _ela)
                    self.altar_ess_msg = f"⏱ Зачекай ще {_rem2//3600:02d}:{(_rem2%3600)//60:02d}:{_rem2%60:02d}"
            # За 1 ключ
            elif getattr(self,"btn_ess_key",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.keys >= 1:
                    self.keys -= 1
                    _new = _random_essence()
                    self.my_essences.append(_new)
                    self.altar_ess_msg = f"✅ Ключ витрачено! Отримано: {_new['name']} Lv{_new['level']}!"
                    self.save_game()
                else:
                    self.altar_ess_msg = "❌ Немає ключів!"
            # За 3 ключі — 5 есенцій
            elif getattr(self,"btn_ess_3key",pygame.Rect(0,0,0,0)).collidepoint(pos):
                if self.keys >= 3:
                    self.keys -= 3
                    _news = [_random_essence() for _ in range(5)]
                    self.my_essences.extend(_news)
                    self.altar_ess_msg = f"✅ 5 есенцій отримано! (витрачено 3 🗝️)"
                    self.save_game()
                else:
                    self.altar_ess_msg = f"❌ Потрібно 3 ключі (є {self.keys})!"

        elif self.state == MY_ESSENCES:
            if self.btn_back.collidepoint(pos): self.state = ALTAR_ESSENCE; return
            if getattr(self,"btn_ess_prev",pygame.Rect(0,0,0,0)).collidepoint(pos):
                self.ess_page = max(0, getattr(self,'ess_page',0) - 1); return
            if getattr(self,"btn_ess_next",pygame.Rect(0,0,0,0)).collidepoint(pos):
                _per = 12
                _pages = max(1,(len(getattr(self,'my_essences',[]))+_per-1)//_per)
                self.ess_page = min(_pages-1, getattr(self,'ess_page',0)+1); return
            # Клік по есенції
            for _ei2, _er in enumerate(getattr(self,'ess_rects',[])):
                if _er and _er.collidepoint(pos):
                    _real_i2 = getattr(self,'ess_page',0)*12 + _ei2
                    if getattr(self,'sel_ess_idx',None) == _real_i2:
                        self.sel_ess_idx = None
                    else:
                        self.sel_ess_idx = _real_i2
                    return

        elif self.state == ABILITY_EVO:
            if self.btn_back.collidepoint(pos): self.state = EVOLUTION; self.abil_msg = ""; return
            # Клік по карті ліворуч
            for _ci2,_cr in enumerate(getattr(self,'abil_card_rects',[])):
                if _cr and _cr.collidepoint(pos):
                    self.abil_card_idx = _ci2
                    self.abil_sel_idx  = None
                    self.abil_msg      = ""
                    return
            # Клік по здібності праворуч
            for _ai2,_ar in enumerate(getattr(self,'abil_ability_rects',[])):
                if _ar and _ar.collidepoint(pos):
                    self.abil_sel_idx = None if getattr(self,'abil_sel_idx',None)==_ai2 else _ai2
                    self.abil_msg = ""
                    return
            # Скормити есенцію
            if getattr(self,"btn_abil_feed",pygame.Rect(0,0,0,0)).collidepoint(pos):
                _ci3 = getattr(self,'abil_card_idx',None)
                _ai3 = getattr(self,'abil_sel_idx',None)
                if _ci3 is not None and _ai3 is not None and _ci3 < len(self.all_heroes):
                    _card3 = self.all_heroes[_ci3]
                    _abils3 = get_abilities_for_card(_card3)
                    if _ai3 < len(_abils3):
                        _ab3 = _abils3[_ai3]
                        _need3 = get_essence_for_ability(_ab3[1])
                        _matching3 = [(i,e) for i,e in enumerate(getattr(self,'my_essences',[]))
                                      if e['name']==_need3]
                        if _matching3:
                            _use_idx, _use_ess = _matching3[0]
                            _xp_add = _use_ess['level']
                            if not hasattr(_card3,'ability_levels'): _card3.ability_levels = {}
                            _cur_lv = _card3.ability_levels.get(_ab3[1], 1)
                            _cur_xp = _card3.ability_levels.get(_ab3[1]+"_xp", 0)
                            if _cur_lv >= 20:
                                self.abil_msg = f"⭐ Здібність '{_ab3[1]}' вже MAX (20)!"
                            else:
                                _cur_xp += _xp_add
                                _leveled_up = False
                                while _cur_xp >= ESSENCE_XP_PER_CARD and _cur_lv < 20:
                                    _cur_xp -= ESSENCE_XP_PER_CARD
                                    _cur_lv  += 1
                                    _leveled_up = True
                                _card3.ability_levels[_ab3[1]]       = _cur_lv
                                _card3.ability_levels[_ab3[1]+"_xp"] = _cur_xp
                                self.my_essences.pop(_use_idx)
                                if _leveled_up:
                                    self.abil_msg = f"⭐ '{_ab3[1]}' → Lv{_cur_lv}! (+{_xp_add} XP)"
                                else:
                                    self.abil_msg = f"+{_xp_add} XP до '{_ab3[1]}' (Lv{_cur_lv}, {_cur_xp}/100)"
                                self.save_game()
                        else:
                            self.abil_msg = f"❌ Немає есенції '{_need3}'!"

        elif self.state == MY_SHARDS:
            if self.btn_back.collidepoint(pos): self.state = MENU

        elif self.state == TITAN_STATE:
            if self.btn_back.collidepoint(pos): self.state = MENU
            # Клік по іменах Титанів що в owned — перемикання активного
            for tid, (nr, _) in getattr(self, "titan_name_rects", {}).items():
                if nr.collidepoint(pos) and tid in self.owned_titans:
                    self.titan_id = tid; break

        elif self.state == GAME_OVER:
            if self.btn_back.collidepoint(pos):
                # Бонусна карта переходить у Мою Армію
                if getattr(self, "victory_bonus_card", None):
                    self.all_heroes.append(self.victory_bonus_card)
                    self.victory_bonus_card = None
                    self.save_game()
                self.state = TITAN_MONSTER_LOCS if self.is_titan_monster_battle else MENU

    def draw(self, w, h):
        screen.fill(BLACK)
        if self.state not in [BATTLE, GAME_OVER, LB_BATTLE]: self.draw_wallet()

        if self.state == MENU:
            # --- Налаштування сітки вкладок меню 4x3 (4 колонки, 3 рядки) ---
            COLS = 4
            # Адаптивна ширина: вміщуємо 4 колонки у ширину екрана
            BTN_W = max(160, min(220, (w - 120) // 4))
            BTN_H = 38
            V_GAP = 14
            H_GAP = V_GAP  # горизонтальний відступ = вертикальному
            IMG_W = BTN_W
            IMG_H = int(BTN_W * 0.55)
            CELL_H = IMG_H + BTN_H + 4
            TAB_COLOR = (30, 160, 30)

            total_grid_w = COLS * BTN_W + (COLS - 1) * H_GAP
            grid_x0 = w // 2 - total_grid_w // 2
            # 12 слотів: 4 колонки × 3 рядки
            ROWS = 3
            total_grid_h = ROWS * CELL_H + (ROWS - 1) * V_GAP
            grid_y0 = max(55, h // 2 - total_grid_h // 2 + 20)

            # Дані: (назва кнопки, ім'я файлу фото, rect-атрибут)
            tabs = [
                # Слот 0 — Титан (TITAN_SLOT=0)
                # Слот 1
                ("МОЯ АРМІЯ",       "my_army",        "btn_army"),
                # Слот 2
                ("МОЇ РУНИ",        "my_runes",       "btn_my_runes"),
                # Слот 3
                ("АЛТАРЬ",          "altar_hub",       "btn_altar_hub"),
                # Слот 4 — порожній (12 слотів: 11 вкладок + 1 Титан = 12)
                ("КЛАДОВИЩЕ",       "graveyard",       "btn_graveyard"),
                # Слот 5
                ("БИТВА",           "battle_hub",      "btn_battle_hub"),
                # Слот 6
                ("ЕВОЛЮЦІЯ",        "evolution",       "btn_evolution"),
                # Слот 7
                ("РИНОК",           "market",          "btn_market"),
                # Слот 8
                ("МОЇ ОСКОЛКИ",     "my_shards",       "btn_my_shards"),
                # Слот 9
                ("НАЛАШТУВАННЯ",    "settings",        "btn_settings"),
                # Слот 10
                ("ВДАЧА ГЕРОЇВ",    "lucky_heroes",    "btn_lucky_heroes"),
                # Слот 11
                ("ЕНЦИКЛОПЕДІЯ",     "encyclopedia",    "btn_encyclopedia"),
            ]

            self.tab_img_rects = {}
            self.chest_rects = {}

            # --- Розміри та позиція сундуків ---
            CW, CH = 70, 55          # ширина/висота сундука
            C_GAP  = 18              # відступ між двома сундуками по вертикалі
            C_MARGIN = 18            # відступ сундука від краю сітки
            # 2 сундуки зліва, 2 справа
            # 4 сундуки горизонтально по центру над сіткою
            total_chests_w = 4 * CW + 3 * C_GAP
            chest_start_x  = w // 2 - total_chests_w // 2
            chest_y        = grid_y0 - CH - 38   # вище сітки
            chest_positions = [
                ("diamond", chest_start_x + 0 * (CW + C_GAP), chest_y),
                ("gold",    chest_start_x + 1 * (CW + C_GAP), chest_y),
                ("ore",     chest_start_x + 2 * (CW + C_GAP), chest_y),
                ("keys",    chest_start_x + 3 * (CW + C_GAP), chest_y),
            ]
            now_t = time.time()
            for cid, cx2, cy2 in chest_positions:
                chest_data = next(c for c in CHESTS if c[0] == cid)
                _, clabel, body_col, lid_col, cooldown, _, _, _, icon_key = chest_data
                elapsed = now_t - self.chest_last_open.get(cid, 0)
                time_left = max(0, int(cooldown - elapsed))
                is_open = (now_t - self.chest_anim.get(cid, 0)) < 2.0
                if not is_open and time_left == 0:
                    self.chest_reward[cid] = ""
                reward_txt = self.chest_reward.get(cid, "") if is_open else ""
                icon_surf = {"diamond": ICON_DIAMOND, "gold": ICON_GOLD,
                             "ore": ICON_ORE, "key": ICON_KEY}.get(icon_key)
                draw_chest(screen, cx2, cy2, CW, CH, body_col, lid_col,
                           is_open, clabel, reward_txt, time_left, icon_surf)
                self.chest_rects[cid] = pygame.Rect(cx2 - 3, cy2 - 3, CW + 6, CH + 6)

            # Сітка 3×3 = 9 позицій: 8 вкладок + Титан у позиції 4 (індекс 4)
            # Всі 9 позицій мають однаковий розмір CELL_H
            TITAN_SLOT = 0   # Титан завжди першим у лівому верхньому куті

            # Підготовка даних Титана
            tdata_m = next((t for t in TITANS_DATA if t[0] == self.titan_id), TITANS_DATA[0])
            _, tname_m, tdesc_m, tcol_m, _ = tdata_m
            titan_max_hp_m = (10 + (self.titan_level-1)*12 + (self.titan_level//10)*50) * 10
            titan_max_xp_m = 10 * self.titan_level
            titan_pts_m    = get_titan_army_limit(self.titan_level)
            titan_slots_m  = get_titan_max_cards(self.titan_level)

            for slot in range(12):
                col = slot % COLS
                row = slot // COLS
                cell_x = grid_x0 + col * (BTN_W + H_GAP)
                cell_y = grid_y0 + row * (CELL_H + V_GAP)

                if slot == TITAN_SLOT:
                    # ── Титан як карта у клітинці ──
                    TT_W, TT_H = BTN_W, CELL_H
                    tt_x, tt_y = cell_x, cell_y
                    # Золота рамка якщо рівень >= 8
                    if self.titan_level >= 8:
                        pygame.draw.rect(screen, GOLD, (tt_x-3, tt_y-3, TT_W+6, TT_H+6), border_radius=14)
                    pygame.draw.rect(screen, tcol_m, (tt_x-2, tt_y-2, TT_W+4, TT_H+4), border_radius=13)
                    pygame.draw.rect(screen, (30, 30, 48), (tt_x, tt_y, TT_W, TT_H), border_radius=12)
                    pygame.draw.rect(screen, tcol_m, (tt_x, tt_y, TT_W, TT_H), 2, border_radius=12)
                    # Ранг
                    rl = font_small.render(f"ТИТАН  Lv {self.titan_level}", True, GOLD)
                    screen.blit(rl, (tt_x + TT_W//2 - rl.get_width()//2, tt_y + 4))
                    # Картинка (займає IMG_H)
                    ia_h = IMG_H; ia_w = TT_W - 8
                    ix2 = tt_x + 4; iy2 = tt_y + 20
                    t_drawn = False
                    for ext2 in [".gif", ".png", ".jpg"]:
                        tp2 = os.path.join("images", "World", self.titan_id + ext2)
                        if os.path.exists(tp2):
                            try:
                                ti2 = pygame.image.load(tp2).convert_alpha()
                                ti2 = pygame.transform.smoothscale(ti2, (ia_w, ia_h))
                                screen.blit(ti2, (ix2, iy2)); t_drawn = True; break
                            except: pass
                    if not t_drawn:
                        pygame.draw.rect(screen, tcol_m, (ix2, iy2, ia_w, ia_h), border_radius=8)
                        ph_m = font_bold.render(tname_m[0], True, WHITE)
                        screen.blit(ph_m, (ix2+ia_w//2-ph_m.get_width()//2, iy2+ia_h//2-ph_m.get_height()//2))
                    # Назва Титана замість кнопки
                    nm2 = font_small.render(tname_m, True, WHITE)
                    ny2 = iy2 + ia_h + 4
                    screen.blit(nm2, (tt_x + TT_W//2 - nm2.get_width()//2, ny2))
                    # Кнопка-назва Титана
                    self.draw_button(tname_m, tt_x, tt_y + TT_H - BTN_H, TT_W, BTN_H, tcol_m)
                    self.titan_menu_rect = pygame.Rect(tt_x, tt_y, TT_W, TT_H)
                    # Руни на Титані (верхній правий кут картинки)
                    t_runes = getattr(self, "titan_runes", {}).get(self.titan_id, [])
                    for ri, tr_rune in enumerate(t_runes[:5]):
                        tr_x = tt_x + TT_W - 14 - ri * 20
                        tr_y = tt_y + 22
                        draw_rune_circle(screen, tr_x, tr_y, 9, tr_rune["level"], (255, 180, 60))

                else:
                    # ── Звичайна вкладка ──
                    tab_i = slot if slot < TITAN_SLOT else slot - 1
                    if tab_i >= len(tabs): continue
                    label, img_name, btn_attr = tabs[tab_i]
                    img = load_tab_image(img_name, IMG_W, IMG_H)
                    if img:
                        screen.blit(img, (cell_x, cell_y))
                        self.tab_img_rects[btn_attr] = pygame.Rect(cell_x, cell_y, IMG_W, IMG_H)
                    btn_y = cell_y + IMG_H + 4
                    btn_rect = self.draw_button(label, cell_x, btn_y, BTN_W, BTN_H, TAB_COLOR)
                    setattr(self, btn_attr, btn_rect)

        elif self.state == BATTLE_HUB:
            # ═══ ХАБ БИТВИ — сітка 2×4 ════════════════════════════
            title_bh = font_big.render("⚔  БИТВА  ⚔", True, (255,80,60))
            screen.blit(title_bh, (w//2-title_bh.get_width()//2, 8))
            # Сітка 2 рядки × 4 колонки = 8 слотів (5 вкладок + 3 порожніх)
            _bh_tiles = [
                ("КАРТА СВІТІВ",   "worlds",         "_bh_btn_worlds",      (20,80,180),  "🗺"),
                ("СУПЕР ЛЮДЕЙ",    "super_humans",   "_bh_btn_sh",          (0,160,100),  "🦸"),
                ("АНГЕЛИ ТА ДЕМОНИ","angel_demons",  "_bh_btn_ad",          (180,120,0),  "😇"),
                ("ТИТАНИ-МОНСТРИ", "titan_monsters", "_bh_btn_tm",          (120,40,160), "👹"),
                ("АРЕНА",          "arena",          "_bh_btn_arena",       (160,40,20),  "⚔"),
                ("ТУРНИР ГЕРОЇВ",  "tournament",     "_bh_btn_tournament",  (20,120,80),  "🏆"),
            ]
            _BH_COLS  = 4
            _BH_ROWS  = 2
            _bh_gap   = 14
            _bh_tw    = (w - 40 - (_BH_COLS - 1) * _bh_gap) // _BH_COLS
            _bh_th_img = int(_bh_tw * 0.60)
            _bh_cell_h = _bh_th_img + 44
            _bh_x0    = 20
            _bh_y0    = 42
            for _bi, (_blbl,_bimg_name,_brect_attr,_bcol,_bemoji) in enumerate(_bh_tiles):
                _bc = _bi % _BH_COLS
                _br = _bi // _BH_COLS
                _bx = _bh_x0 + _bc * (_bh_tw + _bh_gap)
                _by = _bh_y0 + _br * (_bh_cell_h + _bh_gap)
                # Фон плитки
                pygame.draw.rect(screen, (15,15,25), (_bx, _by, _bh_tw, _bh_cell_h), border_radius=12)
                pygame.draw.rect(screen, _bcol,      (_bx, _by, _bh_tw, _bh_cell_h), 2, border_radius=12)
                # Картинка
                _bimg2 = load_tab_image(_bimg_name, _bh_tw-4, _bh_th_img)
                if _bimg2:
                    screen.blit(_bimg2, (_bx+2, _by+2))
                else:
                    pygame.draw.rect(screen, tuple(c//2 for c in _bcol),
                                     (_bx+2, _by+2, _bh_tw-4, _bh_th_img), border_radius=10)
                    _emsf = font_big.render(_bemoji, True, _bcol)
                    screen.blit(_emsf, (_bx+_bh_tw//2-_emsf.get_width()//2,
                                        _by+_bh_th_img//2-_emsf.get_height()//2))
                # Кнопка-підпис
                _brect = self.draw_button(_blbl, _bx, _by+_bh_th_img+4, _bh_tw, 38, _bcol)
                setattr(self, _brect_attr, _brect)
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == ARENA_STATE:
            # ═══ АРЕНА (СИМ боти) ══════════════════════════════════
            title_ar = font_big.render("⚔  АРЕНА  ⚔", True,(255,120,40))
            screen.blit(title_ar,(w//2-title_ar.get_width()//2,12))
            _ar_img = load_tab_image("arena", min(w-40,800),60)
            if _ar_img: screen.blit(_ar_img,(w//2-min(w-40,800)//2,55))
            else:
                pygame.draw.rect(screen,(30,15,5),(w//2-300,55,600,60),border_radius=8)
                pygame.draw.rect(screen,(220,80,20),(w//2-300,55,600,60),2,border_radius=8)
            _ar_list_y = 125
            # Список симульованих кімнат (та сама логіка що була в LIVE_BATTLE sim)
            if not self.lb_sim_rooms: self.lb_sim_rooms = self.lb_generate_sim_rooms()
            _cols_ar = [("Гравець",80),("Карт",35),("Заг.HP",70),("Макс.ATK",70),("Заг.ATK",70),("Локація",60),("Пінг",45),("Статус",65)]
            _cx_ar = 30
            for _cn_ar,_cw_ar in _cols_ar:
                screen.blit(font_small.render(_cn_ar,True,GOLD),(_cx_ar,_ar_list_y))
                _cx_ar+=_cw_ar+10
            pygame.draw.line(screen,(80,80,80),(20,_ar_list_y+18),(w-20,_ar_list_y+18),1)
            self.lb_room_rects=[]
            for _ri_ar,_rm_ar in enumerate(self.lb_sim_rooms[:10]):
                _ry_ar=_ar_list_y+24+_ri_ar*32
                _rbg_ar=(20,35,20) if _rm_ar["status"]=="Чекає" else (35,20,20)
                _rr_ar=pygame.Rect(20,_ry_ar-2,w-40,30)
                pygame.draw.rect(screen,_rbg_ar,_rr_ar,border_radius=5)
                pygame.draw.rect(screen,(60,60,60),_rr_ar,1,border_radius=5)
                self.lb_room_rects.append(_rr_ar)
                _vals_ar=[(_rm_ar["name"],(255,220,100)),(str(_rm_ar["cards"]),(180,255,180)),
                          (str(_rm_ar["total_hp"]),(100,200,255)),(str(_rm_ar["max_atk"]),(255,140,40)),
                          (str(_rm_ar["total_atk"]),(255,100,100)),(f"Лок.{_rm_ar['loc']} С.{_rm_ar['world']}",(180,180,255)),
                          (f"{_rm_ar['ping']}мс",(100,220,100) if _rm_ar["ping"]<50 else (220,180,50)),
                          (_rm_ar["status"],(80,220,80) if _rm_ar["status"]=="Чекає" else (220,80,80))]
                _vx_ar=30
                for (_vt_ar,_vc_ar),(_cn_ar2,_cw_ar2) in zip(_vals_ar,_cols_ar):
                    screen.blit(font_small.render(_vt_ar,True,_vc_ar),(_vx_ar,_ry_ar+6))
                    _vx_ar+=_cw_ar2+10
            _btn_ar_y=h-70
            self.lb_btn_create  = self.draw_button("➕ Створити битву", w//2-320,_btn_ar_y,200,50,(30,140,30))
            self.lb_btn_refresh = self.draw_button("🔄 Оновити список",  w//2-100,_btn_ar_y,200,50,(40,80,160))
            if not self.army:
                screen.blit(font_small.render("⚠ Армія порожня!",True,(220,80,80)),(w//2-80,h-108))
            self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)

        elif self.state == TOURNAMENT_STATE:
            # ═══ ТУРНИР ГЕРОЇВ (онлайн PvP) ════════════════════════
            title_tn = font_big.render("🏆  ТУРНИР ГЕРОЇВ  🏆", True,(80,220,100))
            screen.blit(title_tn,(w//2-title_tn.get_width()//2,12))
            _tn_img = load_tab_image("tournament", min(w-40,800),60)
            if _tn_img: screen.blit(_tn_img,(w//2-min(w-40,800)//2,55))
            else:
                pygame.draw.rect(screen,(5,30,10),(w//2-300,55,600,60),border_radius=8)
                pygame.draw.rect(screen,(40,180,60),(w//2-300,55,600,60),2,border_radius=8)
            _tn_y=125
            # Той самий онлайн UI що був у LIVE_BATTLE/lan
            self._online_ensure_pid()
            _tn_id_s = font_small.render(f"Сервер: {ONLINE_SERVER}  |  Ваш ID: {self.online_pid}  |  Ім'я: {self.online_my_name}",True,GOLD)
            screen.blit(_tn_id_s,(w//2-_tn_id_s.get_width()//2,_tn_y))
            if self.online_status:
                _tn_sc=(80,230,80) if ("✅" in self.online_status or "знайдено" in self.online_status) else \
                       (255,180,60) if ("⏳" in self.online_status or "Шукаємо" in self.online_status) else \
                       (220,80,80)  if ("⚠" in self.online_status or "Помилка" in self.online_status) else (180,180,180)
                _tn_ss=font.render(self.online_status,True,_tn_sc)
                screen.blit(_tn_ss,(w//2-_tn_ss.get_width()//2,_tn_y+24))
            _tn_ob_y=_tn_y+72
            if not self.online_searching and not self.online_room_id:
                self.lb_btn_online_find=self.draw_button("🔍 ЗНАЙТИ СУПЕРНИКА",w//2-160,_tn_ob_y,320,52,(20,120,60))
            elif self.online_searching:
                self.lb_btn_online_find=pygame.Rect(0,0,0,0)
                self.lb_btn_online_cancel=self.draw_button("✖ Скасувати пошук",w//2-140,_tn_ob_y,280,48,(100,30,30))
            elif self.online_room_id and self.state!=LB_BATTLE:
                self.lb_btn_online_find=pygame.Rect(0,0,0,0)
                screen.blit(font_small.render(f"Кімната: {self.online_room_id}  |  Роль: {self.online_role}",True,(180,220,255)),(w//2-130,_tn_ob_y+8))
                self.lb_btn_online_enter=self.draw_button("⚔ УВІЙТИ В БІЙ",w//2-140,_tn_ob_y+36,280,48,(120,30,30))
            for _ii_tn,_il_tn in enumerate([
                "1. Натисни «Знайти суперника» — сервер підбере пару",
                "2. Після знаходження — натисни «Увійти в бій»",
                f"3. Ходи по черзі: хід 1-2=38с, 3-10=28с, 11+=19с",
                "4. Якщо час вийшов — гра сама б'є за тебе"]):
                screen.blit(font_small.render(_il_tn,True,(100,100,100)),(w//2-font_small.size(_il_tn)[0]//2,_tn_ob_y+105+_ii_tn*17))
            if self.online_room_id: self.online_poll_state()
            self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)

        elif self.state == LIVE_BATTLE:
            # ── ЗАГОЛОВОК ──────────────────────────────────────
            title_lb = font_big.render("⚔  БИТВА В ЖИВУ  ⚔", True, (255, 80, 60))
            screen.blit(title_lb, (w//2 - title_lb.get_width()//2, 12))
            # ── ВКЛАДКИ СИМ / LAN ──────────────────────────────
            _tab_y = 58
            _tab_w = 200
            _sim_col = (30,120,200) if self.lb_tab=="sim" else (40,40,60)
            _lan_col = (30,120,200) if self.lb_tab=="lan" else (40,40,60)
            self.lb_btn_sim = self.draw_button("🌐 ОНЛАЙН (СИМ)", w//2 - _tab_w - 4, _tab_y, _tab_w, 36, _sim_col)
            self.lb_btn_lan = self.draw_button("🔗 LAN (МЕРЕЖА)", w//2 + 4,           _tab_y, _tab_w, 36, _lan_col)
            # ── КАРТИНКА банера ────────────────────────────────
            _BW, _BH = min(w - 40, 900), 80
            _bimg = load_tab_image("live_battle", _BW, _BH)
            if _bimg:
                screen.blit(_bimg, (w//2 - _BW//2, 100))
            else:
                pygame.draw.rect(screen, (25,10,10), (w//2-_BW//2, 100, _BW, _BH), border_radius=10)
                pygame.draw.rect(screen, (160,30,30), (w//2-_BW//2, 100, _BW, _BH), 2, border_radius=10)
                _p = abs(math.sin(time.time()*2))
                _bc = font_big.render("⚔  PvP  ⚔", True, (255,int(60+80*_p),0))
                screen.blit(_bc, (w//2-_bc.get_width()//2, 112))
            _list_y = 190   # початок таблиці/контенту

            if self.lb_tab == "sim":
                # ── СИМУЛЯЦІЯ ОНЛАЙН ──────────────────────────
                if not self.lb_sim_rooms:
                    self.lb_sim_rooms = self.lb_generate_sim_rooms()
                # Заголовок колонок
                _cols = [
                    ("Гравець",         80),
                    ("Карт",            35),
                    ("Заг.HP",          70),
                    ("Макс.ATK",        70),
                    ("Заг.ATK",         70),
                    ("Локація",         60),
                    ("Пінг",            45),
                    ("Статус",          65),
                ]
                _cx = 30
                _hdr_y = _list_y
                for _cname, _cw in _cols:
                    _hs = font_small.render(_cname, True, GOLD)
                    screen.blit(_hs, (_cx, _hdr_y))
                    _cx += _cw + 10
                pygame.draw.line(screen, (80,80,80), (20, _hdr_y+18), (w-20, _hdr_y+18), 1)
                # Рядки кімнат
                self.lb_room_rects = []
                _row_h = 32
                for _ri, _room in enumerate(self.lb_sim_rooms[:10]):
                    _ry = _list_y + 24 + _ri * _row_h
                    _rbg = (20,35,20) if _room["status"]=="Чекає" else (35,20,20)
                    _rr = pygame.Rect(20, _ry-2, w-40, _row_h-2)
                    pygame.draw.rect(screen, _rbg, _rr, border_radius=5)
                    pygame.draw.rect(screen, (60,60,60), _rr, 1, border_radius=5)
                    self.lb_room_rects.append(_rr)
                    _vals = [
                        (_room["name"],     (255,220,100)),
                        (str(_room["cards"]),(180,255,180)),
                        (str(_room["total_hp"]), (100,200,255)),
                        (str(_room["max_atk"]),  (255,140,40)),
                        (str(_room["total_atk"]), (255,100,100)),
                        (f"Лок.{_room['loc']} С.{_room['world']}", (180,180,255)),
                        (f"{_room['ping']}мс",  (100,220,100) if _room["ping"]<50 else (220,180,50)),
                        (_room["status"],   (80,220,80) if _room["status"]=="Чекає" else (220,80,80)),
                    ]
                    _vx = 30
                    for (_vtxt, _vcol), (_cname, _cw) in zip(_vals, _cols):
                        _vs = font_small.render(_vtxt, True, _vcol)
                        screen.blit(_vs, (_vx, _ry + 6))
                        _vx += _cw + 10
                # ── Нижні кнопки ──────────────────────────────
                _btn_y = h - 70
                self.lb_btn_create  = self.draw_button("➕ Створити битву",     w//2-320, _btn_y, 200, 50, (30,140,30))
                self.lb_btn_refresh = self.draw_button("🔄 Оновити список",     w//2-100, _btn_y, 200, 50, (40,80,160))
                _no_army = not self.army
                _hint = font_small.render("⚠ Армія порожня! Додай героїв у Моя Армія" if _no_army else
                                          "Натисни на кімнату щоб приєднатись | або Створи свою", True,
                                          (220,80,80) if _no_army else (140,140,140))
                screen.blit(_hint, (w//2 - _hint.get_width()//2, h - 110))

            else:
                # ── ОНЛАЙН ЧЕРЕЗ ІНТЕРНЕТ (PythonAnywhere) ──────────
                _oy = _list_y
                # Заголовок + сервер
                _ot = font.render("🌐 Онлайн PvP через Інтернет", True, (100,220,255))
                screen.blit(_ot, (w//2-_ot.get_width()//2, _oy))
                _os = font_small.render(f"Сервер: {ONLINE_SERVER}", True, (100,120,140))
                screen.blit(_os, (w//2-_os.get_width()//2, _oy+26))
                # Ім'я гравця
                self._online_ensure_pid()
                _nm_lbl = font_small.render(f"Ваш ID: {self.online_pid}  |  Ім'я: {self.online_my_name}", True, GOLD)
                screen.blit(_nm_lbl, (w//2-_nm_lbl.get_width()//2, _oy+48))
                # Статус
                if self.online_status:
                    _sc = (80,230,80) if ("✅" in self.online_status or "знайдено" in self.online_status) else                           (255,180,60) if ("⏳" in self.online_status or "Шукаємо" in self.online_status) else                           (220,80,80)  if ("⚠" in self.online_status or "Помилка" in self.online_status) else                           (180,180,180)
                    _ss = font.render(self.online_status, True, _sc)
                    screen.blit(_ss, (w//2-_ss.get_width()//2, _oy+74))
                # Кнопки
                _ob_y = _oy + 118
                if not self.online_searching and not self.online_room_id:
                    self.lb_btn_online_find = self.draw_button(
                        "🔍 ЗНАЙТИ СУПЕРНИКА", w//2-160, _ob_y, 320, 52, (20,120,60))
                elif self.online_searching:
                    self.lb_btn_online_find = pygame.Rect(0,0,0,0)
                    self.lb_btn_online_cancel = self.draw_button(
                        "✖ Скасувати пошук", w//2-140, _ob_y, 280, 48, (100,30,30))
                elif self.online_room_id and self.state != LB_BATTLE:
                    self.lb_btn_online_find = pygame.Rect(0,0,0,0)
                    _rinfo = font_small.render(
                        f"Кімната: {self.online_room_id}  |  Роль: {self.online_role}", True, (180,220,255))
                    screen.blit(_rinfo, (w//2-_rinfo.get_width()//2, _ob_y+8))
                    self.lb_btn_online_enter = self.draw_button(
                        "⚔ УВІЙТИ В БІЙ", w//2-140, _ob_y+36, 280, 48, (120,30,30))
                # Інструкція
                for _ii2, _il2 in enumerate([
                    "1. Натисни «Знайти суперника» — сервер підбере пару",
                    "2. Після знаходження — натисни «Увійти в бій»",
                    f"3. Ходи по черзі: хід 1-2 = 38с, 3-10 = 28с, 11+ = 19с",
                    "4. Якщо час вийшов — гра сама б'є за тебе"
                ]):
                    _iz = font_small.render(_il2, True, (120,120,120))
                    screen.blit(_iz, (w//2-_iz.get_width()//2, _ob_y+100+_ii2*18))
                # Якщо в кімнаті — опитати сервер
                if self.online_room_id:
                    self.online_poll_state()

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == ALTAR_HUB:
            title_h = font_big.render("АЛТАРЬ", True, GOLD)
            screen.blit(title_h, (w//2 - title_h.get_width()//2, 20))
            sub_h = font.render("Оберіть, куди зайти:", True, (180, 180, 200))
            screen.blit(sub_h, (w//2 - sub_h.get_width()//2, 65))

            HUB_W   = 260
            HUB_IMG = 130
            HUB_BTN = 50
            HUB_GAP = 30
            HUB_CELL_H = HUB_IMG + HUB_BTN + 6
            total_hub_w = 3 * HUB_W + 2 * HUB_GAP
            hub_x0  = w // 2 - total_hub_w // 2
            hub_y0  = h // 2 - HUB_CELL_H // 2

            _hub_tabs = [
                ("АЛТАРЬ ГЕРОЇВ",  "altar",         ALTAR,         (80, 50, 180)),
                ("АЛТАРЬ РУН",     "rune_altar",     RUNE_ALTAR,    (180, 100, 20)),
                ("АЛТАРЬ ЕСЕНЦІЯ", "altar_essence",  ALTAR_ESSENCE, (80, 40, 160)),
            ]
            _pe3 = abs(math.sin(time.time() * 2.2))
            self.hub_tab_rects = {}
            for _hi, (_hlabel, _himg, _hstate, _hcol) in enumerate(_hub_tabs):
                _hx = hub_x0 + _hi * (HUB_W + HUB_GAP)
                _img_h = load_tab_image(_himg, HUB_W, HUB_IMG)
                if _img_h:
                    screen.blit(_img_h, (_hx, hub_y0))
                else:
                    pygame.draw.rect(screen, (20, 15, 35),
                        (_hx, hub_y0, HUB_W, HUB_IMG), border_radius=12)
                    pygame.draw.rect(screen, _hcol,
                        (_hx, hub_y0, HUB_W, HUB_IMG), 3, border_radius=12)
                    _sym = {"altar":"⚔️","rune_altar":"✦","altar_essence":"💎"}.get(_himg,"⭐")
                    _si2 = font_big.render(_sym, True, _hcol)
                    screen.blit(_si2, (_hx + HUB_W//2 - _si2.get_width()//2, hub_y0 + HUB_IMG//2 - _si2.get_height()//2))
                _hr = self.draw_button(_hlabel, _hx, hub_y0 + HUB_IMG + 6, HUB_W, HUB_BTN, _hcol)
                self.hub_tab_rects[_hstate] = _hr

            self.btn_hub_altar         = self.hub_tab_rects.get(ALTAR,         pygame.Rect(0,0,0,0))
            self.btn_hub_rune_altar    = self.hub_tab_rects.get(RUNE_ALTAR,    pygame.Rect(0,0,0,0))
            self.btn_hub_altar_essence = self.hub_tab_rects.get(ALTAR_ESSENCE, pygame.Rect(0,0,0,0))
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, GRAY)

        elif self.state == REMOVE_RUNE_STATE:
            # --- ЗНЯТИ РУНУ З КАРТИ ---
            title_rr = font_big.render("ЗАБРАТИ РУНУ З КАРТИ", True, (255, 140, 40))
            screen.blit(title_rr, (w//2 - title_rr.get_width()//2, 12))
            hint_rr = font_small.render(
                "Натисни на карту щоб вибрати, потім 'ЗНЯТИ РУНУ'", True, GRAY)
            screen.blit(hint_rr, (w//2 - hint_rr.get_width()//2, 55))
            # Список карт з рунами
            cards_with_runes = [c for c in self.all_heroes if getattr(c, "runes", [])]
            if not cards_with_runes:
                ns_rr = font.render("Немає карт з рунами!", True, GRAY)
                screen.blit(ns_rr, (w//2 - ns_rr.get_width()//2, h//2))
            else:
                for ci, card in enumerate(cards_with_runes):
                    cx_rr = 60 + (ci % 5) * 220
                    cy_rr = 80 + (ci // 5) * 260
                    card.draw(screen, cx_rr, cy_rr, 160, 240)
                    # Підсвітити вибрану
                    sel_rr = self.remove_rune_selected_card
                    if sel_rr and card is sel_rr:
                        pygame.draw.rect(screen, (255,180,0),
                            (cx_rr-3, cy_rr-3, 166, 246), 3, border_radius=12)
            # Панель знизу
            sel_c = self.remove_rune_selected_card
            if sel_c and getattr(sel_c, "runes", []):
                rune_names = ", ".join(f"{r['name']} Lv{r['level']}" for r in sel_c.runes)
                info_rr = font_small.render(f"Руни на {sel_c.name}: {rune_names}", True, GOLD)
                screen.blit(info_rr, (w//2 - info_rr.get_width()//2, h - 125))
                self.btn_strip_rune = self.draw_button(
                    "ЗНЯТИ ПЕРШУ РУНУ", w//2 - 150, h - 100, 300, 50, (200, 80, 20))
            else:
                self.btn_strip_rune = pygame.Rect(0,0,0,0)
                if sel_c:
                    no_r = font_small.render("На цій карті немає рун!", True, RED)
                    screen.blit(no_r, (w//2 - no_r.get_width()//2, h - 110))
            if self.rune_msg:
                mc_rr = GREEN if "знято" in self.rune_msg else RED
                ms_rr = font.render(self.rune_msg, True, mc_rr)
                screen.blit(ms_rr, (w//2 - ms_rr.get_width()//2, h - 44))
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, GRAY)

        elif self.state == ALTAR:
            # Заголовок
            title_surf = font_big.render("АЛТАРЬ ГЕРОЇВ", True, GOLD)
            screen.blit(title_surf, (w//2 - title_surf.get_width()//2, 30))

            # Інфо про діапазон рівня
            lvl_min, lvl_max = self.altar_get_card_level_range()
            info_surf = font.render(f"Рівень карти залежить від прогресу: {lvl_min}–{lvl_max} рівень", True, WHITE)
            screen.blit(info_surf, (w//2 - info_surf.get_width()//2, 100))

            # Таймер кулдауну
            now = time.time()
            elapsed = now - self.altar_last_draw_time
            if elapsed < ALTAR_COOLDOWN:
                remaining = int(ALTAR_COOLDOWN - elapsed)
                mins = remaining // 60
                secs = remaining % 60
                cd_text = f"Наступне витягування через: {mins}хв {secs}с"
                cd_col = RED
            else:
                cd_text = "Витягування доступне!"
                cd_col = GREEN
            cd_surf = font.render(cd_text, True, cd_col)
            screen.blit(cd_surf, (w//2 - cd_surf.get_width()//2, 140))

            # Кнопка витягнути
            can_draw = elapsed >= ALTAR_COOLDOWN
            draw_col = (120, 60, 180) if can_draw else GRAY
            self.btn_altar_draw = self.draw_button("ВИТЯГНУТИ КАРТУ ГЕРОЯ", w//2 - 200, 200, 400, 55, draw_col, active=can_draw)
            # Кнопка витягнути за ключ
            has_key = self.keys >= 1
            key_col = (40, 140, 200) if has_key else (60, 60, 60)
            key_label = f"ВИТЯГНУТИ КАРТУ ГЕРОЯ ЗА КЛЮЧ  🔑 ({self.keys})"
            self.btn_altar_key_draw = self.draw_button(key_label, w//2 - 200, 265, 400, 50, key_col, active=has_key)
            # Показ витягнутої за ключ карти
            _akm = getattr(self, "altar_key_message", "")
            if _akm:
                _akm_col = GREEN if "Витягнуто" in _akm or "витягнуто" in _akm or "додано" in _akm else RED
                _akm_surf = font_small.render(_akm, True, _akm_col)
                screen.blit(_akm_surf, (w//2 - _akm_surf.get_width()//2, 324))

            # Показ витягнутої карти
            if self.altar_drawn_card:
                # --- Мерехтлива рамка залежно від рангу ---
                rank_stars = {"НОВАЧОК": 0, "ВЕТЕРАН": 1, "МАЙСТЕР": 2, "ГЕРОЙ": 3, "ЛЕГЕНДА": 4, "БОЖЕСТВО": 5}
                star_colors = [(100,100,100), (150,220,50), (80,180,255), (255,140,0), (255,60,180), (255,215,0)]
                stars_count = rank_stars.get(self.altar_drawn_card.rank, 0)
                if stars_count > 0:
                    glow_col = star_colors[stars_count]
                    # Пульсація: яскравість змінюється за синусом часу
                    pulse = abs(math.sin(time.time() * 3.5))
                    glow_alpha = int(80 + 175 * pulse)
                    thickness = int(3 + 4 * pulse)
                    card_x, card_y, card_w, card_h = w//2 - 80, 290, 160, 240
                    for t in range(thickness, 0, -1):
                        alpha_step = glow_alpha * t // thickness
                        r = min(255, int(glow_col[0] * alpha_step / 255 + glow_col[0] * 0.4))
                        g = min(255, int(glow_col[1] * alpha_step / 255 + glow_col[1] * 0.4))
                        b = min(255, int(glow_col[2] * alpha_step / 255 + glow_col[2] * 0.4))
                        pygame.draw.rect(screen, (r, g, b),
                            (card_x - t, card_y - t, card_w + t*2, card_h + t*2),
                            2, border_radius=12)
                self.altar_drawn_card.draw(screen, w//2 - 80, 290, 160, 240)
                self.btn_altar_buy = self.draw_button("КУПИТИ ЗА 1 🪙", w//2 - 120, 550, 240, 50, GREEN if self.gold >= 1 else GRAY)
            else:
                self.btn_altar_buy = pygame.Rect(0, 0, 0, 0)
                placeholder = font.render("Натисни кнопку вище, щоб витягнути героя", True, GRAY)
                screen.blit(placeholder, (w//2 - placeholder.get_width()//2, 400))

            # Повідомлення
            if self.altar_message:
                if "відправлений" in self.altar_message or "витягнуто" in self.altar_message:
                    msg_col = GREEN
                elif "Недостатньо" in self.altar_message or "Зачекайте" in self.altar_message:
                    msg_col = RED
                else:
                    msg_col = WHITE
                msg_surf = font.render(self.altar_message, True, msg_col)
                screen.blit(msg_surf, (w//2 - msg_surf.get_width()//2, 620))

            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, GRAY)

        elif self.state == SETTINGS:
            title = font_big.render("НАЛАШТУВАННЯ", True, WHITE)
            screen.blit(title, (w//2 - title.get_width()//2, h//2 - 230))
            # Галочка музики
            music_col = (30, 160, 30) if self.music_enabled else (80, 80, 80)
            music_label = "[✓] МУЗИКА" if self.music_enabled else "[  ] МУЗИКА"
            self.btn_toggle_music = self.draw_button(music_label, w//2 - 150, h//2 - 155, 300, 50, music_col)
            # Галочка звуків
            sound_col = (30, 160, 30) if self.sounds_enabled else (80, 80, 80)
            sound_label = "[✓] ЗВУКИ БОЮ" if self.sounds_enabled else "[  ] ЗВУКИ БОЮ"
            self.btn_toggle_sounds = self.draw_button(sound_label, w//2 - 150, h//2 - 90, 300, 50, sound_col)
            self.btn_reset = self.draw_button("СКИНУТИ ПРОГРЕС", w//2 - 150, h//2 - 25, 300, 60, RED)
            self.btn_save_exit = self.draw_button("ЗБЕРЕГТИ ТА ВИЙТИ", w//2 - 150, h//2 + 45, 300, 55, (60,60,60))
            self.btn_back = self.draw_button("НАЗАД", w//2 - 150, h//2 + 115, 300, 50, GRAY)

        elif self.state == GRAVEYARD:
            screen.blit(font_big.render(f"Кладовище: {len(self.graveyard)}", True, RED), (w//2 - 150, 20))
            rows_g = max(1, (len(self.graveyard) + 4) // 5)
            content_h_g = 100 + rows_g * 260
            visible_h_g = h - 160
            sy = self.scroll_offset.get("graveyard", 0)
            sy = max(0, min(sy, max(0, content_h_g - visible_h_g)))
            self.scroll_offset["graveyard"] = sy
            for i, card in enumerate(self.graveyard):
                cy_pos = 100 + (i // 5) * 260 - sy
                if -260 < cy_pos < h:
                    card.draw(screen, 100 + (i % 5) * 220, cy_pos, 160, 240, grayscale=True)
            up_r, dn_r = self.draw_scroll_arrows("graveyard", content_h_g, visible_h_g, sy, w - 28, 65, h - 175)
            self.scroll_arrows_rects["graveyard"] = (up_r, dn_r)

            res_col  = GREEN if self.dead_selected_card else (50, 50, 50)
            bury_col = (160, 60, 20) if self.dead_selected_card else (50, 50, 50)
            price_text = ""
            if self.dead_selected_card:
                lvl = self.dead_selected_card.level
                price_text = f" (1🔑 {lvl*10}🪙 {lvl*5}⛏ {lvl*1}💎)"
            # Дві кнопки в один рядок, вдвічі менші по висоті
            _row_y = h - 128
            _mid   = w // 2
            _bw    = min(340, (w - 60) // 2)
            _bh    = 26
            self.btn_resurrect = self.draw_button_with_icons(
                f"ОЖИВИТИ{price_text}", _mid - _bw - 6, _row_y, _bw, _bh, res_col,
                [ICON_KEY, ICON_GOLD, ICON_ORE, ICON_DIAMOND] if self.dead_selected_card else []
            )
            self.btn_bury = self.draw_button_with_icons(
                "ПОХОРОНИТИ (+1💎 +2⛏ +3🪙)" if self.dead_selected_card else "ПОХОРОНИТИ",
                _mid + 6, _row_y, _bw, _bh, bury_col,
                [ICON_DIAMOND, ICON_ORE, ICON_GOLD] if self.dead_selected_card else []
            )
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, GRAY)

        elif self.state == MY_ARMY:
            max_cards  = get_titan_max_cards(self.titan_level)
            army_limit = get_titan_army_limit(self.titan_level)
            cur_pts    = sum(get_titan_card_points(c) for c in self.army)
            info_str   = f"Герої: {len(self.all_heroes)} | В строю: {len(self.army)}/{max_cards} | Очки: {cur_pts}/{army_limit} | Титан Lv{self.titan_level}"
            screen.blit(font.render(info_str, True, GOLD), (10, 55))
            # Здібність активного Титана
            t_ab = TITAN_ABILITIES.get(self.titan_id)
            if t_ab:
                _em2, _nm2, _dc2, _trg2, _key2 = t_ab
                tdata_cur = next((t for t in TITANS_DATA if t[0] == self.titan_id), None)
                tcol_cur = tdata_cur[3] if tdata_cur else (180, 200, 255)
                hits_str = "/".join(str(x) for x in _trg2)
                ability_info = f"{_em2} {_nm2}: {_dc2}  (ходи: {hits_str})"
                screen.blit(font_small.render(ability_info, True, tcol_cur), (10, 77))
            rows_a = max(1, (len(self.all_heroes) + 4) // 5)
            content_h_a = 100 + rows_a * 260
            visible_h_a = h - 160
            sy = self.scroll_offset.get("my_army", 0)
            for i, card in enumerate(self.all_heroes):
                cy_pos = 100 + (i // 5) * 260 - sy
                cx_pos = 100 + (i % 5) * 220
                if -260 < cy_pos < h:
                    card.draw(screen, cx_pos, cy_pos, 160, 240)
                    # Стрілочка над виділеною картою
                    if card.selected:
                        _arr_cx = cx_pos + 80
                        _arr_cy = cy_pos - 18
                        _ap = abs(math.sin(time.time() * 4.0)) * 5
                        _arr_pts = [
                            (_arr_cx,        int(_arr_cy - 14 - _ap)),
                            (_arr_cx - 12,   int(_arr_cy +  2 - _ap)),
                            (_arr_cx -  5,   int(_arr_cy +  2 - _ap)),
                            (_arr_cx -  5,   int(_arr_cy + 12 - _ap)),
                            (_arr_cx +  5,   int(_arr_cy + 12 - _ap)),
                            (_arr_cx +  5,   int(_arr_cy +  2 - _ap)),
                            (_arr_cx + 12,   int(_arr_cy +  2 - _ap)),
                        ]
                        pygame.draw.polygon(screen, (255, 220, 0), _arr_pts)
                        pygame.draw.polygon(screen, (200, 140, 0), _arr_pts, 2)
            up_r, dn_r = self.draw_scroll_arrows("my_army", content_h_a, visible_h_a, sy, w - 28, 65, h - 175)
            self.scroll_arrows_rects["my_army"] = (up_r, dn_r)
            self.btn_cancel_army = self.draw_button("❌ СКАСУВАТИ ВИБІР", w - 240, h - 70, 220, 50, (140, 40, 40))
            self.btn_back = self.draw_button("ГОТОВО", 20, h - 70, 150, 50, GREEN)

        elif self.state == MARKET:
            title_surf = font_big.render("РИНОК", True, GOLD)
            screen.blit(title_surf, (w//2 - title_surf.get_width()//2, 20))
            MKT_BTN_W, MKT_BTN_H = 230, 50
            MKT_IMG_W, MKT_IMG_H = 230, 120
            MKT_CELL_H = MKT_IMG_H + MKT_BTN_H + 6
            MKT_GAP = 20
            mkt_tabs = [
                ("КАРТИ ЗА АЛМАЗИ",        "cards_diamond",    CARDS_DIAMOND),
                ("КАРТИ ЗА ЗОЛОТО",         "cards_gold",       CARDS_GOLD),
                ("КАРТИ ЗА РУДУ",           "cards_ore",        CARDS_ORE),
                ("ТИТАНИ",                  "titans",           TITANS_STATE),
                ("КАРТИ ЗА ОСКОЛКИ","super_human_shop", SUPER_HUMAN_SHOP),
                ("КАРТИ ЗА ОСКОЛКИ ІІ","arch_shop", ARCH_SHOP),
            ]
            _row1 = mkt_tabs[:3]
            _row2 = mkt_tabs[3:]
            self.mkt_rects = {}
            for _ri, _row in enumerate([_row1, _row2]):
                _total_rw = len(_row)*MKT_BTN_W + (len(_row)-1)*MKT_GAP
                _rx0 = w//2 - _total_rw//2
                _ry0 = 75 + _ri*(MKT_CELL_H + 22)
                for mi,(mlabel,mimg,mstate) in enumerate(_row):
                    mx = _rx0 + mi*(MKT_BTN_W+MKT_GAP)
                    img = load_tab_image(mimg, MKT_IMG_W, MKT_IMG_H)
                    if img:
                        screen.blit(img, (mx, _ry0))
                        self.mkt_rects[mstate+"_img"] = pygame.Rect(mx,_ry0,MKT_IMG_W,MKT_IMG_H)
                    else:
                        _bg_c = (20,40,80) if "АЛМАЗИ" in mlabel else (80,60,10) if "ЗОЛОТО" in mlabel else (40,30,10) if "РУДУ" in mlabel else (60,10,80) if "ТИТАН" in mlabel else (0,60,40)
                        pygame.draw.rect(screen, _bg_c, (mx,_ry0,MKT_IMG_W,MKT_IMG_H), border_radius=10)
                        _sym_m = {"КАРТИ ЗА АЛМАЗИ":"💎","КАРТИ ЗА ЗОЛОТО":"🪙","КАРТИ ЗА РУДУ":"⛏️","ТИТАНИ":"⚔️","КАРТИ ЗА ОСКОЛКИ":"🧩"}.get(mlabel,"?")
                        _sm = font_big.render(_sym_m, True, GOLD)
                        screen.blit(_sm, (mx+MKT_IMG_W//2-_sm.get_width()//2, _ry0+MKT_IMG_H//2-_sm.get_height()//2))
                    btn_r = self.draw_button(mlabel, mx, _ry0+MKT_IMG_H+6, MKT_BTN_W, MKT_BTN_H, (30,160,30))
                    self.mkt_rects[mstate] = btn_r
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, RED)

        elif self.state in [CARDS_DIAMOND, CARDS_GOLD, CARDS_ORE]:
            title = "Магазин: Алмази" if self.state == CARDS_DIAMOND else "Магазин: Золото" if self.state == CARDS_GOLD else "Магазин: Руда"
            screen.blit(font_big.render(title, True, GOLD), (w//2 - 150, 20))
            cards = self.diamond_cards if self.state == CARDS_DIAMOND else self.gold_cards if self.state == CARDS_GOLD else self.ore_cards
            for i, card in enumerate(cards):
                card.draw(screen, 100 + (i % 5) * 220, 100 + (i // 5) * 260, 160, 240)
            buy_col = GREEN if self.shop_selected_card else (50, 50, 50)
            price_text = ""
            if self.shop_selected_card:
                lvl = self.shop_selected_card.level
                if self.state == CARDS_DIAMOND: price_text = f" ({lvl} )"
                elif self.state == CARDS_GOLD: price_text = f" ({lvl*100} )"
                elif self.state == CARDS_ORE: price_text = f" ({lvl*30} )"
            buy_icon = (ICON_DIAMOND if self.state == CARDS_DIAMOND
                        else ICON_GOLD if self.state == CARDS_GOLD
                        else ICON_ORE)
            self.btn_buy = self.draw_button_with_icons(f"КУПИТИ{price_text}", w//2 - 150, h - 140, 300, 50, buy_col, [buy_icon] if self.shop_selected_card else [])
            self.btn_refresh = self.draw_button_with_icons("ОНОВИТИ (1 )", w - 230, 70, 190, 40, BLUE, [ICON_KEY])
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == SUPER_HUMAN_SHOP:
            _ts_sh = font_big.render("🧩  КАРТИ ЗА ОСКОЛКИ  🧩", True, (80,255,180))
            screen.blit(_ts_sh, (w//2-_ts_sh.get_width()//2, 8))
            _sub_sh = font_small.render("Раса «Супер Люди» · Осколки або купівля за гривні", True, (160,220,200))
            screen.blit(_sub_sh, (w//2-_sub_sh.get_width()//2, 48))
            _CW_SH,_CH_SH,_GAP_SH = 175,270,10
            _COLS_SH = max(1,(w-40)//(_CW_SH+_GAP_SH))
            _sy_sh = getattr(self,'scroll_offset',{}).get('super_human_shop',0)
            _sh_sel = getattr(self,'sh_sel',None)
            self.sh_card_rects=[]
            for _shi,sh_data in enumerate(SUPER_HUMAN_CARDS):
                orig,name,lvl,shard_cost,uah_price = sh_data
                _col_s=_shi%_COLS_SH; _row_s=_shi//_COLS_SH
                _sx=10+_col_s*(_CW_SH+_GAP_SH); _sy2=68+_row_s*(_CH_SH+_GAP_SH)-_sy_sh
                if _sy2+_CH_SH<68 or _sy2>h-100: self.sh_card_rects.append(None); continue
                _is_unl=orig in self.unlocked_super_humans
                _cur_sh=self.super_human_shards.get(orig,0)
                _is_sel_sh=(_sh_sel==_shi)
                _fc=(0,200,120) if _is_unl else (255,215,0) if _is_sel_sh else (60,60,80)
                if _is_sel_sh:
                    for _gt in range(4,0,-1):
                        pygame.draw.rect(screen,(255,215,0),(_sx-_gt,_sy2-_gt,_CW_SH+_gt*2,_CH_SH+_gt*2),1,border_radius=14)
                pygame.draw.rect(screen,(15,12,28),(_sx,_sy2,_CW_SH,_CH_SH),border_radius=12)
                pygame.draw.rect(screen,_fc,(_sx,_sy2,_CW_SH,_CH_SH),3,border_radius=12)
                _img_p=os.path.join("images","super_humans",f"{orig}.png")
                if os.path.exists(_img_p):
                    try:
                        _im=pygame.image.load(_img_p).convert_alpha()
                        screen.blit(pygame.transform.smoothscale(_im,(_CW_SH-8,130)),(_sx+4,_sy2+4))
                    except:
                        _ph=font_big.render("🦸",True,(80,255,180)); screen.blit(_ph,(_sx+_CW_SH//2-_ph.get_width()//2,_sy2+46))
                else:
                    pygame.draw.rect(screen,(20,40,30),(_sx+4,_sy2+4,_CW_SH-8,130),border_radius=8)
                    _ph=font_big.render("🦸",True,(80,255,180)); screen.blit(_ph,(_sx+_CW_SH//2-_ph.get_width()//2,_sy2+46))
                screen.blit(font_bold.render(name[:17],True,(80,255,180) if _is_unl else WHITE),(_sx+_CW_SH//2-font_bold.size(name[:17])[0]//2,_sy2+138))
                screen.blit(font_small.render(f"LVL {lvl}",True,(200,200,100)),(_sx+_CW_SH//2-font_small.size(f"LVL {lvl}")[0]//2,_sy2+156))
                _prog=min(1.0,_cur_sh/max(1,shard_cost))
                pygame.draw.rect(screen,(40,40,40),(_sx+6,_sy2+176,_CW_SH-12,10))
                pygame.draw.rect(screen,(0,200,120) if _is_unl else (255,180,0),(_sx+6,_sy2+176,int((_CW_SH-12)*_prog),10))
                screen.blit(font_small.render(f"🧩 {_cur_sh}/{shard_cost}",True,(255,215,0) if not _is_unl else (0,220,120)),(_sx+_CW_SH//2-font_small.size(f"🧩 {_cur_sh}/{shard_cost}")[0]//2,_sy2+190))
                screen.blit(font_small.render(f"💳 {uah_price} грн",True,(200,180,255)),(_sx+_CW_SH//2-font_small.size(f"💳 {uah_price} грн")[0]//2,_sy2+208))
                _st_lbl=("✅ У АРМІЇ" if _is_unl else
                         "🔓 РОЗБЛОКУВАТИ" if _cur_sh>=shard_cost else
                         f"🔒 {shard_cost-_cur_sh}🧩")
                _st_c=((0,220,120) if _is_unl else
                       (255,215,0) if _cur_sh>=shard_cost else
                       (150,150,150))
                screen.blit(font_small.render(_st_lbl,True,_st_c),(_sx+_CW_SH//2-font_small.size(_st_lbl)[0]//2,_sy2+228))
                self.sh_card_rects.append(pygame.Rect(_sx,_sy2,_CW_SH,_CH_SH))
            _rows_sh=max(1,(len(SUPER_HUMAN_CARDS)+_COLS_SH-1)//_COLS_SH)
            _cont_sh=68+_rows_sh*(_CH_SH+_GAP_SH); _vis_sh=h-150
            _up_sh,_dn_sh=self.draw_scroll_arrows("super_human_shop",_cont_sh,_vis_sh,_sy_sh,w-28,68,h-150)
            self.scroll_arrows_rects["super_human_shop"]=(_up_sh,_dn_sh)
            if _sh_sel is not None and _sh_sel<len(SUPER_HUMAN_CARDS):
                sh_d2=SUPER_HUMAN_CARDS[_sh_sel]; orig2,name2,lvl2,sc2,up2=sh_d2
                _iu2=orig2 in self.unlocked_super_humans; _cs2=self.super_human_shards.get(orig2,0)
                _py2=h-92
                pygame.draw.rect(screen,(12,10,22),(0,_py2-4,w,h))
                pygame.draw.line(screen,(60,60,80),(0,_py2-4),(w,_py2-4),1)
                if not _iu2:
                    # Якщо вистачає осколків — велика кнопка РОЗБЛОКУВАТИ
                    if _cs2 >= sc2:
                        self.btn_sh_unlock = self.draw_button(
                            f"🔓 РОЗБЛОКУВАТИ ({sc2}🧩)",
                            w//2-290, _py2, 260, 48, (30,160,60))
                        _hint_u = font_small.render(
                            "Карта з'явиться у МОЯ АРМІЯ  (Lv1, здібності з Lv20)",
                            True, (120,220,120))
                        screen.blit(_hint_u,(w//2-290,_py2+52))
                    else:
                        self.btn_sh_unlock = pygame.Rect(0,0,0,0)
                        _nd_s = font_small.render(
                            f"Не вистачає: {sc2-_cs2}🧩 (є {_cs2}/{sc2})",
                            True,(180,180,180))
                        screen.blit(_nd_s,(w//2-290,_py2+14))
                    # Кнопка КУПИТИ
                    self.btn_sh_buy = self.draw_button(
                        f"💳 КУПИТИ {up2} грн",
                        w//2+10, _py2, 260, 48,
                        (120,60,0) if MONOBANK_MERCHANT_TOKEN else (50,50,50),
                        active=bool(MONOBANK_MERCHANT_TOKEN))
                    _hint_b = font_small.render(
                        "Оплата → карта одразу у МОЯ АРМІЯ", True, (180,140,80))
                    screen.blit(_hint_b,(w//2+10,_py2+52))
                    # Кнопка перевірити
                    _has_inv = bool(getattr(self,'_sh_pay_pending_invoice',''))
                    self.btn_sh_check = self.draw_button(
                        "🔍 ПЕРЕВІРИТИ", w//2+280, _py2, 150, 48,
                        (50,120,50) if _has_inv else (35,55,35), active=_has_inv)
                else:
                    _ok=font.render(f"✅ {name2} вже у МОЯ АРМІЯ  (Lv{min(125,max(c.level for c in self.all_heroes if getattr(c,'_super_human_orig','') == orig2) if any(getattr(c,'_super_human_orig','') == orig2 for c in self.all_heroes) else 1)})",
                                    True,(0,220,120))
                    screen.blit(_ok,(w//2-_ok.get_width()//2,_py2+12))
                    self.btn_sh_unlock=self.btn_sh_buy=self.btn_sh_check=pygame.Rect(0,0,0,0)
            _spm=getattr(self,'sh_pay_msg','')
            if _spm:
                for _li2,_ln2 in enumerate(_spm.split("\n")):
                    _lc2=(80,255,80) if "✅" in _ln2 else (255,200,50) if "⏳" in _ln2 else (255,80,80)
                    _ls2=font_small.render(_ln2,True,_lc2); screen.blit(_ls2,(w//2-_ls2.get_width()//2,h-52+_li2*16))
            self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,RED)


        elif self.state == ARCH_SHOP:
            _ts_ar = font_big.render("😇  КАРТИ ЗА ОСКОЛКИ ІІ  👿", True, (255, 180, 80))
            screen.blit(_ts_ar, (w//2-_ts_ar.get_width()//2, 8))
            _sub_ar = font_small.render("Раса «Архангели та Архдемони» · Осколки або купівля за гривні", True, (220, 190, 140))
            screen.blit(_sub_ar, (w//2-_sub_ar.get_width()//2, 48))
            _CW_AR, _CH_AR, _GAP_AR = 175, 270, 10
            _COLS_AR = max(1, (w-40)//(_CW_AR+_GAP_AR))
            _sy_ar = getattr(self, 'scroll_offset', {}).get('arch_shop', 0)
            _ar_sel = getattr(self, 'arch_sel', None)
            self.arch_card_rects = []
            for _ari, ar_data in enumerate(ARCH_CARDS):
                orig_a, name_a, lvl_a, shard_cost_a, uah_a = ar_data
                _col_a = _ari % _COLS_AR; _row_a = _ari // _COLS_AR
                _ax = 10 + _col_a * (_CW_AR+_GAP_AR); _ay = 68 + _row_a*(_CH_AR+_GAP_AR) - _sy_ar
                if _ay+_CH_AR < 68 or _ay > h-100:
                    self.arch_card_rects.append(None); continue
                _is_unl_a = orig_a in self.unlocked_arch
                _cur_ar   = self.arch_shards.get(orig_a, 0)
                _is_sel_a = (_ar_sel == _ari)
                _fc_a = (255,160,0) if _is_unl_a else (255,215,0) if _is_sel_a else (60,45,80)
                if _is_sel_a:
                    for _gt in range(4, 0, -1):
                        pygame.draw.rect(screen, (255,215,0),
                            (_ax-_gt, _ay-_gt, _CW_AR+_gt*2, _CH_AR+_gt*2), 1, border_radius=14)
                pygame.draw.rect(screen, (20,10,30), (_ax, _ay, _CW_AR, _CH_AR), border_radius=12)
                pygame.draw.rect(screen, _fc_a, (_ax, _ay, _CW_AR, _CH_AR), 3, border_radius=12)
                _img_ar = os.path.join("images", "arch_cards", f"{orig_a}.png")
                if os.path.exists(_img_ar):
                    try:
                        _im_a = pygame.image.load(_img_ar).convert_alpha()
                        screen.blit(pygame.transform.smoothscale(_im_a, (_CW_AR-8, 130)), (_ax+4, _ay+4))
                    except:
                        _ph_a = font_big.render("😇", True, (255,180,80))
                        screen.blit(_ph_a, (_ax+_CW_AR//2-_ph_a.get_width()//2, _ay+46))
                else:
                    pygame.draw.rect(screen, (35,20,10), (_ax+4, _ay+4, _CW_AR-8, 130), border_radius=8)
                    _ph_a = font_big.render("😇" if "Angel" in orig_a or "AR_G" in orig_a or "AR_M" in orig_a
                                            else "👿", True, (255,180,80))
                    screen.blit(_ph_a, (_ax+_CW_AR//2-_ph_a.get_width()//2, _ay+46))
                screen.blit(font_bold.render(name_a[:17], True, (255,180,80) if _is_unl_a else WHITE),
                            (_ax+_CW_AR//2-font_bold.size(name_a[:17])[0]//2, _ay+138))
                screen.blit(font_small.render(f"LVL {lvl_a}", True, (200,200,100)),
                            (_ax+_CW_AR//2-font_small.size(f"LVL {lvl_a}")[0]//2, _ay+156))
                _prog_a = min(1.0, _cur_ar/max(1, shard_cost_a))
                pygame.draw.rect(screen, (40,40,40), (_ax+6, _ay+176, _CW_AR-12, 10))
                pygame.draw.rect(screen, (255,160,0) if _is_unl_a else (255,180,0),
                                 (_ax+6, _ay+176, int((_CW_AR-12)*_prog_a), 10))
                screen.blit(font_small.render(f"🧩 {_cur_ar}/{shard_cost_a}", True,
                                              (255,215,0) if not _is_unl_a else (255,180,0)),
                            (_ax+_CW_AR//2-font_small.size(f"🧩 {_cur_ar}/{shard_cost_a}")[0]//2, _ay+190))
                screen.blit(font_small.render(f"💳 {uah_a} грн", True, (200,180,255)),
                            (_ax+_CW_AR//2-font_small.size(f"💳 {uah_a} грн")[0]//2, _ay+208))
                _st_lbl_a = ("✅ У АРМІЇ" if _is_unl_a else
                             "🔓 РОЗБЛОКУВАТИ" if _cur_ar >= shard_cost_a else
                             f"🔒 {shard_cost_a-_cur_ar}🧩")
                _st_c_a = ((255,180,0) if _is_unl_a else
                           (255,215,0) if _cur_ar >= shard_cost_a else
                           (150,150,150))
                screen.blit(font_small.render(_st_lbl_a, True, _st_c_a),
                            (_ax+_CW_AR//2-font_small.size(_st_lbl_a)[0]//2, _ay+228))
                self.arch_card_rects.append(pygame.Rect(_ax, _ay, _CW_AR, _CH_AR))
            _rows_ar = max(1, (len(ARCH_CARDS)+_COLS_AR-1)//_COLS_AR)
            _cont_ar = 68+_rows_ar*(_CH_AR+_GAP_AR); _vis_ar = h-150
            _up_ar, _dn_ar = self.draw_scroll_arrows("arch_shop", _cont_ar, _vis_ar, _sy_ar, w-28, 68, h-150)
            self.scroll_arrows_rects["arch_shop"] = (_up_ar, _dn_ar)
            if _ar_sel is not None and _ar_sel < len(ARCH_CARDS):
                ar_d2 = ARCH_CARDS[_ar_sel]; orig_a2, name_a2, lvl_a2, sc_a2, up_a2 = ar_d2
                _iu_a2 = orig_a2 in self.unlocked_arch; _cs_a2 = self.arch_shards.get(orig_a2, 0)
                _py_a = h-92
                pygame.draw.rect(screen, (18,8,28), (0, _py_a-4, w, h))
                pygame.draw.line(screen, (80,60,100), (0, _py_a-4), (w, _py_a-4), 1)
                if not _iu_a2:
                    if _cs_a2 >= sc_a2:
                        self.btn_ar_unlock = self.draw_button(
                            f"🔓 РОЗБЛОКУВАТИ ({sc_a2}🧩)",
                            w//2-290, _py_a, 260, 48, (160,80,0))
                        _hint_au = font_small.render(
                            "Карта з'явиться у МОЯ АРМІЯ (Lv1, прокачка у ЕВОЛЮЦІЯ)",
                            True, (220,180,120))
                        screen.blit(_hint_au, (w//2-290, _py_a+52))
                    else:
                        self.btn_ar_unlock = pygame.Rect(0,0,0,0)
                        _nd_a = font_small.render(
                            f"Не вистачає: {sc_a2-_cs_a2}🧩 (є {_cs_a2}/{sc_a2})",
                            True, (180,180,180))
                        screen.blit(_nd_a, (w//2-290, _py_a+14))
                    self.btn_ar_buy = self.draw_button(
                        f"💳 КУПИТИ {up_a2} грн",
                        w//2+10, _py_a, 260, 48,
                        (120,60,0) if MONOBANK_MERCHANT_TOKEN else (50,50,50),
                        active=bool(MONOBANK_MERCHANT_TOKEN))
                    _hint_ab = font_small.render("Оплата → карта одразу у МОЯ АРМІЯ", True, (180,140,80))
                    screen.blit(_hint_ab, (w//2+10, _py_a+52))
                    _has_inv_a = bool(getattr(self, '_arch_pay_pending_invoice', ''))
                    self.btn_ar_check = self.draw_button(
                        "🔍 ПЕРЕВІРИТИ", w//2+280, _py_a, 150, 48,
                        (50,120,50) if _has_inv_a else (35,55,35), active=_has_inv_a)
                else:
                    _lvl_ar = max((c.level for c in self.all_heroes if getattr(c,'_arch_orig','') == orig_a2), default=1)
                    _ok_a = font.render(f"✅ {name_a2} вже у МОЯ АРМІЯ  (Lv{_lvl_ar})", True, (255,180,0))
                    screen.blit(_ok_a, (w//2-_ok_a.get_width()//2, _py_a+12))
                    self.btn_ar_unlock = self.btn_ar_buy = self.btn_ar_check = pygame.Rect(0,0,0,0)
            _apm = getattr(self, 'arch_pay_msg', '')
            if _apm:
                for _li_a, _ln_a in enumerate(_apm.split("\n")):
                    _lc_a = (80,255,80) if "✅" in _ln_a else (255,200,50) if "⏳" in _ln_a else (255,80,80)
                    _ls_a = font_small.render(_ln_a, True, _lc_a)
                    screen.blit(_ls_a, (w//2-_ls_a.get_width()//2, h-52+_li_a*16))
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, RED)
        elif self.state == BATTLE:
            prog_w, prog_h = 400, 20
            px, py = w//2 - prog_w//2, 30
            pygame.draw.rect(screen, (50, 50, 50), (px, py, prog_w, prog_h), border_radius=10)
            hits_ratio = self.total_hits / self.max_hits
            pygame.draw.rect(screen, BLUE, (px, py, prog_w * hits_ratio, prog_h), border_radius=10)
            p_txt = font_small.render(f"ПРОГРЕС: {self.total_hits}/{self.max_hits}", True, WHITE)
            screen.blit(p_txt, (px + prog_w//2 - p_txt.get_width()//2, py - 20))

            lore_text = get_battle_lore(self.selected_world, self.selected_location)
            lore_surf = font.render(lore_text, True, GOLD)
            screen.blit(lore_surf, (w//2 - lore_surf.get_width()//2, py + 35))

            for i, enemy in enumerate(self.enemies):
                if enemy.is_alive: enemy.draw(screen, (w // (len(self.enemies)+1)) * (i + 1) - 70, 100, 140, 240)
            for i, hero in enumerate(self.army):
                if hero.is_alive: hero.draw(screen, (w // (len(self.army)+1)) * (i + 1) - 70, h - 350, 140, 240)
            # Кнопка УДАРИТИ — сіра якщо чекаємо контратаки ворога
            _waiting_enemy = bool(getattr(self, '_pending_enemy_attack', None))
            _can_hit = (self.selected_hero and self.selected_monster) and not _waiting_enemy
            self.btn_battle = self.draw_button(
                "⏳ ВОРОГ АТАКУЄ..." if _waiting_enemy else "УДАРИТИ",
                w // 2 - 100, h // 2 - 30, 200, 60,
                (80, 40, 0) if _waiting_enemy else (RED if _can_hit else (50, 50, 50))
            )
            self.btn_back = self.draw_button("ВТЕКТИ", 20, h - 70, 150, 50, (80, 80, 80))

            # --- Панель здібностей вибраного героя ---
            if self.selected_hero:
                abilities = get_abilities_for_card(self.selected_hero)
                if abilities:
                    panel_x = w // 2 - 160
                    panel_y = h // 2 + 40
                    panel_w = 320
                    panel_h = 20 + len(abilities) * 18
                    pygame.draw.rect(screen, (20, 20, 35), (panel_x, panel_y, panel_w, panel_h), border_radius=8)
                    pygame.draw.rect(screen, GOLD, (panel_x, panel_y, panel_w, panel_h), 1, border_radius=8)
                    title_s = font_small.render(f"Здібності {self.selected_hero.name}:", True, GOLD)
                    screen.blit(title_s, (panel_x + 6, panel_y + 3))
                    for ai, (em, nm, dsc, _) in enumerate(abilities):
                        ab_surf = font_small.render(f"{em} {nm} — {dsc}", True, WHITE)
                        screen.blit(ab_surf, (panel_x + 6, panel_y + 18 + ai * 18))

            # --- Ефекти знищення карт ---
            for p in death_effects[:]:
                p.update(); p.draw(screen)
                if p.life <= 0: death_effects.remove(p)

            # --- Крижані ефекти (сосульки на рамці карти) ---
            now_fx = time.time()
            for ice in ice_effects[:]:
                if now_fx > ice["end_time"]: ice_effects.remove(ice); continue
                cr = ice["card"].rect
                if cr.width > 0:
                    for si in range(5):
                        sx = cr.left + si * cr.width // 5 + random.randint(0,10)
                        sy_i = cr.top - random.randint(0, 8)
                        sh_i = random.randint(10, 22)
                        pygame.draw.polygon(screen, (180,230,255),
                            [(sx, sy_i),(sx+4,sy_i),(sx+2,sy_i+sh_i)])
                    pygame.draw.rect(screen, (80,200,255), cr, 3, border_radius=10)

            # --- Щит-ефекти (золоте коло навколо карти) ---
            for sh_e in shield_effects[:]:
                if now_fx > sh_e["end_time"]: shield_effects.remove(sh_e); continue
                cr = sh_e["card"].rect
                if cr.width > 0:
                    pulse_sh = abs(math.sin(now_fx * 4)) * 4
                    pygame.draw.rect(screen, (255,220,0),
                        (cr.left-int(pulse_sh), cr.top-int(pulse_sh),
                         cr.width+int(pulse_sh*2), cr.height+int(pulse_sh*2)), 4, border_radius=12)

            # --- Трав'яні ефекти (зелені іскри навколо карти) ---
            for gr in grass_effects[:]:
                if now_fx > gr["end_time"]: grass_effects.remove(gr); continue
                cr = gr["card"].rect
                if cr.width > 0:
                    for _ in range(3):
                        gx = random.randint(cr.left, cr.right)
                        gy = random.randint(cr.top, cr.bottom)
                        pygame.draw.circle(screen, (50,220,80), (gx,gy), random.randint(2,5))
                    pygame.draw.rect(screen, (30,180,60), cr, 2, border_radius=10)

            # --- Стрілові ефекти (жовті риски) ---
            for ar in arrow_effects[:]:
                if now_fx > ar["end_time"]: arrow_effects.remove(ar); continue
                cr = ar["card"].rect
                if cr.width > 0:
                    for _ in range(4):
                        ax_s = cr.centerx + random.randint(-20,20)
                        ay_s = cr.top - random.randint(10,30)
                        pygame.draw.line(screen,(255,220,50),(ax_s,ay_s),(ax_s+3,ay_s+18),3)

            # --- Ланцюгові ефекти (блакитна рамка) ---
            for ch in chain_effects[:]:
                if now_fx > ch["end_time"]: chain_effects.remove(ch); continue
                cr = ch["card"].rect
                if cr.width > 0:
                    pygame.draw.rect(screen,(80,160,255), cr, 4, border_radius=10)

            # --- Вогняна анімація нижньої рамки (Вогняний Колос) ---
            _now = time.time()
            for _fe in fire_card_effects[:]:
                if _now > _fe["end_time"]:
                    fire_card_effects.remove(_fe); continue
                _r = _fe["card"].rect
                if not _r: continue
                _remain = _fe["end_time"] - _now          # скільки лишилось
                _fade   = min(1.0, _remain / 1.5)         # затухає в останні 1.5 с
                # Нові частинки вздовж нижньої кромки карти
                _flame_count = int(5 * _fade) + 1
                for _ in range(_flame_count):
                    _fx = random.randint(_r.left + 4, _r.right - 4)
                    _fy = _r.bottom - random.randint(0, 8)
                    _fvx = random.uniform(-0.8, 0.8)
                    _fvy = random.uniform(-3.2, -1.2)
                    _heat = random.random()
                    if _heat > 0.65:   _fc = (255, 230, 60)
                    elif _heat > 0.35: _fc = (255, 110, 15)
                    else:              _fc = (210, 30, 0)
                    _fe["particles"].append({
                        "x": float(_fx), "y": float(_fy),
                        "vx": _fvx, "vy": _fvy,
                        "life": random.randint(20, 40),
                        "max_life": 40,
                        "color": _fc,
                        "size": random.uniform(2.5, 6.0)
                    })
                # Оновлення та рендер частинок
                for _fp in _fe["particles"][:]:
                    _fp["x"] += _fp["vx"]
                    _fp["y"] += _fp["vy"]
                    _fp["vy"] -= 0.18
                    _fp["vx"] *= 0.94
                    _fp["life"] -= 1
                    if _fp["life"] <= 0:
                        _fe["particles"].remove(_fp); continue
                    _ratio = _fp["life"] / _fp["max_life"] * _fade
                    _s = max(1, int(_fp["size"] * _ratio))
                    _alpha_col = tuple(min(255, int(c * _ratio * 1.4)) for c in _fp["color"])
                    pygame.draw.circle(screen, _alpha_col,
                                       (int(_fp["x"]), int(_fp["y"])), _s)
                # Пульсуюча вогняна рамка знизу карти
                _pulse = abs(math.sin(_now * 9)) * _fade
                _rim_w = 3 + int(3 * _pulse)
                _rim_c = (255, int(60 + 100 * _pulse), 0)
                # Нижня горизонтальна лінія (рамка низу)
                _bot_y = _r.bottom
                pygame.draw.line(screen, _rim_c,
                                 (_r.left + 6,  _bot_y - 1),
                                 (_r.right - 6, _bot_y - 1), _rim_w)
                pygame.draw.line(screen, _rim_c,
                                 (_r.left + 6,  _bot_y - 1 - _rim_w),
                                 (_r.right - 6, _bot_y - 1 - _rim_w), max(1, _rim_w - 1))
                # Бокові вертикальні смуги (нижня третина карти)
                _side_top = _r.top + (_r.height * 2 // 3)
                pygame.draw.line(screen, _rim_c,
                                 (_r.left + 1, _side_top), (_r.left + 1, _bot_y - 4), _rim_w)
                pygame.draw.line(screen, _rim_c,
                                 (_r.right - 2, _side_top), (_r.right - 2, _bot_y - 4), _rim_w)

            # Показ смішної фрази
            if battle_phrase and time.time() - battle_phrase_timer < 2.5:
                phrase_surf = font.render(battle_phrase, True, GOLD)
                pygame.draw.rect(screen, (20, 20, 20), (w//2 - phrase_surf.get_width()//2 - 10, h//2 + 45, phrase_surf.get_width() + 20, 30), border_radius=8)
                screen.blit(phrase_surf, (w//2 - phrase_surf.get_width()//2, h//2 + 50))

        elif self.state == TITAN_STATE:
            tdata = next((t for t in TITANS_DATA if t[0] == self.titan_id), TITANS_DATA[0])
            _, tname, tdesc, tcol, _ = tdata
            title_s = font_big.render(f"ТИТАН: {tname}", True, tcol)
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 15))
            # GIF / статичний прямокутник Титана (2× ширше вкладки = 480, 3× вище = 360)
            TW, TH = 480, 360
            tx, ty = w//2 - TW//2, 65
            gif_path = os.path.join("images", "World", self.titan_id + ".gif")
            png_path = os.path.join("images", "World", self.titan_id + ".png")
            drawn = False
            for p in [gif_path, png_path]:
                if os.path.exists(p):
                    try:
                        img = pygame.image.load(p).convert_alpha()
                        img = pygame.transform.smoothscale(img, (TW, TH))
                        screen.blit(img, (tx, ty)); drawn = True; break
                    except: pass
            if not drawn:
                pygame.draw.rect(screen, tcol, (tx, ty, TW, TH), border_radius=16)
                pygame.draw.rect(screen, WHITE, (tx, ty, TW, TH), 3, border_radius=16)
                ph = font_big.render(tname[0], True, WHITE)
                screen.blit(ph, (tx + TW//2 - ph.get_width()//2, ty + TH//2 - ph.get_height()//2))
            # Параметри Титана
            titan_max_hp  = (10 + (self.titan_level-1)*12 + (self.titan_level//10)*50) * 10
            titan_max_xp  = 10 * self.titan_level
            titan_atk_lim = get_titan_army_limit(self.titan_level)
            titan_cards   = get_titan_max_cards(self.titan_level)
            py2 = ty + TH + 12
            for line, col in [
                (f"Рівень: {self.titan_level}  |  HP: {titan_max_hp}  |  XP: {self.titan_xp}/{titan_max_xp}", GOLD),
                (f"Ліміт очок армії: {titan_atk_lim}  |  Макс. карт у армії: {titan_cards}", (180,220,255)),
                (f"{tdesc}", (180,180,180)),
            ]:
                s = font.render(line, True, col)
                screen.blit(s, (w//2 - s.get_width()//2, py2))
                py2 += 24
            # XP бар Титана
            xp_ratio = self.titan_xp / max(1, titan_max_xp)
            pygame.draw.rect(screen, (40,40,40), (w//2-200, py2+4, 400, 16), border_radius=8)
            pygame.draw.rect(screen, tcol,       (w//2-200, py2+4, max(0, min(400, int(400*max(0.0,min(1.0,xp_ratio))))), 16), border_radius=8)
            # Здібність Титана під XP-баром
            TITAN_ABILITIES_DISPLAY = {
                "titan_1": ("⚔", (180, 200, 255),
                    lambda tl: (
                        f"Здібність: Призов Клона — на 3-му ході битви призиває клона "
                        f"союзного героя (HP/XP {-35+(tl-1):+d}, ATK {-30+(tl-1):+d})")),
                "titan_2": ("🔥", (255, 150, 40),
                    lambda tl: (
                        f"Здібність: Вогняний Удар — хід 6: -{500+(tl-1)*2} HP, "
                        f"хід 10: -{400+(tl-1)*2} HP, "
                        f"хід 13: -{300+(tl-1)*2} HP ворогу з найбільшим HP")),
            }
            _tab = TITAN_ABILITIES_DISPLAY.get(self.titan_id)
            if _tab:
                _em, _col, _desc_fn = _tab
                _aline = f"{_em} {_desc_fn(self.titan_level)}"
                _as = font_small.render(_aline, True, _col)
                screen.blit(_as, (w//2 - _as.get_width()//2, py2 + 26))
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
            # Зберігаємо rect для назв інших власних Титанів
            self.titan_name_rects = {}
            ox = 20; oy = h - 120
            screen.blit(font_small.render("Ваші Титани:", True, GRAY), (ox, oy - 16))
            for tid in self.owned_titans:
                td = next((t for t in TITANS_DATA if t[0] == tid), None)
                if td:
                    col2 = td[3] if tid == self.titan_id else GRAY
                    ns = font_small.render(td[1], True, col2)
                    nr = pygame.Rect(ox, oy, ns.get_width()+8, ns.get_height()+4)
                    pygame.draw.rect(screen, (30,30,40), nr, border_radius=4)
                    screen.blit(ns, (ox+4, oy+2))
                    self.titan_name_rects[tid] = (nr, None)
                    ox += nr.width + 8
                    if ox > w - 120: ox = 20; oy += 22

        elif self.state == TITANS_STATE:
            title_s = font_big.render("ТИТАНИ", True, GOLD)
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 10))
            T_COLS = 4; TIW = 180; TIH = 270; T_GAP = 18
            total_tw = T_COLS * TIW + (T_COLS-1) * T_GAP
            tx0 = w//2 - total_tw//2
            sy_t = self.scroll_offset.get("titans", 0)
            content_h_t = 60 + 5*(TIH+T_GAP+30)
            visible_h_t = h - 120
            ty0 = 60 - sy_t
            self.titan_buy_rects = {}
            for i, tdata in enumerate(TITANS_DATA):
                tid, tname, tdesc, tcol, cost = tdata
                tc = i % T_COLS; tr = i // T_COLS
                cx2 = tx0 + tc*(TIW+T_GAP)
                cy2 = ty0 + tr*(TIH+T_GAP+30)
                # Рамка
                owned = tid in self.owned_titans
                border = tcol if owned else GRAY
                pygame.draw.rect(screen, (20,20,30), (cx2, cy2, TIW, TIH), border_radius=10)
                pygame.draw.rect(screen, border,     (cx2, cy2, TIW, TIH), 2, border_radius=10)
                # Картинка
                drawn2 = False
                for ext in [".gif",".png",".jpg"]:
                    p2 = os.path.join("images","World", tid+ext)
                    if os.path.exists(p2):
                        try:
                            img2 = pygame.image.load(p2).convert_alpha()
                            img2 = pygame.transform.smoothscale(img2, (TIW-4, TIH-50))
                            screen.blit(img2, (cx2+2, cy2+2)); drawn2=True; break
                        except: pass
                if not drawn2:
                    pygame.draw.rect(screen, tcol, (cx2+2, cy2+2, TIW-4, TIH-50), border_radius=8)
                    ph2 = font_small.render(tname[:8], True, WHITE)
                    screen.blit(ph2, (cx2+TIW//2-ph2.get_width()//2, cy2+TIH//2-60))
                # Кількість осколків у верху рамки на картинці
                shards = self.titan_shards.get(tid, 0)
                sh_top = font_small.render(f"🔷 {shards}/100", True, (120,200,255) if shards>=100 else WHITE)
                pygame.draw.rect(screen, (0,0,0,180), (cx2+2, cy2+2, sh_top.get_width()+8, 18))
                screen.blit(sh_top, (cx2+6, cy2+4))
                # Назва
                ns2 = font_small.render(tname, True, tcol if owned else GRAY)
                screen.blit(ns2, (cx2+TIW//2-ns2.get_width()//2, cy2+TIH-46))
                # Кнопка — тільки осколки, алмази заборонені
                if owned:
                    btn_col = (30,130,30) if tid==self.titan_id else (50,80,50)
                    btn_lbl = "АКТИВНИЙ" if tid==self.titan_id else "ВИБРАТИ"
                elif shards >= 100:
                    btn_col = (0, 120, 200)
                    btn_lbl = "ОБМІНЯТИ 100🔷"
                else:
                    btn_col = (50,50,50)
                    btn_lbl = f"Потрібно {shards}/100🔷"
                brect = self.draw_button(btn_lbl, cx2, cy2+TIH-30, TIW, 28, btn_col)
                self.titan_buy_rects[tid] = (brect, None)
            up_rt, dn_rt = self.draw_scroll_arrows("titans", content_h_t, visible_h_t, sy_t, w-28, 65, h-115)
            self.scroll_arrows_rects["titans"] = (up_rt, dn_rt)
            # ── Тултіп здібності Титана при наведенні ────────────────
            _mx_t, _my_t = pygame.mouse.get_pos()
            for _ti, _td in enumerate(TITANS_DATA):
                _tid_t, _tname_t, _tdesc_t, _tcol_t, _ = _td
                _tc_t = _ti % T_COLS; _tr_t = _ti // T_COLS
                _cx_t = tx0 + _tc_t*(TIW+T_GAP)
                _cy_t = ty0 + _tr_t*(TIH+T_GAP+30)
                _card_rect_t = pygame.Rect(_cx_t, _cy_t, TIW, TIH)
                if _card_rect_t.collidepoint(_mx_t, _my_t):
                    _tab_t = TITAN_ABILITIES.get(_tid_t)
                    # Будуємо рядки тултіпу
                    _tip_lines = [f"⚔ {_tname_t}", f"  {_tdesc_t}"]
                    if _tab_t:
                        _em_t, _nm_t, _dc_t, _hits_t, _ = _tab_t
                        _hits_str = ", ".join(str(x) for x in _hits_t)
                        _tip_lines.append(f"  {_em_t} {_nm_t}: {_dc_t}")
                        _tip_lines.append(f"  Спрацьовує на ходах: {_hits_str}")
                    else:
                        _tip_lines.append("  Здібність: відсутня")
                    # Розміри панелі
                    _tp_pad = 8
                    _tp_lh  = 18
                    _tp_w   = max(font_small.size(l)[0] for l in _tip_lines) + _tp_pad*2
                    _tp_h   = len(_tip_lines) * _tp_lh + _tp_pad*2
                    _tp_x   = min(_mx_t + 14, w - _tp_w - 4)
                    _tp_y   = max(4, _my_t - _tp_h//2)
                    pygame.draw.rect(screen, (15, 15, 30), (_tp_x, _tp_y, _tp_w, _tp_h), border_radius=8)
                    pygame.draw.rect(screen, _tcol_t,      (_tp_x, _tp_y, _tp_w, _tp_h), 2, border_radius=8)
                    for _li_t, _line_t in enumerate(_tip_lines):
                        _lc_t = GOLD if _li_t == 0 else (WHITE if _li_t < 2 else (180, 220, 255))
                        _ls_t = font_small.render(_line_t, True, _lc_t)
                        screen.blit(_ls_t, (_tp_x + _tp_pad, _tp_y + _tp_pad + _li_t * _tp_lh))
                    break
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == MY_SHARDS:
            title_s = font_big.render("МОЇ ОСКОЛКИ", True, (120, 200, 255))
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 15))
            T_COLS2 = 4; TIW2 = 180; TIH2 = 220; T_GAP2 = 18
            total_tw2 = T_COLS2 * TIW2 + (T_COLS2-1) * T_GAP2
            tx02 = w//2 - total_tw2//2
            sy_ms = self.scroll_offset.get("my_shards", 0)
            content_h_ms = 65 + 5*(TIH2+T_GAP2+10)
            visible_h_ms = h - 120
            ty02 = 65 - sy_ms
            for i, tdata in enumerate(TITANS_DATA):
                tid, tname, tdesc, tcol, cost = tdata
                shards = self.titan_shards.get(tid, 0)
                if shards == 0: continue  # показуємо тільки тих де є осколки
                tc2 = i % T_COLS2; tr2 = i // T_COLS2
                cx3 = tx02 + tc2*(TIW2+T_GAP2); cy3 = ty02 + tr2*(TIH2+T_GAP2+10)
                pygame.draw.rect(screen, (20,20,30), (cx3, cy3, TIW2, TIH2), border_radius=10)
                pygame.draw.rect(screen, tcol, (cx3, cy3, TIW2, TIH2), 2, border_radius=10)
                drawn3 = False
                for ext3 in [".gif",".png",".jpg"]:
                    p3 = os.path.join("images","World", tid+ext3)
                    if os.path.exists(p3):
                        try:
                            img3 = pygame.image.load(p3).convert_alpha()
                            img3 = pygame.transform.smoothscale(img3, (TIW2-4, TIH2-60))
                            screen.blit(img3, (cx3+2, cy3+2)); drawn3=True; break
                        except: pass
                if not drawn3:
                    pygame.draw.rect(screen, tcol, (cx3+2, cy3+2, TIW2-4, TIH2-60), border_radius=8)
                ns3 = font_small.render(tname, True, tcol)
                screen.blit(ns3, (cx3+TIW2//2-ns3.get_width()//2, cy3+TIH2-56))
                sh3 = font_bold.render(f"🔷 {shards}/100", True, (120,200,255) if shards>=100 else WHITE)
                screen.blit(sh3, (cx3+TIW2//2-sh3.get_width()//2, cy3+TIH2-36))
                # прогрес бар
                pygame.draw.rect(screen, (40,40,60), (cx3+4, cy3+TIH2-18, TIW2-8, 10), border_radius=5)
                pygame.draw.rect(screen, (120,200,255), (cx3+4, cy3+TIH2-18, int((TIW2-8)*min(1,shards/100)), 10), border_radius=5)
            if not any(self.titan_shards.get(t[0], 0) > 0 for t in TITANS_DATA):
                ph3 = font.render("У вас ще немає осколків. Грайте Титани-Монстри!", True, GRAY)
                screen.blit(ph3, (w//2-ph3.get_width()//2, h//2))
            up_ms, dn_ms = self.draw_scroll_arrows("my_shards", content_h_ms, visible_h_ms, sy_ms, w-28, 65, h-115)
            self.scroll_arrows_rects["my_shards"] = (up_ms, dn_ms)
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == TITAN_MONSTERS:
            title_s = font_big.render("ТИТАНИ-МОНСТРИ", True, RED)
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 10))
            T_COLS3 = 4; TIW3 = 180; TIH3 = 240; T_GAP3 = 18
            total_tw3 = T_COLS3 * TIW3 + (T_COLS3-1) * T_GAP3
            tx03 = w//2 - total_tw3//2
            sy_tm = self.scroll_offset.get("titan_monsters", 0)
            content_h_tm = 60 + 5*(TIH3+T_GAP3+28)
            visible_h_tm = h - 120
            ty03 = 60 - sy_tm
            self.tm_titan_rects = {}
            for i, tdata in enumerate(TITANS_DATA):
                tid, tname, tdesc, tcol, cost = tdata
                shards = self.titan_shards.get(tid, 0)
                tc3 = i % T_COLS3; tr3 = i // T_COLS3
                cx4 = tx03 + tc3*(TIW3+T_GAP3); cy4 = ty03 + tr3*(TIH3+T_GAP3+28)
                pygame.draw.rect(screen, (25,10,10), (cx4, cy4, TIW3, TIH3), border_radius=10)
                pygame.draw.rect(screen, RED, (cx4, cy4, TIW3, TIH3), 2, border_radius=10)
                drawn4 = False
                for ext4 in [".gif",".png",".jpg"]:
                    p4 = os.path.join("images","World", tid+ext4)
                    if os.path.exists(p4):
                        try:
                            img4 = pygame.image.load(p4).convert_alpha()
                            img4 = pygame.transform.smoothscale(img4, (TIW3-4, TIH3-55))
                            screen.blit(img4, (cx4+2, cy4+2)); drawn4=True; break
                        except: pass
                if not drawn4:
                    pygame.draw.rect(screen, (80,20,20), (cx4+2, cy4+2, TIW3-4, TIH3-55), border_radius=8)
                ns4 = font_small.render(tname, True, RED)
                screen.blit(ns4, (cx4+TIW3//2-ns4.get_width()//2, cy4+TIH3-50))
                sh4 = font_small.render(f"🔷{shards}/100", True, (120,200,255))
                screen.blit(sh4, (cx4+TIW3//2-sh4.get_width()//2, cy4+TIH3-34))
                btn4 = self.draw_button("БИТИСЬ", cx4, cy4+TIH3-22, TIW3, 20, (120,20,20))
                self.tm_titan_rects[tid] = pygame.Rect(cx4, cy4, TIW3, TIH3+20)
            up_tm, dn_tm = self.draw_scroll_arrows("titan_monsters", content_h_tm, visible_h_tm, sy_tm, w-28, 65, h-115)
            self.scroll_arrows_rects["titan_monsters"] = (up_tm, dn_tm)
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == TITAN_MONSTER_LOCS:
            tid5 = self.selected_titan_monster or "titan_1"
            tdata5 = next((t for t in TITANS_DATA if t[0] == tid5), TITANS_DATA[0])
            _, tname5, _, tcol5, _ = tdata5
            title_s5 = font_big.render(f"МОНСТР: {tname5}", True, RED)
            screen.blit(title_s5, (w//2-title_s5.get_width()//2, 15))
            shards5 = self.titan_shards.get(tid5, 0)
            sh5 = font.render(f"Ваші осколки: 🔷 {shards5}/100", True, (120,200,255))
            screen.blit(sh5, (w//2-sh5.get_width()//2, 60))
            WEEK6 = 7 * 24 * 3600
            last_ref6 = self.titan_refresh_time.get(tid5, 0)
            time_since_ref = time.time() - last_ref6
            ref_available = time_since_ref >= WEEK6
            if ref_available:
                ref_lbl = "ОНОВИТИ (доступно!)"
                ref_col = (120, 60, 0)
            else:
                rem6 = int(WEEK6 - time_since_ref)
                d6 = rem6 // 86400; h6 = (rem6 % 86400) // 3600; m6 = (rem6 % 3600) // 60
                ref_lbl = f"ОНОВИТИ  ще {d6}д {h6:02d}г {m6:02d}хв"
                ref_col = (50, 30, 10)
            self.btn_tm_refresh = self.draw_button(ref_lbl, w-240, 60, 225, 35, ref_col)
            self.tm_loc_rects = []
            for i in range(10):
                loc_id5 = i + 1
                mult5 = 5 + i
                saved5 = (self.titan_monster_hp.get(tid5, {}).get(str(loc_id5)))
                if saved5 == "done":
                    btn_lbl5 = f"✓ Локація {loc_id5} — ПРОЙДЕНО (+10🔷)"
                    btn_col5 = (20, 60, 20)
                elif saved5:
                    alive5 = sum(1 for s in saved5 if isinstance(s, dict) and s.get("hp",0) > 0)
                    btn_lbl5 = f"Лок {loc_id5}  (×{mult5} HP) — живих: {alive5}/6"
                    btn_col5 = (60,40,0) if alive5 > 0 else (20,80,20)
                else:
                    btn_lbl5 = f"Локація {loc_id5}  (HP ×{mult5})"
                    btn_col5 = (80, 20, 20)
                lr5 = self.draw_button(btn_lbl5, w//2-250, 110+i*58, 500, 48, btn_col5)
                self.tm_loc_rects.append(lr5)
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == WORLDS:
            _title_w = font_big.render("🗺  КАРТА СВІТІВ  🗺", True, (80, 160, 255))
            screen.blit(_title_w, (w//2 - _title_w.get_width()//2, 8))
            self.world_rects = []
            _COLS_W = max(1, (w - 40) // 230)
            content_h_w = 80 + ((20 + _COLS_W - 1) // _COLS_W) * 120
            visible_h_w = h - 120
            sy = self.scroll_offset.get("worlds", 0)
            for i in range(20):
                w_id = i + 1
                is_done   = len(self.completed_levels.get(w_id, [])) >= 10
                prev_done = (w_id == 1) or (len(self.completed_levels.get(w_id - 1, [])) >= 10)
                is_locked = not prev_done
                wx = 20 + (i % _COLS_W) * 230
                wy = 55 + (i // _COLS_W) * 120 - sy
                if wy + 110 < 55 or wy > h - 60: self.world_rects.append(None); continue
                # Фон
                _wc = (0, 120, 60) if is_done else (20, 80, 180) if not is_locked else (30, 30, 50)
                pygame.draw.rect(screen, _wc, (wx, wy, 210, 100), border_radius=12)
                pygame.draw.rect(screen, (0,200,100) if is_done else (80,140,255) if not is_locked else (50,50,80),
                                 (wx, wy, 210, 100), 2, border_radius=12)
                # Назва світу
                _wname = WORLD_NAMES[i] if i < len(WORLD_NAMES) else f"Світ {w_id}"
                _wlbl_s = font_small.render(f"СВІТ {w_id}", True, (150,200,255) if not is_locked else (80,80,100))
                _wname_s = font_bold.render(_wname[:18], True, (255,255,255) if not is_locked else (80,80,100))
                screen.blit(_wlbl_s, (wx + 105 - _wlbl_s.get_width()//2, wy + 12))
                screen.blit(_wname_s, (wx + 105 - _wname_s.get_width()//2, wy + 32))
                if is_locked:
                    _lock_s = font_bold.render("🔒", True, (120,120,140))
                    screen.blit(_lock_s, (wx + 105 - _lock_s.get_width()//2, wy + 58))
                elif is_done:
                    _done_s = font_small.render("✅ ПРОЙДЕНО", True, (0, 220, 120))
                    screen.blit(_done_s, (wx + 105 - _done_s.get_width()//2, wy + 62))
                else:
                    _prog_s = font_small.render(f"Лок: {len(self.completed_levels.get(w_id,[]))}/10", True, (200,200,100))
                    screen.blit(_prog_s, (wx + 105 - _prog_s.get_width()//2, wy + 62))
                _rect_w = pygame.Rect(wx, wy, 210, 100)
                self.world_rects.append(_rect_w if not is_locked and not is_done else None)
            up_r, dn_r = self.draw_scroll_arrows("worlds", content_h_w, visible_h_w, sy, w - 28, 55, h - 115)
            self.scroll_arrows_rects["worlds"] = (up_r, dn_r)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == LEVELS:
            _wname_lv = WORLD_NAMES[self.selected_world - 1] if self.selected_world <= len(WORLD_NAMES) else f"Світ {self.selected_world}"
            _title_lv = font_big.render(f"🗺  СВІТ {self.selected_world}: {_wname_lv}", True, (80, 160, 255))
            screen.blit(_title_lv, (w//2 - _title_lv.get_width()//2, 8))
            self.loc_rects = []
            _done_locs = self.completed_levels.get(self.selected_world, [])
            _COLS_L = 2
            _LW, _LH = (w - 60) // 2 - 10, 80
            for i in range(10):
                l_id = i + 1
                is_done    = l_id in _done_locs
                prev_done  = (l_id == 1) or ((l_id - 1) in _done_locs)
                is_locked  = not prev_done
                lx = 20 + (i % _COLS_L) * (_LW + 20)
                ly = 58 + (i // _COLS_L) * 90
                _lc = (0, 120, 60) if is_done else (20, 80, 160) if not is_locked else (30, 30, 50)
                pygame.draw.rect(screen, _lc, (lx, ly, _LW, _LH), border_radius=10)
                pygame.draw.rect(screen, (0,200,100) if is_done else (80,140,255) if not is_locked else (50,50,80),
                                 (lx, ly, _LW, _LH), 2, border_radius=10)
                _loc_lbl = font_small.render(f"Локація {l_id}", True, (160,200,255) if not is_locked else (70,70,90))
                _loc_names = LOCATION_NAMES.get(self.selected_world, LOCATION_NAMES.get(1, []))
                _loc_name  = _loc_names[i] if i < len(_loc_names) else f"Локація {l_id}"
                _loc_name_s= font_bold.render(_loc_name[:22], True, WHITE if not is_locked else (70,70,90))
                screen.blit(_loc_lbl,   (lx + _LW//2 - _loc_lbl.get_width()//2, ly + 8))
                screen.blit(_loc_name_s,(lx + _LW//2 - _loc_name_s.get_width()//2, ly + 28))
                if is_locked:
                    _ls = font_small.render("🔒 Заблоковано", True, (100,100,120))
                    screen.blit(_ls, (lx + _LW//2 - _ls.get_width()//2, ly + 54))
                elif is_done:
                    _ds = font_small.render("✅ Пройдено", True, (0, 220, 120))
                    screen.blit(_ds, (lx + _LW//2 - _ds.get_width()//2, ly + 54))
                self.loc_rects.append(pygame.Rect(lx, ly, _LW, _LH) if not is_locked and not is_done else None)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == SH_WORLDS:
            _title_shw = font_big.render("🦸  СУПЕР ЛЮДЕЙ — ВИБІР СВІТУ  🦸", True, (80, 255, 180))
            screen.blit(_title_shw, (w//2 - _title_shw.get_width()//2, 8))
            self.sh_world_rects = []
            _COLS_SHW = max(1, (w - 40) // 230)
            content_h_shw = 80 + ((20 + _COLS_SHW - 1) // _COLS_SHW) * 120
            visible_h_shw = h - 120
            sy_shw = self.scroll_offset.get("sh_worlds", 0)
            for i in range(20):
                w_id = i + 1
                is_done   = len(self.sh_completed_levels.get(w_id, [])) >= 10
                prev_done = (w_id == 1) or (len(self.sh_completed_levels.get(w_id - 1, [])) >= 10)
                is_locked = not prev_done
                wx2 = 20 + (i % _COLS_SHW) * 230
                wy2 = 55 + (i // _COLS_SHW) * 120 - sy_shw
                if wy2 + 110 < 55 or wy2 > h - 60: self.sh_world_rects.append(None); continue
                _wc2 = (0, 130, 80) if is_done else (0, 100, 60) if not is_locked else (20, 35, 30)
                pygame.draw.rect(screen, _wc2, (wx2, wy2, 210, 100), border_radius=12)
                pygame.draw.rect(screen, (0,220,130) if is_done else (0,200,120) if not is_locked else (30,60,45),
                                 (wx2, wy2, 210, 100), 2, border_radius=12)
                _shwn = SH_WORLD_NAMES[i] if i < len(SH_WORLD_NAMES) else f"Світ {w_id}"
                _shwlbl = font_small.render(f"СВІТ {w_id}", True, (150,255,200) if not is_locked else (60,100,80))
                _shwname_s = font_bold.render(_shwn[:18], True, (255,255,255) if not is_locked else (60,100,80))
                screen.blit(_shwlbl, (wx2 + 105 - _shwlbl.get_width()//2, wy2 + 12))
                screen.blit(_shwname_s, (wx2 + 105 - _shwname_s.get_width()//2, wy2 + 32))
                if is_locked:
                    _sl = font_bold.render("🔒", True, (80,120,100))
                    screen.blit(_sl, (wx2 + 105 - _sl.get_width()//2, wy2 + 58))
                elif is_done:
                    _sd = font_small.render("✅ ПРОЙДЕНО", True, (0, 220, 120))
                    screen.blit(_sd, (wx2 + 105 - _sd.get_width()//2, wy2 + 62))
                else:
                    _sp = font_small.render(f"Лок: {len(self.sh_completed_levels.get(w_id,[]))}/10", True, (180,255,180))
                    screen.blit(_sp, (wx2 + 105 - _sp.get_width()//2, wy2 + 62))
                self.sh_world_rects.append(pygame.Rect(wx2, wy2, 210, 100) if not is_locked and not is_done else None)
            up_r2, dn_r2 = self.draw_scroll_arrows("sh_worlds", content_h_shw, visible_h_shw, sy_shw, w - 28, 55, h - 115)
            self.scroll_arrows_rects["sh_worlds"] = (up_r2, dn_r2)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == SH_LEVELS:
            _shwn_lv = SH_WORLD_NAMES[self.sh_selected_world - 1] if self.sh_selected_world <= len(SH_WORLD_NAMES) else f"Світ {self.sh_selected_world}"
            _title_shlv = font_big.render(f"🦸  СВІТ {self.sh_selected_world}: {_shwn_lv}", True, (80, 255, 180))
            screen.blit(_title_shlv, (w//2 - _title_shlv.get_width()//2, 8))
            self.sh_loc_rects = []
            _sh_done_locs = self.sh_completed_levels.get(self.sh_selected_world, [])
            _COLS_SHL = 2
            _SLW, _SLH = (w - 60) // 2 - 10, 80
            for i in range(10):
                l_id = i + 1
                is_done   = l_id in _sh_done_locs
                prev_done = (l_id == 1) or ((l_id - 1) in _sh_done_locs)
                is_locked = not prev_done
                slx = 20 + (i % _COLS_SHL) * (_SLW + 20)
                sly = 58 + (i // _COLS_SHL) * 90
                _slc = (0, 130, 80) if is_done else (0, 100, 60) if not is_locked else (20, 35, 30)
                pygame.draw.rect(screen, _slc, (slx, sly, _SLW, _SLH), border_radius=10)
                pygame.draw.rect(screen, (0,220,130) if is_done else (0,200,120) if not is_locked else (30,60,45),
                                 (slx, sly, _SLW, _SLH), 2, border_radius=10)
                _shloc_lbl = font_small.render(f"Локація {l_id}", True, (150,255,200) if not is_locked else (60,100,80))
                _sh_loc_names = SH_LOCATION_NAMES.get(self.sh_selected_world, SH_LOCATION_NAMES.get(1, []))
                _sh_loc_name  = _sh_loc_names[i] if i < len(_sh_loc_names) else f"Локація {l_id}"
                _shloc_name_s = font_bold.render(_sh_loc_name[:22], True, WHITE if not is_locked else (60,100,80))
                screen.blit(_shloc_lbl,   (slx + _SLW//2 - _shloc_lbl.get_width()//2, sly + 8))
                screen.blit(_shloc_name_s,(slx + _SLW//2 - _shloc_name_s.get_width()//2, sly + 28))
                if is_locked:
                    _sll = font_small.render("🔒 Заблоковано", True, (60,100,80))
                    screen.blit(_sll, (slx + _SLW//2 - _sll.get_width()//2, sly + 54))
                elif is_done:
                    _sld = font_small.render("✅ Пройдено", True, (0, 220, 120))
                    screen.blit(_sld, (slx + _SLW//2 - _sld.get_width()//2, sly + 54))
                self.sh_loc_rects.append(pygame.Rect(slx, sly, _SLW, _SLH) if not is_locked and not is_done else None)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)


        elif self.state == LUCKY_HEROES:
            # Перевірка таймера відновлення
            import math as _lmath, time as _ltime
            _elapsed_lh, _interval_lh = self._lucky_refresh_if_needed()
            _remain_lh = max(0.0, _interval_lh - _elapsed_lh)
            _h_lh  = int(_remain_lh // 3600)
            _m_lh  = int((_remain_lh % 3600) // 60)
            _s_lh  = int(_remain_lh % 60)
            _t = font_big.render("🍀  ВДАЧА ГЕРОЇВ", True, (80, 200, 100))
            screen.blit(_t, (w//2 - _t.get_width()//2, 14))
            _sub = font_small.render("Оберіть міні-гру", True, GRAY)
            screen.blit(_sub, (w//2 - _sub.get_width()//2, 48))
            _bw = min(260, (w - 60) // 2)
            self.btn_go_mono = self.draw_button("🎲  МОНОПОЛІЯ", w//2 - _bw - 10, 90, _bw, 120, (20, 100, 60))
            self.btn_go_fort = self.draw_button("🌀  ФОРТУНА",  w//2 + 10,       90, _bw, 120, (100, 60, 20))
            _ms = font_small.render("Кидай кубики, збирай скарби", True, GRAY)
            _fs = font_small.render("Крути колесо, виграй приз", True, GRAY)
            screen.blit(_ms, (w//2 - _bw - 10 + _bw//2 - _ms.get_width()//2, 220))
            screen.blit(_fs, (w//2 + 10 + _bw//2 - _fs.get_width()//2, 220))
            _tries_s = font_small.render(
                f"Монополія: 🎲 x{getattr(self,'lucky_free_tries',3)} безплатно / x{getattr(self,'lucky_gold_tries',3)} за золото  |  "
                f"Фортуна: ⭐ x{getattr(self,'fort_free_tries',3)} безплатно / x{getattr(self,'fort_ore_tries',3)} за руду",
                True, GRAY)
            screen.blit(_tries_s, (w//2 - _tries_s.get_width()//2, 250))
            # ── Таймер відновлення ──────────────────────────────────────
            if _remain_lh > 0:
                _timer_col = (80, 200, 100) if _remain_lh < 3600 else (200, 160, 40)
                _timer_str = f"⏳ Оновлення спроб через: {_h_lh:02d}:{_m_lh:02d}:{_s_lh:02d}"
            else:
                _timer_col = (80, 255, 120)
                _timer_str = "✅ Спроби оновлено!"
            _timer_sf = font_bold.render(_timer_str, True, _timer_col)
            screen.blit(_timer_sf, (w//2 - _timer_sf.get_width()//2, 272))
            # Прогрес-бар таймера
            _bar_w = min(400, w - 60)
            _bar_x = w//2 - _bar_w//2
            _bar_y = 294
            pygame.draw.rect(screen, (30, 30, 30), (_bar_x, _bar_y, _bar_w, 8), border_radius=4)
            _prog = max(0.0, 1.0 - _remain_lh / max(1, _interval_lh))
            pygame.draw.rect(screen, _timer_col,
                             (_bar_x, _bar_y, int(_bar_w * _prog), 8), border_radius=4)
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == MONOPOLY_STATE:
            _t = font_big.render("🎲  МОНОПОЛІЯ", True, (80, 200, 100))
            screen.blit(_t, (w//2 - _t.get_width()//2, 6))
            # ── Дошка 16 клітинок ──
            _CELL = min(70, (min(w, h) - 60) // 5)
            _BW = _CELL * 4
            _bx0 = w//2 - _BW//2
            _by0 = 38
            prizes_m = self._get_mono_prizes()
            self._mono_cell_rects = []
            for _ci in range(16):
                if   _ci < 4:  _cx, _cy = _bx0 + _ci*_CELL, _by0
                elif _ci < 8:  _cx, _cy = _bx0 + _BW - _CELL, _by0 + (_ci-4)*_CELL
                elif _ci < 12: _cx, _cy = _bx0 + _BW - (_ci-8)*_CELL - _CELL, _by0 + _BW - _CELL
                else:          _cx, _cy = _bx0, _by0 + _BW - (_ci-12)*_CELL - _CELL
                _is_cur = (_ci == self.mono_pos)
                _cc = (20, 140, 80) if _is_cur else (15, 15, 25)
                _bc = (0, 255, 120) if _is_cur else (60, 120, 80)
                pygame.draw.rect(screen, _cc, (_cx, _cy, _CELL-2, _CELL-2), border_radius=6)
                pygame.draw.rect(screen, _bc, (_cx, _cy, _CELL-2, _CELL-2), 2 if _is_cur else 1, border_radius=6)
                _p = prizes_m[_ci]
                _es = font_small.render(_p["emoji"], True, WHITE)
                screen.blit(_es, (_cx + (_CELL-2)//2 - _es.get_width()//2, _cy + 6))
                _ns = pygame.font.Font(None, 14).render(str(_ci+1), True, GRAY)
                screen.blit(_ns, (_cx + (_CELL-2) - 12, _cy + _CELL - 16))
                self._mono_cell_rects.append(pygame.Rect(_cx, _cy, _CELL-2, _CELL-2))
            # Фішка в центрі
            _token_pos = self._mono_cell_rects[self.mono_pos]
            pygame.draw.circle(screen, (220, 60, 30),
                               (_token_pos.x + _token_pos.w//2, _token_pos.y + _token_pos.h//2 - 4), 10)
            _hs = font_bold.render("H", True, WHITE)
            screen.blit(_hs, (_token_pos.x + _token_pos.w//2 - _hs.get_width()//2,
                               _token_pos.y + _token_pos.h//2 - 4 - _hs.get_height()//2))
            # Кубики
            _d1v = getattr(self, '_mono_d1', 1); _d2v = getattr(self, '_mono_d2', 1)
            _dc_x = w//2 - 60; _dc_y = _by0 + _BW + 8
            for _di, _dv in enumerate([_d1v, _d2v]):
                _dx = _dc_x + _di * 62
                pygame.draw.rect(screen, WHITE, (_dx, _dc_y, 44, 44), border_radius=8)
                pygame.draw.rect(screen, GRAY,  (_dx, _dc_y, 44, 44), 1, border_radius=8)
                _dot_pos = {1:[(22,22)],2:[(12,12),(32,32)],3:[(12,12),(22,22),(32,32)],
                            4:[(12,12),(32,12),(12,32),(32,32)],5:[(12,12),(32,12),(22,22),(12,32),(32,32)],
                            6:[(12,10),(32,10),(12,22),(32,22),(12,34),(32,34)]}
                for _dpx, _dpy in _dot_pos.get(_dv, [(22,22)]):
                    pygame.draw.circle(screen, (20,20,20), (_dx+_dpx, _dc_y+_dpy), 4)
            # Спроби
            _tr_y = _dc_y + 55
            # таймер відновлення
            import time as _mtm
            _mel, _mint = self._lucky_refresh_if_needed()
            _mrem = max(0.0, _mint - _mel)
            _mh2,_mm2,_ms2 = int(_mrem//3600), int((_mrem%3600)//60), int(_mrem%60)
            _tr_s = font_small.render(
                f"🎲 Безплатно: {getattr(self,'lucky_free_tries',3)}/3   💛 За золото: {getattr(self,'lucky_gold_tries',3)}/3"
                + (f"   ⏳ {_mh2:02d}:{_mm2:02d}:{_ms2:02d}" if _mrem>0 else "   ✅ Оновлено"),
                True, (80,200,100) if _mrem<3600 else GRAY)
            screen.blit(_tr_s, (w//2 - _tr_s.get_width()//2, _tr_y))
            # Лог
            _log_s = font_small.render(getattr(self,'lucky_msg','Кидай кубики!'), True, (180,255,180))
            screen.blit(_log_s, (w//2 - _log_s.get_width()//2, _tr_y + 20))
            # Приз на отримання
            if self.lucky_prize_pending:
                _pz = self.lucky_prize_pending
                _px0, _pw, _ph = w//2 - 130, 260, 90
                _py0 = _tr_y + 44
                pygame.draw.rect(screen, (15,40,25), (_px0, _py0, _pw, _ph), border_radius=10)
                pygame.draw.rect(screen, (0,200,100), (_px0, _py0, _pw, _ph), 2, border_radius=10)
                _pe = font_big.render(_pz["emoji"], True, WHITE)
                screen.blit(_pe, (_px0+14, _py0+18))
                _pn = font_bold.render(_pz["name"], True, WHITE)
                screen.blit(_pn, (_px0+60, _py0+16))
                self.btn_claim = self.draw_button("ЗАБРАТИ", _px0+60, _py0+42, 130, 32, (0,160,80))
            else:
                self.btn_claim = pygame.Rect(0,0,0,0)
            # Пропуск ходу
            if self.mono_skip:
                _sk = font_bold.render("⛔ ПРОПУСК ХОДУ — натисни щоб скасувати", True, (220,80,60))
                screen.blit(_sk, (w//2 - _sk.get_width()//2, h - 120))
                self.btn_skip_use = self.draw_button("Скасувати пропуск", w//2-100, h-95, 200, 30, RED)
            else:
                self.btn_skip_use = pygame.Rect(0,0,0,0)
            # Кнопки
            _btn_y = h - 68
            self.btn_roll_free = self.draw_button(
                f"🎲 Кинути безплатно ({getattr(self,'lucky_free_tries',3)})",
                w//2 - 310, _btn_y, 200, 46, (20,140,80) if getattr(self,'lucky_free_tries',3)>0 else GRAY)
            self.btn_roll_gold = self.draw_button(
                f"💛 За 3000 золота ({getattr(self,'lucky_gold_tries',3)})",
                w//2 - 100, _btn_y, 200, 46, (180,120,0) if getattr(self,'lucky_gold_tries',3)>0 else GRAY)
            self.btn_back = self.draw_button("НАЗАД", w//2 + 110, _btn_y, 110, 46, GRAY)

        elif self.state == FORTUNA_STATE:
            _t = font_big.render("🌀  ФОРТУНА", True, (220, 140, 0))
            screen.blit(_t, (w//2 - _t.get_width()//2, 6))
            prizes_f = self._get_fort_prizes()
            _WR = min(260, min(w, h) - 180)
            _wx = w//2; _wy = 42 + _WR
            _n = 16; _seg = 2 * math.pi / _n
            _ang = math.radians(self.wheel_angle)
            _wheel_colors = [(20,140,80),(180,120,0),(200,40,20),(20,80,180),
                             (100,20,180),(180,60,20),(20,160,140),(120,40,160)]*2
            for _i in range(_n):
                _a1 = _ang + _i*_seg; _a2 = _a1 + _seg
                _pts = [(_wx, _wy)]
                for _a in [_a1 + k*_seg/8 for k in range(9)]:
                    _pts.append((_wx + _WR*math.cos(_a), _wy + _WR*math.sin(_a)))
                pygame.draw.polygon(screen, _wheel_colors[_i], _pts)
                pygame.draw.polygon(screen, (40,40,40), _pts, 1)
                _mid = _a1 + _seg/2
                _tx = _wx + _WR*0.68*math.cos(_mid); _ty = _wy + _WR*0.68*math.sin(_mid)
                _es = font_small.render(prizes_f[_i]["emoji"], True, WHITE)
                screen.blit(_es, (_tx - _es.get_width()//2, _ty - _es.get_height()//2))
            pygame.draw.circle(screen, (15,15,25), (_wx, _wy), 26)
            pygame.draw.circle(screen, (0,200,100), (_wx, _wy), 26, 2)
            _ct = font_small.render("КРУТИ", True, (0,200,100))
            screen.blit(_ct, (_wx - _ct.get_width()//2, _wy - _ct.get_height()//2))
            _arr_pts = [(_wx-8, _wy - _WR + 18), (_wx+8, _wy - _WR + 18), (_wx, _wy - _WR - 6)]
            pygame.draw.polygon(screen, (220,60,30), _arr_pts)
            _tr_y = _wy + _WR + 10
            # таймер відновлення
            import time as _ftm
            _fel, _fint = self._lucky_refresh_if_needed()
            _frem = max(0.0, _fint - _fel)
            _fh2,_fm2,_fs2 = int(_frem//3600), int((_frem%3600)//60), int(_frem%60)
            _tr_s = font_small.render(
                f"⭐ Безплатно: {getattr(self,'fort_free_tries',3)}/3   ⛏ За руду: {getattr(self,'fort_ore_tries',3)}/3"
                + (f"   ⏳ {_fh2:02d}:{_fm2:02d}:{_fs2:02d}" if _frem>0 else "   ✅ Оновлено"),
                True, (200,160,40) if _frem<3600 else GRAY)
            screen.blit(_tr_s, (w//2 - _tr_s.get_width()//2, _tr_y))
            _log_s = font_small.render(getattr(self,'lucky_msg','Крути колесо!'), True, (255,200,100))
            screen.blit(_log_s, (w//2 - _log_s.get_width()//2, _tr_y + 20))
            if self.lucky_prize_pending:
                _pz = self.lucky_prize_pending
                _px0, _pw, _ph = w//2 - 130, 260, 90
                _py0 = _tr_y + 44
                pygame.draw.rect(screen, (40,20,0), (_px0, _py0, _pw, _ph), border_radius=10)
                pygame.draw.rect(screen, (220,140,0), (_px0, _py0, _pw, _ph), 2, border_radius=10)
                _pe = font_big.render(_pz["emoji"], True, WHITE)
                screen.blit(_pe, (_px0+14, _py0+18))
                _pn = font_bold.render(_pz["name"], True, WHITE)
                screen.blit(_pn, (_px0+60, _py0+16))
                self.btn_claim = self.draw_button("ЗАБРАТИ", _px0+60, _py0+42, 130, 32, (180,120,0))
            else:
                self.btn_claim = pygame.Rect(0,0,0,0)
            _btn_y = h - 68
            self.btn_spin_free = self.draw_button(
                f"⭐ Безплатно ({getattr(self,'fort_free_tries',3)})",
                w//2 - 340, _btn_y, 180, 46, (20,140,80) if getattr(self,'fort_free_tries',3)>0 else GRAY)
            self.btn_spin_ore = self.draw_button(
                f"⛏ За 3000 руди ({getattr(self,'fort_ore_tries',3)})",
                w//2 - 150, _btn_y, 190, 46, (90,80,60) if getattr(self,'fort_ore_tries',3)>0 else GRAY)
            self.btn_refresh_prizes = self.draw_button(
                "🔑 Змінити призи (1 ключ)", w//2 + 50, _btn_y, 210, 46, (60,40,120))
            self.btn_back = self.draw_button("НАЗАД", w//2 + 270, _btn_y, 90, 46, GRAY)

        elif self.state == AD_WORLDS:
            _title_adw = font_big.render("😇  АНГЕЛИ ТА ДЕМОНИ — ВИБІР СВІТУ  😇", True, (255, 200, 60))
            screen.blit(_title_adw, (w//2 - _title_adw.get_width()//2, 8))
            self.ad_world_rects = []
            _COLS_ADW = max(1, (w - 40) // 230)
            content_h_adw = 80 + ((20 + _COLS_ADW - 1) // _COLS_ADW) * 120
            visible_h_adw = h - 120
            sy_adw = self.scroll_offset.get("ad_worlds", 0)
            for i in range(20):
                w_id = i + 1
                is_done   = len(self.ad_completed_levels.get(w_id, [])) >= 10
                prev_done = (w_id == 1) or (len(self.ad_completed_levels.get(w_id - 1, [])) >= 10)
                is_locked = not prev_done
                wx3 = 20 + (i % _COLS_ADW) * 230
                wy3 = 55 + (i // _COLS_ADW) * 120 - sy_adw
                if wy3 + 110 < 55 or wy3 > h - 60: self.ad_world_rects.append(None); continue
                _wc3 = (120, 80, 0) if is_done else (160, 100, 0) if not is_locked else (50, 30, 0)
                pygame.draw.rect(screen, _wc3, (wx3, wy3, 210, 100), border_radius=12)
                pygame.draw.rect(screen, (255,200,60) if is_done else (220,170,40) if not is_locked else (100,70,20),
                                 (wx3, wy3, 210, 100), 2, border_radius=12)
                _adwn = AD_WORLD_NAMES[i] if i < len(AD_WORLD_NAMES) else f"Світ {w_id}"
                _adwlbl   = font_small.render(f"СВІТ {w_id}", True, (255,230,120) if not is_locked else (100,80,30))
                _adwname_s = font_bold.render(_adwn[:18], True, (255,255,255) if not is_locked else (100,80,30))
                screen.blit(_adwlbl,   (wx3 + 105 - _adwlbl.get_width()//2,   wy3 + 12))
                screen.blit(_adwname_s,(wx3 + 105 - _adwname_s.get_width()//2, wy3 + 32))
                if is_locked:
                    _al = font_bold.render("🔒", True, (100,70,20))
                    screen.blit(_al, (wx3 + 105 - _al.get_width()//2, wy3 + 58))
                elif is_done:
                    _ad = font_small.render("✅ ПРОЙДЕНО", True, (255, 200, 60))
                    screen.blit(_ad, (wx3 + 105 - _ad.get_width()//2, wy3 + 62))
                else:
                    _ap = font_small.render(f"Лок: {len(self.ad_completed_levels.get(w_id,[]))}/10", True, (255,230,120))
                    screen.blit(_ap, (wx3 + 105 - _ap.get_width()//2, wy3 + 62))
                self.ad_world_rects.append(pygame.Rect(wx3, wy3, 210, 100) if not is_locked and not is_done else None)
            up_r3, dn_r3 = self.draw_scroll_arrows("ad_worlds", content_h_adw, visible_h_adw, sy_adw, w - 28, 55, h - 115)
            self.scroll_arrows_rects["ad_worlds"] = (up_r3, dn_r3)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == AD_LEVELS:
            _adwn_lv = AD_WORLD_NAMES[self.ad_selected_world - 1] if self.ad_selected_world <= len(AD_WORLD_NAMES) else f"Світ {self.ad_selected_world}"
            _title_adlv = font_big.render(f"😇  СВІТ {self.ad_selected_world}: {_adwn_lv}", True, (255, 200, 60))
            screen.blit(_title_adlv, (w//2 - _title_adlv.get_width()//2, 8))
            self.ad_loc_rects = []
            _ad_done_locs = self.ad_completed_levels.get(self.ad_selected_world, [])
            _COLS_ADL = 2
            _ADLW, _ADLH = (w - 60) // 2 - 10, 80
            for i in range(10):
                l_id = i + 1
                is_done   = l_id in _ad_done_locs
                prev_done = (l_id == 1) or ((l_id - 1) in _ad_done_locs)
                is_locked = not prev_done
                alx = 20 + (i % _COLS_ADL) * (_ADLW + 20)
                aly = 58 + (i // _COLS_ADL) * 90
                _alc = (120, 80, 0) if is_done else (160, 100, 0) if not is_locked else (50, 30, 0)
                pygame.draw.rect(screen, _alc, (alx, aly, _ADLW, _ADLH), border_radius=10)
                pygame.draw.rect(screen, (255,200,60) if is_done else (220,170,40) if not is_locked else (100,70,20),
                                 (alx, aly, _ADLW, _ADLH), 2, border_radius=10)
                _adloc_lbl   = font_small.render(f"Локація {l_id}", True, (255,230,120) if not is_locked else (100,80,30))
                _ad_loc_names = AD_LOCATION_NAMES.get(self.ad_selected_world, AD_LOCATION_NAMES.get(1, []))
                _ad_loc_name  = _ad_loc_names[i] if i < len(_ad_loc_names) else f"Локація {l_id}"
                _adloc_name_s = font_bold.render(_ad_loc_name[:22], True, WHITE if not is_locked else (100,80,30))
                screen.blit(_adloc_lbl,   (alx + _ADLW//2 - _adloc_lbl.get_width()//2,   aly + 8))
                screen.blit(_adloc_name_s,(alx + _ADLW//2 - _adloc_name_s.get_width()//2, aly + 28))
                if is_locked:
                    _all2 = font_small.render("🔒 Заблоковано", True, (100,70,20))
                    screen.blit(_all2, (alx + _ADLW//2 - _all2.get_width()//2, aly + 54))
                elif is_done:
                    _ald = font_small.render("✅ Пройдено", True, (255, 200, 60))
                    screen.blit(_ald, (alx + _ADLW//2 - _ald.get_width()//2, aly + 54))
                self.ad_loc_rects.append(pygame.Rect(alx, aly, _ADLW, _ADLH) if not is_locked and not is_done else None)
            self.btn_back = self.draw_button("НАЗАД", 20, h - 70, 150, 50, RED)

        elif self.state == ENCYCLOPEDIA:
            # ─── ЕНЦИКЛОПЕДІЯ ──────────────────────────────────────────────
            _t_e = font_big.render("📖  ЕНЦИКЛОПЕДІЯ", True, (80, 200, 255))
            screen.blit(_t_e, (w//2 - _t_e.get_width()//2, 5))
            _ENC_SECTIONS = [
                "Огляд гри","Вкладки","Раси карт","Здібності",
                "Руни","Есенції","Режими бою","Еволюція","Ринок","Осколки","Вдача Героїв"
            ]
            _ENC_ICONS = ["🎮","📂","⚔","✨","🔮","💎","🏆","🌟","🏪","🧩","🍀"]
            _sec = getattr(self,"enc_section",0)
            # --- навігаційні кнопки зверху ---
            _nb_w = max(60, (w-20)//(len(_ENC_SECTIONS)+1))
            _nb_y = 32
            self._enc_nav_btns = []
            for _ni, _ns in enumerate(_ENC_SECTIONS):
                _nx = 10 + _ni * (_nb_w+2)
                _nc = (20,120,180) if _ni==_sec else (20,20,30)
                pygame.draw.rect(screen, _nc, (_nx,_nb_y,_nb_w,22), border_radius=5)
                _nbs = pygame.font.Font(None,16).render(_ENC_ICONS[_ni]+" "+_ns[:7],True,WHITE if _ni==_sec else GRAY)
                screen.blit(_nbs,(_nx+2,_nb_y+3))
                self._enc_nav_btns.append(pygame.Rect(_nx,_nb_y,_nb_w,22))
            _cy = 62
            _font_h = pygame.font.Font(None,20)
            _font_b = pygame.font.Font(None,17)
            _font_s = pygame.font.Font(None,15)
            def _enc_text(txt, x, y, font, color=None, max_w=None):
                _c = color or GRAY
                _mw = max_w or w-30
                _words = txt.split()
                _line = ""
                for _w2 in _words:
                    _test = _line+(" " if _line else "")+_w2
                    if font.size(_test)[0] > _mw-x and _line:
                        _s = font.render(_line,True,_c)
                        screen.blit(_s,(x,y)); y+=font.get_height()+1; _line=_w2
                    else: _line=_test
                if _line:
                    _s = font.render(_line,True,_c); screen.blit(_s,(x,y)); y+=font.get_height()+1
                return y
            def _enc_head(txt,y):
                pygame.draw.line(screen,(40,80,120),(10,y),(w-10,y),1)
                _s=_font_h.render(txt,True,(100,200,255)); screen.blit(_s,(10,y+2)); return y+_font_h.get_height()+4
            _CLIP_TOP=62; _CLIP_BOT=h-55
            screen.set_clip(pygame.Rect(0,_CLIP_TOP,w,_CLIP_BOT-_CLIP_TOP))
            _sy = self.scroll_offset.get("encyclopedia",0)
            _content_y = _cy - _sy
            if _sec==0:  # Огляд гри
                _content_y=_enc_head("🎮 Deck Heroes — карткова стратегічна RPG",_content_y)
                for _p in ["Збирайте армію карт різних рас, прокачуйте їх через еволюцію та скормку, розблоковуйте здібності та вступайте у бій. Мета — зібрати найсильнішу армію та стати Верховним Богом.",
                    "Механіка бою: оберіть свою карту → оберіть ціль ворога → спочатку спрацьовують усі здібності → потім фізичний удар ATK → ворог б'є у відповідь. Бій триває поки одна сторона не втратить усі карти.",
                    "Параметри карт: HP — здоров'я (при 0 карта гине), ATK — базова фізична атака, Рівень (1-130) — зростає через скормку та еволюцію, Зірки (1-5) — кількість здібностей, Раса — визначає тип та ексклюзивні здібності."]:
                    _content_y=_enc_text(_p,12,_content_y,_font_b)
            elif _sec==1:  # Вкладки
                for _tab,_desc in [("МОЯ АРМІЯ","Всі ваші карти — Герої, Монстри, Супер Люди, Архангели. Обирайте для бою або скормки."),
                    ("БИТВА","Хаб: Карта Світів, Супер Люди, Ангели та Демони, Титани, Арена, Турнір."),
                    ("КЛАДОВИЩЕ","Загиблі карти. Воскресіння або Поклик Предків можуть повернути їх."),
                    ("ЕВОЛЮЦІЯ","Скормка, Еволюція Божеств, Альтар Рун — прокачка карт та рун."),
                    ("РИНОК","Купівля карт за алмази, золото, руду або осколки. 5 розділів."),
                    ("МОЇ ОСКОЛКИ","Прогрес збору осколків Супер Людей та Архангелів."),
                    ("МОЇ РУНИ","Колекція рун. Прикріплюйте до карт для бонусів."),
                    ("ВІВТАР РУН","3 руни Lv1 = 1 руна Lv2. Комбінуйте для посилення."),
                    ("МОЇ ЕСЕНЦІЇ","Магічні субстанції що підсилюють здібності карт."),
                    ("ВДАЧА ГЕРОЇВ","Міні-ігри Монополія та Фортуна. Виграй призи."),
                    ("НАЛАШТУВАННЯ","Звук, музика, збереження прогресу."),
                    ("ЕНЦИКЛОПЕДІЯ","Довідник гри — цей розділ.")]:
                    _content_y=_enc_head(_tab,_content_y)
                    _content_y=_enc_text(_desc,14,_content_y,_font_b)
            elif _sec==2:  # Раси
                for _race,_color,_chars,_desc in [
                    ("⚔ Герої",(80,160,255),"HP:50–2000 | ATK:10–400 | Max:Lv100","Базова раса. Ринок(алмази/золото/руда). Еволюція→Бог після Lv100."),
                    ("👾 Монстри",(200,80,80),"HP:30–1800 | ATK:8–380 | Max:Lv100","Вороги та союзники. Ті самі характеристики що й Герої."),
                    ("🦸 Супер Люди",(80,200,120),"HP:30–2800 | ATK:8–625 | Max:Lv125","25 карт. Осколки(Турнір) або купівля 111–330 грн. Еволюція з Lv1."),
                    ("😇 Архангели та Архдемони",(160,120,255),"HP:42–2900 | ATK:11–785 | Max:Lv130","20 карт. Осколки 30–60 або купівля 122–350 грн. Найпотужніша раса."),
                    ("🌟 Боги (Еволюційні)",(255,200,0),"HP:2000–10000 | ATK:400–2000 | Max:Lv100","Герой/Монстр після Lv100→Еволюція. Найвищий урон у стандартних расах."),
                    ("⚔ Титани",(200,100,40),"HP:5000–50000 | ATK:800–5000 | Боси","Боси в режимі Титани-Монстри. Осколки для прокачки."),
                ]:
                    _content_y=_enc_head(_race,_content_y)
                    pygame.draw.rect(screen,tuple(c//4 for c in _color),(12,_content_y,w-24,_font_s.get_height()+2),border_radius=3)
                    _cs=_font_s.render(_chars,True,_color); screen.blit(_cs,(14,_content_y+1)); _content_y+=_font_s.get_height()+4
                    _content_y=_enc_text(_desc,14,_content_y,_font_b)
            elif _sec==3:  # Здібності
                _apl = getattr(self,"enc_abil_page",0)
                _APAGE=30
                _enc_abilities=[
                    ("🔥","Удар Вогню","ATK×1.2","damage"),("🌊","Хвиля Води","ATK×1.3","damage"),("⚡","Блискавка","ATK×1.5","damage"),
                    ("🌀","Телекінез","ATK×1.1","damage"),("⚔","Подвійний Удар","ATK×2.0","damage"),("🩸","Вампіризм","ATK×1+heal","damage"),
                    ("💣","Вибух","ATK×2.5","damage"),("☄","Метеорит","ATK×3.0","damage"),("👁","Третє Око","ATK×2.0","damage"),
                    ("🗡","Тіньовий Клинок","ATK×1.8 ігнор.брон.","damage"),("🐺","Зграя Вовків","ATK×3×0.5","damage"),("🦷","Укус Дракона","ATK×1.6 -броня","damage"),
                    ("🏹","Стріла Долі","ATK×1.4 ніколи не мах","damage"),("🌋","Лавовий Потік","ATK×1.7+підпал","damage"),("🔱","Тризуб","ATK×3×0.6","damage"),
                    ("💠","Ел.Сплеск","ATK×1.9","damage"),("🌪","Вихр Сталі","ATK×1.5+½AOE","damage"),("⚰","Удар Мил.","ATK×2.5(>50%HP)","damage"),
                    ("↩","Дзеркальний Вип.","контратака","damage"),("😤","Жага Крові","ATK+stack","damage"),("💥","Несподіванка","ATK×4(хід1)","damage"),
                    ("🔪","Проникаюче Лезо","ATK×1.3+pierce","damage"),("🌩","Грозовий Розряд","ATK×2.0","damage"),("🔥","Сфера Попелу","ATK×1.8+burn","damage"),
                    ("🧊","Крист.Шип","ATK×1.7+freeze","damage"),("🌀","Круговий Замах","ATK×1.4×3цілі","damage"),("🗡","Наск.Прокол","ATK×1.5×2","damage"),
                    ("😱","Сліпа Лють","ATK×1~3","damage"),("🎯","Вив.Постріл","ATK×1~5","damage"),("⚔","Шквал Клинків","ATK×2.0","damage"),
                    ("🌍","Землетрус","ATK×0.8×всі","aoe"),("💚","Масове Зціл.","HP+30%×армія","aoe"),("🦠","Чума","ATK×0.4+отрута","aoe"),
                    ("🌟","Зорян.Вибух","ATK×1.0×всі","aoe"),("🌪","Торнадо","ATK×0.7+stun×всі","aoe"),("🐉","Подих Дракона","ATK×1.2×ряд","aoe"),
                    ("🧊","Крижан.Шторм","ATK×0.9+freeze×всі","aoe"),("💥","Ядерна Хвиля","ATK×2.0×всі","aoe"),("🌩","Буря Блискавок","ATK×1.1×всі","aoe"),
                    ("💢","Інф.Буря","ATK×1.3×всі","aoe"),("🩸","Кривава Жатва","ATK×0.8+heal×всі","aoe"),("⚡","Небесний Гнів","ATK×1.5+stun×всі","aoe"),
                    ("🌑","Темна Жертва","ATK×1.0+self heal","aoe"),("❄","Заморозка","-50%ATK 2ходи","debuff"),("☠","Яд","-15%HP×4ходи","debuff"),
                    ("😴","Летаргія","sleep×2","debuff"),("🕸","Павутиння","immob×2","debuff"),("🫀","Спов.Серця","-50%ATK","debuff"),
                    ("🌫","Туман","miss 50%","debuff"),("🪄","Прокляття","-1 level","debuff"),("🩻","Осл.Кісток","armor=0","debuff"),
                    ("🧊","Кріостазис","skip×1","control"),("⚡","Нейрошок","no ATK×1","control"),("🔇","Мент.Блок","no skills×2","control"),
                    ("⏰","Часова Петля","delay×2","control"),("🤪","Безумство","friendly fire","control"),("⛓","Кандали","+30%dmg taken","control"),
                    ("🔮","Прост.Кишеня","remove×2ходи","control"),("💪","Атрофія Сил","ATK→мін","debuff"),("🔇","Печать Тиші","no magic","control"),
                    ("👀","Горгона","pause×2","control"),("🌑","Некроз","-5%maxHP/хід","debuff"),("🕹","Ляльковод","control×1","control"),
                    ("🎲","Рулетка Долі","random debuff","control"),("🦠","Їдка Іржа","armor=0 perm","debuff"),("😶","Глиб.Летаргія","sleep until hit","control"),
                    ("🌿","Сила Трави","+20%HP","heal"),("🌺","Кв.Відродж.","+5%HP×4ходи","heal"),("🫧","Крист.Роса","+40%HP","heal"),
                    ("🧬","Регенерація","+10%HP/хід","heal"),("💡","Благодать","+30%HP","heal"),("✨","Очисне Сяйво","+20%HP×армія","heal"),
                    ("🙏","Причастя","+35%HP self","heal"),("⚔","Бойовий Дух","+25%ATK раса","buff"),("💫","Останній Іспит","+15%ATK×армія","buff"),
                    ("📜","Поклик Предків","revive ally","special"),("🔥","Дух Фенікса","revive self 40%","special"),("💪","Сигнал Авангарду","+30%ATK×1хід","buff"),
                    ("🌀","Кругова Порука","+20%DEF 2сусіди","buff"),("⚡","Жага Помсти","+40%ATK","buff"),("💧","Сльози Богині","+8%HP/хід","heal"),
                    ("🦁","Левине Серце","immune fear","buff"),("❄","Морозна Мантія","+25%DEF союзн.","buff"),("💚","Абс.Чистота","cleanse army","special"),
                    ("🎯","Фокус Чакл.","+50%skill dmg","buff"),("🩸","Крив.Пакт","+40%ATK -10%HP","buff"),("🔄","Бойова Синерг.","+10%HP союзн.","buff"),
                    ("🔮","Воскресіння","revive","special"),("💀","Відродження","self revive","special"),("🌑","Темна Діра","absorb attack","special"),
                    ("🛡","Щит","block×1","special"),("🦅","Крило Орла","dodge×1","special"),("🍀","Удача","×2 reward","special"),
                    ("🌈","Веселкова Аура","immune×1","special"),("🕳","Портал","teleport+ATK×1.5","special"),("🌠","Зоряна Сила","ATK×3.0 next","special"),
                    ("⏳","Зупинка Часу","skip enemy×1","special"),("🧿","Відбиття","reflect×1.0","special"),("🦁","Рев Лева","-30%ATK всі","special"),
                    ("🔑","Ключ Долі","unlock skill","special"),("🌙","Місячна Тінь","ATK×2 stealth","special"),("🌑","Безодня","kill 30%","special"),
                    ("🌀","Астр.Висилка","banish×1","special"),("🪞","Дзерк.Втіл.","copy enemy","special"),("🌌","Чорна Діра","instant kill","special"),
                    ("💀","Дотик Жнеця","kill 25%","special"),("💫","Глоб.Колапс","all cards die","special"),("👻","Безтілесний","phys immune","special"),
                    ("🧠","Викривл.Розуму","self-harm skill","control"),("⚗","Деградація","transform enemy","special"),("🔄","Інверсія","swap ATK↔HP","special"),
                    ("💀","Смертельні Узи","chain death","special"),("🎲","Сліпий Шанс","random ×0.5~5","special"),("🌟","Вища Форма","all skills×1","special"),
                    ("🔇","Мертва Тиша","no passives","control"),
                ]
                _TYPE_CLR={"damage":(200,80,30),"aoe":(130,80,220),"debuff":(140,130,120),"control":(30,100,180),"heal":(30,160,100),"buff":(180,130,20),"special":(150,40,100)}
                _total_pages=(len(_enc_abilities)+_APAGE-1)//_APAGE
                _pg_ab=min(_apl,_total_pages-1)
                _slice=_enc_abilities[_pg_ab*_APAGE:(_pg_ab+1)*_APAGE]
                _content_y=_enc_head(f"✨ Здібності — сторінка {_pg_ab+1}/{_total_pages} (всього {len(_enc_abilities)})",_content_y)
                _col_w=[(w-20)//4,(w-20)//4,(w-20)//4,(w-20)//4]
                for _i,(_em,_nm,_dmg,_tp) in enumerate(_slice):
                    _row=_i//2; _col=_i%2
                    _rx=12+_col*(w-20)//2; _ry=_content_y+_row*18
                    _tc=_TYPE_CLR.get(_tp,(150,150,150))
                    _ab_s=_font_s.render(f"{_em}{_nm[:14]}",True,WHITE)
                    screen.blit(_ab_s,(_rx,_ry))
                    _dm_s=_font_s.render(_dmg[:12],True,_tc)
                    screen.blit(_dm_s,(_rx+(w-20)//4,_ry))
                _content_y+=(_APAGE//2+1)*18+4
                # Кнопки сторінок
                for _pi in range(_total_pages):
                    _pb=pygame.Rect(12+_pi*28,_content_y,24,16)
                    pygame.draw.rect(screen,(20,100,180) if _pi==_pg_ab else (20,20,30),_pb,border_radius=4)
                    _ps=pygame.font.Font(None,14).render(str(_pi+1),True,WHITE); screen.blit(_ps,(_pb.x+5,_pb.y+2))
                    if not hasattr(self,"_enc_abil_pbtns"): self._enc_abil_pbtns=[]
                    if _pi>=len(getattr(self,"_enc_abil_pbtns",[])): 
                        if not hasattr(self,"_enc_abil_pbtns"): self._enc_abil_pbtns=[]
                        self._enc_abil_pbtns.append(_pb)
                _content_y+=22
            elif _sec==4:  # Руни
                _content_y=_enc_head("🔮 Руни — посилення карт",_content_y)
                for _p in ["Руни прикріплюються до карт (до 3 штук) і дають бонуси. Отримати: Ринок(руда), Вдача Героїв, Вівтар рун(3 руни Lv1=1 Lv2).",
                    "Вівтар рун: ЕВОЛЮЦІЯ→Альтар Рун→оберіть 3 руни→Комбінувати. Максимум Lv5."]:
                    _content_y=_enc_text(_p,12,_content_y,_font_b)
                for _rn,_rb in [("🔥 Руна Вогню","ATK+8%"),("❄ Руна Льоду","Заморозка+15%"),("⚡ Руна Грому","Крит+10%"),
                    ("🌍 Руна Землі","HP+12%"),("🌑 Руна Тіні","Ухилення+8%"),("🩸 Руна Крові","Вамп.+10%"),
                    ("🛡 Руна Щита","DEF+15%"),("🍀 Руна Удачі","Предмети+20%"),("🌀 Руна Хаосу","Рандом ефект"),
                    ("💫 Руна Відродж.","Воск.15%"),("☠ Руна Отрути","Отрута+12%"),("⏳ Руна Часу","Швидк.+10%"),
                    ("👻 Руна Духа","Проникн.+18%"),("🌟 Руна Зорі","Всі пар.+5%"),("🔱 Руна Пекла","AOE+15%"),("☁ Руна Небес","Зціл.+20%")]:
                    _rs=_font_s.render(f"{_rn}: {_rb}",True,GRAY); screen.blit(_rs,(14,_content_y)); _content_y+=_font_s.get_height()+2
            elif _sec==5:  # Есенції
                _content_y=_enc_head("✨ Есенції — магічні субстанції",_content_y)
                for _p in ["Есенції підсилюють здібності карт (на відміну від рун — параметри). Одноразові.",
                    "Отримати: Ринок, нагорода за локації, Вдача Героїв, Турнір. Застосувати: МОЇ ЕСЕНЦІЇ→обери есенцію→обери карту→Застосувати.",
                    "Типи: Атакуючі(+5–25% урон від damage/aoe), Захисні(+5–25% heal/special), Контрольні(+1–3 ходи тривалість дебафів), Бафф(посилюють buff ефекти).",
                    "Рівень есенцій: Lv1=+3%, Lv5=+15%. 3 есенції Lv1 = 1 есенція Lv2 через Вівтар."]:
                    _content_y=_enc_text(_p,12,_content_y,_font_b)
            elif _sec==6:  # Режими бою
                for _bm,_diff,_desc in [
                    ("🗺 Карта Світів","⭐–⭐⭐⭐⭐⭐","20 світів × 10 локацій. Відкриваються послідовно. Стандартні параметри ворогів. Нагорода: золото, руда, ключі."),
                    ("🦸 Супер Люди","⭐⭐–⭐⭐⭐⭐","20 світів. Вороги +10%HP +5%ATK. Серед ворогів — 3 карти Супер Людей."),
                    ("😇 Ангели та Демони","⭐⭐⭐–⭐⭐⭐⭐⭐","20 світів. Вороги +20%HP +10%ATK. Серед ворогів — 2 Супер Люди та 2 Архангели."),
                    ("👹 Титани-Монстри","⭐⭐⭐⭐","Боси-Титани. Особлива механіка клонів. Нагорода: осколки Титана."),
                    ("⚔ Арена","⭐⭐⭐","Бої з ботами. Тренування без ризику. Нагорода: золото."),
                    ("🏆 Турнір Героїв","⭐⭐⭐⭐⭐","Онлайн-бій з гравцями. Перемога: +3 ключі +2 осколки×2 раси. Поразка: +1 ключ +1 осколок×2 раси."),
                ]:
                    _content_y=_enc_head(_bm+" "+_diff,_content_y)
                    _content_y=_enc_text(_desc,14,_content_y,_font_b)
            elif _sec==7:  # Еволюція
                for _eh,_ed in [
                    ("⚗ Скормка","Базова прокачка. Оберіть карту-ціль → оберіть карти-їжу → Скормити. 100 XP (рівнів їжі) = +1 рівень карти. Карти-їжа зникають."),
                    ("🌟 Еволюція Божеств","Герой/Монстр Lv100 → Бог Lv1. Потрібні жертвені карти. Бог прокачується через Еволюцію Рівня Божеств: 100 рівнів їжі = +1 рівень Бога. Max: Lv100."),
                    ("🦸 Еволюція Супер Людей","Прокачка з 1-го рівня через Еволюцію Рівня Божеств. 100 XP = +1 рівень. Max: Lv125."),
                    ("😇 Еволюція Архангелів","Аналогічно Супер Людям. 100 XP = +1 рівень. Max: Lv130 — найвищий у грі."),
                    ("🏺 Вівтар рун","ЕВОЛЮЦІЯ→Альтар Рун: 3 руни нижчого рівня → 1 руна вищого. Max рівень руни: 5."),
                    ("👑 Верховний Бог","Прокачайте Бога до Lv100 + пройдіть Світ 20 у всіх 3 кампаніях = статус Верховного Бога."),
                ]:
                    _content_y=_enc_head(_eh,_content_y)
                    _content_y=_enc_text(_ed,14,_content_y,_font_b)
            elif _sec==8:  # Ринок
                for _mh,_md in [
                    ("💎 Карти за Алмази","30 унікальних карт. 50–500 алмазів. Алмази: Турнір, Вдача Героїв."),
                    ("🪙 Карти за Золото","Стандартні Герої та Монстри. 500–5000 золота. Золото: кожен бій."),
                    ("⛏ Карти за Руду","Особливі Монстри. 200–3000 руди. Руда: Карта Світів."),
                    ("🧩 Супер Люди (Осколки)","25 карт. 1–30 осколків або 111–330 грн. Осколки: Турнір."),
                    ("💠 Архангели (Осколки II)","20 карт. 30–60 осколків або 122–350 грн. Наймогутніші."),
                    ("💰 Гаманець","Золото(боїв), Алмази(Турнір), Руда(Карта Світів), Ключі(1/перемогу). Використовуються для покупок, Монополії(золото), Фортуни(руда)."),
                ]:
                    _content_y=_enc_head(_mh,_content_y)
                    _content_y=_enc_text(_md,14,_content_y,_font_b)
            elif _sec==9:  # Осколки
                for _sh,_sd in [
                    ("🧩 Навіщо осколки","Єдиний безплатний спосіб отримати Супер Людей та Архангелів — наймогутніших карт гри."),
                    ("📥 Отримання","Турнір(перемога): +2 СЛ +2 Арх. Турнір(поразка): +1 СЛ +1 Арх. Вдача Героїв: +1 будь-якої раси."),
                    ("🔓 Розблокування","РИНОК→відповідний розділ→знайдіть карту→Розблокувати (при наявності осколків). Карта переходить у МОЯ АРМІЯ Lv1."),
                    ("💳 Купівля","Кожна карта має ціну в гривнях. Натисніть КУПИТИ→оплата через Monobank→ПЕРЕВІРИТИ."),
                ]:
                    _content_y=_enc_head(_sh,_content_y)
                    _content_y=_enc_text(_sd,14,_content_y,_font_b)
            elif _sec==10:  # Вдача Героїв
                for _lh,_ld in [
                    ("🎲 Монополія","16 клітинок по периметру. 3 безплатні кидки + 3 за 3000 золота. Дубль = бонусні кроки. Пропуск ходу = втрачаєте наступний кидок. Після зупинки — забираєте приз."),
                    ("🌀 Фортуна","16-секторне колесо. 3 безплатні + 3 за 3000 руди. Змінити призи = 1 ключ."),
                    ("🎁 Призи","Герой/Монстр Lv5-15, 1000–2000 золота, 500–1000 алмазів, 750 руди, 1–3 ключі, осколок Супер Людей, осколок Архангела, 10 осколків Титана, Есенція, Руна, Пропуск ходу."),
                ]:
                    _content_y=_enc_head(_lh,_content_y)
                    _content_y=_enc_text(_ld,14,_content_y,_font_b)
            screen.set_clip(None)
            _total_content_h = _content_y + _sy + 40
            up_re, dn_re = self.draw_scroll_arrows("encyclopedia", _total_content_h, _CLIP_BOT-_CLIP_TOP, _sy, w-28, _CLIP_TOP, _CLIP_BOT)
            self.scroll_arrows_rects["encyclopedia"] = (up_re, dn_re)
            self.btn_back = self.draw_button("НАЗАД", 20, h-48, 130, 40, GRAY)

        elif self.state == MY_RUNES:
            title_s = font_big.render("МОЇ РУНИ", True, (180, 130, 255))
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 12))

            # ── Вкладки ──
            tab_labels = [("hero","РУНИ ДЛЯ КАРТ"), ("titan","РУНИ ДЛЯ ТИТАНІВ")]
            self.my_runes_tab_rects = {}
            tx0 = w//2 - 240
            for ti, (tk, tl) in enumerate(tab_labels):
                tc = (80,40,160) if self.my_runes_tab == tk else (40,40,70)
                tr = pygame.Rect(tx0 + ti*250, 50, 230, 38)
                pygame.draw.rect(screen, tc, tr, border_radius=8)
                pygame.draw.rect(screen, (180,130,255), tr, 2, border_radius=8)
                ls = font_bold.render(tl, True, WHITE)
                screen.blit(ls, (tr.x+tr.w//2-ls.get_width()//2, tr.y+tr.h//2-ls.get_height()//2))
                self.my_runes_tab_rects[tk] = tr

            filtered = [(i, r) for i, r in enumerate(self.my_runes)
                        if (self.my_runes_tab == "titan") == r.get("for_titan", False)]

            # ── Параметри сітки та пагінації ──
            R_COLS = 4; R_W = min(215,(w-60)//R_COLS-12); R_H = 100; R_GAP = 10
            total_rw = R_COLS*R_W+(R_COLS-1)*R_GAP
            rx0 = w//2-total_rw//2; ry0 = 98
            _avail_h   = h - 220          # висота для карток
            _rows_vis  = max(1, _avail_h // (R_H + R_GAP))
            _per_page  = R_COLS * _rows_vis
            _rune_page = max(0, getattr(self, 'rune_page', 0))
            _total     = len(filtered)
            _pages     = max(1, (_total + _per_page - 1) // _per_page)
            _rune_page = min(_rune_page, _pages - 1)
            self.rune_page = _rune_page
            _page_items = filtered[_rune_page*_per_page : (_rune_page+1)*_per_page]

            cnt_s = font_small.render(
                f"Рун: {_total}  |  Стор. {_rune_page+1}/{_pages}", True, GRAY)
            screen.blit(cnt_s, (10, ry0 - 16))

            # ── Малюємо руни поточної сторінки ──
            self.rune_rects = []; self._rune_orig_map = []
            mx_h, my_h = pygame.mouse.get_pos()
            for fi, (orig_i, rune) in enumerate(_page_items):
                col_r = fi % R_COLS; row_r = fi // R_COLS
                rx = rx0 + col_r*(R_W+R_GAP)
                ry = ry0 + row_r*(R_H+R_GAP)
                is_sel = (self.rune_selected_idx == orig_i)
                rr = pygame.Rect(rx, ry, R_W, R_H)
                self.rune_rects.append(rr); self._rune_orig_map.append(orig_i)
                pygame.draw.rect(screen, (70,35,120) if is_sel else (25,15,48), rr, border_radius=10)
                _bc = (255,220,80) if is_sel else (160,100,255) if rr.collidepoint(mx_h,my_h) else (130,80,220)
                pygame.draw.rect(screen, _bc, rr, 2 if not is_sel else 3, border_radius=10)
                draw_rune_circle(screen, rx+22, ry+R_H//2, 16, rune["level"])
                screen.blit(font_bold.render(rune["name"][:18], True, WHITE), (rx+46, ry+8))
                typ_col = (255,200,80) if rune.get("for_titan") else (100,220,100)
                screen.blit(font_small.render("Для Титана" if rune.get("for_titan") else "Для Карти",
                            True, typ_col), (rx+46, ry+30))
                screen.blit(font_small.render(f"+{rune['level']}% до здібності",
                            True, (180,180,255)), (rx+46, ry+50))
                lv_col = GOLD if rune['level'] >= 8 else (180,180,180)
                screen.blit(font_bold.render(f"Lv{rune['level']}", True, lv_col),
                            (rx+R_W-font_bold.size(f"Lv{rune['level']}")[0]-8, ry+R_H-22))

            # ── ПАГІНАЦІЯ — кнопки з цифрами внизу ──
            _pg_y = h - 150
            pygame.draw.line(screen, (60,60,80), (0, _pg_y-4), (w, _pg_y-4), 1)

            _PBW = 36; _PBH = 32; _PGAP = 5
            _max_vis_btns = min(_pages, max(5, (w-120)//(_PBW+_PGAP)))
            _half = _max_vis_btns // 2
            _pg_start = max(0, _rune_page - _half)
            _pg_end   = min(_pages, _pg_start + _max_vis_btns)
            _pg_start = max(0, _pg_end - _max_vis_btns)
            _btns_w   = _max_vis_btns*(_PBW+_PGAP) - _PGAP + (_PBW+_PGAP)*2
            _pgi_x0   = w//2 - _btns_w//2

            # ◀ кнопка
            _prev_col = (70,70,120) if _rune_page > 0 else (30,30,40)
            self._btn_rune_pg_prev = self.draw_button("◀", _pgi_x0, _pg_y, _PBW, _PBH,
                                                       _prev_col, active=_rune_page>0)
            # Цифри
            self._rune_pg_btn_list = []
            for _pi in range(_pg_start, _pg_end):
                _bx = _pgi_x0 + (_PBW+_PGAP) + (_pi-_pg_start)*(_PBW+_PGAP)
                _is_cur = (_pi == _rune_page)
                _pc = (120,60,220) if _is_cur else (50,50,90)
                _pbr = self.draw_button(str(_pi+1), _bx, _pg_y, _PBW, _PBH, _pc)
                if _is_cur:
                    pygame.draw.rect(screen, (200,150,255), _pbr, 2, border_radius=6)
                self._rune_pg_btn_list.append((_pi, _pbr))
            # ▶ кнопка
            _next_col = (70,70,120) if _rune_page < _pages-1 else (30,30,40)
            self._btn_rune_pg_next = self.draw_button(
                "▶", _pgi_x0+(_PBW+_PGAP)+_max_vis_btns*(_PBW+_PGAP),
                _pg_y, _PBW, _PBH, _next_col, active=_rune_page<_pages-1)

            # ── Панель дій вибраної руни ──
            sel_rune = (self.my_runes[self.rune_selected_idx]
                        if self.rune_selected_idx is not None
                        and self.rune_selected_idx < len(self.my_runes) else None)
            _btn_ay = h - 108
            btn_col = (50,120,200) if sel_rune else GRAY
            bw2 = min(230, (w-60)//3-10)
            bx1 = w//2 - bw2*3//2 - 15
            self.btn_place_rune       = self.draw_button("СТАВИТИ НА КАРТУ",  bx1,           _btn_ay, bw2, 42, btn_col, active=bool(sel_rune))
            self.btn_place_rune_titan = self.draw_button("СТАВИТИ НА ТИТАНА", bx1+bw2+15,    _btn_ay, bw2, 42, btn_col, active=bool(sel_rune))
            self.btn_remove_rune      = self.draw_button("ВИДАЛИТИ РУНУ",     bx1+bw2*2+30,  _btn_ay, bw2, 42, RED if sel_rune else GRAY, active=bool(sel_rune))

            if sel_rune:
                info_s = font_small.render(
                    f"Вибрана: {sel_rune['name']}  Lv{sel_rune['level']}  "
                    f"({'Для Титана' if sel_rune.get('for_titan') else 'Для Карти'})",
                    True, GOLD)
                screen.blit(info_s, (w//2-info_s.get_width()//2, h-62))

            if self.rune_msg:
                mc = (GREEN if ("встановлена" in self.rune_msg or "знято" in self.rune_msg)
                      else RED if ("Немає" in self.rune_msg or "Недостат" in self.rune_msg
                                   or "Ця руна" in self.rune_msg) else WHITE)
                ms = font.render(self.rune_msg, True, mc)
                screen.blit(ms, (w//2-ms.get_width()//2, h-40))

            self.btn_remove_rune_mode = self.draw_button("ЗАБРАТИ РУНУ З КАРТИ", w-230, h-70, 210, 44, (140,60,20))
            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == RUNE_ALTAR:
            # --- АЛТАРЬ РУН ---
            title_s = font_big.render("АЛТАРЬ РУН", True, (255, 180, 60))
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 12))

            # Три вкладки
            altar_tabs = [
                ("dia",  "РУНИ ЗА АЛМАЗИ 💎",  f"100 💎 за 5 рун", (60, 120, 220)),
                ("ore",  "РУНИ ЗА РУДУ ⛏️",    f"500 ⛏️ за 5 рун", (120, 100, 60)),
                ("gold", "РУНИ ЗА ЗОЛОТО 🪙",  f"1000 🪙 за 5 рун",(160, 130, 20)),
            ]
            self.rune_altar_tab_rects = {}
            tw = (w - 80) // 3
            for ti, (tk, tlbl, tcost, tcol) in enumerate(altar_tabs):
                is_act = self.rune_altar_tab == tk
                bg = tcol if is_act else (30, 30, 50)
                tr = pygame.Rect(40 + ti*(tw+10), 60, tw, 48)
                pygame.draw.rect(screen, bg, tr, border_radius=9)
                pygame.draw.rect(screen, tcol, tr, 2, border_radius=9)
                ls = font_bold.render(tlbl, True, WHITE)
                screen.blit(ls, (tr.x + tr.w//2 - ls.get_width()//2, tr.y+8))
                cs = font_small.render(tcost, True, (220, 220, 180))
                screen.blit(cs, (tr.x + tr.w//2 - cs.get_width()//2, tr.y+28))
                self.rune_altar_tab_rects[tk] = tr

            # Поточна вкладка — опис рівнів
            cur_tab = next(t for t in altar_tabs if t[0] == self.rune_altar_tab)
            desc_lines = {
                "dia":  ["Алмазний алтарь — найвищі шанси!", "Рівні рун: переважно 5–10", "Шанс руни для Титана: 20%"],
                "ore":  ["Рудний алтарь — середні шанси.",   "Рівні рун: переважно 2–6",  "Шанс руни для Титана: 20%"],
                "gold": ["Золотий алтарь — нижчі шанси.",    "Рівні рун: переважно 1–4",  "Шанс руни для Титана: 20%"],
            }
            py = 130
            for dl in desc_lines[self.rune_altar_tab]:
                ds = font.render(dl, True, (180, 200, 220))
                screen.blit(ds, (w//2 - ds.get_width()//2, py)); py += 28

            # Показ гаманця (поточний ресурс)
            bal_str = f"Баланс: {self.diamonds} 💎  {self.ore} ⛏️  {self.gold} 🪙"
            bs = font.render(bal_str, True, GOLD)
            screen.blit(bs, (w//2 - bs.get_width()//2, py + 10))

            # Кнопка покупки
            cost = RUNE_PULL_COSTS[self.rune_altar_tab]
            if self.rune_altar_tab == "dia":   can_buy = self.diamonds >= cost
            elif self.rune_altar_tab == "ore": can_buy = self.ore >= cost
            else:                              can_buy = self.gold >= cost
            btn_c = cur_tab[3] if can_buy else GRAY
            self.btn_rune_pull = self.draw_button(f"ОТРИМАТИ 5 РУН", w//2-180, h//2+20, 360, 60, btn_c, active=can_buy)

            # Останні отримані руни (preview)
            if hasattr(self, "_last_pulled_runes") and self._last_pulled_runes:
                ps = font_small.render("Останні 5 рун:", True, (180, 180, 255))
                screen.blit(ps, (w//2 - ps.get_width()//2, h//2+100))
                for pi, pr in enumerate(self._last_pulled_runes):
                    col = (255, 200, 80) if pr.get("for_titan") else (100, 255, 150)
                    rs = font_small.render(f"  {pr['name']} Lv{pr['level']} ({'Титан' if pr.get('for_titan') else 'Карта'})", True, col)
                    screen.blit(rs, (w//2 - rs.get_width()//2, h//2+120+pi*20))

            if self.rune_msg:
                mc = GREEN if "Отримано" in self.rune_msg else RED
                ms = font.render(self.rune_msg, True, mc)
                screen.blit(ms, (w//2 - ms.get_width()//2, h-80))

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == EVOLUTION:
            title_s = font_big.render("ЕВОЛЮЦІЯ", True, (80, 220, 255))
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 8))

            # Сітка: два ряди по 3 вкладки (як РИНОК)
            EVO_W   = 260
            EVO_IMG = 110
            EVO_BTN = 44
            EVO_GAP = 26
            EVO_CELL_H = EVO_IMG + EVO_BTN + 6
            EVO_ROW_GAP = 18
            total_evo_w = 3 * EVO_W + 2 * EVO_GAP
            evo_x0  = w // 2 - total_evo_w // 2

            # ── Рядок 1: РУН / БОЖЕСТВ / РІВЕНЬ БОЖЕСТВ ──────────────────
            evo_y0 = 52

            # 1-1: ЕВОЛЮЦІЯ РУН
            _e1x = evo_x0
            _rune_cnt_s = sum(1 for r in self.my_runes if not r.get("for_titan"))
            _img_e1 = load_tab_image("rune_evolution", EVO_W, EVO_IMG)
            if _img_e1:
                screen.blit(_img_e1, (_e1x, evo_y0))
            else:
                pygame.draw.rect(screen,(40,80,120),(_e1x,evo_y0,EVO_W,EVO_IMG),border_radius=10)
                _ph1 = font_big.render("✦",True,(80,220,255))
                screen.blit(_ph1,(_e1x+EVO_W//2-_ph1.get_width()//2, evo_y0+EVO_IMG//2-_ph1.get_height()//2))
            _rc_s = font_small.render(f"Рун для прокачки: {_rune_cnt_s}", True, (180,220,255))
            screen.blit(_rc_s, (_e1x + EVO_W//2 - _rc_s.get_width()//2, evo_y0 + EVO_IMG + 2))
            self.btn_rune_evo = self.draw_button("ЕВОЛЮЦІЯ РУН", _e1x, evo_y0+EVO_IMG+EVO_BTN-4, EVO_W, EVO_BTN, (40,100,180))

            # 1-2: ЕВОЛЮЦІЯ БОЖЕСТВ
            _e2x = evo_x0 + EVO_W + EVO_GAP
            _pulse_d = abs(math.sin(time.time()*2.0))
            _div_cnt = sum(1 for c in self.all_heroes if c.level >= 100 and not getattr(c,'evolved',False))
            _img_e2 = load_tab_image("divine_evolution", EVO_W, EVO_IMG)
            if _img_e2:
                screen.blit(_img_e2, (_e2x, evo_y0))
            else:
                _gc2 = (255,int(200+55*_pulse_d),0)
                pygame.draw.rect(screen,(int(40+30*_pulse_d),int(20+10*_pulse_d),int(60+30*_pulse_d)),(_e2x,evo_y0,EVO_W,EVO_IMG),border_radius=10)
                pygame.draw.rect(screen,_gc2,(_e2x,evo_y0,EVO_W,EVO_IMG),3,border_radius=10)
                _ph2=font_big.render("⚜",True,_gc2); screen.blit(_ph2,(_e2x+EVO_W//2-_ph2.get_width()//2,evo_y0+EVO_IMG//2-_ph2.get_height()//2))
            _d2_s=font_small.render(f"Готово: {_div_cnt}",True,(255,215,0)); screen.blit(_d2_s,(_e2x+EVO_W//2-_d2_s.get_width()//2,evo_y0+EVO_IMG+2))
            self.btn_divine_evo = self.draw_button("ЕВОЛЮЦІЯ БОЖЕСТВ",_e2x,evo_y0+EVO_IMG+EVO_BTN-4,EVO_W,EVO_BTN,(80,40,140) if _div_cnt>0 else (50,50,70),active=(_div_cnt>0))

            # 1-3: ЕВОЛЮЦІЯ РІВНЯ БОЖЕСТВ
            _e3x = evo_x0 + 2*(EVO_W+EVO_GAP)
            _pulse_l = abs(math.sin(time.time()*1.8+1.0))
            _evo_cnt = sum(1 for c in self.all_heroes if getattr(c,'evolved',False) and getattr(c,'evo_tier',0)==1 and c.level < DIVINE_MAX_LEVEL)
            _img_e3 = load_tab_image("divine_level_evo", EVO_W, EVO_IMG)
            if _img_e3:
                screen.blit(_img_e3, (_e3x, evo_y0))
            else:
                _gc3=(int(80+175*_pulse_l),255,int(80+100*_pulse_l))
                pygame.draw.rect(screen,(int(20+20*_pulse_l),int(40+20*_pulse_l),int(20+20*_pulse_l)),(_e3x,evo_y0,EVO_W,EVO_IMG),border_radius=10)
                pygame.draw.rect(screen,_gc3,(_e3x,evo_y0,EVO_W,EVO_IMG),3,border_radius=10)
                _ph3=font_big.render("⬆",True,_gc3); screen.blit(_ph3,(_e3x+EVO_W//2-_ph3.get_width()//2,evo_y0+EVO_IMG//2-_ph3.get_height()//2))
            _d3_s=font_small.render(f"Еволюц: {_evo_cnt}",True,(80,255,150)); screen.blit(_d3_s,(_e3x+EVO_W//2-_d3_s.get_width()//2,evo_y0+EVO_IMG+2))
            self.btn_divine_lvl = self.draw_button("РІВЕНЬ БОЖЕСТВ",_e3x,evo_y0+EVO_IMG+EVO_BTN-4,EVO_W,EVO_BTN,(30,100,60) if _evo_cnt>0 else (50,50,70),active=(_evo_cnt>0))

            # ── Рядок 2: ВЕРХОВНІ БОГИ / РІВЕНЬ ВЕРХОВНИХ / ЗДІБНОСТІ ─────
            evo_y1 = evo_y0 + EVO_CELL_H + EVO_ROW_GAP + 22

            # 2-1: ЕВОЛЮЦІЯ ВЕРХОВНИХ БОГІВ (110 рівень → EvII)
            _e4x = evo_x0
            _pulse_s = abs(math.sin(time.time()*2.5))
            _sup_cnt = sum(1 for c in self.all_heroes if c.level >= DIVINE_MAX_LEVEL and getattr(c,'evo_tier',0) < 2)
            _img_e4 = load_tab_image("supreme_evolution", EVO_W, EVO_IMG)
            if _img_e4:
                screen.blit(_img_e4, (_e4x, evo_y1))
            else:
                _gc4=(255,int(180+75*_pulse_s),int(20+20*_pulse_s))
                pygame.draw.rect(screen,(int(30+20*_pulse_s),int(15+10*_pulse_s),0),(_e4x,evo_y1,EVO_W,EVO_IMG),border_radius=10)
                pygame.draw.rect(screen,_gc4,(_e4x,evo_y1,EVO_W,EVO_IMG),3,border_radius=10)
                _ph4=font_big.render("👑",True,_gc4); screen.blit(_ph4,(_e4x+EVO_W//2-_ph4.get_width()//2,evo_y1+EVO_IMG//2-_ph4.get_height()//2))
            _d4_s=font_small.render(f"110 рівень: {_sup_cnt}",True,(255,200,80)); screen.blit(_d4_s,(_e4x+EVO_W//2-_d4_s.get_width()//2,evo_y1+EVO_IMG+2))
            self.btn_supreme_evo = self.draw_button("ВЕРХОВНІ БОГИ",_e4x,evo_y1+EVO_IMG+EVO_BTN-4,EVO_W,EVO_BTN,(140,80,0) if _sup_cnt>0 else (50,50,70),active=(_sup_cnt>0))

            # 2-2: ЕВОЛЮЦІЯ РІВНЯ ВЕРХОВНИХ БОГІВ
            _e5x = evo_x0 + EVO_W + EVO_GAP
            _pulse_sv = abs(math.sin(time.time()*1.6+0.5))
            _sup_lv_cnt = sum(1 for c in self.all_heroes if getattr(c,'evo_tier',0)==2 and c.level < SUPREME_MAX_LEVEL)
            _img_e5 = load_tab_image("supreme_level_evo", EVO_W, EVO_IMG)
            if _img_e5:
                screen.blit(_img_e5, (_e5x, evo_y1))
            else:
                _gc5=(255,int(200+55*_pulse_sv),0)
                pygame.draw.rect(screen,(int(25+20*_pulse_sv),int(10+10*_pulse_sv),int(30+20*_pulse_sv)),(_e5x,evo_y1,EVO_W,EVO_IMG),border_radius=10)
                pygame.draw.rect(screen,_gc5,(_e5x,evo_y1,EVO_W,EVO_IMG),3,border_radius=10)
                _ph5=font_big.render("🌟",True,_gc5); screen.blit(_ph5,(_e5x+EVO_W//2-_ph5.get_width()//2,evo_y1+EVO_IMG//2-_ph5.get_height()//2))
            _d5_s=font_small.render(f"Верховних: {_sup_lv_cnt}",True,(255,220,100)); screen.blit(_d5_s,(_e5x+EVO_W//2-_d5_s.get_width()//2,evo_y1+EVO_IMG+2))
            self.btn_supreme_lvl = self.draw_button("РІВЕНЬ ВЕРХОВНИХ",_e5x,evo_y1+EVO_IMG+EVO_BTN-4,EVO_W,EVO_BTN,(100,60,0) if _sup_lv_cnt>0 else (50,50,70),active=(_sup_lv_cnt>0))

            # 2-3: ЗДІБНОСТІ
            _e6x = evo_x0 + 2*(EVO_W+EVO_GAP)
            _p6 = abs(math.sin(time.time()*1.5+2.0))
            _ess_cnt6 = len(getattr(self,'my_essences',[]))
            _img_e6 = load_tab_image("abilities_evo", EVO_W, EVO_IMG)
            if _img_e6:
                screen.blit(_img_e6, (_e6x, evo_y1))
            else:
                _gc6=(255,int(200+55*_p6),0)
                pygame.draw.rect(screen,(int(30+20*_p6),int(25+15*_p6),int(10+5*_p6)),(_e6x,evo_y1,EVO_W,EVO_IMG),border_radius=10)
                pygame.draw.rect(screen,_gc6,(_e6x,evo_y1,EVO_W,EVO_IMG),3,border_radius=10)
                _ph6=font_big.render("⭐",True,_gc6); screen.blit(_ph6,(_e6x+EVO_W//2-_ph6.get_width()//2,evo_y1+EVO_IMG//2-_ph6.get_height()//2))
            _d6_s=font_small.render(f"Есенцій: {_ess_cnt6}",True,(255,200,60)); screen.blit(_d6_s,(_e6x+EVO_W//2-_d6_s.get_width()//2,evo_y1+EVO_IMG+2))
            self.btn_ability_evo = self.draw_button("ЗДІБНОСТІ",_e6x,evo_y1+EVO_IMG+EVO_BTN-4,EVO_W,EVO_BTN,(100,80,20))

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == RUNE_EVOLUTION:
            # --- ЕВОЛЮЦІЯ РУН ---
            title_s = font_big.render("ЕВОЛЮЦІЯ РУН", True, (80, 220, 255))
            screen.blit(title_s, (w//2 - title_s.get_width()//2, 12))

            hero_runes = [r for r in self.my_runes if not r.get("for_titan")]
            if not hero_runes:
                ns = font.render("Немає рун для прокачки! Отримай руни в Алтарі Рун.", True, GRAY)
                screen.blit(ns, (w//2 - ns.get_width()//2, h//2))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
                return

            # Інструкція
            if self.rune_evo_target_idx is None:
                inst = font_small.render("1. Натисни руну яку хочеш прокачати", True, (180, 220, 180))
            else:
                needed = runes_needed_for_upgrade(hero_runes[self.rune_evo_target_idx]["level"])
                cur_prog = sum(rune_feed_progress(hero_runes[i]["level"])
                               for i in self.rune_evo_feed_idxs if i < len(hero_runes))
                inst = font_small.render(f"2. Вибирай руни для скормлення (потрібно {needed} прогресу, є {cur_prog})", True, (180, 220, 180))
            screen.blit(inst, (10, 55))

            # Сітка рун
            R_COLS = 6; R_W = 138; R_H = 90; R_GAP = 8
            total_rw = R_COLS * R_W + (R_COLS-1) * R_GAP
            rx0 = w//2 - total_rw//2
            ry0 = 78
            VISIBLE_ROWS = 4  # скільки рядків видно одночасно
            grid_h = VISIBLE_ROWS * (R_H + R_GAP)

            # Стрілки прокрутки
            total_rows = (len(hero_runes) + R_COLS - 1) // R_COLS
            max_scroll = max(0, total_rows - VISIBLE_ROWS)
            scroll = min(getattr(self, "rune_evo_scroll", 0), max_scroll)
            self.rune_evo_scroll = scroll

            arrow_x = w - 52
            arrow_mid_y = ry0 + grid_h // 2
            up_col   = (80, 180, 255) if scroll > 0        else (60, 60, 80)
            down_col = (80, 180, 255) if scroll < max_scroll else (60, 60, 80)
            self.btn_rune_scroll_up   = pygame.Rect(arrow_x, arrow_mid_y - 52, 44, 44)
            self.btn_rune_scroll_down = pygame.Rect(arrow_x, arrow_mid_y + 8,  44, 44)
            pygame.draw.rect(screen, up_col,   self.btn_rune_scroll_up,   border_radius=8)
            pygame.draw.rect(screen, down_col, self.btn_rune_scroll_down, border_radius=8)
            # Символи стрілок
            arr_up_s   = font.render("▲", True, WHITE if scroll > 0         else (80,80,90))
            arr_down_s = font.render("▼", True, WHITE if scroll < max_scroll else (80,80,90))
            screen.blit(arr_up_s,   (self.btn_rune_scroll_up.centerx   - arr_up_s.get_width()//2,
                                     self.btn_rune_scroll_up.centery   - arr_up_s.get_height()//2))
            screen.blit(arr_down_s, (self.btn_rune_scroll_down.centerx - arr_down_s.get_width()//2,
                                     self.btn_rune_scroll_down.centery - arr_down_s.get_height()//2))

            # Кількість рядків — індикатор
            if total_rows > VISIBLE_ROWS:
                pg_s = font_small.render(f"Рядок {scroll+1}-{min(scroll+VISIBLE_ROWS, total_rows)}/{total_rows}", True, (150,150,200))
                screen.blit(pg_s, (arrow_x - pg_s.get_width()//2 + 22, ry0 + grid_h + 6))

            # Зона відсікання щоб руни не малювалися поза сіткою
            clip_rect = pygame.Rect(0, ry0 - 4, w, grid_h + 8)
            screen.set_clip(clip_rect)

            self.evo_rune_rects = []
            for fi, rune in enumerate(hero_runes):
                col = fi % R_COLS
                row = fi // R_COLS
                # Зміщення скролу
                vis_row = row - scroll
                rx = rx0 + col*(R_W+R_GAP)
                ry = ry0 + vis_row*(R_H+R_GAP)
                # Зберігаємо rect лише для видимих рядків
                rr = pygame.Rect(rx, ry, R_W, R_H)
                self.evo_rune_rects.append(rr)
                if vis_row < 0 or vis_row >= VISIBLE_ROWS:
                    continue  # не малюємо — поза видимою зоною
                is_target = (self.rune_evo_target_idx == fi)
                is_feed   = (fi in self.rune_evo_feed_idxs)
                if is_target:
                    bg_col = (50, 20, 100); border_col = (255, 215, 0)
                elif is_feed:
                    bg_col = (20, 70, 30); border_col = (80, 220, 80)
                else:
                    bg_col = (25, 20, 45); border_col = (100, 80, 180)
                pygame.draw.rect(screen, bg_col, rr, border_radius=8)
                pygame.draw.rect(screen, border_col, rr, 2 if (is_target or is_feed) else 1, border_radius=8)
                draw_rune_circle(screen, rx+18, ry+R_H//2, 15, rune["level"],
                                 (255, 215, 0) if is_target else (80, 220, 80) if is_feed else (130, 80, 220))
                nm_s = font_small.render(rune["name"][:15], True, WHITE)
                screen.blit(nm_s, (rx+36, ry+8))
                tag = "ЦІЛЬ" if is_target else ("КОРМ" if is_feed else "")
                if tag:
                    tg_s = font_small.render(tag, True, (255,215,0) if is_target else (80,220,80))
                    screen.blit(tg_s, (rx+36, ry+28))
                prog_s = font_small.render(f"+{rune_feed_progress(rune['level'])} прогресу", True, (160,160,220))
                screen.blit(prog_s, (rx+36, ry+46))
                max_s  = font_small.render(f"Lv{rune['level']}/10", True, (180,180,180))
                screen.blit(max_s, (rx+36, ry+64))

            screen.set_clip(None)

            # Кнопка "Скормити"
            can_feed = (self.rune_evo_target_idx is not None and bool(self.rune_evo_feed_idxs))
            self.btn_feed_runes = self.draw_button("ПРОКАЧАТИ (СКОРМИТИ)", w//2-180, h-115, 360, 50, (40, 160, 60) if can_feed else GRAY, active=can_feed)

            if self.rune_msg:
                mc = GREEN if "прокачана" in self.rune_msg else RED if "Потрібно" in self.rune_msg or "макс" in self.rune_msg else WHITE
                ms = font.render(self.rune_msg, True, mc)
                screen.blit(ms, (w//2 - ms.get_width()//2, h-60))

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == DIVINE_EVO:
            # ═══════════════════════════════════════════════════════════════
            # ЕВОЛЮЦІЯ БОЖЕСТВ — вибір карти 100 рівня та тип еволюції
            # ═══════════════════════════════════════════════════════════════
            _pulse_d = abs(math.sin(time.time() * 2.5))
            _tc_d    = (255, int(200 + 55 * _pulse_d), 0)
            title_d  = font_big.render("⚜  ЕВОЛЮЦІЯ БОЖЕСТВ  ⚜", True, _tc_d)
            screen.blit(title_d, (w//2 - title_d.get_width()//2, 8))

            # Список карт 100-рівня (не еволюційованих)
            _div_cards = [c for c in self.all_heroes if c.level >= 100 and not getattr(c, 'evolved', False)]
            _evo_sel   = getattr(self, 'divine_evo_sel', None)

            if not _div_cards:
                _ns = font.render("Немає карт 100 рівня для еволюції!", True, GRAY)
                screen.blit(_ns, (w//2 - _ns.get_width()//2, h//2 - 20))
                _hint = font_small.render("Прокачай карту до 100 рівня у бою", True, (150, 150, 150))
                screen.blit(_hint, (w//2 - _hint.get_width()//2, h//2 + 20))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
                return

            # Сітка карт
            _CW, _CH = 160, 240
            _CGAP = 14
            _COLS = min(5, len(_div_cards))
            _total_cw = _COLS * _CW + (_COLS - 1) * _CGAP
            _cx0 = w // 2 - _total_cw // 2
            self.divine_evo_card_rects = []
            for _ci, _card in enumerate(_div_cards):
                _col = _ci % _COLS
                _row = _ci // _COLS
                _cx  = _cx0 + _col * (_CW + _CGAP)
                _cy  = 58 + _row * (_CH + 12)
                _is_sel = (_evo_sel == _ci)
                if _is_sel:
                    # Золота пульсуюча рамка навколо вибраної
                    _gp = abs(math.sin(time.time() * 4))
                    _gc = (255, int(200 + 55 * _gp), 0)
                    for _t in range(5, 0, -1):
                        pygame.draw.rect(screen, _gc, (_cx - _t, _cy - _t, _CW + _t*2, _CH + _t*2), 1, border_radius=14)
                _card.draw(screen, _cx, _cy, _CW, _CH)
                self.divine_evo_card_rects.append(pygame.Rect(_cx, _cy, _CW, _CH))

            # Кнопки еволюції — з'являються при виборі карти
            if _evo_sel is not None and _evo_sel < len(_div_cards):
                _sel_card = _div_cards[_evo_sel]
                _btn_y    = h - 160
                _btn_w    = 240
                _btn_gap  = 16
                _btns_total = 3 * _btn_w + 2 * _btn_gap
                _btn_x0   = w // 2 - _btns_total // 2

                _has_dia  = self.diamonds >= DIVINE_EVO_COSTS["diamonds"]
                _has_ore  = self.ore      >= DIVINE_EVO_COSTS["ore"]
                _has_gold = self.gold     >= DIVINE_EVO_COSTS["gold"]

                self.btn_evo_dia  = self.draw_button(
                    f"⚜ АЛМАЗИ (-{DIVINE_EVO_COSTS['diamonds']}💎)",
                    _btn_x0, _btn_y, _btn_w, 50,
                    (60, 0, 180) if _has_dia else (50, 50, 60), active=_has_dia)
                self.btn_evo_ore  = self.draw_button(
                    f"⚙ РУДА (-{DIVINE_EVO_COSTS['ore']}⛏)",
                    _btn_x0 + _btn_w + _btn_gap, _btn_y, _btn_w, 50,
                    (100, 60, 0) if _has_ore else (50, 50, 60), active=_has_ore)
                self.btn_evo_gold = self.draw_button(
                    f"✦ ЗОЛОТО (-{DIVINE_EVO_COSTS['gold']}🪙)",
                    _btn_x0 + 2*(_btn_w + _btn_gap), _btn_y, _btn_w, 50,
                    (140, 100, 0) if _has_gold else (50, 50, 60), active=_has_gold)

                # Підказки балансу
                _bal_s = font_small.render(
                    f"💎 {self.diamonds}  |  ⛏ {self.ore}  |  🪙 {self.gold}",
                    True, (200, 200, 200))
                screen.blit(_bal_s, (w//2 - _bal_s.get_width()//2, _btn_y - 24))

                # Ефект блиску вибраної карти
                _sh = abs(math.sin(time.time() * 3.5))
                _shine_c = (255, int(200 + 55 * _sh), 0)
                _sn = font_small.render(f"Обрано: {_sel_card.name}  LVL {_sel_card.level}", True, _shine_c)
                screen.blit(_sn, (w//2 - _sn.get_width()//2, _btn_y - 46))
            else:
                _hint2 = font_small.render("👆 Обери карту 100 рівня для еволюції", True, (200, 200, 100))
                screen.blit(_hint2, (w//2 - _hint2.get_width()//2, h - 90))

            # Повідомлення
            _dmsg = getattr(self, 'divine_msg', '')
            if _dmsg:
                _dc = (80, 255, 80) if "✅" in _dmsg else (255, 80, 80)
                _ds = font.render(_dmsg, True, _dc)
                screen.blit(_ds, (w//2 - _ds.get_width()//2, h - 56))

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == DIVINE_LEVEL_EVO:
            # ═══════════════════════════════════════════════════════════════
            # ЕВОЛЮЦІЯ РІВНЯ БОЖЕСТВ — скормка карт еволюціонованій карті
            # ═══════════════════════════════════════════════════════════════
            _pulse_l = abs(math.sin(time.time() * 2.0))
            _tc_l    = (int(80 + 175 * _pulse_l), 255, int(80 + 100 * _pulse_l))
            title_l  = font_big.render("⬆  ЕВОЛЮЦІЯ РІВНЯ БОЖЕСТВ  ⬆", True, _tc_l)
            screen.blit(title_l, (w//2 - title_l.get_width()//2, 8))

            # Цілі: еволюціоновані Божества + всі Супер Люди (прокачка з 1-го рівня)
            _evo_cards  = [c for c in self.all_heroes if
                           (getattr(c,'evolved',False) and getattr(c,'evo_tier',0)==1 and c.level < DIVINE_MAX_LEVEL)
                           or getattr(c,'_super_human',False)]
            _feed_cards = [c for c in self.all_heroes if
                           not getattr(c,'evolved',False) and not getattr(c,'_super_human',False) and c.level >= 1]

            _dl_target = getattr(self, 'dl_target_idx', None)
            _dl_feeds  = getattr(self, 'dl_feed_idxs', set())

            if not _evo_cards:
                _ns = font.render("Немає Еволюціонованих карт або Супер Людей!", True, GRAY)
                screen.blit(_ns, (w//2 - _ns.get_width()//2, h//2 - 20))
                _ns2 = font_small.render("Розблокуй карти Супер Людей у Ринку або виконай Еволюцію Божеств", True, (150,150,150))
                screen.blit(_ns2, (w//2 - _ns2.get_width()//2, h//2 + 20))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
                return

            # ── Ліва панель: цілі (цілі) ──
            _LEFT_W = w // 2 - 10
            _tgt_label = font_small.render("ЦІЛЬ (Еволюц. або Супер Люди):", True, (255, 215, 0))
            screen.blit(_tgt_label, (10, 48))

            _TCW, _TCH = 150, 225
            _TCGAP = 10
            self.dl_target_rects = []
            for _ti, _tc in enumerate(_evo_cards):
                _tx = 10 + _ti * (_TCW + _TCGAP)
                _ty = 70
                _is_tgt = (_dl_target == _ti)
                if _is_tgt:
                    _pg = abs(math.sin(time.time() * 4))
                    for _gt in range(5, 0, -1):
                        pygame.draw.rect(screen, (255, int(200+55*_pg), 0),
                            (_tx-_gt, _ty-_gt, _TCW+_gt*2, _TCH+_gt*2), 1, border_radius=14)
                _tc.draw(screen, _tx, _ty, _TCW, _TCH)
                # Прогрес бар divine_xp
                _dxr = _tc.divine_xp / 100.0
                pygame.draw.rect(screen, (30,30,30), (_tx, _ty+_TCH+4, _TCW, 10))
                pygame.draw.rect(screen, (200,140,0), (_tx, _ty+_TCH+4, int(_TCW*_dxr), 10))
                _dxp_s = font_small.render(f"EVO: {_tc.divine_xp}/100 → Lv{_tc.level}", True, (255,200,80))
                screen.blit(_dxp_s, (_tx, _ty+_TCH+16))
                self.dl_target_rects.append(pygame.Rect(_tx, _ty, _TCW, _TCH))

            # Роздільник
            pygame.draw.line(screen, (80, 80, 100), (w//2, 44), (w//2, h - 80), 1)

            # ── Права панель: звичайні карти для скормки ──
            _feed_label = font_small.render("ОБЕРИ КАРТИ ДЛЯ СКОРМКИ:", True, (80, 255, 150))
            screen.blit(_feed_label, (w//2 + 8, 48))

            _FCW, _FCH = 130, 195
            _FCGAP = 8
            _FCOLS = max(1, (w // 2 - 20) // (_FCW + _FCGAP))
            _fx0 = w // 2 + 8
            self.dl_feed_rects = []
            for _fi, _fc in enumerate(_feed_cards):
                _fcol = _fi % _FCOLS
                _frow = _fi // _FCOLS
                _fxp  = _fx0 + _fcol * (_FCW + _FCGAP)
                _fyp  = 70 + _frow * (_FCH + _FCGAP)
                if _fyp + _FCH > h - 130:
                    break
                _is_feed = (_fi in _dl_feeds)
                if _is_feed:
                    pygame.draw.rect(screen, (20, 80, 30),
                        (_fxp - 3, _fyp - 3, _FCW + 6, _FCH + 6), border_radius=12)
                    pygame.draw.rect(screen, (80, 220, 80),
                        (_fxp - 3, _fyp - 3, _FCW + 6, _FCH + 6), 3, border_radius=12)
                _fc.draw(screen, _fxp, _fyp, _FCW, _FCH)
                # Значок скормки
                if _is_feed:
                    _feed_s = font_small.render("КОРМ", True, (80, 255, 80))
                    screen.blit(_feed_s, (_fxp + _FCW//2 - _feed_s.get_width()//2, _fyp - 3))
                self.dl_feed_rects.append(pygame.Rect(_fxp, _fyp, _FCW, _FCH))

            # Підрахунок прогресу скормки
            if _dl_target is not None and _dl_feeds:
                _total_feed_xp = sum(
                    _feed_cards[_fi].level for _fi in _dl_feeds if _fi < len(_feed_cards))
                _lvls_gained   = _total_feed_xp // 100
                _hint_f = font_small.render(
                    f"Вибрано карт: {len(_dl_feeds)}  |  Рівнів скормки: {_total_feed_xp}  →  +{_lvls_gained} EVO XP",
                    True, (200, 255, 180))
                screen.blit(_hint_f, (w//2 - _hint_f.get_width()//2, h - 125))

            # Кнопка скормити
            _can_dl = (_dl_target is not None and bool(_dl_feeds))
            self.btn_dl_feed = self.draw_button(
                "⬆ СКОРМИТИ КАРТКИ", w//2 - 180, h - 100, 360, 48,
                (40, 160, 60) if _can_dl else GRAY, active=_can_dl)

            # Повідомлення
            _lmsg = getattr(self, 'divine_lvl_msg', '')
            if _lmsg:
                _lc = (80, 255, 80) if "рівень" in _lmsg.lower() or "✅" in _lmsg else (255, 150, 50)
                _ls = font.render(_lmsg, True, _lc)
                screen.blit(_ls, (w//2 - _ls.get_width()//2, h - 50))

            self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == SUPREME_EVO:
            # ═══════════════════════════════════════════════════════════════
            # ЕВОЛЮЦІЯ ВЕРХОВНИХ БОГІВ — 110 рівень → EvII (оплата 50 грн)
            # ═══════════════════════════════════════════════════════════════
            _ps = abs(math.sin(time.time()*2.5))
            _tc_s = (255, int(180+75*_ps), int(20+20*_ps))
            _ts_s = font_big.render("👑  ЕВОЛЮЦІЯ ВЕРХОВНИХ БОГІВ  👑", True, _tc_s)
            screen.blit(_ts_s, (w//2-_ts_s.get_width()//2, 8))

            _sup_cards = [c for c in self.all_heroes
                          if c.level >= DIVINE_MAX_LEVEL and getattr(c,'evo_tier',0) < 2]
            _sup_sel   = getattr(self,'supreme_evo_sel',None)

            if not _sup_cards:
                _ns = font.render("Немає карт 110 рівня для Верховної Еволюції!", True, GRAY)
                screen.blit(_ns, (w//2-_ns.get_width()//2, h//2-20))
                _h2 = font_small.render("Досягни 110 рівня у вкладці Еволюція Рівня Божеств", True, (150,150,150))
                screen.blit(_h2, (w//2-_h2.get_width()//2, h//2+20))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
            else:
                _CW,_CH = 160,240; _CGAP=14; _COLS=min(5,len(_sup_cards))
                _total_cw=_COLS*_CW+(_COLS-1)*_CGAP; _cx0=w//2-_total_cw//2
                self.supreme_evo_card_rects=[]
                for _ci,_card in enumerate(_sup_cards):
                    _col=_ci%_COLS; _row=_ci//_COLS
                    _cx=_cx0+_col*(_CW+_CGAP); _cy=58+_row*(_CH+12)
                    _is_sel=(_sup_sel==_ci)
                    if _is_sel:
                        _gp=abs(math.sin(time.time()*4))
                        for _t in range(5,0,-1):
                            pygame.draw.rect(screen,(255,int(180+75*_gp),0),(_cx-_t,_cy-_t,_CW+_t*2,_CH+_t*2),1,border_radius=14)
                    _card.draw(screen,_cx,_cy,_CW,_CH)
                    self.supreme_evo_card_rects.append(pygame.Rect(_cx,_cy,_CW,_CH))

                if _sup_sel is not None and _sup_sel < len(_sup_cards):
                    _sel_c = _sup_cards[_sup_sel]
                    _pay_y = h - 180

                    _pi1 = font.render(
                        f"Обрано: {_sel_c.name}  LVL {_sel_c.level}  →  EvII (111-120 рівень)",
                        True, (255, 215, 0))
                    screen.blit(_pi1, (w//2-_pi1.get_width()//2, _pay_y))

                    _pi2 = font_small.render(
                        f"Сума: {SUPREME_PRICE_UAH} грн · Оплата через Monobank Еквайринг",
                        True, (200, 200, 200))
                    screen.blit(_pi2, (w//2-_pi2.get_width()//2, _pay_y+24))

                    # Показуємо ref та invoice_id якщо є
                    _inv_id  = getattr(self,'_pending_supreme_invoice','')
                    _pay_ref = getattr(self,'_supreme_pay_ref','')
                    if _pay_ref:
                        _pr_s = font_small.render(f"Ref: {_pay_ref}", True, (160,200,255))
                        screen.blit(_pr_s, (w//2-_pr_s.get_width()//2, _pay_y+44))
                    if _inv_id:
                        _inv_s = font_small.render(f"Invoice: {_inv_id[:28]}…", True, (120,160,200))
                        screen.blit(_inv_s, (w//2-_inv_s.get_width()//2, _pay_y+62))

                    _has_token   = bool(MONOBANK_MERCHANT_TOKEN.strip())
                    _has_invoice = bool(_inv_id)
                    _has_card    = bool(getattr(self,'_pending_supreme_card',None))

                    # Кнопка ОПЛАТИТИ
                    _pay_lbl = f"💳 ЕВОЛЮЦІЯ ЗА ГРОШІ ({SUPREME_PRICE_UAH} грн)"
                    _pay_col = (160, 90, 0) if _has_token else (60, 60, 60)
                    self.btn_supreme_pay = self.draw_button(
                        _pay_lbl, w//2-210, h-118, 420, 50, _pay_col, active=_has_token)

                    # Кнопка ПЕРЕВІРИТИ — активна лише коли є invoice
                    _chk_col = (50, 140, 50) if _has_invoice else (40, 60, 40)
                    self.btn_supreme_check = self.draw_button(
                        "🔍 ПЕРЕВІРИТИ ОПЛАТУ",
                        w//2-210, h-60, 420, 46, _chk_col, active=_has_invoice)

                    if not _has_token:
                        _nt = font_small.render(
                            "⚠ Встав MONOBANK_MERCHANT_TOKEN у код!", True, (255, 80, 80))
                        screen.blit(_nt, (w//2-_nt.get_width()//2, h-12))
                else:
                    _hint_s=font_small.render("👆 Обери карту 110 рівня для Верховної Еволюції",True,(200,200,100))
                    screen.blit(_hint_s,(w//2-_hint_s.get_width()//2,h-90))

                _smsg = getattr(self,'supreme_msg','')
                if _smsg:
                    _smsg_lines = _smsg.split("\n")
                    _smsg_y0 = h - 55 - (len(_smsg_lines)-1)*18
                    for _sli, _sline in enumerate(_smsg_lines):
                        _sc = (80,255,80) if "✅" in _sline else (255,200,60) if "⏳" in _sline else (255,80,80)
                        _ss2 = font_small.render(_sline, True, _sc)
                        screen.blit(_ss2,(w//2-_ss2.get_width()//2, _smsg_y0+_sli*18))

                self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)

        elif self.state == SUPREME_LEVEL_EVO:
            # ═══════════════════════════════════════════════════════════════
            # ЕВОЛЮЦІЯ РІВНЯ ВЕРХОВНИХ БОГІВ — скормка карт EvII (111-120)
            # ═══════════════════════════════════════════════════════════════
            _ps2=abs(math.sin(time.time()*2.0))
            _tc_sl=(255,int(200+55*_ps2),0)
            _ts_sl=font_big.render("🌟  РІВЕНЬ ВЕРХОВНИХ БОГІВ  🌟",True,_tc_sl)
            screen.blit(_ts_sl,(w//2-_ts_sl.get_width()//2,8))

            _sup_evo_cards=[c for c in self.all_heroes if getattr(c,'evo_tier',0)==2 and c.level<SUPREME_MAX_LEVEL]
            _feed_cards_s =[c for c in self.all_heroes if getattr(c,'evo_tier',0)<2]
            _sl_target=getattr(self,'sl_target_idx',None)
            _sl_feeds =getattr(self,'sl_feed_idxs',set())

            if not _sup_evo_cards:
                _ns2=font.render("Немає карт EvII! Виконай Верховну Еволюцію.",True,GRAY)
                screen.blit(_ns2,(w//2-_ns2.get_width()//2,h//2-20))
                self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)
            else:
                # Ліва панель — EvII карти
                _tgt_lbl=font_small.render("ЦІЛЬ (EvII карта):",True,(255,215,0))
                screen.blit(_tgt_lbl,(10,48))
                _TCW,_TCH,_TCGAP=150,225,10
                self.sl_target_rects=[]
                for _ti,_tc in enumerate(_sup_evo_cards):
                    _tx=10+_ti*(_TCW+_TCGAP); _ty=70
                    _is_tgt=(_sl_target==_ti)
                    if _is_tgt:
                        _pg2=abs(math.sin(time.time()*4))
                        for _gt in range(5,0,-1):
                            pygame.draw.rect(screen,(255,int(180+75*_pg2),0),(_tx-_gt,_ty-_gt,_TCW+_gt*2,_TCH+_gt*2),1,border_radius=14)
                    _tc.draw(screen,_tx,_ty,_TCW,_TCH)
                    _dxr=_tc.divine_xp/100.0
                    pygame.draw.rect(screen,(30,30,30),(_tx,_ty+_TCH+4,_TCW,10))
                    pygame.draw.rect(screen,(200,140,0),(_tx,_ty+_TCH+4,int(_TCW*_dxr),10))
                    _dxp_s2=font_small.render(f"EVO: {_tc.divine_xp}/100 → Lv{_tc.level}",True,(255,200,80))
                    screen.blit(_dxp_s2,(_tx,_ty+_TCH+16))
                    self.sl_target_rects.append(pygame.Rect(_tx,_ty,_TCW,_TCH))

                pygame.draw.line(screen,(80,80,100),(w//2,44),(w//2,h-80),1)

                # Права панель — кормові карти
                _feed_lbl2=font_small.render("ОБЕРИ КАРТИ ДЛЯ СКОРМКИ:",True,(255,200,80))
                screen.blit(_feed_lbl2,(w//2+8,48))
                _FCW2,_FCH2,_FCGAP2=130,195,8
                _FCOLS2=max(1,(w//2-20)//(_FCW2+_FCGAP2))
                _fx02=w//2+8
                self.sl_feed_rects=[]
                for _fi,_fc in enumerate(_feed_cards_s):
                    _fcol2=_fi%_FCOLS2; _frow2=_fi//_FCOLS2
                    _fxp2=_fx02+_fcol2*(_FCW2+_FCGAP2); _fyp2=70+_frow2*(_FCH2+_FCGAP2)
                    if _fyp2+_FCH2>h-130: break
                    _is_feed2=(_fi in _sl_feeds)
                    if _is_feed2:
                        pygame.draw.rect(screen,(20,60,20),(_fxp2-3,_fyp2-3,_FCW2+6,_FCH2+6),border_radius=12)
                        pygame.draw.rect(screen,(80,220,80),(_fxp2-3,_fyp2-3,_FCW2+6,_FCH2+6),3,border_radius=12)
                    _fc.draw(screen,_fxp2,_fyp2,_FCW2,_FCH2)
                    if _is_feed2:
                        _f2s=font_small.render("КОРМ",True,(80,255,80))
                        screen.blit(_f2s,(_fxp2+_FCW2//2-_f2s.get_width()//2,_fyp2-3))
                    self.sl_feed_rects.append(pygame.Rect(_fxp2,_fyp2,_FCW2,_FCH2))

                if _sl_target is not None and _sl_feeds:
                    _total_f2=sum(_feed_cards_s[_fi].level for _fi in _sl_feeds if _fi<len(_feed_cards_s))
                    _lg2=_total_f2//100
                    _hf2=font_small.render(f"Вибрано: {len(_sl_feeds)} | XP: {_total_f2} → +{_lg2} рівнів",True,(200,255,180))
                    screen.blit(_hf2,(w//2-_hf2.get_width()//2,h-125))

                _can_sl=(_sl_target is not None and bool(_sl_feeds))
                self.btn_sl_feed=self.draw_button("🌟 СКОРМИТИ",w//2-180,h-100,360,48,(100,60,0) if _can_sl else GRAY,active=_can_sl)

                _slmsg=getattr(self,'supreme_lvl_msg','')
                if _slmsg:
                    _slc=(80,255,80) if "✅" in _slmsg or "рівень" in _slmsg.lower() else (255,150,50)
                    _sls=font.render(_slmsg,True,_slc)
                    screen.blit(_sls,(w//2-_sls.get_width()//2,h-50))

                self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)

        elif self.state == ALTAR_ESSENCE:
            # ═══════════════════════════════════════════════════
            # АЛТАРЬ ЕСЕНЦІЯ — витягування есенцій
            # ═══════════════════════════════════════════════════
            _pe = abs(math.sin(time.time()*2.0))
            _tc = (int(140+115*_pe), int(80+80*_pe), 255)
            _ts = font_big.render("💎  АЛТАРЬ ЕСЕНЦІЯ  💎", True, _tc)
            screen.blit(_ts, (w//2-_ts.get_width()//2, 8))

            _desc = font_small.render("Витягуй есенції для прокачки здібностей карт!", True, (200,180,255))
            screen.blit(_desc, (w//2-_desc.get_width()//2, 55))

            # Картинка алтаря есенцій
            _img_ae = load_tab_image("altar_essence", min(300, w//3), 180)
            if _img_ae:
                screen.blit(_img_ae, (w//2-150, 78))
            else:
                _ar = pygame.Rect(w//2-120, 78, 240, 170)
                pygame.draw.rect(screen, (25,10,50), _ar, border_radius=14)
                pygame.draw.rect(screen, _tc, _ar, 3, border_radius=14)
                _gem = font_big.render("💎", True, _tc)
                screen.blit(_gem, (w//2-_gem.get_width()//2, 128))
                _aura = font_small.render("✦ АЛТАРЬ ЕСЕНЦІЙ ✦", True, (200,160,255))
                screen.blit(_aura, (w//2-_aura.get_width()//2, 196))

            # Таймер
            _now_ae = time.time()
            _ela = _now_ae - getattr(self,'essence_last_draw',0)
            _rem = max(0, ESSENCE_COOLDOWN - _ela)
            if _rem > 0:
                _hh,_mm,_ss = int(_rem//3600), int((_rem%3600)//60), int(_rem%60)
                _timer_s = font.render(f"⏱ Наступне витягання: {_hh:02d}:{_mm:02d}:{_ss:02d}", True, (200,180,255))
                screen.blit(_timer_s, (w//2-_timer_s.get_width()//2, 260))
                _can_draw = False
            else:
                _free_s = font.render("✅ Можна витягнути БЕЗКОШТОВНО!", True, (80,255,150))
                screen.blit(_free_s, (w//2-_free_s.get_width()//2, 260))
                _can_draw = True

            # Баланс ключів
            _ks = font_small.render(f"🗝️ Ключів: {self.keys}", True, (255,215,0))
            screen.blit(_ks, (w//2-_ks.get_width()//2, 290))

            # Кнопки
            _bw, _bh = 280, 52
            _by = 320
            self.btn_ess_free = self.draw_button(
                "✨ ВИТЯГНУТИ ЕСЕНЦІЮ (Безкоштовно)",
                w//2-_bw//2, _by, _bw, _bh,
                (60,120,200) if _can_draw else (40,40,60), active=_can_draw)
            self.btn_ess_key = self.draw_button(
                f"🗝️ ВИТЯГНУТИ ЗА КЛЮЧ (1 🗝️)",
                w//2-_bw//2, _by+_bh+10, _bw, _bh,
                (120,80,0) if self.keys>0 else (40,40,60), active=(self.keys>0))
            self.btn_ess_3key = self.draw_button(
                f"🗝️×3 ВИТЯГНУТИ 5 ЕСЕНЦІЙ (3 🗝️)",
                w//2-_bw//2, _by+(_bh+10)*2, _bw, _bh,
                (140,60,0) if self.keys>=3 else (40,40,60), active=(self.keys>=3))

            # Кількість есенцій
            _ec2 = font_small.render(f"Є у вас: {len(getattr(self,'my_essences',[]))} есенцій", True, (180,160,220))
            screen.blit(_ec2, (w//2-_ec2.get_width()//2, _by+(_bh+10)*3+10))

            # Повідомлення
            _ae_msg = getattr(self,'altar_ess_msg','')
            if _ae_msg:
                _mc = (80,255,80) if "✅" in _ae_msg else (255,100,100)
                _ms = font.render(_ae_msg, True, _mc)
                screen.blit(_ms, (w//2-_ms.get_width()//2, h-55))

            self.btn_my_ess = self.draw_button("📦 МОЇ ЕСЕНЦІЇ", w//2-140, h-110, 280, 44, (60,40,120))
            self.btn_back   = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == MY_ESSENCES:
            # ═══════════════════════════════════════════════════
            # МОЇ ЕСЕНЦІЇ — сітка 4×3 з есенціями
            # ═══════════════════════════════════════════════════
            _ts2 = font_big.render("📦  МОЇ ЕСЕНЦІЇ", True, (180,140,255))
            screen.blit(_ts2, (w//2-_ts2.get_width()//2, 8))

            _ess_list = getattr(self,'my_essences',[])
            if not _ess_list:
                _ne = font.render("Есенцій немає — витягни у Алтарь Есенція!", True, GRAY)
                screen.blit(_ne, (w//2-_ne.get_width()//2, h//2-20))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)
            else:
                # Сітка 4 × 3 = 12 на сторінці
                _COLS, _ROWS = 4, 3
                _EW, _EH, _EGAP = (w-60)//_COLS - 10, 130, 10
                _EX0 = 20
                _EY0 = 55
                _page = getattr(self,'ess_page',0)
                _per_page = _COLS * _ROWS
                _total = len(_ess_list)
                _pages = max(1, (_total + _per_page - 1) // _per_page)
                _page = max(0, min(_page, _pages-1))
                self.ess_page = _page
                self.ess_rects = []
                _sel_ess = getattr(self,'sel_ess_idx',None)
                for _ei in range(_per_page):
                    _real_i = _page * _per_page + _ei
                    if _real_i >= _total: break
                    _ess = _ess_list[_real_i]
                    _col = _ei % _COLS; _row = _ei // _COLS
                    _ex = _EX0 + _col * (_EW + _EGAP)
                    _ey = _EY0 + _row * (_EH + _EGAP)
                    _ec = _ess.get("col",(180,80,255))
                    _is_sel = (_sel_ess == _real_i)
                    # Рамка
                    if _is_sel:
                        _gp = abs(math.sin(time.time()*4))
                        for _gt in range(4,0,-1):
                            pygame.draw.rect(screen, (int(_ec[0]),int(_ec[1]),min(255,int(_ec[2]+100))),
                                (_ex-_gt,_ey-_gt,_EW+_gt*2,_EH+_gt*2),1,border_radius=10)
                    pygame.draw.rect(screen,(20,15,35),(_ex,_ey,_EW,_EH),border_radius=10)
                    pygame.draw.rect(screen,_ec,(_ex,_ey,_EW,_EH),2,border_radius=10)
                    # Картинка-заготовка (квадрат)
                    _iw,_ih = int(_EW*0.38), int(_EH*0.55)
                    _img_box = pygame.Rect(_ex+(_EW-_iw)//2, _ey+5, _iw, _ih)
                    pygame.draw.rect(screen,(30,20,50),_img_box,border_radius=8)
                    pygame.draw.rect(screen,_ec,_img_box,1,border_radius=8)
                    # Спробуємо завантажити картинку есенції
                    _ess_img_path = os.path.join("images","essences",f"{_ess['name']}.png")
                    if os.path.exists(_ess_img_path):
                        try:
                            _ei_img = pygame.image.load(_ess_img_path).convert_alpha()
                            _ei_img = pygame.transform.smoothscale(_ei_img,(_iw-4,_ih-4))
                            screen.blit(_ei_img,(_ex+(_EW-_iw)//2+2,_ey+7))
                        except: pass
                    else:
                        _sym = "💎"
                        _ss = font_bold.render(_sym,True,_ec)
                        screen.blit(_ss,(_ex+(_EW-_ss.get_width())//2,_ey+_ih//2-_ss.get_height()//2+5))
                    # Рівень
                    _lv_s = font_bold.render(f"Lv{_ess['level']}",True,_ec)
                    screen.blit(_lv_s,(_ex+_EW//2-_lv_s.get_width()//2,_ey+_ih+8))
                    # Назва
                    _nm_s = font_small.render(_ess['name'][:18],True,(220,200,255))
                    screen.blit(_nm_s,(_ex+_EW//2-_nm_s.get_width()//2,_ey+_ih+26))
                    self.ess_rects.append(pygame.Rect(_ex,_ey,_EW,_EH))

                # Пагінація
                _py = h - 115
                if _page > 0:
                    self.btn_ess_prev = self.draw_button("◀", w//2-130, _py, 55, 38, (60,60,80))
                else:
                    self.btn_ess_prev = pygame.Rect(0,0,0,0)
                _pg_s = font_small.render(f"{_page+1}/{_pages}",True,WHITE)
                screen.blit(_pg_s,(w//2-_pg_s.get_width()//2,_py+8))
                if _page < _pages-1:
                    self.btn_ess_next = self.draw_button("▶", w//2+75, _py, 55, 38, (60,60,80))
                else:
                    self.btn_ess_next = pygame.Rect(0,0,0,0)

                # Інфо про вибрану есенцію
                if _sel_ess is not None and _sel_ess < _total:
                    _se = _ess_list[_sel_ess]
                    _se_info = font_small.render(f"Обрано: {_se['name']} Lv{_se['level']} → здібність: {ALL_ABILITIES[ESSENCE_NAMES.index(_se['name']) if _se['name'] in ESSENCE_NAMES else 0][1] if _se['name'] in ESSENCE_NAMES else '?'}", True, (255,215,0))
                    screen.blit(_se_info,(w//2-_se_info.get_width()//2,h-75))

                _ec_tot = font_small.render(f"Всього есенцій: {_total}",True,GRAY)
                screen.blit(_ec_tot,(w//2-_ec_tot.get_width()//2,h-50))
                self.btn_back = self.draw_button("НАЗАД", 20, h-70, 150, 50, GRAY)

        elif self.state == ABILITY_EVO:
            _ts3 = font_big.render("⭐  ПРОКАЧКА ЗДІБНОСТЕЙ  ⭐", True, (255,215,0))
            screen.blit(_ts3,(w//2-_ts3.get_width()//2,8))

            HALF = w // 2
            _army_sel  = getattr(self,'abil_card_idx',None)
            _abil_sel  = getattr(self,'abil_sel_idx',None)
            _army_cards = self.all_heroes

            # ── Ліва панель: список карт (50% ширини) з прокруткою ──
            _cl = font_small.render("ОБЕРІТЬ КАРТУ:", True, (200,200,100))
            screen.blit(_cl,(8,50))
            _CW2,_CH2,_CGAP2 = 130, 195, 8
            _CCOLS = max(1,(HALF-12)//(_CW2+_CGAP2))
            _sy_ab = getattr(self,'scroll_offset',{}).get('abil_card',0)
            _rows_ab = max(1,(len(_army_cards)+_CCOLS-1)//_CCOLS)
            _content_h_ab = 55 + _rows_ab*(_CH2+_CGAP2)
            _visible_h_ab = h - 140
            self.abil_card_rects = []
            for _ci,_card in enumerate(_army_cards):
                _ccol = _ci % _CCOLS
                _crow = _ci // _CCOLS
                _cx_ab = 8 + _ccol*(_CW2+_CGAP2)
                _cy_ab = 55 + _crow*(_CH2+_CGAP2) - _sy_ab
                if _cy_ab+_CH2 < 50 or _cy_ab > h-80:
                    self.abil_card_rects.append(None); continue
                _is_sel_ab = (_army_sel == _ci)
                if _is_sel_ab:
                    _gp3 = abs(math.sin(time.time()*4))
                    for _gt3 in range(4,0,-1):
                        pygame.draw.rect(screen,(255,215,0),(_cx_ab-_gt3,_cy_ab-_gt3,_CW2+_gt3*2,_CH2+_gt3*2),1,border_radius=12)
                _card.draw(screen,_cx_ab,_cy_ab,_CW2,_CH2)
                self.abil_card_rects.append(pygame.Rect(_cx_ab,_cy_ab,_CW2,_CH2))
            # Стрілки прокрутки карт
            _up_r_ab,_dn_r_ab = self.draw_scroll_arrows("abil_card",_content_h_ab,_visible_h_ab,_sy_ab,HALF-25,55,h-140)
            self.scroll_arrows_rects["abil_card"] = (_up_r_ab,_dn_r_ab)

            # Роздільник
            pygame.draw.line(screen,(80,80,100),(HALF,44),(HALF,h-80),1)

            # ── Права панель: здібності вибраної карти ──
            _RX = HALF+10
            if _army_sel is not None and _army_sel < len(_army_cards):
                _sel_card = _army_cards[_army_sel]
                _abils = get_abilities_for_card(_sel_card)
                _ab_hdr = font_small.render(f"ЗДІБНОСТІ: {_sel_card.name}", True, (255,215,0))
                screen.blit(_ab_hdr,(_RX,50))

                _abil_levels = getattr(_sel_card,'ability_levels',{})
                self.abil_ability_rects = []
                for _ai, _ab in enumerate(_abils):
                    _em,_nm,_desc,_tp = _ab
                    _ay = 78 + _ai * 72
                    if _ay > h - 150: break
                    _is_ab_sel = (_abil_sel == _ai)
                    _ab_rect = pygame.Rect(_RX,_ay,w-_RX-10,64)
                    _ab_bg = (30,25,50) if not _is_ab_sel else (50,40,80)
                    pygame.draw.rect(screen,_ab_bg,_ab_rect,border_radius=10)
                    _ab_col = (255,215,0) if _is_ab_sel else ESSENCE_COLORS.get(_tp,(200,200,200))
                    pygame.draw.rect(screen,_ab_col,_ab_rect,2,border_radius=10)
                    _ab_lv = _abil_levels.get(_nm,1)
                    _ab_xp = _abil_levels.get(_nm+"_xp",0)
                    # Рівень 1-20
                    _abn_s = font_bold.render(f"{_em} {_nm}",True,_ab_col)
                    screen.blit(_abn_s,(_RX+8,_ay+6))
                    _ablv_s = font_small.render(f"Рівень: {_ab_lv}/20",True,WHITE)
                    screen.blit(_ablv_s,(_RX+8,_ay+26))
                    _bar_w = int((w-_RX-100)*0.65)
                    pygame.draw.rect(screen,(40,40,40),(_RX+8,_ay+44,_bar_w,8))
                    _xp_ratio = min(1.0,_ab_xp/ESSENCE_XP_PER_CARD)
                    pygame.draw.rect(screen,_ab_col,(_RX+8,_ay+44,int(_bar_w*_xp_ratio),8))
                    _xp_s = font_small.render(f"{_ab_xp}/{ESSENCE_XP_PER_CARD}",True,GRAY)
                    screen.blit(_xp_s,(_RX+8+_bar_w+4,_ay+40))
                    _ess_nm = get_essence_for_ability(_nm)
                    _ess_s = font_small.render(f"→ {_ess_nm[:22]}",True,(180,160,220))
                    screen.blit(_ess_s,(_RX+int((_ab_rect.width)*0.45)+8,_ay+6))
                    self.abil_ability_rects.append(_ab_rect)

                if _abil_sel is not None and _abil_sel < len(_abils):
                    _sel_ab = _abils[_abil_sel]
                    _need_ess = get_essence_for_ability(_sel_ab[1])
                    _matching = [(i,e) for i,e in enumerate(getattr(self,'my_essences',[]))
                                 if e['name']==_need_ess]
                    _feed_y = 78 + len(_abils)*72 + 8
                    if _feed_y < h-120:
                        _fhdr=font_small.render(f"'{_need_ess}': {len(_matching)} шт.",True,(255,215,0))
                        screen.blit(_fhdr,(_RX,_feed_y))
                        _cur_lv3=_abil_levels.get(_sel_ab[1],1)
                        _is_max=(_cur_lv3>=20)
                        self.btn_abil_feed=self.draw_button(
                            "MAX (20)" if _is_max else f"⭐ СКОРМИТИ ({_need_ess[:18]})",
                            _RX,_feed_y+22,w-_RX-10,42,
                            (60,60,40) if _is_max else (60,120,60) if _matching else (50,50,60),
                            active=bool(_matching) and not _is_max)
                _ab_msg=getattr(self,'abil_msg','')
                if _ab_msg:
                    _amc=(80,255,80) if "⭐" in _ab_msg or "+" in _ab_msg else (255,100,100)
                    _ams=font.render(_ab_msg,True,_amc)
                    screen.blit(_ams,(w//2-_ams.get_width()//2,h-45))
            else:
                _hint_ab2=font_small.render("👆 Обери карту ліворуч",True,(200,200,100))
                screen.blit(_hint_ab2,(_RX+10,h//2))

            self.btn_back=self.draw_button("НАЗАД",20,h-70,150,50,GRAY)

        elif self.state == GAME_OVER:
            txt = font_big.render(self.winner_text, True, GOLD)
            rew = font.render(self.reward_text, True, GREEN)
            screen.blit(txt, (w//2 - txt.get_width()//2, h//2 - 80))
            screen.blit(rew, (w//2 - rew.get_width()//2, h//2 - 20))
            # Бонусна карта (локація 10) — вище ПЕРЕМОГА!
            _vbc = getattr(self, "victory_bonus_card", None)
            if _vbc and self.winner_text == "ПЕРЕМОГА!":
                _vbc_label = font.render("🎁 Бонусна карта за проходження локації 10!", True, GOLD)
                screen.blit(_vbc_label, (w//2 - _vbc_label.get_width()//2, h//2 - 310))
                _vbc_sub = font_small.render("Натисни \"В МЕНЮ\" щоб додати до Моєї Армії", True, (200, 200, 100))
                screen.blit(_vbc_sub, (w//2 - _vbc_sub.get_width()//2, h//2 - 285))
                # Пульсуюча золота рамка навколо карти
                _vbc_x, _vbc_y, _vbc_w, _vbc_h = w//2 - 75, h//2 - 278, 150, 220
                _pulse_v = abs(math.sin(time.time() * 3.0))
                _glow_t = 2 + int(4 * _pulse_v)
                _glow_c = (255, int(180 + 75 * _pulse_v), 0)
                for _t in range(_glow_t, 0, -1):
                    pygame.draw.rect(screen, _glow_c,
                        (_vbc_x - _t, _vbc_y - _t, _vbc_w + _t*2, _vbc_h + _t*2),
                        1, border_radius=12)
                _vbc.draw(screen, _vbc_x, _vbc_y, _vbc_w, _vbc_h)
            self.btn_back = self.draw_button("В МЕНЮ", w//2 - 150, h//2 + 50, 300, 60, BLUE)


        elif self.state == LB_LOBBY_HOST:
            # ═══ ЛОБІ — ВИ ХОСТ ═══════════════════════════════════════
            pygame.draw.rect(screen, (8,18,8), (0,0,w,h))
            _t = font_big.render("🏠  ЛОБІ — ВИ ХОСТ", True, (80,220,80))
            screen.blit(_t, (w//2 - _t.get_width()//2, 18))
            _p2 = abs(math.sin(time.time()*2))
            pygame.draw.rect(screen,(int(20+30*_p2),int(60+60*_p2),int(20+30*_p2)),
                (w//2-320,14,640,50),2,border_radius=10)
            # Колода гравця
            _my_hp  = sum(c.max_hp  for c in self.army) if self.army else 0
            _my_atk = sum(c.attack  for c in self.army) if self.army else 0
            _my_max = max((c.attack for c in self.army), default=0)
            _cw2, _ch2, _gap2 = 100, 148, 12
            _mc_total = min(len(self.army), 6)
            _mc_start = w//2 - (_mc_total*(_cw2+_gap2))//2
            for _mi, _mc in enumerate(self.army[:6]):
                _mc.draw(screen, _mc_start + _mi*(_cw2+_gap2), 75, _cw2, _ch2)
            _dinfo = font_small.render(
                f"Ваша колода: {len(self.army)} карт  |  HP: {_my_hp}  |  ATK: {_my_atk}  |  Макс: {_my_max}",
                True, (150,200,150))
            screen.blit(_dinfo, (w//2-_dinfo.get_width()//2, 232))
            # ── Статус залежно від режиму ──
            _elapsed_h = time.time() - self.lb_lobby_start
            if self.lb_tab == "lan":
                _ws  = self.lb_lan_status or "Запуск хосту..."
                _wc  = (80,220,80) if self.lb_lan_connected else (220,180,60)
                _can_ready = self.lb_lan_connected  # кнопка активна коли клієнт підключився
            else:
                _dots = "." * (int(_elapsed_h*2) % 4)
                _can_ready = (_elapsed_h >= self.lb_lobby_countdown)
                _ws = "✅ Суперник знайдений! Натисни ГОТОВО ДО БОЮ" if _can_ready else                       f"🔍 Шукаємо суперника{_dots}  ({max(0,self.lb_lobby_countdown-_elapsed_h):.0f}с)"
                _wc = (80,220,80) if _can_ready else (220,180,60)
                if _can_ready and (not hasattr(self,"_lb_host_fake_room") or self._lb_host_fake_room is None):
                    _loc_h2 = random.randint(1,10)
                    _lmn2, _lmx2 = LOCATION_LEVELS.get(_loc_h2,(1,5))
                    self._lb_host_fake_room = {
                        "deck": [Card(get_monster_name(_loc_h2,i), random.randint(_lmn2,_lmx2), True) for i in range(random.randint(4,8))],
                        "name": "Суперник"
                    }
            # Анімація очікування
            if not _can_ready:
                for _ri3 in range(3):
                    _ra = abs(math.sin(time.time()*2+_ri3))
                    pygame.draw.circle(screen,(int(30+80*_ra),int(100+80*_ra),int(30+80*_ra)),(w//2,300),10+_ri3*14,2)
            _ws2 = font.render(_ws, True, _wc)
            screen.blit(_ws2, (w//2-_ws2.get_width()//2, 258))
            # VS + статус готовності для LAN
            if _can_ready:
                _vs_s = font_big.render("VS", True, (255,60,60))
                screen.blit(_vs_s, (w//2-_vs_s.get_width()//2, 285))
                if self.lb_tab == "lan":
                    # Показуємо стан кожного гравця
                    _s1_col = (80,220,80) if self.lb_lan_self_ready     else (180,180,60)
                    _s2_col = (80,220,80) if self.lb_lan_opponent_ready else (180,60,60)
                    _s1 = font.render("✅ ВИ: ГОТОВО"         if self.lb_lan_self_ready     else "⏳ ВИ: Не готові",       True, _s1_col)
                    _s2 = font.render("✅ КЛІЄНТ: ГОТОВО"     if self.lb_lan_opponent_ready else "⏳ КЛІЄНТ: Чекаємо...",  True, _s2_col)
                    screen.blit(_s1, (w//2-320, 328))
                    screen.blit(_s2, (w//2+20,  328))
                    if self.lb_lan_self_ready and self.lb_lan_opponent_ready:
                        _go = font_big.render("⚔ ОБИДВА ГОТОВІ — НАТИСНИ ЩЕ РАЗ ГОТОВО!", True, (255,220,40))
                        screen.blit(_go, (w//2-_go.get_width()//2, 365))
                else:
                    _opp_lb = font.render("🤖 Суперник знайдений!", True, (255,200,80))
                    screen.blit(_opp_lb, (w//2-_opp_lb.get_width()//2, 328))
            # Кнопки
            _rc2 = (30,160,30) if _can_ready else (50,50,50)
            _ready_lbl = "✅ ГОТОВО ДО БОЮ" if not (self.lb_tab=="lan" and self.lb_lan_self_ready and not self.lb_lan_opponent_ready) else "✅ ЧЕКАЄМО КЛІЄНТА..."
            self.lb_btn_ready  = self.draw_button(_ready_lbl, w//2-170, h-140, 340, 55, _rc2, active=_can_ready)
            self.lb_btn_cancel = self.draw_button("❌ Скасувати", w//2-80, h-75, 160, 42, (100,30,30))
            self.draw_wallet()

        elif self.state == LB_LOBBY_JOIN:
            # ═══ ЛОБІ — ПРИЄДНАННЯ ════════════════════════════════════
            pygame.draw.rect(screen, (8,8,22), (0,0,w,h))
            _t2 = font_big.render("🔌  ЛОБІ — ПРИЄДНАННЯ", True, (100,180,255))
            screen.blit(_t2, (w//2-_t2.get_width()//2, 18))
            _p3 = abs(math.sin(time.time()*2.5))
            pygame.draw.rect(screen,(int(20+30*_p3),int(40+40*_p3),int(60+80*_p3)),
                (w//2-320,14,640,50),2,border_radius=10)
            # Ваша колода (мініатюри)
            _cw3, _ch3, _gap3 = 100, 148, 12
            _mc3_total = min(len(self.army), 6)
            _mc3_start = w//2 - (_mc3_total*(_cw3+_gap3))//2
            for _mi3, _mc3 in enumerate(self.army[:6]):
                _mc3.draw(screen, _mc3_start + _mi3*(_cw3+_gap3), 70, _cw3, _ch3)
            _my3_hp  = sum(c.max_hp for c in self.army) if self.army else 0
            _my3_atk = sum(c.attack for c in self.army) if self.army else 0
            _yd = font_small.render(
                f"Ваша армія: {len(self.army)} карт  |  HP {_my3_hp}  |  ATK {_my3_atk}",
                True, GOLD)
            screen.blit(_yd, (w//2-_yd.get_width()//2, 228))
            # ── SIM: інфо про кімнату + кнопка ──
            _room = self.lb_selected_room
            self.lb_btn_start_sim = pygame.Rect(0,0,0,0)
            self.lb_btn_ready_lan = pygame.Rect(0,0,0,0)
            if self.lb_tab == "sim" and _room:
                for _il4, (_rl4,_rc4b) in enumerate([
                    (f"👤 Хост: {_room['name']}", (255,220,100)),
                    (f"Карт: {_room['cards']}  HP: {_room['total_hp']}  ATK: {_room['total_atk']}  Макс.ATK: {_room['max_atk']}", (180,220,255)),
                    (f"Локація {_room['loc']} / Світ {_room['world']}  |  Пінг: {_room['ping']}мс", (150,200,150)),
                ]):
                    _rls4 = font.render(_rl4, True, _rc4b)
                    screen.blit(_rls4, (w//2-_rls4.get_width()//2, 248+_il4*24))
                _cs3 = bool(self.army)
                self.lb_btn_start_sim = self.draw_button(
                    "⚔  ПОЧАТИ БІЙ  ⚔", w//2-160, h-140, 320, 55,
                    (180,30,30) if _cs3 else (60,60,60), active=_cs3)
            # ── LAN: статус + кнопки ГОТОВО ──
            elif self.lb_tab == "lan":
                _lan_st = self.lb_lan_status or "Підключення..."
                _lan_c  = (80,220,80) if self.lb_lan_connected else (220,180,60)
                _lst_s  = font.render(_lan_st, True, _lan_c)
                screen.blit(_lst_s, (w//2-_lst_s.get_width()//2, 248))
                if self.lb_lan_connected:
                    # Статус готовності обох сторін
                    _jr1_c = (80,220,80) if self.lb_lan_self_ready     else (180,180,60)
                    _jr2_c = (80,220,80) if self.lb_lan_opponent_ready else (180,60,60)
                    _jr1 = font.render("✅ ВИ: ГОТОВО"    if self.lb_lan_self_ready     else "⏳ ВИ: Натисни ГОТОВО", True, _jr1_c)
                    _jr2 = font.render("✅ ХОСТ: ГОТОВО"  if self.lb_lan_opponent_ready else "⏳ ХОСТ: Чекаємо...",   True, _jr2_c)
                    screen.blit(_jr1, (w//2-320, 285))
                    screen.blit(_jr2, (w//2+20,  285))
                    if self.lb_lan_self_ready and self.lb_lan_opponent_ready:
                        _go2 = font_big.render("⚔ ОБИДВА ГОТОВІ — НАТИСНИ ГОТОВО ЩЕ РАЗ!", True, (255,220,40))
                        screen.blit(_go2, (w//2-_go2.get_width()//2, 320))
                    _can_lan = self.lb_lan_connected
                    _lan_btn_lbl = "✅ ГОТОВО ДО БОЮ" if not self.lb_lan_self_ready else                                    ("⚔ ПОЧАТИ БІЙ" if self.lb_lan_opponent_ready else "✅ ЧЕКАЄМО ХОСТА...")
                    self.lb_btn_ready_lan = self.draw_button(
                        _lan_btn_lbl, w//2-170, h-140, 340, 55,
                        (30,160,30) if _can_lan else (60,60,60), active=_can_lan)
            self.lb_btn_cancel = self.draw_button("❌ Назад", w//2-80, h-75, 160, 42, (80,40,40))
            self.draw_wallet()

        elif self.state == LB_BATTLE:
            # ═══ АРЕНА PvP ════════════════════════════════════════════
            _lb_res = getattr(self, "lb_battle_result", None)

            if _lb_res:
                # ── РЕЗУЛЬТАТ ─────────────────────────────────────────
                _pulse_r = abs(math.sin(time.time() * 2.5))
                _rc4 = (int(50+80*_pulse_r),220,int(50+80*_pulse_r)) if _lb_res=="win" else (220,int(50+80*_pulse_r),int(50+80*_pulse_r))
                # Фон
                pygame.draw.rect(screen,(5,15,5) if _lb_res=="win" else (15,5,5),(0,0,w,h))
                # Рамка
                pygame.draw.rect(screen,_rc4,(w//2-340,h//2-130,680,280),3,border_radius=20)
                pygame.draw.rect(screen,(10,10,10),(w//2-337,h//2-127,674,274),border_radius=18)
                # Текст
                _rt4 = "🏆  ПЕРЕМОГА!  🏆" if _lb_res=="win" else "💀  ПОРАЗКА...  💀"
                _rts = font_big.render(_rt4, True, _rc4)
                screen.blit(_rts, (w//2-_rts.get_width()//2, h//2-110))
                _rw4 = "+100 💎 нараховано!" if _lb_res=="win" else "+10 💎 за участь"
                _rws = font.render(_rw4, True, (200,200,200))
                screen.blit(_rws, (w//2-_rws.get_width()//2, h//2-55))
                # Стат
                _sv4 = sum(1 for c in self.army    if c.is_alive)
                _se4 = sum(1 for c in self.enemies if c.is_alive)
                _st4 = font_small.render(
                    f"Вижило героїв: {_sv4}/{len(self.army)}  |  Ворогів залишилось: {_se4}/{len(self.enemies)}  |  Ходів: {self.total_hits}",
                    True,(140,140,140))
                screen.blit(_st4,(w//2-_st4.get_width()//2, h//2-18))
                # Феєрверк
                if _lb_res=="win":
                    for _ in range(2):
                        spawn_death_effect(random.randint(w//4,3*w//4), random.randint(h//4,h//2), 4)
                for p in death_effects[:]:
                    p.update(); p.draw(screen)
                    if p.life<=0: death_effects.remove(p)
                # Кнопки
                self.lb_btn_back_res  = self.draw_button("🏠 В ЛОБІ",     w//2-220, h//2+40, 190, 50, (30,80,180))
                self.lb_btn_new_battle= self.draw_button("🔄 НОВА БИТВА", w//2+30,  h//2+40, 190, 50, (120,30,30))
                self.draw_wallet()

            else:
                # ── АКТИВНА БИТВА ─────────────────────────────────────
                _now_lb2 = time.time()
                # Фон: верх-ворог червоний, низ-гравець синій
                pygame.draw.rect(screen,(22,5,5),  (0,0,   w,h//2))
                pygame.draw.rect(screen,(5,5,22),  (0,h//2,w,h//2))
                # Пульсуюча розділювальна лінія
                _dp = abs(math.sin(_now_lb2*3))
                pygame.draw.line(screen,(int(60+80*_dp),20,int(60+80*_dp)),(0,h//2),(w,h//2),2)
                # Кутові прикраси
                for _qx,_qy in [(20,20),(w-20,20),(20,h-20),(w-20,h-20)]:
                    pygame.draw.circle(screen,(80,30,100),(_qx,_qy),8)
                    pygame.draw.circle(screen,(160,60,200),(_qx,_qy),8,2)
                # Заголовок
                _is_pvp8      = getattr(self,"_lb_is_lan_pvp",False)
                _is_online8   = getattr(self,"_lb_is_online_pvp",False)
                _my_turn8     = getattr(self,"lb_lan_my_turn",True)
                # --- Визначаємо залишок часу та чий хід для будь-якого онлайн/LAN PvP ---
                if _is_online8:
                    _my_turn8  = (self.online_whose_turn == self.online_role)
                    # Час з online_deadline (сервер) або з локального таймера
                    _od8 = getattr(self,'online_deadline',0)
                    if _od8 > 0:
                        _remain8 = max(0.0, _od8 - time.time())
                    elif self.lb_turn_timer_start > 0:
                        _remain8 = max(0.0, self.lb_turn_timeout - (time.time() - self.lb_turn_timer_start))
                    else:
                        _remain8 = float(self.lb_get_turn_timeout())
                    _timeout8 = float(self.lb_get_turn_timeout())
                    _show_timer8 = True
                elif _is_pvp8 and self.lb_turn_timer_start > 0:
                    _elapsed8 = time.time() - self.lb_turn_timer_start
                    _remain8  = max(0.0, self.lb_turn_timeout - _elapsed8)
                    _timeout8 = float(self.lb_turn_timeout)
                    _show_timer8 = True
                else:
                    _remain8 = 0.0; _timeout8 = 38.0; _show_timer8 = False
                # Таймер ходу — круговий (для LAN та Онлайн PvP)
                if _show_timer8:
                    _tr = 28
                    _tcx, _tcy = w//2, h//2 - 58
                    _t_ratio = _remain8 / max(1, _timeout8)
                    _r8 = max(0,min(255,int(220*(1-_t_ratio)+40*_t_ratio)))
                    _g8 = max(0,min(255,int(180*_t_ratio+20*(1-_t_ratio))))
                    _t_col = (_r8, _g8, 20)
                    pygame.draw.circle(screen,(30,30,30),(_tcx,_tcy),_tr)
                    if _t_ratio > 0:
                        import math as _mth
                        _arc_end = -_mth.pi/2 + 2*_mth.pi*_t_ratio
                        pygame.draw.arc(screen,_t_col,
                            (_tcx-_tr,_tcy-_tr,_tr*2,_tr*2),
                            -_mth.pi/2, _arc_end if _remain8>0 else -_mth.pi/2+0.01, 6)
                    # Мигання при < 5 секунд
                    _blink8 = _remain8 < 5 and (int(time.time()*4) % 2 == 0)
                    _sec_col8 = (255,60,60) if _blink8 else ((100,255,100) if _my_turn8 else (255,160,40))
                    _tsec = font_big.render(f"{int(_remain8)}", True, _sec_col8)
                    screen.blit(_tsec,(_tcx-_tsec.get_width()//2, _tcy-_tsec.get_height()//2))
                    if _is_online8:
                        _mode_prefix8 = "🌐"
                    else:
                        _mode_prefix8 = "🔗"
                    if _my_turn8:
                        _tlbl8 = font_small.render(f"{_mode_prefix8} ВАШ ХІД ⚡",True,(100,255,100))
                    else:
                        _tlbl8 = font_small.render(f"{_mode_prefix8} Хід суперника ⏳",True,(255,160,40))
                    screen.blit(_tlbl8,(_tcx-_tlbl8.get_width()//2,_tcy+_tr+4))
                    # Мигаючий індикатор у кутку
                    _tip8 = abs(math.sin(time.time()*4))
                    _ti_c8 = (int(50+180*_tip8),int(200+55*_tip8),50) if _my_turn8 else (int(200+55*_tip8),int(100+80*_tip8),20)
                    pygame.draw.circle(screen,_ti_c8,(w-22,18),9)
                    pygame.draw.circle(screen,(0,0,0),(w-22,18),9,1)
                    # Підказка авто-удару при < 6 секунд
                    if _my_turn8 and _remain8 < 6:
                        _auto_hint = font_small.render("⚠ Авто-удар!", True, (255,80,80))
                        screen.blit(_auto_hint, (_tcx - _auto_hint.get_width()//2, _tcy + _tr + 20))
                else:
                    if getattr(self, "lb_lan_conn", None):
                        _my_t8 = getattr(self, "lb_lan_my_turn", True)
                        _tc8   = (80,220,80) if _my_t8 else (220,80,80)
                        _pvp_lbl8 = f"🔗 LAN  |  {'ВАШ ХІД ⚔' if _my_t8 else 'ХІД СУПЕРНИКА ⏳'}"
                    else:
                        _tc8 = (200,80,200)
                        _pvp_lbl8 = "⚔ БИТВА В ЖИВУ  —  Обери героя та ворога"
                    _tlb = font_small.render(_pvp_lbl8, True, _tc8)
                    screen.blit(_tlb,(w//2-_tlb.get_width()//2, 5))
                # Прогрес
                pygame.draw.rect(screen,(30,30,30),(w//2-220,22,440,13),border_radius=6)
                _hr3 = self.total_hits/max(1,self.max_hits)
                pygame.draw.rect(screen,(int(30+180*_hr3),int(120-80*_hr3),200),
                    (w//2-220,22,int(440*_hr3),13),border_radius=6)
                _prs = font_small.render(f"ХІД {self.total_hits}/{self.max_hits}",True,WHITE)
                screen.blit(_prs,(w//2-_prs.get_width()//2,22))
                # Ім'я та HP ворога
                _ea5 = [e for e in self.enemies if e.is_alive]
                _ehp5 = sum(e.hp for e in _ea5)
                _emx5 = sum(e.max_hp for e in self.enemies)
                _oname = getattr(self,"_lb_opp_name","Суперник")
                # Показати чи LAN PvP чи симуляція
                _pvp_mode = getattr(self, "lb_lan_conn", None) is not None
                _mode_label = "⚔ PvP через LAN" if _pvp_mode else "🤖 Симуляція"
                _ons = font.render(f"👤 {_oname} [{_mode_label}]  ❤ {_ehp5}/{_emx5}  карт: {len(_ea5)}/{len(self.enemies)}",True,(255,120,120))
                screen.blit(_ons,(w//2-_ons.get_width()//2,40))
                pygame.draw.rect(screen,(60,10,10),(w//2-200,60,400,9),border_radius=4)
                pygame.draw.rect(screen,(220,40,40),(w//2-200,60,max(0,min(400,int(400*max(0.0,min(1.0,_ehp5/max(1,_emx5)))))),9),border_radius=4)
                # Ім'я та HP гравця
                _ha5 = [h for h in self.army if h.is_alive]
                _hhp5 = sum(h.hp for h in _ha5)
                _hmx5 = sum(h.max_hp for h in self.army)
                _mes5 = font.render(f"🧑 ВИ  ❤ {_hhp5}/{_hmx5}  карт: {len(_ha5)}/{len(self.army)}",True,(120,180,255))
                screen.blit(_mes5,(w//2-_mes5.get_width()//2,h-78))
                pygame.draw.rect(screen,(10,10,60),(w//2-200,h-92,400,9),border_radius=4)
                pygame.draw.rect(screen,(40,80,220),(w//2-200,h-92,max(0,min(400,int(400*max(0.0,min(1.0,_hhp5/max(1,_hmx5)))))),9),border_radius=4)
                # Карти ворогів (верхня зона)
                _enc5 = len(self.enemies)
                for _ei5,_en5 in enumerate(self.enemies):
                    if not _en5.is_alive: continue
                    _ex5 = (w//(_enc5+1))*(_ei5+1)-70
                    _ey5 = 75
                    if self.selected_monster is _en5:
                        pygame.draw.rect(screen,(255,50,50),(_ex5-4,_ey5-4,148,248),3,border_radius=14)
                    _en5.draw(screen,_ex5,_ey5,140,240)
                # Карти гравця (нижня зона)
                _myc5 = len(self.army)
                for _hi5,_hh5 in enumerate(self.army):
                    if not _hh5.is_alive: continue
                    _hx5 = (w//(_myc5+1))*(_hi5+1)-70
                    _hy5 = h-348
                    if self.selected_hero is _hh5:
                        pygame.draw.rect(screen,(50,120,255),(_hx5-4,_hy5-4,148,248),3,border_radius=14)
                    _hh5.draw(screen,_hx5,_hy5,140,240)
                # Центральна кнопка
                _ch5 = bool(self.selected_hero and self.selected_monster)
                _can_hit8 = _ch5 and (not _is_pvp8 or _my_turn8)
                _bp5 = abs(math.sin(_now_lb2*4)) if _can_hit8 else 0
                if _is_pvp8 and not _my_turn8:
                    _bc5 = (30,30,30)
                    _hit_lbl8 = "⏳ Хід суперника..."
                elif _can_hit8:
                    _bc5 = (int(160+60*_bp5),int(15+10*_bp5),int(15+10*_bp5))
                    _hit_lbl8 = "⚔  ЗАВДАТИ УДАР  ⚔"
                else:
                    _bc5 = (50,50,50)
                    _hit_lbl8 = "⬆ Ворог    ⬇ Герой"
                self.lb_btn_hit = self.draw_button(
                    _hit_lbl8, w//2-150,h//2-30,300,52,_bc5,active=_can_hit8)
                # Панель здібностей
                if self.selected_hero:
                    _ab5 = get_abilities_for_card(self.selected_hero)
                    if _ab5:
                        _apx,_apy = w//2+165,h//2-30
                        _apw = 195; _aph = 14+len(_ab5)*17
                        pygame.draw.rect(screen,(12,12,28),(_apx,_apy,_apw,_aph),border_radius=6)
                        pygame.draw.rect(screen,(70,50,110),(_apx,_apy,_apw,_aph),1,border_radius=6)
                        screen.blit(font_small.render(f"⭐ {self.selected_hero.name}",True,GOLD),(_apx+4,_apy+2))
                        for _abi5,(_aem5,_anm5,_,__) in enumerate(_ab5):
                            screen.blit(font_small.render(f"{_aem5} {_anm5}",True,(170,170,240)),(_apx+4,_apy+15+_abi5*17))
                # Фраза бою
                if battle_phrase and time.time()-battle_phrase_timer < 2.5:
                    _bps = font.render(battle_phrase,True,GOLD)
                    pygame.draw.rect(screen,(0,0,0),(w//2-_bps.get_width()//2-8,h//2+28,_bps.get_width()+16,28),border_radius=6)
                    screen.blit(_bps,(w//2-_bps.get_width()//2,h//2+30))
                # Ефекти
                for p in death_effects[:]:
                    p.update(); p.draw(screen)
                    if p.life<=0: death_effects.remove(p)
                # Псевдо-3D ефекти здібностей
                draw_ability_effects_3d(screen)
                # Кнопка здатись
                self.lb_btn_flee = self.draw_button("🏳 Здатись",20,h-66,130,40,(70,25,25))
                self.draw_wallet()

        # --- HOVER ТУЛТІП: підказка зі здібностями при наведенні ---
        mx, my = pygame.mouse.get_pos()
        hover_cards = []
        if self.state == MY_ARMY:
            hover_cards = self.all_heroes
        elif self.state == GRAVEYARD:
            hover_cards = self.graveyard
        elif self.state in [MARKET, TITAN_STATE, TITANS_STATE,
                            TITAN_MONSTERS, TITAN_MONSTER_LOCS, MY_SHARDS,
                            ALTAR_HUB]:
            hover_cards = []
        elif self.state == REMOVE_RUNE_STATE:
            hover_cards = [c for c in self.all_heroes if getattr(c, "runes", [])]
        elif self.state in [CARDS_DIAMOND, CARDS_GOLD, CARDS_ORE]:
            hover_cards = (self.diamond_cards if self.state == CARDS_DIAMOND
                           else self.gold_cards if self.state == CARDS_GOLD
                           else self.ore_cards)
        elif self.state == BATTLE:
            hover_cards = ([e for e in self.enemies if e.is_alive] +
                           [h for h in self.army if h.is_alive])
        elif self.state == ALTAR and self.altar_drawn_card:
            hover_cards = [self.altar_drawn_card]
        for card in hover_cards:
            if card.rect.collidepoint(mx, my):
                draw_ability_tooltip(screen, card, mx, my, w, h)
                break

    def _start_titan_monster_battle(self, loc_id):
        """Запускає бій з Титан-Монстрами. HP зберігається між сесіями."""
        if not self.army: self.state = MY_ARMY; return
        tid = self.selected_titan_monster
        mult = 5 + (loc_id - 1)   # локація 1→×5, 2→×6, …, 10→×14
        lvl_min_tm, lvl_max_tm = LOCATION_LEVELS.get(loc_id, (1, 5))
        self.state = BATTLE
        self.total_hits = 0
        self.titan_clone_done = False
        self._pending_enemy_attack = None
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False

        # Якщо локація пройдена — не дозволяти
        saved = self.titan_monster_hp.get(tid, {}).get(str(loc_id))
        if saved == "done":
            self.state = TITAN_MONSTER_LOCS; return
        if saved:
            self.enemies = []
            for sd in saved:
                c = Card(sd["name"], sd["level"], True)
                c.max_hp = sd["max_hp"]
                c.hp     = sd["hp"]
                c.attack = sd["attack"]
                c.is_alive = sd["hp"] > 0
                self.enemies.append(c)
        else:
            self.enemies = []
            for i in range(6):
                lvl_tm = random.randint(lvl_min_tm, lvl_max_tm)
                c = Card(get_monster_name(loc_id, i), lvl_tm, True)
                c.max_hp = int(c.max_hp * mult * 3)   # x3 HP для Титан-монстрів
                c.hp     = c.max_hp
                self.enemies.append(c)
        # Позначаємо що це Титан-монстр бій
        self.is_titan_monster_battle = True
        self.current_titan_monster_loc = loc_id

    def start_battle(self):
        if not self.army: self.state = MY_ARMY; return
        self.state = BATTLE; self.total_hits = 0; self.titan_clone_done = False
        self.titan_used_hits = set()
        self._pending_enemy_attack = None
        ice_effects.clear(); shield_effects.clear(); grass_effects.clear()
        arrow_effects.clear(); chain_effects.clear()
        self.is_titan_monster_battle = False
        self.sh_is_battle = False
        self.ad_is_battle = False
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        loc = self.selected_location
        lvl_min, lvl_max = LOCATION_LEVELS.get(loc, (1, 5))
        # Коригуємо за номером світу
        world_bonus = (self.selected_world - 1) * 3
        self.enemies = []
        for i in range(6):
            lvl_e = random.randint(lvl_min + world_bonus, lvl_max + world_bonus)
            e = Card(get_monster_name(loc, i), lvl_e, True)
            e.max_hp = int(e.max_hp * 2)   # x2 HP для тривалішого бою
            e.hp     = e.max_hp
            e.runes = get_monster_runes(loc, self.selected_world)
            self.enemies.append(e)

    def start_sh_battle(self):
        """Запускає бій у режимі СУПЕР ЛЮДЕЙ (+10% HP, +5% ATK монстрів)."""
        if not self.army: self.state = MY_ARMY; return
        self.state = BATTLE; self.total_hits = 0; self.titan_clone_done = False
        self.titan_used_hits = set()
        self._pending_enemy_attack = None
        ice_effects.clear(); shield_effects.clear(); grass_effects.clear()
        arrow_effects.clear(); chain_effects.clear()
        self.is_titan_monster_battle = False
        self.sh_is_battle = True
        self.ad_is_battle = False
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        loc = self.sh_selected_location
        lvl_min, lvl_max = LOCATION_LEVELS.get(loc, (1, 5))
        world_bonus = (self.sh_selected_world - 1) * 3
        # Беремо 3 звичайних монстри + 3 Супер Людей як вороги
        self.enemies = []
        sh_enemies = list(SUPER_HUMAN_CARDS)
        random.shuffle(sh_enemies)
        for i in range(3):
            lvl_e = random.randint(lvl_min + world_bonus, lvl_max + world_bonus)
            e = Card(get_monster_name(loc, i), lvl_e, True)
            # +10% HP, +5% ATK для SH-режиму, загалом x2 від базового
            e.max_hp = int(e.max_hp * 2.20)
            e.hp     = e.max_hp
            e.attack = int(e.attack * 1.05)
            e.runes  = get_monster_runes(loc, self.sh_selected_world)
            self.enemies.append(e)
        for i in range(3):
            sh_d = sh_enemies[i % len(sh_enemies)]
            orig, name, base_lvl, shards, uah = sh_d
            lvl_e = max(1, base_lvl + world_bonus)
            e = create_super_human_card(sh_d)
            e.level  = min(125, lvl_e)
            e.max_hp, e.attack = get_super_human_stats(e.level)
            # x2 HP для балансу
            e.max_hp = int(e.max_hp * 2.20)
            e.hp     = e.max_hp
            e.attack = int(e.attack * 1.05)
            e.is_monster = True
            self.enemies.append(e)

    def _mono_do_roll(self):
        """Кидає кубики і рухає фішку Монополії."""
        d1 = random.randint(1, 6); d2 = random.randint(1, 6)
        self._mono_d1 = d1; self._mono_d2 = d2
        is_dbl = d1 == d2
        steps = d1 + d2 + (d1 if is_dbl else 0)
        self.mono_pos = (self.mono_pos + steps) % 16
        prize = self._get_mono_prizes()[self.mono_pos]
        if prize["type"] == "skip":
            self.mono_skip = True
            self.lucky_msg = f"🎲 {d1}+{d2}{'(ДУБЛЬ!)' if is_dbl else ''} → кл.{self.mono_pos+1}: 🚫 Пропуск ходу!"
            self.lucky_prize_pending = None
        else:
            self.lucky_msg = f"🎲 {d1}+{d2}{'(ДУБЛЬ! +'+str(d1)+' кроків)' if is_dbl else ''} → кл.{self.mono_pos+1}: {prize['emoji']} {prize['name']}!"
            self.lucky_prize_pending = prize

    def _fort_do_spin(self):
        """Обертає колесо та визначає приз."""
        prizes = self._get_fort_prizes()
        idx = random.randint(0, 15)
        seg = 360 / 16
        self.wheel_angle = (self.wheel_angle + 1440 + idx * seg + random.uniform(0, seg)) % 360
        prize = prizes[idx]
        self.lucky_msg = f"🌀 Випало: {prize['emoji']} {prize['name']}!"
        self.lucky_prize_pending = prize

    def _get_mono_prizes(self):
        if not hasattr(self, '_mono_prizes'):
            self._mono_prizes = (LUCKY_PRIZES * 2)[:16]
        return self._mono_prizes

    def _get_fort_prizes(self):
        if not hasattr(self, '_fort_prizes'):
            self._fort_prizes = (LUCKY_PRIZES * 2)[:16]
        return self._fort_prizes

    def start_ad_battle(self):
        """Запускає бій у режимі АНГЕЛИ ТА ДЕМОНИ."""
        if not self.army: self.state = MY_ARMY; return
        self.state = BATTLE; self.total_hits = 0; self.titan_clone_done = False
        self.titan_used_hits = set()
        self._pending_enemy_attack = None
        ice_effects.clear(); shield_effects.clear(); grass_effects.clear()
        arrow_effects.clear(); chain_effects.clear()
        self.is_titan_monster_battle = False
        self.sh_is_battle  = False
        self.ad_is_battle  = True
        for h in self.army: h.hp = h.max_hp; h.is_alive = True; h.selected = False
        loc = self.ad_selected_location
        lvl_min, lvl_max = LOCATION_LEVELS.get(loc, (1, 5))
        world_bonus = (self.ad_selected_world - 1) * 3
        self.enemies = []
        # 3 звичайних монстри (x2 HP для балансу, +10% ATK)
        for i in range(3):
            lvl_e = random.randint(lvl_min + world_bonus, lvl_max + world_bonus)
            e = Card(get_monster_name(loc, i), lvl_e, True)
            e.max_hp = int(e.max_hp * 2.40); e.hp = e.max_hp
            e.attack = int(e.attack * 1.10)
            e.runes  = get_monster_runes(loc, self.ad_selected_world)
            self.enemies.append(e)
        # 2 герої (x2 HP, +10% ATK)
        for i in range(2):
            lvl_e = random.randint(lvl_min + world_bonus, lvl_max + world_bonus)
            e = Card(f"Герой {random.randint(1,5)}", lvl_e)
            e.max_hp = int(e.max_hp * 2.40); e.hp = e.max_hp
            e.attack = int(e.attack * 1.10)
            self.enemies.append(e)
        # 2 Супер Людей як вороги (x2 HP)
        sh_pool = list(SUPER_HUMAN_CARDS); random.shuffle(sh_pool)
        for i in range(2):
            sh_d = sh_pool[i % len(sh_pool)]
            lvl_e = max(1, sh_d[2] + world_bonus)
            e = create_super_human_card(sh_d)
            e.level = min(125, lvl_e)
            e.max_hp, e.attack = get_super_human_stats(e.level)
            e.max_hp = int(e.max_hp * 2.40); e.hp = e.max_hp
            e.attack = int(e.attack * 1.10)
            e.is_monster = True; self.enemies.append(e)
        # 2 Архангели/Архдемони як вороги (x2 HP)
        ar_pool = list(ARCH_CARDS); random.shuffle(ar_pool)
        for i in range(2):
            ar_d = ar_pool[i % len(ar_pool)]
            lvl_e = max(1, ar_d[2] + world_bonus)
            e = create_arch_card(ar_d)
            e.level = min(130, lvl_e)
            e.max_hp, e.attack = get_arch_stats(e.level)
            e.max_hp = int(e.max_hp * 2.40); e.hp = e.max_hp
            e.attack = int(e.attack * 1.10)
            e.is_monster = True; self.enemies.append(e)

    def check_end_game(self):
        living_h = [h for h in self.army if h.is_alive]
        living_e = [e for e in self.enemies if e.is_alive]
        if self.total_hits >= self.max_hits or not living_h or not living_e:
            if living_h:
                self.winner_text = "ПЕРЕМОГА!"
                self.play_snd(SND_WIN)
                self.set_battle_phrase(random.choice(BATTLE_WIN_PHRASES))
                # Титан отримує XP за перемогу
                self.titan_xp += 1
                titan_max_xp = 10 * self.titan_level
                if self.titan_xp >= titan_max_xp:
                    self.titan_level += 1
                    self.titan_levels[self.titan_id] = self.titan_level
                    self.titan_xp = 0
                if self.is_titan_monster_battle:
                    tid = self.selected_titan_monster
                    self.titan_shards[tid] = self.titan_shards.get(tid, 0) + 10
                    # Закрити локацію (позначити як пройдену)
                    if tid not in self.titan_monster_hp:
                        self.titan_monster_hp[tid] = {}
                    self.titan_monster_hp[tid][str(self.current_titan_monster_loc)] = "done"
                    self.reward_text = f"+10 осколків! Всього: {self.titan_shards.get(tid,0)}/100"
                    self.save_game()
                added_keys = 0
                if getattr(self, 'ad_is_battle', False):
                    # AD режим — зберігаємо прогрес в ad_completed_levels
                    if self.ad_selected_world not in self.ad_completed_levels:
                        self.ad_completed_levels[self.ad_selected_world] = []
                    if self.ad_selected_location not in self.ad_completed_levels[self.ad_selected_world]:
                        self.ad_completed_levels[self.ad_selected_world].append(self.ad_selected_location)
                        self.keys += 1; added_keys += 1
                        if len(self.ad_completed_levels[self.ad_selected_world]) == 10:
                            self.keys += 5; added_keys += 5
                elif getattr(self, 'sh_is_battle', False):
                    # SH режим — зберігаємо прогрес в sh_completed_levels
                    if self.sh_selected_world not in self.sh_completed_levels:
                        self.sh_completed_levels[self.sh_selected_world] = []
                    if self.sh_selected_location not in self.sh_completed_levels[self.sh_selected_world]:
                        self.sh_completed_levels[self.sh_selected_world].append(self.sh_selected_location)
                        self.keys += 1; added_keys += 1
                        if len(self.sh_completed_levels[self.sh_selected_world]) == 10:
                            self.keys += 5; added_keys += 5
                else:
                    if self.selected_world not in self.completed_levels: self.completed_levels[self.selected_world] = []
                    if not self.is_titan_monster_battle and self.selected_location not in self.completed_levels[self.selected_world]:
                        self.completed_levels[self.selected_world].append(self.selected_location)
                        self.keys += 1; added_keys += 1
                        if len(self.completed_levels[self.selected_world]) == 10: self.keys += 5; added_keys += 5
                # Бонусна карта: тільки при перемозі у локації 10 (тільки звичайні світи)
                self.victory_bonus_card = None
                _cur_loc_for_bonus = self.selected_location
                if getattr(self, 'ad_is_battle', False):
                    _cur_loc_for_bonus = self.ad_selected_location
                elif getattr(self, 'sh_is_battle', False):
                    _cur_loc_for_bonus = self.sh_selected_location
                if not self.is_titan_monster_battle and not getattr(self,'ad_is_battle',False) and not getattr(self,'sh_is_battle',False) and _cur_loc_for_bonus == 10:
                    lvl_min, lvl_max = self.altar_get_card_level_range()
                    _vlvl = random.randint(lvl_min, lvl_max)
                    if random.random() < 0.5:  # 50% герой, 50% монстр
                        _vnum = random.randint(1, 5)
                        self.victory_bonus_card = Card(f"Герой {_vnum}", _vlvl)
                    else:
                        _monster_names = [n for n in RENAME_DICT.values()]
                        _vname = random.choice(_monster_names)
                        self.victory_bonus_card = Card(_vname, _vlvl, is_monster=False)

                # Нагорода рахується від поточного активного режиму
                if getattr(self, 'ad_is_battle', False):
                    base_lvl = (self.ad_selected_world - 1) * 5 + self.ad_selected_location
                elif getattr(self, 'sh_is_battle', False):
                    base_lvl = (self.sh_selected_world - 1) * 5 + self.sh_selected_location
                else:
                    base_lvl = (self.selected_world - 1) * 5 + self.selected_location
                g, o, d = base_lvl*20, base_lvl*8, random.randint(1, 3)
                self.gold += g; self.ore += o; self.diamonds += d
                # Шанс отримати осколок Супер Людей (~30%)
                _sh_reward_text = ""
                if random.random() < 0.30:
                    _sh_r = random.choice(SUPER_HUMAN_CARDS)
                    self.super_human_shards[_sh_r[0]] = self.super_human_shards.get(_sh_r[0],0) + 1
                    _sh_reward_text = f" +1🧩{_sh_r[1][:8]}"
                    for sh in SUPER_HUMAN_CARDS:
                        if (self.super_human_shards.get(sh[0],0) >= sh[3]
                                and sh[0] not in self.unlocked_super_humans):
                            self.unlocked_super_humans.append(sh[0])
                            if not any(getattr(c, '_super_human_orig', None) == sh[0] for c in self.all_heroes):
                                nc = create_super_human_card(sh)
                                self.all_heroes.append(nc)
                self.reward_text = f"+{g} 🪙  +{o} ⛏️  +{d} 💎" + (f" +{added_keys} 🔑" if added_keys > 0 else "") + _sh_reward_text
                self.save_game()
            else:
                self.winner_text = "ПОРАЗКА..."; self.reward_text = "Герої пали на полі бою!"
                self.play_snd(SND_LOSE)
                self.set_battle_phrase(random.choice(BATTLE_LOSE_PHRASES))
            self.sh_is_battle = False
            self._pending_enemy_attack = None
            self.state = GAME_OVER

    def play_snd(self, snd):
        if self.sounds_enabled and snd:
            try: snd.play()
            except: pass

    def set_battle_phrase(self, phrase):
        global battle_phrase, battle_phrase_timer
        battle_phrase = phrase
        battle_phrase_timer = time.time()

    def execute_battle(self):
        if not (self.selected_hero and self.selected_monster):
            return
        hero    = self.selected_hero
        monster = self.selected_monster
        hcx, hcy = _card_center(hero)
        mcx, mcy = _card_center(monster)
        living_h = [h for h in self.army    if h.is_alive]
        living_m = [e for e in self.enemies if e.is_alive]

        # ══════════════════════════════════════════════════════
        # ФАЗА 1 — МАГІЯ / ЗДІБНОСТІ (перед ATK, як у Deck Heroes)
        # Кожна здібність спрацьовує окремо зі своїм шансом
        # ══════════════════════════════════════════════════════
        abilities       = get_abilities_for_card(hero)
        ability_phrases = []
        stars           = len(abilities)
        if abilities:
            # "Вища Форма" — всі 3 навички одразу
            if any(a[1] == "Вища Форма" for a in abilities):
                triggered = abilities[:min(3, len(abilities))]
            else:
                base_chance = ABILITY_CHANCE_BY_STARS.get(stars, 0.3)
                triggered   = [a for a in abilities if random.random() < base_chance]

            for ability in triggered:
                emoji, name, desc, typ = ability
                # ── Спец-механіки нових здібностей ──────────────────
                extra_phrase = None
                # Deck Heroes: Удар Милосердя +150% якщо >50% HP
                if name == "Удар Милосердя" and monster.hp > monster.max_hp * 0.5:
                    bonus_dmg = int(hero.attack * 1.5)
                    monster.hp -= bonus_dmg
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"⚰️ Удар Милосердя: -{bonus_dmg} (>50% HP)!"
                # Вибух Milka Deck Heroes: Вбивство Слабких +80% якщо <50% HP
                elif name == "Вбивство Слабких" and monster.hp < monster.max_hp * 0.5:
                    bonus_dmg = int(hero.attack * 0.8)
                    monster.hp -= bonus_dmg
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🎯 Вбивство Слабких: -{bonus_dmg} (ворог слабкий)!"
                # Deck Heroes: Вихр Сталі — шкода сусідам
                elif name == "Вихр Сталі":
                    side_dmg = hero.attack // 2
                    hit_cnt  = 0
                    for e in living_m:
                        if e is not monster and e.is_alive:
                            e.hp -= side_dmg
                            if e.hp <= 0: e.is_alive = False; spawn_death_effect(int(e.rect.centerx),int(e.rect.centery),2)
                            hit_cnt += 1
                            if hit_cnt >= 2: break
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🌪️ Вихр Сталі: -{side_dmg} {hit_cnt} сусідам!"
                # Deck Heroes: Проникаюче Лезо — частина шкоди йде герою
                elif name == "Проникаюче Лезо":
                    pass_dmg = hero.attack // 3
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🔪 Проникаюче Лезо: -{pass_dmg} герою суперника!"
                # Deck Heroes: Жага Крові / Спрага Бою — бафф атаки
                elif name in ("Жага Крові","Спрага Бою"):
                    hero.attack = int(hero.attack * 1.15)
                    spawn_ability_effect(name, hcx, hcy, hcx, hcy)
                    extra_phrase = f"😤 {name}: ATK +15%! (тепер {hero.attack})"
                # Deck Heroes: Ефект Несподіванки — тільки перший хід
                elif name == "Ефект Несподіванки" and self.total_hits == 0:
                    bonus_dmg = hero.attack * 2
                    monster.hp -= bonus_dmg
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"💥 Несподіванка: -{bonus_dmg} (перший хід)!"
                # Deck Heroes: Шквал Клинків — атака двічі
                elif name == "Шквал Клинків":
                    monster.hp -= hero.attack
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"⚔️ Шквал Клинків: +{hero.attack} доп. удар!"
                # Deck Heroes: Безодня — великий удар 55% HP, без миттєвого вбивства
                elif name == "Безодня":
                    big_dmg = max(1, int(monster.hp * 0.55))
                    monster.hp = max(1, monster.hp - big_dmg)
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🌑 БЕЗОДНЯ: -{big_dmg} HP (важкий удар)!"
                # Deck Heroes: Чорна Діра — 50% HP, без миттєвого вбивства
                elif name == "Чорна Діра":
                    big_dmg2 = max(1, int(monster.hp * 0.50))
                    monster.hp = max(1, monster.hp - big_dmg2)
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🌌 ЧОРНА ДІРА: -{big_dmg2} HP!"
                # Deck Heroes: Сліпа Лють — 200%+ шанс
                elif name == "Сліпа Лють" and random.random() < 0.35:
                    mul  = random.uniform(2.0, 3.5)
                    fury = int(hero.attack * mul)
                    monster.hp -= fury
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"😱 Сліпа Лють: -{fury} ({int(mul*100)}% ATK)!"
                # Deck Heroes: Вивірений Постріл — множник
                elif name == "Вивірений Постріл" and random.random() < 0.40:
                    mul  = random.choice([1.5, 2.0, 2.5])
                    prec = int(hero.attack * mul)
                    monster.hp -= prec
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🎯 Постріл x{mul}: -{prec}!"
                # Deck Heroes: Посмертна Детонація — ефект на смерть
                elif name == "Посмертна Детонація":
                    hero._detonation = True  # спрацює при смерті
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = "💀 Детонація заряджена!"
                # Deck Heroes: Дугова Каскад — 3 рандомних цілі
                elif name == "Дугова Каскад":
                    cas_targets = random.sample(living_m, min(3, len(living_m)))
                    cas_dmg     = hero.attack // 2
                    for ct in cas_targets:
                        ct.hp -= cas_dmg
                        if ct.hp <= 0: ct.is_alive = False; spawn_death_effect(int(ct.rect.centerx),int(ct.rect.centery),2)
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🔮 Каскад: -{cas_dmg} × {len(cas_targets)} цілі!"
                # Контроль: Крiостазис
                elif name == "Крiостазис":
                    monster._frozen_until = time.time() + 6
                    monster.attack = max(0, int(monster.attack * 0.1))
                    ice_effects.append({"card": monster, "end_time": time.time() + 6})
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🧊 Крiостазис: {monster.name} пропускає хід!"
                # Контроль: Нейрошок
                elif name == "Нейрошок":
                    monster._frozen_until = time.time() + 3
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"⚡ Нейрошок: {monster.name} не може атакувати!"
                # Контроль: Атрофія Сил
                elif name == "Атрофія Сил":
                    monster.attack = max(1, int(monster.attack * 0.3))
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"💪 Атрофія: ATK ворога → {monster.attack}!"
                # Інверсія Параметрів
                elif name == "Інверсія Параметрів":
                    monster.attack, monster.hp = min(monster.attack, monster.hp), max(monster.attack, monster.hp)
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🔄 Інверсія: HP↔ATK ворога!"
                # Викривлення Розуму — ворог б'є себе
                elif name == "Викривлення Розуму":
                    self_dmg = int(monster.attack * 0.8)
                    monster.hp -= self_dmg
                    if monster.hp <= 0: monster.is_alive = False
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"🧠 Викривлення: {monster.name} б'є себе -{self_dmg}!"
                # Деградація — слизь
                elif name == "Деградація" and random.random() < 0.30:
                    monster.attack  = max(1, monster.attack // 4)
                    monster.max_hp  = max(10, monster.max_hp // 3)
                    monster.hp      = min(monster.hp, monster.max_hp)
                    monster.name    = f"Слизь:{monster.name[:6]}"
                    spawn_ability_effect(name, hcx, hcy, mcx, mcy)
                    extra_phrase = f"⚗️ Деградація: {monster.name} стала Слизью!"
                # Дзеркальне Втілення
                elif name == "Дзеркальне Втілення":
                    from copy import copy as _copy
                    clone_m = _copy(monster)
                    clone_m.name = f"Міраж:{monster.name[:8]}"
                    clone_m.is_monster = False
                    max_c = get_titan_max_cards(self.titan_level)
                    if len(self.army) < max_c: self.army.append(clone_m)
                    spawn_ability_effect(name, hcx, hcy, hcx, hcy)
                    extra_phrase = f"🪞 Дзеркало: клон {clone_m.name} призваний!"
                # Баф: Сигнал Авангарду
                elif name == "Сигнал Авангарду":
                    hero.attack = int(hero.attack * 1.25)
                    spawn_ability_effect(name, hcx, hcy, hcx, hcy)
                    extra_phrase = f"🔥 Авангард: ATK +25% → {hero.attack}!"
                # Баф: Кругова Порука — броня сусідам
                elif name == "Кругова Порука":
                    for h in living_h[:3]:
                        h._shielded_until = time.time() + 4
                        shield_effects.append({"card": h, "end_time": time.time() + 4})
                    spawn_ability_effect(name, hcx, hcy, hcx, hcy)
                    extra_phrase = f"🌀 Порука: щит для {min(3,len(living_h))} союзників!"
                else:
                    # Стандартне apply_ability
                    extra_phrase = apply_ability(ability, hero, monster, living_h, living_m)

                if extra_phrase:
                    ability_phrases.append(extra_phrase)
                self.play_snd(ABILITY_SOUNDS.get(name))

        # ══════════════════════════════════════════════════════
        # ФАЗА 2 — ФІЗИЧНИЙ ATK УДАР (після магії)
        # ══════════════════════════════════════════════════════
        if monster.is_alive:
            # Перевірка уникання ворога
            _dodge_t = getattr(monster, '_dodge_until', 0)
            if time.time() < _dodge_t and random.random() < 0.5:
                self.set_battle_phrase(f"👁️ {monster.name} ухилився від удару!")
            else:
                # Перевірка щита героя-атакуючого (Ефект Несподіванки і т.д.)
                atk_val = hero.attack
                # Непробивний Щит — блок наступного удару
                _shield_t = getattr(hero, '_shielded_until', 0)
                if time.time() < _shield_t:
                    pass  # герой вже захищений — ATK посилений? Ні, просто б'є нормально
                monster.hp -= atk_val
                monster.offset_y = -20
                hero.add_xp()
                # 3D-ефект фізичного удару
                spawn_ability_effect("Подвійний Удар", hcx, hcy, mcx, mcy)

        if monster.hp <= 0:
            monster.is_alive = False
            hero_stars = len(get_abilities_for_card(hero))
            spawn_death_effect(mcx, mcy, hero_stars)
            self.play_snd(SND_KILL)
            if getattr(self,"lb_lan_conn",None) and self.state == LB_BATTLE:
                self.lb_lan_send_hit(hero.original_name, monster.original_name, hero.attack)
            # Посмертна Детонація
            if getattr(monster, '_detonation', False):
                det_dmg = hero.attack
                for e in [e for e in self.enemies if e.is_alive]:
                    e.hp -= det_dmg
                    if e.hp <= 0: e.is_alive = False; spawn_death_effect(int(e.rect.centerx),int(e.rect.centery),2)
                spawn_ability_effect("Вибух", mcx, mcy, mcx, mcy)
            phrase = ability_phrases[-1] if ability_phrases else random.choice(BATTLE_KILL_PHRASES)
            self.set_battle_phrase(phrase)
        else:
            self.play_snd(SND_HIT)
            if getattr(self,"lb_lan_conn",None) and self.state == LB_BATTLE:
                self.lb_lan_send_hit(hero.original_name, monster.original_name, hero.attack)
            phrase = ability_phrases[-1] if ability_phrases else random.choice(BATTLE_HIT_PHRASES)
            self.set_battle_phrase(phrase)

        # ══════════════════════════════════════════════════════
        # ФАЗА 3 — КОНТРАТАКА ВОРОГА (відкладена: зберігаємо в _pending_enemy_attack)
        # ══════════════════════════════════════════════════════
        _lan_pvp_ex = (getattr(self,'_lb_is_lan_pvp',False) and getattr(self,'lb_lan_conn',None) is not None)
        living_m2 = [m for m in self.enemies if m.is_alive]
        living_h2 = [h for h in self.army    if h.is_alive]
        if living_m2 and living_h2 and not _lan_pvp_ex:
            enemy_attacker = random.choice(living_m2)
            target_hero    = random.choice(living_h2)
            # Зберігаємо контратаку — вона виконається через 0.9 сек (в game loop)
            self._pending_enemy_attack = {
                "attacker": enemy_attacker,
                "target":   target_hero,
                "time":     time.time() + 0.9,   # затримка 0.9 сек
            }
            self.set_battle_phrase(f"⚠️ {enemy_attacker.name} готується до удару...")

        self.total_hits += 1

        # --- Реген еволюціонованих героїв за хід ---
        for _rh in self.army:
            if _rh.is_alive and getattr(_rh,'evolved',False) and getattr(_rh,'regen',0) > 0:
                _rh.hp = min(_rh.max_hp, _rh.hp + _rh.regen)

        # --- Здібність активного Титана ---
        _lan_pvp_ta = getattr(self,'_lb_is_lan_pvp',False)
        t_ability   = TITAN_ABILITIES.get(self.titan_id)
        if t_ability and not _lan_pvp_ta:
            _em, _nm, _dc, _trigger_hits, _key = t_ability
            if self.total_hits in _trigger_hits and self.total_hits not in self.titan_used_hits:
                t_level   = self.titan_levels.get(self.titan_id, 1)
                phrase_ta = apply_titan_ability(self, _key, t_level)
                if phrase_ta: self.set_battle_phrase(phrase_ta)
                self.titan_used_hits.add(self.total_hits)

        # Скидаємо таймер ходу якщо LAN PvP
        if getattr(self,'_lb_is_lan_pvp',False):
            self.lb_turn_timer_start = time.time()
            self.lb_turn_timeout     = self.lb_get_turn_timeout()
        hero.selected = monster.selected = False
        self.selected_hero = self.selected_monster = None
        # Якщо є відкладена атака ворога — check_end_game буде після неї
        # Якщо немає (LAN/PvP або всі вороги мертві) — перевіряємо одразу
        if not getattr(self, '_pending_enemy_attack', None):
            self.check_end_game()


def main():
    global screen, _is_fullscreen
    game = Game()
    clock = pygame.time.Clock()
    while True:
        game.draw(*screen.get_size())
        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                game.save_game(); pygame.quit(); sys.exit()
            if event.type == pygame.MOUSEBUTTONDOWN:
                game.handle_click(event.pos)
            if event.type == pygame.KEYDOWN:
                # Ввід IP для LAN
                if getattr(game, "lb_ip_active", False):
                    if event.key == pygame.K_BACKSPACE:
                        game.lb_ip_input = game.lb_ip_input[:-1]
                    elif event.key == pygame.K_RETURN:
                        game.lb_ip_active = False
                    elif len(game.lb_ip_input) < 20:
                        ch = event.unicode
                        if ch in "0123456789.": game.lb_ip_input += ch
                # F11 — перемикання повний екран / вікно
                if event.key == pygame.K_F11:
                    if _is_fullscreen:
                        screen = pygame.display.set_mode((WIDTH, HEIGHT), pygame.RESIZABLE)
                        _is_fullscreen = False
                    else:
                        info = pygame.display.Info()
                        screen = pygame.display.set_mode((info.current_w, info.current_h), pygame.FULLSCREEN | pygame.NOFRAME)
                        _is_fullscreen = True
                # Escape — вихід з повноекранного режиму у вікно
                elif event.key == pygame.K_ESCAPE and _is_fullscreen:
                    screen = pygame.display.set_mode((WIDTH, HEIGHT), pygame.RESIZABLE)
                    _is_fullscreen = False
            if event.type == pygame.VIDEORESIZE:
                screen = pygame.display.set_mode((event.w, event.h), pygame.RESIZABLE)
        # Обробка зміни розміру через кнопки налаштувань
        if getattr(game, "pending_resolution", None):
            rw, rh = game.pending_resolution
            screen = pygame.display.set_mode((rw, rh), pygame.RESIZABLE)
            _is_fullscreen = False
            game.pending_resolution = None
        # ── Обробка черги LAN повідомлень ──────────────────────────────────
        while not _lan_msg_queue.empty():
            try:
                _lmsg_type, _lmsg_data = _lan_msg_queue.get_nowait()

                if _lmsg_type == "DECK":
                    game.lb_lan_enemy_deck = _lmsg_data

                elif _lmsg_type == "TURN" and game.state == LB_BATTLE:
                    # Партнер передає хід нам — скидаємо таймер
                    game.lb_lan_my_turn      = True
                    game.lb_turn_timer_start = time.time()
                    game.lb_turn_timeout     = game.lb_get_turn_timeout()
                    game.selected_hero    = None
                    game.selected_monster = None
                    game.set_battle_phrase('⚡ Ваш хід!')

                elif _lmsg_type == "ONLINE_START":
                    if game.state not in (LB_BATTLE,):
                        game.online_start_battle_from_state(_lmsg_data)

                elif _lmsg_type == "SURRENDER" and game.state == LB_BATTLE:
                    game.lb_battle_result = 'win'
                    game.diamonds += 100
                    game.play_snd(SND_WIN)
                    game.save_game()

                elif _lmsg_type == "HIT" and game.state == LB_BATTLE:
                    _mname = _lmsg_data.get('m', '')
                    _hdmg  = _lmsg_data.get('d', 0)
                    _done  = False
                    for _hc in game.army:
                        if _hc.is_alive and (_hc.original_name == _mname or _hc.name == _mname):
                            _hc.hp = max(0, _hc.hp - _hdmg)
                            _hc.offset_y = 20
                            if _hc.hp <= 0:
                                _hc.is_alive = False
                                spawn_death_effect(int(_hc.rect.centerx),int(_hc.rect.centery),2)
                                if _hc in game.all_heroes: game.all_heroes.remove(_hc)
                                if _hc in game.army:       game.army.remove(_hc)
                                game.graveyard.append(_hc)
                            _done = True
                            game.lb_check_end()
                            break
                    if not _done:  # fallback — б'ємо першу живу
                        for _hc2 in game.army:
                            if _hc2.is_alive:
                                _hc2.hp = max(0, _hc2.hp - _hdmg)
                                _hc2.offset_y = 20
                                if _hc2.hp <= 0:
                                    _hc2.is_alive = False
                                    if _hc2 in game.all_heroes: game.all_heroes.remove(_hc2)
                                    if _hc2 in game.army:       game.army.remove(_hc2)
                                    game.graveyard.append(_hc2)
                                game.lb_check_end()
                                break
            except: pass

        # ── Авто-удар при закінченні таймера ────────────────────────────────
        if (game.state == LB_BATTLE
                and getattr(game,'_lb_is_lan_pvp',False)
                and getattr(game,'lb_lan_my_turn',False)
                and getattr(game,'lb_battle_result',None) is None
                and game.lb_turn_timer_start > 0):
            _elapsed_t = time.time() - game.lb_turn_timer_start
            if _elapsed_t >= game.lb_turn_timeout:
                # Таймер вийшов — автоматично б'ємо першою живою картою по першому живому ворогу
                _auto_hero = next((h for h in game.army if h.is_alive), None)
                _auto_mon  = next((e for e in game.enemies if e.is_alive), None)
                if _auto_hero and _auto_mon:
                    # Завдаємо урон
                    _auto_mon.hp -= _auto_hero.attack
                    _auto_mon.offset_y = -20
                    _auto_hero.add_xp()
                    if _auto_mon.hp <= 0:
                        _auto_mon.is_alive = False
                        spawn_death_effect(int(_auto_mon.rect.centerx),int(_auto_mon.rect.centery),2)
                        game.play_snd(SND_KILL)
                    else:
                        game.play_snd(SND_HIT)
                    # Надсилаємо HIT + TURN партнеру
                    game.lb_lan_send_hit(_auto_hero.original_name, _auto_mon.original_name, _auto_hero.attack)
                    game.total_hits += 1
                    game.lb_lan_my_turn      = False
                    game.lb_turn_timer_start = time.time()
                    game.lb_turn_timeout     = game.lb_get_turn_timeout()
                    game.set_battle_phrase('⏱ Час вийшов! Автоудар!')
                    game.lb_check_end()
                else:
                    game.lb_check_end()

        # ── Онлайн: опитування сервера щокадру ─────────────────────────────
        if getattr(game, 'online_room_id', '') and game.state in (LIVE_BATTLE, LB_BATTLE):
            game.online_poll_state()

        # ── Онлайн: обробити результат та оновити HP з сервера ──────────────
        if getattr(game, '_lb_is_online_pvp', False) and game.state == LB_BATTLE:
            _ores = getattr(game, 'online_result', None)
            # Обробляємо результат ТІЛЬКИ ОДИН РАЗ (захист від повторного нарахування)
            if (_ores
                    and getattr(game, 'lb_battle_result', None) is None
                    and getattr(game, '_lb_online_result_shown_at', 0) == 0):
                _winner = _ores.get('winner', '')
                if _winner == game.online_role or _winner == 'win':
                    game.lb_battle_result = 'win'
                    game.diamonds += 100
                    game.keys += 3
                    # Перемога онлайн → 2 осколки для Супер Людей
                    _sh1 = random.choice(SUPER_HUMAN_CARDS)
                    _sh2 = random.choice(SUPER_HUMAN_CARDS)
                    game.super_human_shards[_sh1[0]] = game.super_human_shards.get(_sh1[0], 0) + 1
                    game.super_human_shards[_sh2[0]] = game.super_human_shards.get(_sh2[0], 0) + 1
                    game._last_shard_reward = f"🏆 +2 осколки: {_sh1[1]} та {_sh2[1]}"
                    # Авто-розблокування якщо зібрали достатньо
                    for _sh in SUPER_HUMAN_CARDS:
                        if (game.super_human_shards.get(_sh[0], 0) >= _sh[3]
                                and _sh[0] not in game.unlocked_super_humans):
                            game.unlocked_super_humans.append(_sh[0])
                            if not any(getattr(c, '_super_human_orig', None) == _sh[0] for c in game.all_heroes):
                                game.all_heroes.append(create_super_human_card(_sh))
                    # Архангели: +2 осколки за перемогу онлайн
                    _ar_ow1 = random.choice(ARCH_CARDS); _ar_ow2 = random.choice(ARCH_CARDS)
                    game.arch_shards[_ar_ow1[0]] = game.arch_shards.get(_ar_ow1[0], 0) + 1
                    game.arch_shards[_ar_ow2[0]] = game.arch_shards.get(_ar_ow2[0], 0) + 1
                    for _ar in ARCH_CARDS:
                        if (game.arch_shards.get(_ar[0], 0) >= _ar[3] and _ar[0] not in game.unlocked_arch):
                            game.unlocked_arch.append(_ar[0])
                            if not any(getattr(c,'_arch_orig',None)==_ar[0] for c in game.all_heroes):
                                game.all_heroes.append(create_arch_card(_ar))
                    game.play_snd(SND_WIN)
                elif _winner:  # є результат але не win — програш
                    game.lb_battle_result = 'lose'
                    game.diamonds += 10
                    game.keys += 1
                    # Поразка онлайн → 1 осколок для Супер Людей
                    _sh_l = random.choice(SUPER_HUMAN_CARDS)
                    game.super_human_shards[_sh_l[0]] = game.super_human_shards.get(_sh_l[0], 0) + 1
                    game._last_shard_reward = f"💀 +1 осколок: {_sh_l[1]}"
                    for _sh in SUPER_HUMAN_CARDS:
                        if (game.super_human_shards.get(_sh[0], 0) >= _sh[3]
                                and _sh[0] not in game.unlocked_super_humans):
                            game.unlocked_super_humans.append(_sh[0])
                            if not any(getattr(c, '_super_human_orig', None) == _sh[0] for c in game.all_heroes):
                                game.all_heroes.append(create_super_human_card(_sh))
                    # Архангели: +1 осколок за поразку онлайн
                    _ar_ol = random.choice(ARCH_CARDS)
                    game.arch_shards[_ar_ol[0]] = game.arch_shards.get(_ar_ol[0], 0) + 1
                    for _ar in ARCH_CARDS:
                        if (game.arch_shards.get(_ar[0], 0) >= _ar[3] and _ar[0] not in game.unlocked_arch):
                            game.unlocked_arch.append(_ar[0])
                            if not any(getattr(c,'_arch_orig',None)==_ar[0] for c in game.all_heroes):
                                game.all_heroes.append(create_arch_card(_ar))
                    game.play_snd(SND_LOSE)
                else:
                    pass  # результат ще не визначений — ігноруємо
                if game.lb_battle_result:
                    game.save_game()
                    game._lb_online_result_shown_at = time.time()
            # Авто-удар таймера для онлайн — коли час вийшов, б'ємо першою живою картою
            if not _ores and game.lb_turn_timer_start > 0:
                _od = getattr(game, 'online_deadline', 0)
                _is_my_turn_online = (game.online_whose_turn == game.online_role)
                # Локальний таймер авто-удару (не залежить від сервера)
                _auto_elapsed = time.time() - game.lb_turn_timer_start
                _local_timeout = game.lb_turn_timeout if game.lb_turn_timeout > 0 else game.lb_get_turn_timeout()
                _timer_expired = _auto_elapsed >= _local_timeout
                # Також перевіряємо дедлайн сервера якщо є
                if _od > 0:
                    _timer_expired = _timer_expired or (time.time() >= _od)
                # Захист від повторного авто-удару — перевіряємо прапор
                _already_auto = getattr(game, '_online_auto_fired', False)
                if _timer_expired and _is_my_turn_online and not _already_auto:
                    game._online_auto_fired = True   # блокуємо повторне спрацювання
                    _auto_h = next((h for h in game.army if h.is_alive), None)
                    _live_enemies = [e for e in game.enemies if e.is_alive]
                    _auto_m = random.choice(_live_enemies) if _live_enemies else None
                    if _auto_h and _auto_m:
                        # ── Ефекти здібностей при авто-ударі ──
                        _ah_abs = get_abilities_for_card(_auto_h)
                        _ah_cx, _ah_cy = _card_center(_auto_h)
                        _am_cx, _am_cy = _card_center(_auto_m)
                        for _ah_ab in _ah_abs:
                            spawn_ability_effect(_ah_ab[1], _ah_cx, _ah_cy, _am_cx, _am_cy)
                        game.online_hit(_auto_h, _auto_m)
                        game.total_hits += 1
                        game.set_battle_phrase("⏱ Час вийшов! Автоудар!")
                        game.lb_turn_timer_start = time.time()
                        game.lb_turn_timeout = game.lb_get_turn_timeout()
                    else:
                        game.lb_check_end()
                # Скидаємо прапор коли хід переходить до нас знову
                if not _is_my_turn_online:
                    game._online_auto_fired = False

        # Авто-перехід на BATTLE_HUB після онлайн-бою (через 4 сек після результату)
        if (getattr(game,"_lb_is_online_pvp",False) and game.state == LB_BATTLE
                and getattr(game,"lb_battle_result",None) is not None
                and getattr(game,"_lb_online_result_shown_at",0) > 0):
            if time.time() - game._lb_online_result_shown_at >= 4.0:
                # Повний скид + новий PID — уникаємо 400 Bad Request при наступному матчі
                game.online_full_reset()
                game.state = BATTLE_HUB

        # Авто-старт LAN PvP — тільки CLIENT при отриманні START від хоста
        if getattr(game, "_lb_lan_battle_start", False) and game.state in (LB_LOBBY_HOST, LB_LOBBY_JOIN):
            _opp_raw7 = getattr(game, "lb_lan_enemy_deck", [])
            if _opp_raw7:
                game._lb_lan_battle_start = False
                if getattr(game, "lb_lan_role", None) == "client":
                    game.lb_start_lan_battle(_opp_raw7, opp_name="LAN Хост")
                # HOST вже стартував у click-handler
            # else: колода ще не прийшла — спробуємо наступного кадру

        # ── Відкладена контратака ворога (BATTLE стан) ──────────────────────
        _pea = getattr(game, '_pending_enemy_attack', None)
        if _pea and game.state == BATTLE and time.time() >= _pea["time"]:
            game._pending_enemy_attack = None
            _ea_attacker = _pea["attacker"]
            _ea_target   = _pea["target"]
            # Перевіряємо що обидва ще живі
            if _ea_attacker.is_alive and _ea_target.is_alive:
                ecx2, ecy2 = _card_center(_ea_attacker)
                thcx, thcy = _card_center(_ea_target)
                # Здібності ворога
                e_abilities = get_abilities_for_card(_ea_attacker)
                if e_abilities:
                    e_stars  = len(e_abilities)
                    e_chance = ABILITY_CHANCE_BY_STARS.get(e_stars, 0.25) * 0.7
                    for ea in e_abilities:
                        if random.random() < e_chance:
                            apply_ability(ea, _ea_attacker, _ea_target,
                                          [_ea_attacker], [_ea_target])
                            spawn_ability_effect(ea[1], ecx2, ecy2, thcx, thcy)
                # Щит / ухилення героя
                _htshield = getattr(_ea_target, '_shielded_until', 0)
                if time.time() < _htshield:
                    game.set_battle_phrase(f"🛡️ {_ea_target.name} заблокував удар!")
                else:
                    _htdodge = getattr(_ea_target, '_dodge_until', 0)
                    if time.time() < _htdodge and random.random() < 0.5:
                        game.set_battle_phrase(f"🦅 {_ea_target.name} ухилився!")
                    else:
                        e_atk = _ea_attacker.attack
                        _ea_target.hp -= e_atk
                        _ea_target.offset_y = 20
                        spawn_ability_effect("Вогонь", ecx2, ecy2, thcx, thcy)
                        game.play_snd(SND_HIT)
                        game.set_battle_phrase(f"💥 {_ea_attacker.name} завдає {e_atk} шкоди {_ea_target.name}!")
                    if _ea_target.hp <= 0:
                        _ea_target.is_alive = False
                        _ea_target.level = max(1, _ea_target.level - 1)
                        _ea_target.xp = 0; _ea_target._update_stats()
                        if _ea_target in game.all_heroes: game.all_heroes.remove(_ea_target)
                        if _ea_target in game.army:       game.army.remove(_ea_target)
                        game.graveyard.append(_ea_target)
                        attacker_stars = len(get_abilities_for_card(_ea_attacker))
                        spawn_death_effect(thcx, thcy, attacker_stars)
                        game.play_snd(SND_HERO_DIE)
                        game.set_battle_phrase(random.choice(BATTLE_HERO_DIE_PHRASES))
            # Тепер перевіряємо кінець бою
            game.check_end_game()

        pygame.display.flip(); clock.tick(60)

if __name__ == "__main__":
    main()